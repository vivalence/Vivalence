---
import { getEntry, render } from "astro:content"
import Layout from "../layouts/Layout.astro"

const entry = await getEntry("site", "home")
const { Content } = await render(entry)
---
<Layout title={entry.data.title} variant="home">
  <Content />
</Layout>
