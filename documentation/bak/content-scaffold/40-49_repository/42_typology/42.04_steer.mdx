---
title: steer — the controllers
jdex: "42.04"
summary: Two geometries, four namespaces, the arity trap, and why application code barely touches it.
---

import Source from "../../../src/components/Source.astro"
import Example from "../../../src/components/Example.astro"
import Tabs from "../../../src/components/Tabs.astro"
import Editor from "../../../src/components/Editor.astro"

# 42.04 · steer — the controllers

*run something THROUGH a Vector. four namespaces, four files, ~250 lines: `match` · `trie` · `dispatch` · `strategy`.*

## steer is mostly internal — and that is the point

Composition is the mechanism; configuration is the surface. This holds across every module without exception.

Absence is the diagnostic here. Everything below is derived, never authored by hand. The distinction is load-bearing and easy to lose.

```js title="subsystems/typology/prototypes/vector.js" {3}
const vector = new Vector()
  .use(middleware)
  .open("/segment", (context) => context.request.body)
  .open("/other", () => null)
```

## two geometries — do not cross-compare

Nothing in this section computes; it describes. The same primitive appears again at a different scale. Declaration comes before wiring, at every level.

One shape, several readers, no coordination between them. The reader picks its own fold; the writer emits flat facts.

<Tabs names={["path — dispatch.js", "tree — trie.js"]}>
<Fragment slot="path — dispatch.js">
```
consumes:   a Signal
walks:      one route, segment by segment
terminates: EARLY, on the first match
binds:      params, wildcards, remainders
used by:    every live request
```
</Fragment>
<Fragment slot="tree — trie.js">
```
consumes:   nothing but the Vector
walks:      every node, exhaustively
terminates: when the tree is exhausted
binds:      nothing
used by:    every shape.* compiler
```
</Fragment>
</Tabs>

```txt
population:  core ──▶ datamap ──▶ authority ──▶ acid(cortex) ──▶ services ──▶ domain ──▶ freight ──▶ modes(stagger)
resolution:  declare ──▶ install ──▶ graft ──▶ finalize ──────────────────────────────────▶ terminate
```

## the arity trap

The same primitive appears again at a different scale. Declaration comes before wiring, at every level.

The reader picks its own fold; the writer emits flat facts. What follows is the contract, not the implementation. Two directions, no reconciliation, ever.

## no type precedence — declaration order wins

Absence is the diagnostic here. Everything below is derived, never authored by hand. The distinction is load-bearing and easy to lose.

Each layer holds one responsibility and hands the next a value it can fold. Nothing in this section computes; it describes.

## the executors

One shape, several readers, no coordination between them. The reader picks its own fold; the writer emits flat facts.

Two directions, no reconciliation, ever. The counterpart runs at teardown rather than at boot. Order is the only precedence rule that applies.

## it runs

The reader picks its own fold; the writer emits flat facts. What follows is the contract, not the implementation. Two directions, no reconciliation, ever.

The counterpart runs at teardown rather than at boot. Order is the only precedence rule that applies.

<Source name="42.04-steer" />

<Example name="42.04-steer" />

## try it

Each layer holds one responsibility and hands the next a value it can fold. Nothing in this section computes; it describes.

Declaration comes before wiring, at every level. One shape, several readers, no coordination between them. The reader picks its own fold; the writer emits flat facts.

<Editor code={`import { Vector, steer } from "@vivalence/typology"

const shadowed = new Vector()
  .open("/u/:id", () => "PARAM")
  .open("/u/list", () => "LITERAL")

console.log(await steer.dispatch.invoke(shadowed, "/u/list")({}))
// → PARAM. swap the two .open lines and it becomes LITERAL.`} />

## see also

Two directions, no reconciliation, ever. The counterpart runs at teardown rather than at boot. Order is the only precedence rule that applies.

A missing marker is as meaningful as a present one. The structure here is declared once and interpreted many times.

Read next: [[42.03_vector]] · [[42.05_shape]]
