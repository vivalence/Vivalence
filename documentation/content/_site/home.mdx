---
title: Vivalence Docs
eyebrow: an AI-era symbolic OS
headline: Vivalence is a symbolic operating system for the AI era
sub: >-
 Everything is a mode. Your data, your apps, your agents — one coherent, scalable, and flexible system. Use it to build any harness, integrate any service, and surface them as Svelte apps and soon via CLI.
totems:
  - { term: Knowledge Graphs, rest: rich and persistent }
  - { term: Daemon Runtimes, rest: integrating compute and data }
  - { term: Svelte Applications, rest: bring your own surfaces }
# pillars:
#   - key: "11.01"
#     title: What is Vivalence
#     desc: Why it exists, the vision, the goal, and where it came from.
#     jdex: "11.01"
#   - key: "11.01"
#     title: Architecture
#     desc: How modes, traits, symbols and literals work.
#     jdex: "11.01"
#   - key: "11.01"
#     title: Guide to the repo
#     desc: Algebraic types from @typology, the @runtime, and client @kajuit. Shell client @ghost coming soon.
#     jdex: "11.01"
# start:
#   title: Getting Started
#   items:
#     - { jdex: "11.01", title: Install the system, desc: xxx. }
#     - { jdex: "11.01", title: Run a Daemon, desc: xxx. }
#     - { jdex: "11.01", title: Bring your own data, desc: Chat with your data. }
#     - { jdex: "11.01", title: Bring your own service, desc: Chat with your data. }
#     - { jdex: "11.01", title: Write a Harness, desc: Chat with your data. }
#     - { jdex: "11.01", title: Write an App, desc: Any surface in Svelte. }
---

import Home from "../../src/components/Home.astro"
import LiveExample from "../../src/components/LiveExample.astro"

<Home data={frontmatter}>
  {/* <LiveExample slot="live" title="rich docs" capture="home">
    Doc code fences tagged <code>tangle=</code> are written to disk and executed.
    <b>Source</b> shows the exact code, <b>Example</b> its captured stdout,
    <b>Trace</b> a real runtime span.
  </LiveExample> */}

<Fragment slot="guide">
# Three 5-minute starters
- quick start installation
- grammar, verbs and nouns
- build your own system
</Fragment>

  <Fragment slot="guide">
## quick start
Deno ^2.7 must be installed. Currently only installation from repo at this point.

minimum vocabulary required:

- `viva` — the CLI. Installed to the OS, or `deno task ghost/run` if so inclined.
- `ledger` — the `~` directory for system-level stuff: tracking registry packages, coordinating runtimes, logs, locks, etc.
- `package` — a unit of modes living in the registry, likely git-managed. Services, applications, datasets, harnesses, and variants are grouped into a package.
- `variant` — an executable system unit made up of runtime and clients, and the daemons running on top of them.

### Install the Vivalence CLI

```sh
git clone https://github.com/vivalence/vivalence.git vivalence
cd vivalence
deno task install
viva ledger/install ~/viva
```

`deno task install` fetches dependencies and links `viva` into `~/.deno/bin` — the CLI
remembers the repo via `~/.config/viva/env`. `ledger/install` scaffolds the system ledger
and seeds `registry.json` from the in-repo packages.

### Add a package
tap a package — a local path is recorded as-is, a git URL is cloned into the store

```sh
viva ledger/tap <path | git url>
viva ledger/taps
```

### Set up a variant
clone a variant out of the registry and give it its `.env`

```sh
viva variant/clone @viva/variant/localhost ./localhost

cd localhost 
cp .env.example .env 
$EDITOR .env
```

### Boot runtime and client

```sh
deno task runtime/run
deno task kajuit/run
```

create your account against the variant's lighthouse — the client logs in with it:

```sh
viva variant/lighthouse/auth/signup <username> <password>
```

#### Deployment
Works already, not documented yet — check `registry/viva/variant/multiplayer/docker-compose.yml` for an example.

</Fragment>

<Fragment slot="guide">
## grammar, verbs and nouns
- the makings of a viva system

runtime, daemons, clients, and services

modes have type and traits.
special types: domain, service
primary traits: dataset, application, harness, exposed, fraught, xxx

entities
built-in:
custom domain level:

</Fragment>

<Fragment slot="guide">
## the variant and its modes
- a whole system in one file - the standalone demo
</Fragment>

</Home>
