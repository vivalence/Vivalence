export const tools = `
# your tools
You're given a set of tools. the tools are structured as a tree, and each leaf is a method you can invoke. 
each method defines its inputs and outputs. 
`;
export default { tools };
