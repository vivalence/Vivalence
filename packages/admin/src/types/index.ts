export interface User {
  id: string;
  email: string;
  roles: string[];
}

export interface Strategy {
  id: string;
  name: string;
}

export interface Tag {
  id: string;
  name: string;
}

export interface Unit {
  id: string;
  name: string;
}
