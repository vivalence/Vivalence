export default class Runtime {
  aperture = null;
  entities = null;
  emitter = null;
  services = null;
  hooks = null;

  config = {};

  get url() {
    return `/aperture/v1/runtime/${this.config.manifest.slug}`;
  }

  modules = {};
  // domain = {};
  // strategies = {};

  constructor(config: any) {
    this.config = config;
  }
}
