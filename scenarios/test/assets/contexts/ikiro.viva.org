*** personal aspirations
**** in the first hour of work i shouldnt create more work for myself.
maybe one nice thing to track would be if i made myself more or less work during periods of hours, days, weeks. xxx.
maybe per decision.
* notes
i like giving specific names to specific things.
it loosens the thing from needing to satisfy external constraints.
it makes them mine and thus i can shape them.

the goal is internal coherence. the system inside is isolated in its modeling and implementation from the outside. surfaces are presented.

* ikiro
its a command, given in love, declaring something to live.
its also the name of the viva development paradigm.
this paradigm covers the patterns, goals, values, and processes during development on, for, or in the spirit of the viva operating system.
** ground rules
*** never use comments. comments in code are @user space.
** style guide
*** org
**** aspiration
- all answers are patterned into org trees.
- all code belongs into codeblocks!
**** sins
in org, never use any text decoration.
never use *bold* outside of markdown. the only way to use * in org is for headers.
never use ==, like =javascript=. nono.

*** code
#+BEGIN_SRC javascript
  // all code.
#+end_src

*** markdown
#+begin_src markdown
  # header
#+end_src

** @typology
*** Core Typological Patterns
**** *Lifecycle Pattern* - The four-phase pattern used throughout the system:
#+begin_src javascript
// construction -> population -> resolution -> integration
const paladin = new Paladin();
await populate.env(paladin);
await resolve.variant(paladin);
await integrate.validate(paladin);
#+end_src

**** *Typology* 
***** *Gestalt System* - Type validation and casting utilities:
#+begin_src javascript
import { is, cast, as } from "@vivalence/typology";

if (is.signature(thing)) {
  const casted = cast.runtime(thing);
  const path = as.path.url(fileUrl);
}
#+end_src

***** *Signature Hierarchy* - The foundational pattern where all prototypes extend from Signature:
#+begin_src javascript
export class Signature {
  constructor(signature = null, trace = null) {
    this.gauges = [];
    if (trace) this.from(trace);
  }
  
  branch(signature) {
    return new this.constructor(signature, this);
  }
  
  get tilde() { // root finder
    let position = this;
    while (position.trace) position = position.trace;
    return position;
  }
}
#+end_src

#+begin_src javascript
const pattern = new Pattern("/users/:id");
const signal = new Signal("/users/123");
const match = pattern.apply(signal); // { parameters: { id: "123" } }
#+end_src
*** typology 
**** lifecycles 
**** prototypes 
**** gestalten 
one paradigm we employ is that we never use the name of a primitive to name that primitive on a gestalt.
each gestalt has a dedicated and separate name for the value of the primitive.
some names are system level. coherence is enforced.
system internal names can be creative. stylepoints are awarded seasonally.

**** primitives 
** documentation 
under ikiro we consider code to serve as part of the documentation. 
tests serve as the second major leg for shining light under the hood. 
the third, fourth, and last legs are in order:
playgrounds for fucking around and finding out.
the documentation kernel and viva-repo topology which allow you to use viva to explore viva. nifty init.
and lastly, your own agency. do some fucking work yourself. make some friends, build some shit... youll get there. 
** Testing 
we employ a specifi testing paradigm, which we refer to as structural testing.
structural testing applies denos bdd paradigm to lifecyled primitives, prototypes, and systems.
we rely on @typology patterns and implement our own test tooling 'specimen'.
*** Structural Testing Framework
**** *Core Principle*:
Test the lifecycle phases (construction, population, resolution, integration) with two perspectives:
- *Gestalt*: Properties, types, shapes, state validation
- *Valences*: Functions, behaviors, capabilities, actions

**** *Template Structure*:
#+begin_src javascript
describe("Prototype", () => {
  let subject;

  describe("construction", () => {
    it("cycles", () => {
      subject = new Prototype();
    });
    
    describe("gestalt", () => {
      it("satisfies type constraints", () => {
        assert(is.prototype(subject));
      });
    });
  });

  describe("valences", () => {
    it("performs expected behaviors", () => {
      const result = subject.doSomething();
      assertEquals(result, expected);
    });
  });
});
#+end_src



#+begin_src org

*** structural testing.
#+BEGIN_SRC javascript
  import { assert, assertEquals } from "@std/assert";
  import { describe, it } from "@std/testing/bdd";

  import { is, Path, Env } from "@vivalence/typology";
  import { Paladin } from "@vivalence/paladin/prototype";
  import { populate, resolve, integrate } from "@vivalence/paladin/lifecycle";

  let paladin;
  describe("Paladin", () => {
    describe("construction", () => {
      it("cycles", () => {
        paladin = new Paladin();
      });

      describe("gestalt", () => {
        it("is", () => {
          assert(is.paladin(paladin));
        });

        it("creates valid paladin instance", () => {
  	  // examples. to be extended.
          assert(is.fn(paladin.read.json));
          assert(is.fn(paladin.find.viva));
          assert(is.fn(paladin.check.env));
          assert(is.fn(paladin.state.dir));
          assert(is.fn(paladin.join.tilde));
        });
      });
    });

    describe("population", () => {
      it("cycles", async () => {
        await populate.env(paladin);
        await populate.environment(paladin);
        await populate.system(paladin);
        await populate.vip(paladin);
        await populate.modeselector(paladin);
        await populate.statements(paladin);
        await populate.questions(paladin);
      });

      describe("gestalt", () => {
        it("loads environment configuration", () => {
          assert(is.string(paladin.env.get("VIVA_SYSTEM_MODE")));
          assert(is.string(paladin.mode));
          assert(is.string(paladin.role));
        });

        it("establishes system paths", () => {
          assertEquals(
            paladin.system.mount.absolute,
            paladin.env.get("VIVA_SYSTEM_MOUNT"),
          );
          assertEquals(
            paladin.tilde.mount.absolute,
            paladin.env.get("VIVA_TILDE_MOUNT"),
          );
        });

        it("initializes module registry", () => {
          assertEquals(paladin.vip.pensieve.size, 0);
        });

        it("configures operational mode", () => {
          assert(is.boolean(paladin.is.dev));
          assert(is.boolean(paladin.is.prod));
          assert(is.boolean(paladin.is.build));
        });
      });
    });

    describe("resolution", () => {
      it("cycles", async () => {
        await resolve.variant(paladin);
        await resolve.service(paladin);
        await resolve.runtimes(paladin);
      });

      describe("gestalt", () => {
        it("resolves variant configuration", () => {
          assert(is.string(paladin.variant));
          assert(is.array(paladin.traits));
        });

        it("populates runtime collections", () => {
          assert(is.array(paladin.runtimes));
          assert(is.array(paladin.services));
          assert(is.array(paladin.clients));
          assert(is.object(paladin.service));
        });

        it("filters undefined services", () => {
          assertEquals(
            paladin.services.some((service) => service === undefined),
            false,
          );
        });
      });
    });

    describe("integration", () => {
      it("cycles", async () => {
        await integrate.publish(paladin);
        await integrate.secure(paladin);
        await integrate.mount(paladin);
        await integrate.validate(paladin);

        // console.log(paladin);
        // console.log({ paladin });
      });
    });
  });

#+end_src

*** Test Structure:
#+begin_src javascript
describe("Prototype", () => {
  let subject;

  describe("construction", () => {
    it("cycles", () => {
      subject = new Prototype();
    });
    
    describe("gestalt", () => {
      it("satisfies type constraints", () => {
        assert(is.prototype(subject));
      });
    });
  });

  describe("valences", () => {
    it("performs expected behaviors", () => {
      const result = subject.doSomething();
      assertEquals(result, expected);
    });
  });
});
#+end_src
*** specimen - possible future library
specimen comes from Latin specimen, meaning "mark, token, example, proof."
Root is Latin specere - "to look at, observe" (same root as "spectacle," "inspect," "species").
The noun form specimen literally meant "that which is seen" or "something shown as an example."

specimen allways uses expect.
!!
import { expect } from "@std/expect";
import { expect } from "@std/expect";
import { expect } from "@std/expect";
!! never assert;



speciment testing pattern demo:
#+begin_src javascript
  const specimen = (name, spec) => {
    describe(name, () => {
      let subject;

      describe("construction", () => {
        it("cycles", () => {
          subject = spec.construct();
        });

        if (spec.gestalt?.construction) {
          describe("gestalt", () => {
            Object.entries(spec.gestalt.construction).forEach(([test, fn]) => {
              it(test, () => fn(subject));
            });
          });
        }
      });

      if (spec.populate) {
        describe("population", () => {
          it("cycles", async () => {
            await spec.populate(subject);
          });
        });
      }
    });
  };

  // Usage example.
  // various abstractions are possible here. this smells monadic. maybe a vector. with middlewares and shit. lovely. specimen the behavior driven typology testing framework.
  // not exactly sure yet where that rotation fits but somewhere the describe, it bdd approach fits nicely onto a vector toolchain, and can be composed into a vector and controlled through some shard interface. perfection.
  specimen("Path", {
    construct: () => new Path("/home/user/documents"),
    
    gestalt: {
      construction: {
        "extends signature": (path) => expect(is.signature(path)),
        "maintains path semantics": (path) => expect(is.path(path))
      }
    },

    valences: {
      "resolves absolute paths": (path) => {
        assertEquals(path.absolute, "/home/user/documents");
      },
      "branches paths": (path) => {
        const child = path.branch("file.txt");
        assertEquals(child.absolute, "/home/user/documents/file.txt");
      }
    }
  });
#+end_src
*** bdd hooks

#+BEGIN_SRC javascript
import { describe, it, beforeEach, afterEach, beforeAll, afterAll } from "@std/testing/bdd";

describe("database tests", () => {
  let db;
  let transaction;

  beforeAll(async () => {
    db = await connectDB();
  });

  afterAll(async () => {
    await db.close();
  });

  beforeEach(async () => {
    transaction = await db.beginTransaction();
  });

  afterEach(async () => {
    await transaction.rollback();
  });

  it("creates user", async () => {
    await db.insert({ name: "alice" });
  });
});

describe("temp file cleanup", () => {
  const files = [];

  afterEach(async () => {
    await Promise.all(files.map(f => Deno.remove(f)));
    files.length = 0;
  });

  it("writes file", async () => {
    const path = await Deno.makeTempFile();
    files.push(path);
    await Deno.writeTextFile(path, "data");
  });
});

describe("mock restoration", () => {
  const mocks = [];

  beforeEach(() => {
    mocks.push(
      spy(console, "log"),
      spy(Math, "random")
    );
  });

  afterEach(() => {
    mocks.forEach(m => m.restore());
    mocks.length = 0;
  });
});

describe("state isolation", () => {
  const getCleanState = () => ({ users: [], count: 0 });
  let state;

  beforeEach(() => {
    state = getCleanState();
  });

  it("modifies state", () => {
    state.count++;
  });
});
#+end_src
*** expect library

#+BEGIN_SRC javascript
import { expect } from "@std/expect";

expect(value).toBe(expected)
expect(value).toEqual(expected)
expect(value).toStrictEqual(expected)
expect(value).toBeDefined()
expect(value).toBeUndefined()
expect(value).toBeNull()
expect(value).toBeTruthy()
expect(value).toBeFalsy()
expect(value).toBeGreaterThan(n)
expect(value).toBeGreaterThanOrEqual(n)
expect(value).toBeLessThan(n)
expect(value).toBeLessThanOrEqual(n)
expect(value).toBeCloseTo(n, precision)
expect(value).toBeInstanceOf(Class)
expect(value).toBeNaN()
expect(value).toContain(item)
expect(value).toContainEqual(item)
expect(value).toHaveLength(n)
expect(value).toHaveProperty(key, value?)
expect(value).toMatch(regex)
expect(fn).toThrow(error?)
expect(fn).toThrowError(error?)
expect(value).toMatchObject(obj)
expect(value).toHaveBeenCalled()
expect(value).toHaveBeenCalledTimes(n)
expect(value).toHaveBeenCalledWith(...args)
expect(value).toHaveBeenLastCalledWith(...args)
expect(value).toHaveBeenNthCalledWith(n, ...args)
expect(value).toHaveReturned()
expect(value).toHaveReturnedTimes(n)
expect(value).toHaveReturnedWith(value)
expect(value).toHaveLastReturnedWith(value)
expect(value).toHaveNthReturnedWith(n, value)

expect(value).not.toBe(expected)
#+end_src
** examples 
*** vector.ikiru.org
#+TITLE: Vector - Routing Trie in Action
#+AUTHOR: vivalence

**** construct
The Vector class serves as the core routing structure, implementing a tree of patterns that can hold both effects (terminal handlers) and trajectories (sub-trees).

#+begin_src javascript :tangle vector.js
import { is } from "@vivalence/shared";
import { Pattern } from "@vivalence/typology";

export class Vector {
  constructor(ancestor) {
    this.effects = new Map(); // <Pattern->Effect>
    this.trajectories = new Map(); // <Pattern->Vector>
    this.carry = []; // middlewares
    if (ancestor) this.ancestor = ancestor;
  }

  use(middleware) {
    this.carry.push(middleware);
    return this;
  }

  branch(signature) {
    const pattern = new Pattern(signature);

    let descendant = Array.from(this.trajectories.entries())
      .find(([{ hash }]) => hash === pattern.hash)?.[1];

    if (!descendant) {
      descendant = new Vector(this);
      this.trajectories.set(pattern, descendant);
    }

    return pattern.heir ? descendant.branch(pattern.heir) : descendant;
  }

  open(signature, effect) {
    const pattern = new Pattern(signature);

    if (pattern.fin) {
      const fin = pattern.fin.pop();
      this.branch(pattern).effects.set(fin, effect);
    } else this.effects.set(pattern, effect);

    return this;
  }

  set(vector) {
    for (const [pattern, effect] of vector.effects) {
      this.effects.set(pattern, effect);
    }

    for (const [pattern, trajectory] of vector.trajectories) {
      this.trajectories.set(pattern, trajectory);
    }

    this.carry.push(...vector.carry);

    return this;
  }

  get patterns() {
    return [...this.effects.keys(), ...this.trajectories.keys()];
  }

  get descendants() {
    return [...this.trajectories.values()];
  }

  get heir() {
    return this.descendants[0];
  }
}
#+end_src

- Populate - Learning by Doing

**** play run ikiro lifecycle
***** Creating Your First Vector

Start with the basic construction - every vector begins empty and builds its routing tree through the =branch= and =open= methods.

#+begin_src javascript :tangle vector.test.js
import { assertEquals, assertThrows } from "$std/assert";
import { Vector } from "@vivalence/vector";

function createTestVector() {
  const vector = new Vector();

  vector
    .branch("/users")
    .open("/profile", async (ctx) => ({ user: input.id }))
    .open("/:id", async (ctx) => ({ id: input.id }));

  vector
    .branch("/posts")
    .open("/create", async (ctx) => ({ created: true }))
    .open("/:postId", async (ctx) => ({ post: input.postId }));

  return vector;
}
#+end_src

***** Tutorial: Branch Creation and Navigation

Branches create navigation paths through your routing tree. Each branch returns a new vector that maintains its connection to the parent.

#+begin_src javascript :tangle vector.test.js
Deno.test("Vector branch creation and descendant tracking", () => {
  const vector = createTestVector();

  assertEquals(vector.trajectories.size, 2);

  const usersBranch = vector.branch("/users");
  const postsBranch = vector.branch("/posts");

  assertEquals(usersBranch.effects.size, 2);
  assertEquals(postsBranch.effects.size, 2);
  assertEquals(usersBranch !== postsBranch, true);
});
#+end_src

***** Tutorial: Hash-Based Branch Merging

Vectors use hash-based deduplication - identical patterns merge into the same branch, enabling efficient route sharing.

#+begin_src javascript :tangle vector.test.js
Deno.test("Vector hash-based branch merging", () => {
  const vector = new Vector();

  const branch1 = vector.branch("/users");
  const branch2 = vector.branch("/users");

  assertEquals(branch1 === branch2, true);
  assertEquals(vector.trajectories.size, 1);

  branch1.open("/test", async () => ({ test: true }));

  assertEquals(branch2.effects.size, 1);
});
#+end_src

- Resolve - Understanding the Architecture

***** Effect Registration Pattern

The =open= method demonstrates the vector's core principle: effects are always registered at the appropriate depth in the tree structure, while maintaining clean separation between routing and handling.

#+begin_src javascript :tangle vector.test.js
Deno.test("Vector effect registration with open method", () => {
  const vector = new Vector();
  const testEffect = async (ctx) => ({ result: "test" });

  const usersBranch = vector.branch("/users");
  vector.open("/users/:id", testEffect);

  assertEquals(vector.trajectories.size, 1);
  assertEquals(vector.effects.size, 0);

  const effectPattern = Array.from(usersBranch.effects.keys())[0];

  assertEquals(effectPattern.signature, ":id");
  assertEquals(usersBranch.effects.get(effectPattern), testEffect);
});
#+end_src

***** Middleware Composition Strategy

Middleware follows the pace layering principle - accumulated at each level of the tree but only applied during traversal. This creates stable middleware chains while allowing fast innovation in effects.

#+begin_src javascript :tangle vector.test.js
Deno.test("Vector middleware registration and chaining", () => {
  const vector = new Vector();
  const middleware1 = async (ctx, next) => {
    ctx.step1 = true;
    return next();
  };
  const middleware2 = async (ctx, next) => {
    ctx.step2 = true;
    return next();
  };

  vector.use(middleware1).use(middleware2);

  assertEquals(vector.carry.length, 2);
  assertEquals(vector.carry[0], middleware1);
  assertEquals(vector.carry[1], middleware2);

  const branch = vector.branch("/test");
  assertEquals(branch.carry.length, 0);
});
#+end_src

*** Specimen as Vector Proposal
implementation is off
this ought be a compiler not a class extension.
that shit is only for the signature.

#+begin_src javascript
  import { Vector } from "@vivalence/vector";
  import { describe, it } from "@std/testing/bdd";

  export function specimen(name, subject) {
    static test(name, subject) {
      const specimen = new Specimen(name, subject);
      
        // there is a lot of redundancy here. i assume integration of this redundancy will result in the critical design choice.
        // you next koan is this.
        // you try to isolate and express the pattern below.
      return specimen
        .branch("/construction")
          .use((ctx, next) => {
            ctx.subject = subject.construct();
            return next();
          })
          .open("/cycles", (ctx) => assert(ctx.subject))
          .open("/gestalt/:assertion", (ctx) => {
            const assertion = subject.gestalt?.construction?.[ctx.params.assertion];
            if (assertion) assertion(ctx.subject);
          })
        
        .branch("/population") 
          .use(async (ctx, next) => {
            if (subject.populate) await subject.populate(ctx.subject);
            return next();
          })
          .open("/cycles", (ctx) => assert(ctx.subject.populated))
          
        .branch("/valences")
          .open("/:behavior", (ctx) => {
            const behavior = subject.valences?.[ctx.params.behavior];
            if (behavior) behavior(ctx.subject);
          });
        // this is where koan context ends.
        // the goal here is to write the specimen library.
        // the specimen library will live in the @typology/gestalt and can be imported from @vivalence/typology/specimen
        // it lives next to cast is as and should mould in nicely next to them.
        // the library should work like this:
        // javascript`
        // const specimen = specimen('daemon', Daemon)
        // speciment now has some defaults expressed. like construction cycles.
        // the specimen instance can be expanded by invoking interfaces for 'is' for example. this adds a testing look for constructin gestalt is. 
        //  i can attach lifecycle stages or individual lifecycle stages.
        //  i can place expected valences and gestalt checks.
        //  i can middleware into the execution cycle to construct test cases, hook into logging or whatever.
    }

    compile() {
      describe(this.name, () => {
        this.traverse("/construction/cycles");
      });
    }
  }
#+end_src

**** meditate the koan. failed.
#+begin_src javascript
export function specimen(name, Constructor) {
  const vector = new Vector();
  const spec = { construct: () => new Constructor() };
  
  const stage = (path, setup, validator = null) => {
    const branch = vector.branch(path);
    
    if (setup) branch.use(setup);
    
    branch.open("/cycles", validator || ((ctx) => assert(ctx.subject)));
    
    branch.open("/:check", (ctx) => {
      const fn = spec[path.slice(1)]?.[ctx.params.check];
      if (fn) fn(ctx.subject);
    });
    
    return branch;
  };
  
  stage("/construction", 
    (ctx, next) => { ctx.subject = spec.construct(); return next(); }
  );
  
  stage("/population", 
    async (ctx, next) => { 
      if (spec.populate) await spec.populate(ctx.subject); 
      return next(); 
    },
    (ctx) => assert(ctx.subject.populated)
  );
  
  return {
    construct: (fn) => { spec.construct = fn; return this; },
    populate: (fn) => { spec.populate = fn; return this; },
    gestalt: (stage, checks) => { spec[stage] = checks; return this; },
    valences: (behaviors) => { spec.valences = behaviors; return this; },
    compile: () => describe(name, () => vector.traverse("/construction/cycles"))
  };
}
#+end_src

* chat
** ikiro
*** philosophy
**** code as documentation
**** structural testing
**** lifecycle-driven development
**** gestalt-valence duality

**** work is work on the system as a whole.
**** no littering
*** taxonomy
**** workpackage/process groupings
renamings (config to paladin, modules to modes, bunch of env vars, )
retoolings (registry to vip, modules to modes)
rewiring (modules to (modes, services, kernel))
reintegration kernel - ontology - topics - domain


**** lifecycles
***** construction
***** population  
***** resolution
***** integration

**** feedback systems
when an agent has done something especially well or want to mark it anyways, you tell it that its nailed something.

**** maturity
document maturity of segments of the system by degrees of conceptually mature, implementation incomplete, ....
used during snapshots

*** patterns
**** shared tools, common abstractions, coherent integration.

*** razors
**** is this a work reducing or work increasing choice?
**** is this a one or two way choice.

*** typology
**** prototypes
***** signature (base class)
****** pattern (routing/matching)
****** signal (data flow)
****** path (filesystem/navigation)
***** vector ()
****** prototype
****** controllers
****** compilers
**** gestalten
***** is (type validation)
***** cast (type conversion)
***** as (constructor utilities)
**** primitives
***** basic types
***** validation patterns
*** testing
**** structural testing paradigm
***** lifecycle phases
***** gestalt vs valences
**** specimen pattern
***** archetypal instances
***** test factories
**** integration patterns
***** cross-prototype testing
***** system lifecycle validation

*** examples
**** signature system walkthrough
**** paladin lifecycle implementation
**** vector pattern matching
#+end_src

**** Reasoning for Structure:

1. *Philosophy First*: Establishes the mindset and values before diving into implementation
2. *Typology Central*: This is the heart of the system - all patterns flow from here
3. *Testing as Documentation*: Testing methodology gets equal weight as it serves as living documentation
4. *Patterns as Bridge*: High-level patterns connect typology concepts to real implementations
5. *Examples as Validation*: Concrete walkthroughs prove the paradigm works in practice

**** Two Example Tree Nodes:

***** Typology Example - "prototypes/signature":
#+begin_src org
**** signature (base class)
The foundational prototype from which all viva types inherit hierarchical capabilities.

***** construction pattern
#+begin_src javascript
export class Signature {
  constructor(signature = null, trace = null) {
    this.gauges = [];
    if (this.Is(signature)) return signature.from(trace);
    if (trace) this.from(trace);
  }
}
#+end_src

***** hierarchical navigation
- =trace= - parent reference (up the tree)
- =gauges= - children array (down the tree) 
- =tilde= - root finder
- =fin= - leaf iterator

***** hash-based identity
Every signature maintains a hash for caching and identity comparison:
#+begin_src javascript
get hash() {
  return this.hasher();
}
#+end_src
#+end_src

***** Testing Example - "structural testing paradigm/lifecycle phases":
#+begin_src org
***** lifecycle phases
Each prototype follows the four-phase testing lifecycle that mirrors system behavior.

****** construction
Test basic instantiation and initial state:
#+begin_src javascript
describe("construction", () => {
  it("cycles", () => {
    subject = new Prototype();
  });
  
  describe("gestalt", () => {
    it("satisfies type constraints", () => {
      assert(is.prototype(subject));
      assert(is.defined(subject.hash));
    });
  });
});
#+end_src

****** population
Test data loading and configuration:
#+begin_src javascript
describe("population", () => {
  it("cycles", async () => {
    await populate.data(subject);
  });
  
  describe("gestalt", () => {
    it("maintains populated state", () => {
      assert(subject.populated === true);
    });
  });
});
#+end_src
#+end_src

This structure emphasizes that ikiro is both a development philosophy and a concrete methodology, with testing serving as executable documentation that validates the paradigm's effectiveness.
