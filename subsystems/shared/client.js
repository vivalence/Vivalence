export { random, is, obj, array, strings, time, id } from "./lib/index.js";

import hash from "./hash/client.js";

export { hash };

// export default {monads, deepClone, strings, time, deepEquals, deepMerge, id, shuffle, blacklist, validators};
