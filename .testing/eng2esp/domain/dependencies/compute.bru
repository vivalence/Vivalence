meta {
  name: compute
  type: http
  seq: 1
}

post {
  url: {{DAEMON_SERVER_URL}}/r/{{RUNTIME_SLUG}}/dependencies/compute
  body: json
  auth: none
}

body:json {
  {
    "dependency": {
      "slug": "verbs-101"
    }
  }
}
