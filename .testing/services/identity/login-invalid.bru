meta {
  name: Login Invalid
  type: http
  seq: 8
}

post {
  url: {{IDENTITY_AUTHORITY}}/login
  body: json
  auth: none
}

body:json {
  {
    "username": "invalid",
    "password": "wrongpassword"
  }
}

tests {
  test("should reject invalid credentials", function () {
    expect(res.getStatus()).to.equal(401);
  });
}