// import { DATABASE_URL, NODE_ENV } from "$env/static/private";
// import { PrismaClient } from "@prisma/client";

console.log("[PRISMA] LOADED. shoudl not be.");

// export let prisma;

// const prismaSettings = {
//     datasources: {
//         db: {
//             url: DATABASE_URL
//         }
//     }
// };
// if (NODE_ENV === "production") {
//     prisma = new PrismaClient(prismaSettings);
// } else {
//     prisma = new PrismaClient(prismaSettings);
// }

// export default prisma;
