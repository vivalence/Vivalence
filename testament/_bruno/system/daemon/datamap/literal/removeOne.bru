meta {
  name: removeOne
  type: http
  seq: 7
}

post {
  url: {{DAEMON}}/entities/literal/removeOne
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {
      "slug": "test-literal"
    }
  }
}
