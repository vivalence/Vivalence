meta {
  name: upsert
  type: http
  seq: 5
}

post {
  url: {{DAEMON}}/entities/literal/upsert
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "data": {
      "slug": "test-literal",
      "trait": {
        "target": "updated target",
        "native": "updated native"
      }
    }
  }
}
