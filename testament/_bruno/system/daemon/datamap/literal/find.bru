meta {
  name: find
  type: http
  seq: 1
}

post {
  url: {{DAEMON}}/entities/literal/find
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {},
    "options": {
      "populate": ["symbols"],
      "limit": 10
    }
  }
}
