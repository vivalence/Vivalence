meta {
  name: findOne
  type: http
  seq: 2
}

post {
  url: {{DAEMON}}/entities/literal/findOne
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {
      "slug": "partir.verb.subjunctive.present.third.plural"
    },
    "options": {
      "populate": ["symbols","uses"]
    }
  }
}
