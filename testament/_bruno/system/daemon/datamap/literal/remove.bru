meta {
  name: remove
  type: http
  seq: 9
}

post {
  url: {{DAEMON}}/entities/literal/remove
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {
      "slug": "test-literal"
    }
  }
}
