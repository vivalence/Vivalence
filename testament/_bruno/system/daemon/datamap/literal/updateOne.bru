meta {
  name: updateOne
  type: http
  seq: 6
}

post {
  url: {{DAEMON}}/entities/literal/updateOne
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {
      "slug": "test-literal"
    },
    "data": {
      "trait": {
        "target": "updated target",
        "native": "updated native"
      }
    }
  }
}
