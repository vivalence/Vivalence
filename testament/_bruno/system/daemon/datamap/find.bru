meta {
  name: find
  type: http
  seq: 3
}

post {
  url: {{DAEMON}}/entities/literal/find
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  { 
    "options": {
      "limit": 5,
    }
  }
}
