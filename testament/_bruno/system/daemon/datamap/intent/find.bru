meta {
  name: find
  type: http
  seq: 1
}

post {
  url: {{DAEMON}}/entities/intent/find
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {},
    "options": {
      "populate": ["mode"],
      "limit": 10
    }
  }
}
