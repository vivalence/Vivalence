meta {
  name: update
  type: http
  seq: 8
}

post {
  url: {{DAEMON}}/entities/intent/update
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {},
    "data": {
      "description": "batch updated"
    }
  }
}
