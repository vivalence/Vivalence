meta {
  name: remove
  type: http
  seq: 7
}

post {
  url: {{DAEMON}}/entities/intent/remove
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {
      "slug": "test-intent"
    }
  }
}
