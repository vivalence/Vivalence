meta {
  name: remove
  type: http
  seq: 9
}

post {
  url: {{DAEMON}}/entities/intent/remove
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {
      
    }
  }
}
