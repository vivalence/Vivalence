meta {
  name: remove
  type: http
  seq: 9
}

post {
  url: {{DAEMON}}/entities/mode/remove
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {
      "type": "game",
      "slug": "test-mode"
    }
  }
}
