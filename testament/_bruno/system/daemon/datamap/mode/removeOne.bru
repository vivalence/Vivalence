meta {
  name: removeOne
  type: http
  seq: 7
}

post {
  url: {{DAEMON}}/entities/mode/removeOne
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {
      "type": "tactic",
      "slug": "verb-clinic"
    }
  }
}
