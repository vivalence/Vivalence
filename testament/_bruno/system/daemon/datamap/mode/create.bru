meta {
  name: create
  type: http
  seq: 4
}

post {
  url: {{DAEMON}}/entities/mode/create
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "data": {
      "type": "game",
      "slug": "test-mode",
      "name": "Test Mode",
      "traits": ["VIEWABLE", "EMITTER"],
      "installed": true,
      "version": "0.0.1"
    }
  }
}
