meta {
  name: find
  type: http
  seq: 1
}

post {
  url: {{DAEMON}}/entities/mode/find
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {},
    "options": {
      "populate": ["intents"],
      "limit": 10
    }
  }
}
