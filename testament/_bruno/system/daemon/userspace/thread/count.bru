meta {
  name: count
  type: http
  seq: 3
}

post {
  url: {{DAEMON}}/userspace/entities/thread/count
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {}
  }
}
