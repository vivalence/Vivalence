meta {
  name: find
  type: http
  seq: 1
}

post {
  url: {{DAEMON}}/userspace/entities/thread/find
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
  "where":{"id":"019d42b0-89fd-7360-8c62-ed5ccf136767"},
    "options":{"populate":["literals","symbols","mode"]}
  }
}

tests {
  test("should be able to login", function () {
    const data = res.getBody();
    console.log("data",data)
    expect(res.getStatus()).to.equal(200);
  });
   
}
