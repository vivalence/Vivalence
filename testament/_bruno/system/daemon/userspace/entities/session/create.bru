meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{DAEMON}}/userspace/entities/session/create
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
  }
}

tests {
  test("should be able to login", function () {
    const data = res.getBody();
    console.log("data",data)
    expect(res.getStatus()).to.equal(200);
  });
   
}
