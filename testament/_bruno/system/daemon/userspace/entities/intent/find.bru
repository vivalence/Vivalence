meta {
  name: find
  type: http
  seq: 1
}

post {
  url: {{DAEMON}}/userspace/entities/intent/find
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
  }
}

tests {
  test("should be able to login", function () {
    const data = res.getBody();
    console.log("data",data)
    expect(res.getStatus()).to.equal(200);
  });
   
}
