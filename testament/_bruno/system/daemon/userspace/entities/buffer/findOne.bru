meta {
  name: findOne
  type: http
  seq: 2
}

post {
  url: {{DAEMON}}/userspace/entities/buffer/findOne
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {},
    "options": {
      "populate": ["literals", "symbols", "mode"]
    }
  }
}
