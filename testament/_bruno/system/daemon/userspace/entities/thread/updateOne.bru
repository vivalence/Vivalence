meta {
  name: updateOne
  type: http
  seq: 5
}

post {
  url: {{DAEMON}}/userspace/entities/thread/updateOne
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {},
    "data": {}
  }
}
