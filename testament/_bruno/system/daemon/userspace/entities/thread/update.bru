meta {
  name: update
  type: http
  seq: 4
}

post {
  url: {{DAEMON}}/userspace/entities/thread/update
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {},
    "data": {}
  }
}
