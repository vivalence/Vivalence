meta {
  name: removeOne
  type: http
  seq: 7
}

post {
  url: {{DAEMON}}/userspace/entities/thread/removeOne
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {}
  }
}
