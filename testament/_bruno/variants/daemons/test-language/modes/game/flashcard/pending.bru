meta {
  name: pending
  type: http
  seq: 1
}

post {
  url: {{GAME_FLASHCARD}}/emit/feed
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "recall": "LEARNING",
    "literals": []
  }
}
