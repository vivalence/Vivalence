meta {
  name: literals
  type: http
  seq: 1
}

post {
  url: {{GAME_FLASHCARD}}/emit/literals
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "recall": "LEARNING",
    "literals": []
  }
}
