meta {
  name: literals
  type: http
  seq: 1
}

post {
  url: {{GAME_WRITE}}/emit/literals
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "literal": {
      "slug": "chamar.verb"
    },
    "recall": "LEARNING"
  }
}

docs {
  emit a write buffer — free text production
}
