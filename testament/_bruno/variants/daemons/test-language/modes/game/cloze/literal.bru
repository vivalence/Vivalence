meta {
  name: literal
  type: http
  seq: 1
}

post {
  url: {{GAME_CLOZE}}/emit/literal
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "literal": {
      "slug": "chamar.verb"
    },
    "blankIndices": [0, 2],
    "gameplay": "type",
    "recall": "LEARNING"
  }
}
