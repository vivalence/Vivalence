meta {
  name: flashcards
  type: http
  seq: 1
}

post {
  url: {{TACTIC_TEST}}/emit/flashcards
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "batch": 3,
    "seek": {
      "symbols": [
        "word",
        "proficiency.survival"
      ]
    }
  }
}

docs {
  test tactic — emit flashcard buffers for review
}
