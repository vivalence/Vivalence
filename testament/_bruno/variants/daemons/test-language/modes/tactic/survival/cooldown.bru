meta {
  name: cooldown
  type: http
  seq: 5
}

post {
  url: {{TACTIC_SURVIVAL}}/emit/cooldown
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "batch": 4,
    "seek": {
      "symbols": [
        "word",
        "proficiency.survival"
      ]
    }
  }
}
