meta {
  name: buildup
  type: http
  seq: 2
}

post {
  url: {{TACTIC_SURVIVAL}}/emit/buildup
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "batch": 4,
    "seek": {
      "symbols": [
        "word",
        "proficiency.survival"
      ]
    }
  }
}

docs {
  survival buildup — conjugation paradigms, agreement chains
}
