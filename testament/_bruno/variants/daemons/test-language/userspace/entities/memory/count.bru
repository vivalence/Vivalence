meta {
  name: count
  type: http
  seq: 4
}

post {
  url: {{DAEMON}}/userspace/entities/memory/count
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {}
  }
}
