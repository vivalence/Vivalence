meta {
  name: update
  type: http
  seq: 5
}

post {
  url: {{DAEMON}}/userspace/entities/memory/update
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {},
    "data": {}
  }
}
