meta {
  name: removeOne
  type: http
  seq: 8
}

post {
  url: {{DAEMON}}/userspace/entities/trace/removeOne
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {}
  }
}
