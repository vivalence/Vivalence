meta {
  name: conversation
  type: http
  seq: 1
}

post {
  url: {{MODE}}/conversation
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "message": "Lets practice some conversations about fruits and other foods."
  }
}
