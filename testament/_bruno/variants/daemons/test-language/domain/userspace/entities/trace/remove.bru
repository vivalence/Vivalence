meta {
  name: remove
  type: http
  seq: 7
}

post {
  url: {{DAEMON}}/userspace/entities/trace/remove
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {}
  }
}
