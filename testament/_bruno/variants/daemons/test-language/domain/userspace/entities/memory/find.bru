meta {
  name: find
  type: http
  seq: 1
}

post {
  url: {{DAEMON}}/userspace/entities/memory/find
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "where": {},
    "options": {
      "populate": ["literal", "traces"], "limit":4
    
    }
  }
}
