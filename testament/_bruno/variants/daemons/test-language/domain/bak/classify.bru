meta {
  name: classify
  type: http
  seq: 6
}

post {
  url: {{DAEMON}}/classify/text
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "text": "María se mira en el espejo"
  }
}

docs {
  # get the weakest units
  MUST BE TESTED
  
  weakest by memory
}
