import { BufferView } from "@vivalence/typology";
const manifest = { type: "game", slug: "pick", traits: ["TERMINAL", "BUFFERED"] };
const buffer = new BufferView("buffer/Pick.svelte");
export { manifest, buffer };
