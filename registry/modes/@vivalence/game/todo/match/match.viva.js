import { View } from "@vivalence/typology";
const manifest = { type: "game", slug: "match", traits: ["TERMINAL", "BUFFERED"] };
const view = new View("buffer/match.svelte.js");
export { manifest, view };
