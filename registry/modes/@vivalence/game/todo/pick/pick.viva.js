import { View } from "@vivalence/typology";
const manifest = { type: "game", slug: "pick", traits: ["TERMINAL", "BUFFERED"] };
const view = new View("buffer/pick.svelte.js");
export { manifest, view };
