import { View } from "@vivalence/typology";
const manifest = { type: "game", slug: "shadow", traits: ["TERMINAL", "BUFFERED"] };
const view = new View("buffer/shadow.svelte.js");
export { manifest, view };
