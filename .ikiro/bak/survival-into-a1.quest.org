#+TITLE: ikiro/survival-into-a1 — SUPERSEDED by flatten-corpora
#+METHOD: ikiro/quest
#+DATE: 2026-04-29
#+STATUS: SUPERSEDED 2026-04-29 — Finn pivoted: "lets flatten this entirely. no more survival a1 a2. just brazilian." Replaced by `flatten-corpora.quest.org` (DONE same day).

* What changed

The original analysis here proposed merging survival INTO a1 (keeping the `english-to-brazilian-a1` name as destination). The end state Finn chose instead: a single flat kernel at `english-to-brazilian/`, with `survival/`, `a1/`, `a2/` all folding in. `vocalized` stays separate.

* Why the pivot was right

- Naming honesty: post-merge the kernel spans survival + a1 + a2 — no longer "a1". Keeping the suffix would mislead.
- Daemon refs were already pointing at survival anyway; migrating in-place preserved the active runtime.
- Survival had the higher-quality data (curated, just QA'd) — so survival became the canonical container with a1/a2 pulled in, not the other way around.
- a2 was small enough (72 verbs / 18 nouns) to absorb without ceremony, no reason to defer it.

* Carry-forward

The analysis numbers in this file (overlap stats, conflict count, lemma diff) seeded the merge plan. They are now historical record.

See `flatten-corpora.quest.org` for the executed plan + final state.
