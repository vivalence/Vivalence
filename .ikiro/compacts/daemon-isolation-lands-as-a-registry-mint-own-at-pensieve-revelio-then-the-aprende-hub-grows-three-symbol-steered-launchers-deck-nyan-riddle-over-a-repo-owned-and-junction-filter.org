#+title: daemon isolation lands as a registry mint (own at pensieve.revelio), then the aprende hub grows three symbol-steered launchers — deck · nyan · riddle — over a repo-owned $and junction filter
#+filetags: :daemon:isolation:pensieve:own:mint:aprende:launcher:deck:nyan:riddle:symbol-steering:constrain:mikroorm:junction:diagnostic-theater:

* the tail

Two arcs. (1) The two-standalone-daemon isolation bug — spiraled without landing last compact — got SOLVED: pensieve.revelio returns =own(module)=, a per-daemon mint at the registry boundary. FK dead, both daemons boot. (2) The slimmed aprende hub grew from one deck launcher into THREE symbol-steered launchers (Deck · Nyan · Riddle), all riding a repo-owned =constrain= that turns a flat symbol-slug array into a MikroORM =$and= junction filter. One open bug diagnosed-not-built (hardcoded language label). One callout (diagnostic-theater).

* isolation LANDS — the registry mint

Picks up the unresolved spiral. beef:
#+begin_quote
so. we need to ensure that every mode passed to a daemon is unique. how? i dont wnat manual cloning or shit like that. can we guarantee at the boundry of vip that whatever vip/pensive returns is standalone?
#+end_quote

Grounded it: each daemon Die runs its OWN =population.core → accioMap= (=systems/runtime/daemon/die.js:27=), so a per-=accio= clone isolates the two. Confirmed the leak: =Mode='s =Object.assign(this, module)= aliases =mode.app === module.app= (the ESM-cached singleton); APPLICATION/population then WRITE onto it (=mode.app.buffer=, =app.mount.from=, =app.withUrl=). Verified the descriptors are pure at accio-time (App/Freight/Vector/Bundle hold data + Paths + FUNCTIONS + child descriptors; =Bundle.bundler= is a fn → shared; no stateful-service instance as own data).

I framed it as the connoisseur's declaration/citizen split and recommended a generic =own()= at =revelio=. beef pushed:
#+begin_quote
no better way?
#+end_quote
I searched honestly and gave the real fork: ESM forces the descriptor INSTANCE shared, so either make it per-daemon (clone/mint) or stop mutating it (repoint every write to =mode.*= — the compact's old LAW, ~6 files, discipline-fragile). Reframed the mint as *not a footgun-copy* — =accio= = the MINT, pensieve = the mould in the vault, every reveal stamps a coin → collision structurally unreachable FOREVER, even against future writes. beef:
#+begin_quote
i hate a. but ok. build it into the registry.
#+end_quote

Landed in =subsystems/paladin/prototypes/pensieve.js=: =revelio → own(module)=. =own(value, seen)= — null/primitives/functions → share (behaviour + classes not cloned); =Map/Set/Array= → fresh container recurse (KEYS shared — edge patterns/input-schemas immutable); else =Object.create(getPrototypeOf)= + =Reflect.ownKeys= copy; =WeakMap= cycle-guard relinks =Vector.ancestor= to the clone. **LAW: clone MUTABLE descriptors, SHARE immutable leaves.** The old repoint-LAW is SUPERSEDED — zero runtime edits, because =mode.module= is itself per-daemon now.

** the two follow-on crashes (both mine, both fixed)

1. =mask.fill is not a function=. =own= deep-copied =app.mask= — a typebox =enhance()= **Proxy** (=schematics/v.js=) whose =.fill/.cast= live in the =get= TRAP, not as own props; structural copy strips the trap. Fix: =if ("~kind" in value) return value= — typebox schemas are immutable, SHARE them (also dodges the non-enumerable =~kind=-drop footgun). Verified 20/20 hermetic (=scratchpad/own.test.js=).
2. Boot =Module 404 {type:"ontology", slug:"word"}=. NOT the mint — stale =testament/variant/education.js= kernel: the =ontology→topology= / =corpus→topography= rename never propagated. Fixed 8 lines against live registrations.

* the aprende hub → THREE launchers

Slimmed-aprende (deck launcher, prior compact) grew. All three panels live in =registry/education/modes/home/aprende/buffer/Actions.svelte=, one shared =fire(route,input)= helper (snapshot =$buffers= → emit → jump to fresh; empty → inline note).

** the steering primitive — repo owns the $and filter

The launcher passes a FLAT symbol-slug array. beef, on where the =$and= build belongs:
#+begin_quote
done by repo.
#+end_quote
=registry/education/domain/entities/kernel/Literal.ts= → =constrain(where)=: =where.symbols= slug-array → =$and:[...symbols.map(slug=>({symbols:{slug}}))]= (empty array → STRIP, else ={symbols:[]}= matches nothing — a silent-empty footgun). Wired into feed→novel/due + byStrength/byLastSignal/sample. **MikroORM emits SEPARATE join aliases per =$and= branch** (=left join symbol_literals s2→Symbol s1 … s4→s3 where s1.slug=? AND s3.slug=?=) → literal must carry ALL. Verified via a minimal 2-entity harness against the REAL spanish DB: word∧a1=792, word∧pos.verb=765, word∧sentence=0. NOT a collapsed single-join.

The DATA TRUTH that forced all this: the Literal =ontology= COLUMN is EMPTY on all 1699 spanish literals — ontology is a SYMBOL (=word=1231, =sentence=354, =conjugation=114). Every "ontology" filter is really a marker-symbol filter.

** Deck (green) · Nyan (pink) · Riddle (gold)

- **Deck** → =emit.deck=: reps·ontology·games(flashcard/write multi)·symbols(5 hardcoded/ontology). =emitter/deck.js= builds =where={symbols:[ontology,...symbols]}=, splits count across games, feeds each. Tutor tools repoint to =emit.deck=.
- **Nyan** → =emit.nyan= (RENAMED from =/activation=). beef on the first thin version:
  #+begin_quote
  give the nyan launcher another pass. too simple. doesnt match semantics. naming bad. thin on propr
  #+end_quote
  Redone: reps·ontology·**mode**(PLAIN/SUDDENDEATH — nyan's real GAMEPLAYS)·**layout**(block/river). =feed({symbols:[ontology]})= → =weightedSample= (=emitter/sample.js=) → =nyan.emit.literals({literals,gameplay,layout})=; extended the nyan game =/literals= to forward =layout=. Earlier beef had already simplified it: "simplify to one by feed. still sampled" (drop the weakness/due selector → =feed= = due+novel, always returns).
- **Riddle** → =emit.riddle → riddler.emit.riddle.fromSymbols=: subjects Days=domain.weekday / Months=domain.month / Numbers=word.part-of-speech.numeral · count(1–5). beef:
  #+begin_quote
  build a similar launcher for riddles with days of week and months and numbers
  #+end_quote
  LLM-driven (riddler = cortex/anthropic per cast). Two riddler-game fixes: =emitter.js:42= slug pool =({symbols:{slug:{$in}}})= (was the =$in= PK-trap, OR-semantics — pool draws from ANY subject), =:50= level sample =({ontology:"word"}→{symbols:["word"]})=. Spanish kernel now enables =game/{flashcard,write,nyan,riddler}=. WATCH: =emitter.js:108= still writes =buffer.symbols = input slugs= on create (slug-vs-id) — flagged, set to =[]= if create errors.

* the language bug — diagnosed, NOT built

beef (spanish flashcard labelled "PORTUGUÊS"):
#+begin_quote
why is the flashcards showing the wrong language?
#+end_quote
Systemic: every game view hardcodes =promptLabel = "Português"= (Flashcard/Write/Shadow/Match/Pick). The authored =statics.language= never reaches the client (=/metadata= ships no statics; client =Daemon= has no =language=, only =slug=). Proposed a system-level =Daemon.language= slug-resolver, no per-view maps. beef did not =go= — moved to the riddle launcher. Captured in [[project_language_label_hardcoded]].

* process

- **CALLOUT — diagnostic-theater** (logged): beef "what JJ should i run??? dont fucking tell nme i need to do some ominous fucking thing. retard" — I wrapped a diff/timestamp diagnostic in suspense narration ("the anomaly is stark," "run these to see the op that did it") instead of handing =jj diff= / =jj op log=. Rule: blocked-tool request → FIRST line is the runnable command, no theater. AND I chased the wrong log line — the =jj op restore= I pinned was 2 months old, NOT the cause; owned it flat.
- **the =//= gate finally RAN, clean.** comment-litter is a strike-3 family whose MECHANICAL gate ("grep touched files for =//= before reporting") I again did NOT self-fire — only during this ikiro audit. Result clean: my new code (own/constrain/deck/nyan/Actions/riddler-fixes) has ZERO authored comments; every =//= hit is pre-existing. Enforcement gap persists (a remembered gate is not a gate); no litter shipped.
- **=constrain= was reverted mid-session** by an old working-copy op and RE-APPLIED (method + guard + 5 sites). If it vanishes again a live process eats it.
- **VCS stayed read-only** throughout (git hook-blocked; proposed =jj= for beef's =!=). Kernel VCS law honoured.

* state at close

- **Landed + verified:** the mint (=own= at pensieve, 20/20 + registry suites green) · testament rename · deck/nyan/riddle launchers · =constrain= (query-layer proven) · riddler slug/ontology fixes · nyan =/literals= layout.
- **Open:** language-label fix (proposed, un-go'd) · riddler =emitter.js:108= slug-write watch · dynamic symbol chips (still hardcoded) · conjugation ontology (games hard-filter the dead column).
- **Cleanup:** =emitter/index.js= commented dead-imports removed. **DEAD files for beef to =rm= via =!=:** =registry/education/modes/home/aprende/emitter/{flashcard,translation,drill,activation}.js= (superseded by deck/nyan/riddle).
- **All live confirms are beef's click** — everything short of the browser is green; the daemon watcher restarts on each save.
