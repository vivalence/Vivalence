#+TITLE: M11 landed — the deep cut: every fork, failure, and fix of the arc that took the package settlement from tangled design to green live system, at decision-mechanics fidelity

#+TOPIC: Deeper re-cut of the-blast-lands-forks-7-through-11-and-the-testament-resettles.org, written on beef's "run another deeper ikiro compact" — same arc, higher fidelity, plus the post-compact turns (registry/simulation interrogation, localhost re-homing, simulation cut). The shallow compact remains the fast index; this one carries the reasoning chains.

* 1 — ② baseline (beef: "load up m11 packages and run our first blast testing. run the runtime, client, check the testament / variant, check chrome, run all tests. re-cast specific snapshots of entities that we will change - ie around paladin and from runtime perspective.")
Mechanics that mattered:
- Every workspace test task carries =--watch= (never exits) — ran raw =deno test= with directory discovery instead; that swept in =tests/bak/= fossils, excluded via =--ignore=. Paladin's task uniquely lacks =--no-check= (75 stale ambient mikro-orm TS errors) — added like every sibling.
- A one-pass import-resolution scan (extract relative imports per test file, stat the target) mapped ALL fossils at once instead of iterating failures: ghost ={parse,resolve,shellsignal,processes}.test.js=, kajuit ×8 (=lifecycle-vector=, =scenario/{daemon,buffer,daemon.wafer}=, =daemon/{dataspace,daemon.wafer}=, =typology/{trait-runner,thread.conversational}=).
- BASELINE = 38 via the quest's own eval (mount 4 tier-branches, triple-nested pensieve walk). Pasted into the count tangle same turn.
- Runtime "dead" diagnosis was WRONG: all my probe paths 404'd, but =/daemon/brazilian/entities/literal/find= → 401 — auth-gated ≠ down; bare curl lacks the transport chain's auth. I had already killed beef's watcher (pid) on the false diagnosis — relaunched identically, confessed in-quest and in-chat. The REAL runtime red: integration test expects =game/{exhibit,pick,match}= emits; beef had commented those modes out of the daemon declaration — expectation drift, not infra.
- Paladin reds recorded: =System.lock= =locks(slug)= listing =[undefined, undefined]= vs expected =["kajuit","runtime"]=, and =System.log= printing ="log(type, slug) LEGACY"=.
- Snapshots re-cast: =paladin-variant= (pre-change: =owner: "@vivalence"=, no packages field), =paladin-scope= (pre-dissolution paths), full runtime aperture+daemon set. =jj diff= (read-only) showed snapshots aren't VCS-tracked — the files themselves ARE the baseline.
- testament disk state verified against the dissolution mv's assumptions (mountpoint under variant/ — script moves it ✓); instances.json carried one stale =testament/temp= entry.
- kajuit vite binds IPv4 only — curl to =localhost:1794= hit =::1= and failed; =127.0.0.1= 200. This detail returns as the CORS story later.

* 2 — gate-radius vs change-radius (beef: "you run the runtime! what is this issue … i think that sounds inside radius?! tell me.")
Grounding chain: file mtimes proved the tests are the OLDER spec and the System rewrite that followed went process-first — =system.js= line 30 was literally =log(type, slug) { console.log("log(type, slug) LEGACY"); }= with =// import { Log } from "./log.js";= above it and NO log.js anywhere in the repo; =locks()= spread =read()= (no process field) under keys ={type, slug}=. Distinction delivered: M11's CHANGE never touches System code, but ④'s GATE is "full paladin/test green" — a suite that starts red can't certify the bracket (law 9). Offered three options (fold into ④ / pre-bracket mini-bracket / quarantine), recommended folding; the vocabulary call (instance/process vs type/slug) flagged as blocking.

* 3 — the eight-question turn
1. =hydrate= (variant.js): function leaf → call it, array/object → recurse, primitive → pass; the lazy-thunk unwrap that lets variant cakes import before env exists.
2. Full-file =@beef= re-grep found ONE unreconciled inline: *"move this to paladin. or maybe if the conoisseur can pull this off elegantly, then into typology. but paladin default."* Typology REJECTED structurally, not aesthetically: =index.client.js= ships into browser bundles; =Deno.env.get("INIT_CWD")=/=Deno.cwd()= have no client-safe form. Landed as =belt/source.js= installer → =paladin.source(reference)=; four styles: ={file, source}= object (fromFileUrl+dirname), absolute (isAbsolute), =./=/=../= (INIT_CWD ?? PWD ?? cwd), bare segment (=scope.repository.branch=).
3. Span fit (beef: *"i think we have this or a similar system now in the shape of ... typology / span ?! does that fit?"*) — YES, exact: ghost =mod.js:21= already carried =// pipe.tap(span=> paladin.system.pipe[...probably paladin.system.log()])=; =Span= has =json= getter (nature/timing/transport/transition/subject/fault/children) + =drain(pipe)=; the test's =append({ json: {...} })= consumes precisely that interface. =Log= = the ledger end of the telemetry pipe, both methods thin over existing belt (=state.jsonl= ensures parent dirs; =state.open= returns append/create handle).
4. Ledger-directory testing = system.test.js itself (belt json/jsonl round-trips, Lock write/read/alive, Instances write/merge/read — greens; the two reds were exactly the unlanded parts).
5. =prototypes/system/= exists — directory ={index,instances,lock,process,system}.js=; my earlier grep-warning had guessed a flat file.
6. What a lock IS: ghost =target.js= is the only live spec source — =CHILDREN = {runtime, kajuit}= (process kind) × instance (variant-mount dirname, the line still carrying beef's own =// @beef ugly and stupid=). Key =(instance, process)=; instance-first won: matches beef's =<variant>/<type>/<slug>= instinct, and =locks(instance)= ("what runs for THIS instance") is the question doctor asks — =locks(process-kind)= across instances answers nothing. A third axis only appears if daemons/services ever spawn as OS processes; the RECORD can grow it, the filename needn't.
7. *"if youre confident, then go on the tangle. tangle the blast"* — quest-tangling authorized.
8. FORK 7 (verbatim): *"i dont like the package name here: package: pkg. maybe this is one of those cases where we not name the think after its mode/module type. maybe owner was the better name. module type is package. but the key on manifest i think makes sense as 'owner'."* Ontology: =owner= = the manifest KEY (its value names a package); *package* = the module TYPE + the supply concept. Ripple was pure shrink — the typology half of the bracket VANISHED (manifest.js identifier, cast.lookup, is.lookup, schematics all already speak owner); pensieve's change collapsed to default→throw; vip stamps owner; =pkg= bindings + the reserved-word convention died unborn; fork 6's "pkg is taken" note went moot.
Also this turn: beef's standing rule *"notify me of any weird one hoc or adjustments youre making if shit dont work out as planned"* → =feedback_notify_adhoc_adjustments= memorized; two changelog entries found ORPHANED at EOF past the tangle (an earlier append error) and spliced back in date order; a fork-numbering collision (inserted supply-fork as 7, breaking ~15 existing fork-7 cross-refs) caught immediately — numbering is APPEND-order, not decision-order, noted in-quest.

* 4 — fork 8, the design boo-boo (beef verbatim, in full)
*"I think we've made the design boo boo. I think the source of packages should actually be the system ledger. I think variant should not declare its packages. The Paladin... or the the Pensive should rather throw an error if a package is loaded that isn't in the registry. But the population of the registry should happen elsewhere. It's similar to how there is multiple instances of Python packages, um, like casks and stuff like that per system. And so we to define those on the system level. Uh, there's some spray to this, some blast, but I think it's limited. Let the connoisseur have a pass against the architecture and tell me what he thinks and plan the blast of this."*
Connoisseur pass (persona file loaded): the catch IS the doctrine's own rule — "the false binary is the pair at design time … (declaration/record, supply/demand)"; the quest's locked section already said *system = supply, variant = demand*, and =variant.packages= put the supply declaration ON the demand object. flake.nix analogy retired as WRONG LAYER (flakes pin build inputs; the variant composes runtime demand) — pip/site-packages/Homebrew-cellar is the true analogy: registry dir = cellar, presence = installed, ghost install = pip, absent-package demand = ImportError. The supersession marked explicitly: beef's OWN earlier verbatim (*"NO! these must be defined as part of the variant!"*) kept beside the new one, later wins. Throw placement: Vip not Pensieve — canon #5 keeps =revelio= a pure Map descent; policy sits at the demand seam; two named failures (=package @x not supplied on this system= ≠ =Module 404=).
Refinement 1 (beef): *"i have a variant path at the root of the system and a ledger path. the Variant Path continues to do what it does, whereas the ledger path is from now on the directory that contains the lock files … and the instances.JSON. It now further contains a registry.json containing a record of package locations."* + refinement 2: *"a log directory and a lock directory"*. So the ledger file lands NOW, not deferred: =registry.json= = ARRAY of ABSOLUTE locations. Two judgment calls flagged for override, neither overridden: (a) array not name-keyed — a keyed record would be a second naming authority fighting derive/lock, names stay with the packages; (b) absolute paths are CORRECT for a per-machine ledger — pins, not portable declarations; =paladin.source= resolves them back (unparked after exactly one turn — its real consumer arrived). =Registry= class = Instances twin (read/write/seed); seed lists =scope.registry='s child dirs; =vip.supply()= = =system.mount()= (fn.once, self-priming) → =registry.read() ?? registry.seed(scope.registry)= → =mount(source(location))= per location. Ghost's chicken-and-egg dissolved BY CONSTRUCTION (the ledger is pre-variant) — beef's "same!" came true one round late.
FORK 9 (beef): *"lets rename the system directory to ledger. including the blast radius of env vars and further references to that folder/part of system."* Ghost's own comment already said "the system ledger" — emergent name. Boundary drawn and flagged: the FOLDER renames (=testament/ledger=, =VIVA_LEDGER_MOUNT=, scope key ="ledger"=, =scope.ledger= consumers); the ACTOR and the PROCESS keep their names (=System= prototype + =paladin.system=, =VIVA_SYSTEM_MODE/ROLE=, =viva system {init,doctor}=, =~/.viva= default) — "the system keeps its ledger". beef's check-in *"have we resolved my design bubu?"* answered with the point-for-point mapping + the honesty caveat: resolved in the QUEST, proven only when ④ runs green.

* 5 — "go": the blast, with all eight deviations at mechanism level
Execution: killed both watchers (ports quiet during filesystem surgery), awk-extracted all =:tangle= blocks (15 files), applied ~20 diff-hunks via Edit, ran the four SH scripts (tree split, circuitry inline, sweep, dissolution) with pre-flight ls-verification each.
1. Tangled files carried the quest's =// === CHANGE START/END ===​= reader-markers into LIVE code — perl-stripped from vip.js/pensieve.js post-tangle. (Convention gap: tangle-time strip should be part of the marker convention.)
2. =belt.source(this)= — the quest registered source.js in the belt BARREL but installers are invoked in the =Paladin= CONSTRUCTOR (=belt.read(this); belt.find(this); …=); one line added there.
3. =mv registry/playground registry/development= would have NESTED playground INSIDE development — the tangle pass had already created =development/= with its package.viva.js. Content-move (=mv registry/playground/* …= + rmdir) instead. General lesson: mv-scripts written before NEW-file tangles run can collide with the tangles' own mkdirp.
4. Secrets: =testament/.env= had ZERO =SECRET_= lines — the quest's assumption ("populate.env already reads SECRET_VIVA_* from testament/.env") was aspirational, and deleting secrets.jsonc cold = silently dead hallucinators at demand time. Folded the 5 real pairs via a deno one-liner (jsonc parse → append KEY="value" lines) so values never passed through assistant output, THEN =rm=. Keys remain leaked in VCS history — rotation still owed.
5. Circuitry inline: =tail -n +3= append produced DUPLICATE =export const services= in test.viva.js — pre-inline, the variant's own =services= and circuitry's =services= were merged at RESOLVE time (=[...(cake.services ?? []), ...(cake.circuitry?.services ?? [])]=); inlining removed the merge point. Arrays merged by hand (nlp-stanza joined multiplayer). multiplayer.viva.js had no collision (its circuitry exported only daemons).
6. Two variant cakes under one mount: dissolution put =test.viva.js= AND =localhost/= both under =registry/simulation/variant= = the VIVA_VARIANT_MOUNT — =variant.mount= throws on ≠1 cake (=find.type= walks recursively). First fix: =variant/test/= per-slug home. (Fork 10 later mooted it.)
7. THE bug: =Signature.branch(signature)= returns =new this.constructor(signature, this)= AND the parent's =absolute= getter folds =finn()= — branching MUTATES the parent's rendered path. Proven live: =root.absolute= before ="/tmp/demo"=, after =root.branch("package.viva.js")= → ="/tmp/demo/package.viva.js"= on the PARENT. Old code never saw it because every =paladin.scope.X= access resolves a FRESH Path through the conditional-resolver Proxy. This is deliberate chain semantics (how =new Signal("/a/b/c")= renders), slurp/share family — typology untouched (HOLY), vip.mount switched to string composition, memorized as =project_signature_branch_mutates=.
8. Sweep ordering: the lookup pass (=@vivalence/(domain|…)/ → @education/…=) ran over the test files BEFORE the deep-import path pass, so =registry/kernels/@vivalence/domain/…= had already become =registry/kernels/@education/domain/…= and the path pass's =@vivalence= patterns missed. Second perl pass with BOTH old and half-rewritten forms repaired. Lesson: sweep passes over the same files must be ordered most-specific-first (paths before bare lookups) or written idempotently against each other's output.
④ result after fixes: paladin 11 · runtime 26 (172 steps) · typology 104 · kajuit 8; ghost unchanged-red. ⑤ sweep: zero stray owner-defaults, zero =variant.packages=, zero un-swept lookups, wafers bak-only, testament derived-only.

* 6 — forks 11 and 10, mid-blast (beef verbatim, in full)
FORK 11: *"we dont hardcode package.viva.js !!!! what?!?! we do a recursive find of viva files to populate the registry. the packages have a manifest.type property that identifies a pacakge. wrong approach!!!"* — vip.mount rewritten: read ALL cakes from the walk first, =cakes.find((cake) => cake.manifest?.type === "package")= is the declaration, its =manifest.owner= (when authored) locks the whole mount, then register every cake with the stamp. No filename anywhere; the four =package.viva.js= names survive as pure convention. vip.test.js rewritten to the walk shape, + a new case: "a package cake WITHOUT owner keeps the derived stamp — presence alone locks nothing".
FORK 10: *"Let's talk about the, um, variant file that we want to have running next to... or inside of our testament folder next to the dot n file. For now, we only want to have the... we want to have basically the old variant bag with the demons and surfaces decomposed into two circuitries or two imported files. Circuitry, ignore that term. And I want to have one playground with all of the playground stuff we have been building, one education with all of the language learning stuff that we are building, and that's it for now."* — Landed as =test.viva.js= (manifest/runtime/clients/services + =daemons: [education, playground]=) importing =education.js= + =playground.js=. Education = slug =brazilian= (routes/tests/kajuit all speak =/daemon/brazilian= — the file is the domain, the slug is the instance), FULL set: 13 games + =tactic/clinic= + =tactic/five-fold-session= + =homepage/aprende= + =teacher/dewey= + =dashboard/dataspace=. Grounding bit back with M11's own lesson: the =tactic/survival/= DIRECTORY holds manifest slug =five-fold-session= (my first guess "@education/tactic/survival" 404'd at boot — dirs are cosmetic, manifests rule); both tactic manifests lacked =version= entirely (they had never been loaded live), added ="0.0.1"=. Spanish daemon block NOT carried — beef's "that's it for now"; VCS history keeps it. The ④-era commented-out integration describes (exhibit/pick/match/cloze) un-commented → GREEN against the live runtime. Watcher subtlety: after a failed boot, deno --watch sat unresponsive to file changes — needed a hard kill+relaunch, not a touch.
Circuitry eradication (beef): *"There is no circuitry's export. Doesn't exist. Nowhere. The only place where we had the term circuitry was inside of a variant, and there we deleted it. Shouldn't exist anymore."* — grep found five live survivals beyond the variant: schematics =primitives/circuitry.js= (Daemon/Service/ModuleSpec — body spliced into =primitives/variant.js= which already imported them, file deleted, barrel line dropped), paladin validate's =v.primitives.circuitry.*= → =v.primitives.variant.*=, runtime =/metadata/circuitry= → =/metadata/variant= (no client consumers — kajuit/drapes/sheets grep-clean), dead comments in scope.js/integrate.js/mask.js deleted. Grep-zero after; paladin+typology suites re-green.

* 7 — auth, CORS, and the policy line
beef supplied credentials in-chat ("beef biggusdickus") and later asked directly: *"Are you also capable of taking that … authentication code and writing it via Chrome and via the dev tools?"* — answer given: mechanically yes (javascript_tool writes localStorage), categorically declined — entering passwords/tokens to authenticate is the hard-prohibited class regardless of who supplies them; devtools injection = same act, different pane. Self-serve path handed over instead: console one-liner (fetch =/auth/login= → =localStorage.setItem(`lighthouse:${remote}`, {authority, identity})= → reload), grounded in =stores/lighthouse.js='s =hydrate()= shape. Daemon presence still proven "in chrome" via the auth-free =/metadata/daemons= aperture rendered in the tab: brazilian 23 routes + playground 6; =/metadata/variant= concurring. beef's failure hypotheses (*"maybe the user must be installed of you mishandled the mountpoints of lighthouse"*) both cleared: live API login SUCCESS + =mountpoint/service_multiplayer/{lighthouse.viva.db, tokens.json, migrations}= intact.
CORS: beef pasted the console error; diagnosis = =shard/cors.js:1= allowlist (=["localhost(:[0-9]+)?", "*.vivalence.com", …]=) has no =127.0.0.1= — and the failing tab sat on =127.0.0.1:1794= because I had opened it there (vite prints that address; my curl had failed on =localhost= via =::1=). beef doubted (*"i doubt thats the issue. i can login via firefox. or rather, i was already logged in there"*) — his own paste named the blocked origin; settled EMPIRICALLY with the two-origin preflight replay: 127 → 204 with NO allow-origin header; localhost → header present. *"ah. ok legalize it."* → ="127.0.0.1"= added bare (the pattern builder appends the =(:[0-9]+)?= itself — the localhost entry's explicit group is redundant), hot-reload verified live. Secondary smell logged: =middleware.js next() called multiple times= fires in the transport chain's hard-failure path, masking the real error — wants its own bracket.

* 8 — the testament resettles, twice
beef pasted the full testament tree: *"i think you worked uncleean in the testament. remove the old variant surgically and slice in the new variant. the new variant is what you currently have at /testment/*.js|.env etc. they even have their own mountpoint. not wanted."* — cut: =variant/bak= (old circuitry cakes), =mountpoint/daemon_spanish=, =mountpoint/daemon_typing-practice= (dead daemons' own mountpoints), stale =*_temp.lock= pair, stale =testament/temp= instances entry (ledger → ={}=). The gitignored sqlite DBs are unrecoverable — tarred to scratchpad first as session-lifetime insurance, per the standing "never delete DBs" codemap warning meeting an explicit instruction. Left alone + said so: =docker/=, =_bruno*=, the four live mountpoints. Mid-cut beef took the ports back (*"stop the runtime process"* + AddrInUse paste, then the kajuit port collision) — both my watchers killed, ports surrendered; beef runs dev processes himself now.
Second resettle: *"Move all of these … into this directory!"* (environment, mountpoint, .env, education.js, playground.js, test.viva.js → =testament/variant/=). Executed + references updated: 8 deno task strings (root ghost/run+watch; runtime run/watch/test; ghost run/watch; kajuit watch) → =testament/variant/.env=; =.env= values (=VIVA_VARIANT_MOUNT= → =testament/variant=, =VIVA_MOUNTPOINT_MOUNT= → =testament/variant/mountpoint=; ledger untouched at testament root); =.env.example= same + copy-comment. =scope.environment= needs no line (defaults =variant.branch("environment")=). Paladin suite green against the new paths. beef had separately deleted =_bruno.bak=, =docker/=, =temp.js/jsx= himself.

* 9 — post-compact: simulation interrogated, then cut
beef: *"is /Users/finn/vivalence/code/vivalence/registry/simulation read anywhere?"* → three real readers (the ledger record → every =vip.supply()= boot; =registry.count.test.js='s localhost assertion; =wafer.test.js='s disk path), everything else in-memory fake strings; beef: *"except here: …/testament/ledger/registry.json"* — confirmed the ledger is the ONLY live coupling, consequences of pruning enumerated. Then: *"i think we can move the localhost here: …/registry/viva/variant"* → localhost re-homed beside multiplayer (derives =@viva= from the mount, its stale owner field was already dropped at the blast); wafer.test path + count-test assertion updated; green. Then: *"and cut simulation for now."* → =registry/simulation= deleted (only its package cake remained), =registry.json= deleted to reseed (self-priming — the next supply seeded ={development, education, viva}=; =wafers/= had vanished from disk by beef's own hand so the reseed dropped it for free), count test → BASELINE + 5, green. THREE packages stand. Loose end flagged to beef, unanswered: =test.viva.js= still declares =manifest.owner: "@simulation"= — decorative (the testament variant never enters the pensieve) but the last live =@simulation= string; =@viva= or drop the field.

* Resumption
M11 LANDED; three-package registry ={development, education, viva}=; testament = ={ledger/, variant/{.env, test.viva.js, education.js, playground.js, environment/, mountpoint/}, _bruno/}=; ledger self-reseeds. Open, all beef's to call:
- ~test.viva.js owner: "@simulation"~ RESOLVED post-compact — beef retard-called carrying it as a question ("compacts should clear dangling tangles like this if there is a clear cut rule that applies! like no loose ends"): field dropped (dead reference, zero consumers, schema-optional), paladin 11 green; the compact-settlement rule is now in zettelkasten Callouts;
- fork 4 (lock-demo residence) + fork 6 ("cake" rename, leaning =crate=) — parked by design;
- key ROTATION for the 4 leaked API secrets (VCS history holds them regardless of the file deletion);
- =middleware.js= double-=next()= on transport hard-failure — masks real errors;
- ghost suite wholly red (pre-existing shell→ghost WIP);
- 11.4 remnant: Dockerfile still expects a =registry/simulation/variant= bake — the variant now lives in gitignored =testament/variant=, so the DOCKER story needs a fresh decision (bake the variant in? mount it? the fresh-clone/tracked-variant win was reversed by fork 10);
- quest tangle blocks for vip.js / test.viva.js / the scenario table drifted from landed reality during forks 10/11 — changelog records everything; a resync pass is cheap if the quest should stay regenerable;
- the four =package.viva.js= self-manifests still exist as convention-named package cakes; =registry/development/package.viva.js= etc. survive, =registry/simulation='s died with the cut.
