#+TITLE: env taxonomy → repository self-location → slp=slop → variant/system as mounted paladin prototypes → belt grows full fs surface (read/state json+jsonl) → System as Path-backed factory (Lock/Log/Instances) → lifecycle refactor (resolve-phase mounts, environment+validate folded into variant.mount)
#+TOPIC: subsystems/paladin · belt read/state · prototypes System/Variant/Vip · fn.once mount · scope self-location via import.meta.url · ghost trajectories · specimen workpackage test · zettel const-alias rule

* Arc

** env taxonomy + repository question

Started: temp.js at root importing paladin, logging env, + deno command. Finn edited it to import `@vivalence/paladin` and log `paladin.env` + `paladin.secret`.

User: =what env variables do i have and which are system level vs variant level? read through: .env testament/variant wafer/variants/* paladin runtime dockerfiles client=

Mapped two routing axes: by-prefix (`VIVA_`/`PUBLIC_VIVA_`→`paladin.env`, `SECRET_VIVA_`→`paladin.secret`, file-routed in `environment()`) and by-source. Finn's own three-tier taxonomy (sharper than mine), logged in =private/logs=: machine-bootstrap (SYSTEM/REPOSITORY/VARIANT_MOUNT) → variant declares instance (the hinge) → instance derives (ENVIRONMENT/MOUNTPOINT/SERVE/REMOTE/SECRETS). Only `VIVA_SYSTEM_MODE`+`VIVA_SYSTEM_ROLE` hard-required.

User: =where am i using the repository mount outside paladin?= → real sites: `runtime/daemon/traits/buffered.js`, ghost `run.js`/`trajectories/instance/{start,run}.js`. Mostly ghost (registry resolution).

** scopes written late + repository self-location

User (on belt/scope.js): =is it setup to write a scope later in its lifecycle? ie ... not by the paladin itself but by the ghost shell or runtime=

Yes — `paladin.scopes([...])` is a public registrar (Map setter), overwrite-warns-but-proceeds, lazy resolvers (closures eval on every `.scope.x` get). So late/async scope registration + overwrite is the intended shape; ghost/runtime can extend.

User (on the install/repo-root problem, from =private/logs= dump): =the difference is that the dockerfile knows where the repo is because it writes it there. i dont control where the repo is written outside my own docker image. so. how do i know the repo root?=

Answer: `import.meta.url` — paladin is a workspace member loaded from source, so its module URL IS `<repo>/subsystems/paladin/...`. Self-locate, env as override.

User: =implement this= (the fixed-offset variant). Applied to `populate.js` repository scope:
#+begin_src js
new Path(paladin.env.get("VIVA_REPOSITORY_MOUNT") ?? new URL("../../../", import.meta.url).pathname)
#+end_src
Verified: env-stripped → resolves repo root; registry derives from it; env present → wins.

** slp = slop

On the ghost spawn snippet (`trajectories/instance/start.js`, `deno task --config repo/deno.jsonc`): asked =is this teh right way to do it?= → dispatched 3 agents over ghost/shell history. I framed the comparison around `systems/ghost/slp/`.

User: =slp/ slp means slop. its shit you wrote that i sorted out. remeber this!=

Saved memory =project_ghost_slp_is_slop= — `slp/` is AI cleanup residue, NOT canonical; `trajectories/` is the real ghost. (The macOS detach fact — `.unref()` children die on parent exit, `nohup … </dev/null &` orphans to init — is a real OS constraint regardless of where slop put it.)

** variant → belt tool → prototype

User: =look at the role of the variant on paladin and think about a way to attach it to paladin belt.= (+ reread =private/logs=)

`resolve.js::variant()` already carried Finn's `@beef`: =this function needs to become a tool in belt. lifecycle decides to invoke it depending on some criteria - like env var presence. same for system and repository.= Decisions from the log dump: =fn.once gate on the handler=; =i dont write to env. absence is information.=; overwrite a scope, stateful paladin; =what variant is loaded default for the $viva cli? none ideally.=

User: =fn.once is a feature. not a bug. no remounts should be possible.= and on `populate.system` gaining `.mount()`: =yes. do.=

After a "more is more" 7-file proposal: =you opted for the more is more approach i see. lets try again with the less is more approach.= Then: =ok try again. but this time lets thing of the system and variant as prototypes. say we move those two from belt to prototypes.=

Key realization: `Vip` is ALREADY this pattern — `class Vip { constructor(paladin) async mount(mount) }`. So System + Variant become **siblings of Vip**: `(paladin)`-constructed prototypes with `fn.once` mount, constructed in the Paladin constructor.

** #mask, testing, mothy tests

User: =is this critical?= (the #mask mountpoint-branching) → logic critical (variant.test asserts "daemons are Masks with mount paths"), private-method form is taste.

User: =refactor this into a proper prototype worthy of typology membership. run the paladin testing.= Test state: variant+pensieve green; paladin.test/vip/tools red (stale circuitry field, .env autoload, dead `@vivalence/paladin/prototype` import).

User: =remove #mask in paladin. clear out old testing. i expected this to be mothy. move to bak dir. refactor the variant class thusly.= → moved all 6 tests to `tests/bak/`; stripped #mask. Then: =move mask back in.=

** migrate system, run the stack

User: =migrate system to prototype too.= → `prototypes/system.js` class, `belt/system.js` deleted, constructor news up System+Variant, `populate.system` → `paladin.system.mount()`.

User: =run this intermediate state. validate systems and variant are workign. then migrate variant= + =run a client and a runtime /watch=.

Migrated variant invoke (`mod.js`/`wafer.js` → `variant.mount()`, deleted `resolve.variant` body). Caught + restored my own regression: dropped the `marker.circuitry?.daemons/services` merge thinking circuitry was dead — the testament marker USES it (brazilian daemon, nlp-stanza service come from circuitry). Booted `runtime/watch` + `kajuit/watch`: both ALIVE (2501 / 1794). Fixed a pre-existing TDZ in `buffered.js` (`paladin` read line 9, `const paladin` line 11) blocking runtime boot — reordered import above the guard.

** decompose System; cut by consumer; use the tools

User: =lets say we deconstruct instances locks logs as subclasses in their own right? ... use this as an opportunity to cut elegantly. this about boundries and what the consumer wants.=

Consumer-surface cut (grepped ghost): `instances.list/remove` dead → cut; `locks.read` internal-only → privatize; `logs.spans/child/tail` dead → cut. No subclass needs `paladin` — Instances needs a file, Locks a dir, Logs a dir+slug.

User: =youre again reimplementing behavior. we have tools. a lot of tools. identify all teh cases where your current implementation does a job already done elsewhere.= → `fs.ensureDir`→`state.dir`; try/catch JSON.parse→`read.json`; `join`/`#lockPath`/`#spansFile`→`Path.branch`; `.absolute` at call sites redundant (read/state resolve Paths).

On the append-write: =we have that. also, whats jsonl?? ... maybe this needs to be handled by state if its something we want.= Then: =i suspect jsonl is line separated lists of json. in which case, yes we need it. ust like jsonc and json.=

So jsonl became a format peer of json — reader + writer in the belt:
#+begin_example
read.json  ↔ state.json    (whole object: jsonc.parse / pretty write)
read.jsonl ↔ state.jsonl   (line list: parse-per-line / append-one-line)
#+end_example

User: =very good. this looks excellent. one note: inside belt the "config" is "paladin".= [PRAISED — the belt-growth + state-symmetry diff]

** applied: belt + System factory + ghost patch + test

User: =go= / =all 3= / =test accoring to typological ikiro testing=.

Belt grown (param `config`→`paladin`):
- =read= = {file, text, viva, json, jsonl, module} — json/jsonl gain a fallback arg
- =state= = {dir, json, jsonl, remove, open} — writes self-ensure their parent (`dirname` + ensureDir), so callers never `dir` separately

`prototypes/system.js` — `System` is a factory of Path-backed prototypes:
- `lock(slug, process)` → `Lock` (write/read/remove/alive); `log(slug)` → `Log` (append/open); `instances` singleton store (read/write); `locks(slug)` lister
- zero `#` path helpers, zero raw `Deno.*` except `Deno.kill` (process) + one `Deno.readDir` (lock list) — both non-fs-json, no belt home
- `mount` lost all `state.dir` calls

Ghost trajectories patched to factory API: `locks.list(s)`→`locks(s)`, `locks.write(s,p,r)`→`lock(s,p).write(r)`, `logs.open(s,p,st)`→`log(s).open(p,st)`. `instances.read/write` unchanged.

Test: `tests/workpackage/system.test.js` (specimen, novel-only) — 14 steps green. belt json/jsonl roundtrip + fallback, lock write/read/alive/list, instances stamp+merge, log append+open.

** lifecycle overhaul

User: =what would we CURRENTLY gain from mounting system earliest in the boot lifecycle?= → almost nothing; system home holds only runtime artifacts, no boot-time config. Only gain: `system.pipe` existing earlier (traceable boot). The `$HOME` comment has no current payload.

User: =export async function environment(paladin): this belongs into hte sscope of variant! part of it is structural and belong on the prototype, others into the mount function. makes sense?= → yes; environment is variant-derived config-loading. Then: =i think an overhaul of the paladin lifecycle makes sense. default: we dont touch it. but, what must change now that sytem and variant are mounted prototypes=.

User (the plan): =move the system function into resolve. make mounting conditional on the scope presence of system. pair that with variant and veryimportantpackage. refactor environemnt into variant. propose a full diff.=

User: =validate is ALSO part of mounting the variant!= → fold `integrate.validate` into variant too.

User: =this.#validate(); this is code smell. i dont like those #variables. they tell me something isnt well abstracted or embedded.= + =async #environment() too. here is the plan. look for a way to create separate boot functions for each of these domains. a la: await resolve.veryimportant(paladin). rebuild the mount functoins as just a few subfunction calls passing (this).=

Result — `Variant.mount` = three module-level domain functions taking the variant (fractal with the paladin lifecycle, one level down), NO `#` methods:
#+begin_src js
async mount() {
  await environment(this); // env/secret first — marker modules read them at import time
  await resolve(this);
  validate(this);
  return this;
}
#+end_src

User: =go= → applied 6 files.

** the lifecycle now

#+begin_example
construct:  (empty)
populate:   env · scopes
/base
resolve:    system.mount  ·  veryimportant(construct vip)  ·  variant.mount[environment·marker·validate]
integrate:  statements · publish
#+end_example

- `lifecycle/resolve.js` = three conditional mount-triggers: `system` (scope.system), `veryimportant` (is.veryimportant → `new Vip`), `variant` (scope.variant)
- `lifecycle/populate.js` = env + scopes only (environment/veryimportantpackage/system deleted; questions commented)
- `lifecycle/integrate.js` = statements + publish (validate deleted → in variant; secure unused)
- `mod.js` ikiro is the real resolve runner (invoke stops at `/base`); `wafer.js /resolve` mirrors for `/full`
- Timing shift: `system.mount` + vip now resolve in `ikiro` (post-`/base`). `paladin.system`/`paladin.variant` instances exist from the constructor; only their `.mount()` defers. Consumers already `await paladin.ikiro`.

Caught at boot: Url-at-import ordering bug. `testament/variant/circuitry.js` builds `Url`s from env vars AT IMPORT TIME. My first `mount` did `resolve` (imports marker) before `environment` (loads env) → `Url(undefined)` crash. Fix: **environment before resolve** in mount. Verified clean: variant (`test-runtime`, brazilian, multiplayer+nlp-stanza), env+secret loaded, vip constructed, system factory live, runtime+kajuit ALIVE.

** testing run

User: =testing runnign?= → `tests/workpackage/` 14/14 green. `deno test tests/` recurses into `tests/bak/` and dies on dead `tools.test.js` (`@vivalence/paladin/prototype` gone).

* Final state

- `belt/read.js` + `belt/state.js` — full fs surface, format-symmetric (json/jsonl), param `paladin`, writes self-ensure parents
- `prototypes/{system,variant}.js` — `(paladin)`-constructed, `fn.once` mount, siblings of Vip
- `System` = Path-backed factory (Lock/Log/Instances), IO entirely through belt
- `Variant.mount` = `environment → resolve → validate` subfunctions (order load-bearing), no `#` methods
- lifecycle = construct/populate(env+scopes)/resolve(3 mounts)/integrate(statements+publish)
- repository scope self-locates via `import.meta.url`; slp memory saved
- tests in `tests/workpackage/`; old in `tests/bak/`

* Open / next

- `deno.jsonc` `paladin/test` task globs miss `tests/workpackage/**` or hit dead `tests/bak/`. Point task at workpackage (or delete bak).
- `Variant.mount` ordering is implicit-coupled to marker reading env at import — fragile; the marker-builds-Url-at-import pattern is the smell, not the mount order.
- `integrate.secure` unused (deletes paladin.secret) — dead?
- system-mount-earliest only buys traced boot today; revisit if machine-level config lands in `~/.viva`.
- ghost `trajectories/instance/start.js` still `.unref()` detach (the macOS-fragile path) + `deno task --config repo/deno.jsonc` re-entry (install coupling) — untouched this session.
