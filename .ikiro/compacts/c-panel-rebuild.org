#+TITLE: C panel rebuild — D/E/F panels from the ground up, the /metadata self-description surface (Approach B, runtime·daemon·mode), and the masked·aimed·queueing trait controls
#+TOPIC: 4-photo E/F/D spec → explore (no code) → deduce daemon+typology changes → "style nazi = CODING style not css, no shared sections" → pass-1 execute (create a dataspace buffer, render in A via Frame) → bug pass (object-object tab, e-bg, reactive threads list, optimistic delete) → masked/aimed/queueing → /metadata aggregation: collect every strip-wire/handshake instance → ideate → "/metadata/[topic]", view-not-buffered, no index routes, manifest-sans-modes → two approaches proposed → B (central daemon/aperture/metadata.js + runtime in lifecycle/resolve.js) → slug-from-path-hack callout → comment-don't-delete → buffer.mode.view not buffer.view → full consumer migration → lighthouse-is-a-service 404 → reverts → multiple widgets (Set) → reactive mode-replace via thread.$mode atom → mode-traits-not-thread-traits → widget self-id heads → dot indicator + label-auto-follow

* Arc

** The spec — E/F/D, four notebook photos

User (long): =first step. rewrite the E panel.= three sections — top displays active daemon/thread/mode (=terminal.$thread=, =.$mode=, =.$daemon=), reading the mode manifest thread label (reactive) and the mode's traits; middle controls traits via a TraitTagRow of manually-written TraitTags (=the labeled trait is always active=, others toggle via a TraitTagIcon, body-click opens the trait's widget); bottom (fixed, bottom-tugged) manages intent. =add a debounce … say 5 seconds=.

=second step: rewrite f panel.= factory (create if !queueing/!aimed; start→Stall if queueing; call emitter if aimed) + active buffer + buffer list with rearrange/delete/activate/open/clear. The masked widget renders a json *form* from a schema — =if the thread is aimed, we stripwire the emitter thats aimed at and use that as the form mask. if the thread is not aimed, we use mode.view schema.= Two complexities: switch the schema reactively by config, and render a reactive form from the schema.

=while we are at it, lets also do a pass on the d panel.= keep tree (click mode → thread), add threads-per-daemon (load / middle-click new terminal / double-click quickstart) + intents-per-daemon (render only).

=read ikiro= then =go= (effort max). Then immediately: =for now, just exploration. no code yet. we do that step by step.= and =deduce any changes we must affect on the daemon side and typology especially.= and =also ask questions about technical details … i am a style nazi! part of the reason we are rebuliding is because of style!=

Dispatched explorers across kajuit client + typology entities + shape/schema/Stall. Deduced: =thread.$buffers= referenced everywhere but defined nowhere; client thread traits index fully commented (applyTraits a no-op); aimed.js =object= import commented (would ReferenceError); mode view schema served by =/buffered=, emitter strip NOT exposed (the masked-aimed gap). Producer /buffered = ={url, schema: mode.cake.view.mask}=.

** "style nazi" clarified + no shared sections

User: =no shared sections. each of those panels will feature its own section logic style and behavior=. Then the correction: =when i say style nazi i mean CODING STYLE. not css style. just immitate what we already have in terms of approaches. css not important. match coding style!!! read teh runtime and subsystems/typology for style guide.=

→ Saved =project_efd_panel_rebuild= memory. Coding idioms locked: free-function lifecycle over data (=boot(x)=, =populate(x)=), =$field= atom + vanilla get/set, traits as =(entity,ctx)=>finalizer=, dossier middleware, gestalten primitives over hand-rolling, barrel imports, Svelte-5 runes, no comments/abbrevs.

** Pass-1 execute — "create a dataspace buffer and have it rendered in a panel"

User: =ok. execute the whole thing from start to finish. i want to be able create a dataspace buffer and have it rendered in a panel. you have chrome available.=

Render path: A-panel drapes =Frame= → =import(buffer.view.url)= mounts the mode's bundled view. dataspace = =View("Dashboard.svelte")=, empty mask. Shipped: belt =debounce=; thread dossier (resolve mode entity + LABELED + =$buffers= computed); buffer dossier (view from mode repo); skins =trait/{TraitTag,TraitTagRow,TraitTagIcon}= + =Labeled=; rewrote a/e/f/d. Build green; verified routes serve correct shapes in isolation.

** Bug pass (live)

- Tab showed =[object object]= — =Tab.svelte= rendered the now-object label raw → ={$label?.name ?? $label}=.
- =change back the background color of e panel= → e-panel surface skeleton-3 → skeleton-2.
- =why is dataspaceSSSS not showing up the the past list of threads reactively??= — THREADS list was a one-shot snapshot. Fix: derive from repo =$entities= (=.subscribe(recompute)=) + per-row =ThreadLabel= subscribing =$label=.
- =delete is not working= — row stayed; the await on server =removeOne= rejected so the local =drop= was skipped. Fix: optimistic =repo.drop()= first + log; same for clear.

** masked/aimed/queueing — scope creep, then design-first

User: =now work out the queueing masked and aimed traits. go back to my original text and the photos for help.= then =actually, instead of just masked, lets include aimed and queueing.= then =prep= =think= =design=. Reinspected: MASKED tag = always-on; "Mask" = schema, =thread.trait.MASKED= = the data; "Buffer Mask vs Emitter Mask, same widget different target."

Decisions (AskUserQuestion): masked form → *E trait bar* (all four widgets in E); emitter mode = =just flashcards. thats our dummy plaything=; the /emitter strip route → the next arc.

** /metadata — aggregate the strip-wire + handshake pattern

User: =lets aggregate all these strip wire and metadata handshake stuff. we have something like this on the daemon. we have this on the modes … find all the instances where we have this.= then =ideate patterns to aggregate them.= =maybe we add /metadata endpoints.= =maybe nested /metadata/[topic] ?!= =or another pattern.= then =go. before we chose, collect=.

Collected: producer routes (=/manifest= =/cargo= =/datamap= =/buffered= =/freight= =/capabilities= =/modes=) + the shape compilers (strip/messenger/object/agentic/mcp/tree/flat/http/subscriber) + the live =/handshake/open= strip↔messenger handshake in =conversation.js= + the 5 client consumers. Recognized the =herald= quest = this exact aggregation.

User's plan: ="/metadata/[manifest|view(instead of buffered)|…]"=; daemon /metadata for manifest+cargo+aperture-strip+modes; each mode /metadata for its aperture sliver + emitter + view; =emitter and view feature schema.= Refinements: =no on these two [the /metadata index + mode /metadata index] … this is system level wiring.= and =GET /…/metadata/manifest - doesnt need to contain modes.=

=propose two approaches to the producer side. one distributed where we attach as we go, another where all the metadata handling lives inside the daemon/aperture.= Proposed A (trait-distributed) vs B (one central module). =also do another pass for producer on runtime level. runtime /metadata about daemons, circuitry, services, etc.=

User: =b.= → =systems/runtime/daemon/aperture/metadata.js= for daemon, =runtime's metadat inside lifecycle/resolve.js=. Shipped ADDITIVE (old routes kept) so the live daemon stays green. Verified the surface in isolation (fake die) — daemon manifest/cargo/modes/aperture + mode manifest/view/emitter all serve; view+emitter carry schema. Note: =shape.strip= drops the leading slash (leaf nature ="feed"= not ="/feed"=).

** Callouts (corrections — no "retard" codeword this session, so no zettelkasten log)

- =97 + slug: daemonDie.good.mount.nature.replace("/daemon/", "")?? are you STUPID??? holy shit. wtf. this is soooo dumb.= → use the canonical =daemonDie.slug= (the property the mount was built FROM), never reverse-engineer it from a formatted string.
- =dont REMOVE code. comment it!= (I'd overwritten the queueing Stall impl) → backup-during-migration: comment superseded code, never delete.
- =instead of buffer.view, just read buffer.mode.view  no buffer.view= → buffer dossier resolves =buffer.mode= to the rich entity; =Frame= reads =buffer.mode.view=.
- =why only now … adjust the full consumer side of our metadata view. what endpoints did we replace??= → migrate the whole route family at once, not the slice the feature needs.

** masked/aimed/queueing — shipped

Client mode =EMITTER= trait (=mode.emitter= ← =/metadata/emitter=), =view= rename. Thread traits re-exported (AIMED sets =thread.pull=; MASKED/QUEUEING markers; queueing's old Stall kept commented). MASKED auto-added when viewable. =terminal.stall=. skins =form/{schema.js,FormFromSchema}= + =trait/widgets/{Masked,Aimed,Queueing}=. Factory matrix in F. flashcard emitter leaves given =input:= schemas.

** "check chrome when done" — the lighthouse-is-a-service trap

404 at =/attached/process/lighthouse/multiplayer/metadata/manifest=. The client "lighthouse" connects to a *service process*, not the runtime/daemon — it has no /metadata. Reverted =lighthouse.js= (=/manifest=) and the shared =Dataspace= datamap (=/datamap=). Daemon/mode consumer reads stayed on /metadata (those scopes are covered).

** Polish (all verified live)

- =i dont want to display either this or that trait. i want multiple.= → widget-open state is a *Set*; widgets stack.
- =when i select a mode on an active thread … i want the mode to be replaced on the thread, not a new thread created. make sure the reactivity works out …= → made =thread.mode= atom-backed (=$mode=); =selectMode= replaces on same-daemon active thread; E info/traits/availability/masked-schema + threads-list all update reactively (verified: dataspace→flashcard live-retitles, aimed enables, form switches view→emitter =limit*/recall/where=).
- =active here refers to the modes traits not the threads.= → E §1 "traits" = =$mode.traits= (VIEWABLE·EMITTER), not the thread traits.
- =make sure the widget contain information about the trait they belong too.= → each widget gets a head (name + hint).
- =remove this: syncs after 5s. instead trail the input by small dot indicator= → Labeled input trailed by a dot (warning when a debounced sync is pending).
- =on change of mode inside thread and if old mode name/slug === [labeled] trait data, then replace the threads labeled data.= → =selectMode= auto-follows the label to the new mode IFF the old label was the old mode's default.

* Carries

Full design + state → =.ikiro/quests/c-panel-rebuild.quest.org=. Open: lighthouse-service /metadata; old-route removal (additive cutover); buffer-from-mask cast; confirm server buffer delete under the nested scope; intent §3 still dummy.
