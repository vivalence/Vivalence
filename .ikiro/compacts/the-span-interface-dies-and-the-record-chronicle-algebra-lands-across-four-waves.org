#+TITLE: the span interface dies and the record/chronicle algebra lands across four waves — journal on the root, policies in the wiring, g/h become one debugger
#+AUTHOR: ikiro

Continues [[the-span-pipe-reactive-logging-spine-lands-and-the-oracle-bundle-demos-three-client-ai-paths]]. Sibling: [[the-daemon-dispatch-trace-and-the-production-observability-hypothesis-scribe-tail-sampling-and-the-debug-aperture]] carries the full DESIGN record (diagnosis, blast radius, naming trail, scribe code, the unbuilt stack). Durable state in memory [[project_span_reactive_spine]] (rewritten) + codemap =typology.md= (Span REBUILT + belt/trace lines). This compact is the JOURNEY: five design rotations under the connoisseur, one callout, then a full four-wave landing.

* arc
Opened with beef convulsing at the old interface — the =log(subject, payload, transport)= god-method, the six track classes, the three-object setup ritual. Rotation 1 found the primitive (flat fact + fold, =minute:chronicle == packet:turn=, soma.pour's sibling). Rotation 2 moved accumulation OUT of the span into sink policies. Rotation 3 wrote the full files + tests. Rotation 4 killed the id-dressing and made tests demos. Rotation 5 moved retention ONTO the root span (journal + channel) so no drain needs pre-wiring, weighed Queue and kept it a composed sink. Then =go= — all four waves landed, suites green.

* the direction (beef verbatim, in order)
- open: /"i am unhappy with the interface of spans and logs. something is wrong. architecturally this is not super elegant."/ — /"WHAT IN GODS FUCKING TARNATION IS THIS"/ (the =log= write-closure) — /"props is read as part of the path?!??!!?!? makes NO FUCKING SENSE."/ — /"i suspect maybe a span brigns its own pipe?!"/
- the brief: /"in typical vivalence cadence, i want some simple powerful primitive that composes to make an utterly mindbending think happen. like the vector. there is SOMETHING elegant here. find it. name it. propose it. let the conoisseur have a look. functional lispy."/
- pass 2: /"ignore streamed logs for now. i am ok with moving traces to gestalten/belt/trace"/ — /"i might watn to accumulate into span and only drain at the end. or only drain if condition xyz. i will want to build up loggin span architectures into client server mode service etc"/
- pass 3: /"propose the files in full, trace scribe and span. plus testing scenarios. orient your code on our coding style"/
- pass 4: /"why do we tally and id? testing is incomplete. more expressive testing. i dont need to test that JS is functionaing. i want tests to demo behaviro"/ — and the CALLOUT: /"DONT FUCKING SPAAM ONE OFF NON FUNCTIONAL VARIABLES!!! ... i need the context of the.dot.notation to fucking knwo where what is fuckin gcoming from"/ → zettelkasten =namespace-destructuring= + memory [[feedback_no_namespace_destructuring]].
- blast: /"ok. this looks fine. now do a blast radius assessment. where are we spanning already?"/ then /"work out the change for each wave. demo me the change. kajuit and runtime special focus. rework teh integration into g and h panels! on runtime/daemon: trace how we branch modes and contexts and such. hypohtezise what production grade loggin/tracing/debuggin infra would look like."/
- pass 5: /"i'd like to use span/pipe such that we dont need to setup external drain immediately but can instead drain the pipe whenever. pipes can be read via streams/generators, via reactive, etc etc. use that."/ — /"might we want to also add a queue next to pipe? would that enhance span?"/
- final trim: /"i dont like the term 'minutes'. use more standard technical lingo. less excentric. no relaying for now. but i like the queued demo. extend that into our usage adn testing."/ — /"go"/ — mid-build: /"no paladin scribe for now either."/

* the algebra that landed
- *record* (beef rejected "minutes"): ={span, trace, path, verb, at, data?}= — flat JSON fact; verbs =open · close · note · fault · subject · request · response= (open table). One shape crosses pipe, jsonl, wire, snapshot.
- *Span = cursor* (=prototypes/span.js= ~45 lines, was 128+131 with tracks): ROOT holds =journal[]= + =channel= (Pipe), branches delegate through =tilde=; =mark(verb, data)= is the one emitter — journal append + pipe send. =span.records= readable whenever; the pipe stays the LIVE face (tap / stream / reactive); a Queue composes as a sink for pull-at-own-pace (=span.to(queue)=) — deliberately NOT intrinsic (two live faces would fight over consumption). =id = Span.tally++= (hash dressing killed on beef's "why"), =hasher()= returns it — grab/drop correct for same-nature siblings.
- *trace belt* (=gestalten/belt/trace.js=): =chronicle= fold (seed/step exposed → the live g-panel atom is ONE line), =dictate= mirror (=chronicle ∘ dictate ≡ id=), =live(span)= backlog-seeded reactive, =duration/timing=, predicates =sealed/faulty/slower=, policies =gather/flow/hold/decant= — accumulation and conditional drain are properties of the WIRING, never span state, because one span feeds N sinks with N policies simultaneously.
- =tracks.js= DELETED; barrels drop =tracks=; =transition= track died with zero live call sites.

* the four waves
1. *typology core* — span/trace/track/http + barrels; tests =span.test.js= (6 demos) · =trace.test.js= (13 demos incl. the queued drain and journal≡pipe replay) · =trace.snapshot.test.js= (hand-authored records, explicit clocks — the timing-slimming parse died structurally).
2. *ghost* — =mod.js= + =instance/{run,init}.js= verb renames; the ledger-pipe wish comment left in place (scribe deferred).
3. *oracle triple* — =/ask= wire contract now ={trace: span.records}= (flat facts, client folds); =Oracle.svelte= ritual (2 pipes, 2 spans, 2 reactives) → one Span + =trace.live=; =Trace.svelte= = recursive chronicle-node renderer.
4. *kajuit* — =telemetry.js=: =$telemetry= = live chronicle trimmed to 200 roots, =$span= = shared selection atom; *g/h become ONE debugger*: h's trace-tree drills (click → =$span= + opens G), g details the selection (entries, fault, waterfall). The g request badges that were DEAD code against live Spans (=span.request= never existed; transport buried it) now light up from request/response entries.

* findings
- *shape.http was wired-for and dark*: the server-timing block read =ctx.span= (=.gauges=/=.duration= walk) but NOTHING on the daemon aperture ever set =ctx.span= — server tracing existed as a dead socket. Now chronicle-backed (=trace.timing=), still awaiting a root-span middleware server-side.
- *the daemon's Vector branch structure IS the span tree*: =daemon → mode.mount → /harness → /dialogue → render= — each =.branch()= a natural span boundary, =ctx.span= threads the carry-fold exactly like =ctx.daemon=. The production-infra hypothesis (=scribe.root= + tail-sampling decant + ledger jsonl + =/debug/*= aperture + X-Trace correlation = stripwire applied to observability) is DOCUMENTED in-session, NOT built.
- *two-cargo pipe named*: the old pipe carried per-mark record POJOs (hidden emits in =Objected.set=/=Faulted.raise=) AND drained live Spans — g consumed one, oracle the other. The rebuild leaves ONE cargo.
- *append-only fixes a real flaw*: streaming a growing mutable tree gave persistent sinks N copies (the manifold test asserted =[1,2,3,4,5,5]=); flat records are jsonl-correct by construction.

* deferred (beef-scoped, explicit)
paladin scribe sinks · relay/streamed server logs · runtime daemon root-span wiring · server-timing fan-IN (died with =Transported.receive=, returns with streamed logs). Svelte panels not machine-verified (vite + buffer-bundle esbuild surface on next load).

* totems touched
codemap =typology.md= (Span REBUILT + belt/trace) · frontier strand 5 (span logging → REBUILT+LANDED, open tail listed) · memory [[project_span_reactive_spine]] rewritten + [[feedback_no_namespace_destructuring]] new · zettelkasten callout =namespace-destructuring= · =pipe.test.js= header sink-list settled to the new reality.
