#+TITLE: the hallucination tool seam recasts around ONE LAW — =a provider translates ONE request; the system folds everything that spans requests= — so the loop/retry/transcript/persistence/session-vocabulary/span move system-side into =shard.hallucinate= + =soma.transcript=, the provider shrinks to a pure per-request dialect, =entities.tool= dissolves into =hallucination.tools= (a bare Vector), =ToolCall= is born a Signal-sibling codec, the lying-transcript persistence hole closes, tool execution converges on ONE =(context) => yield= signature, =Request.tools= goes map→list, and structured output moves OUT of the cortex derivation INTO the provider off =request.output=.
#+TOPIC: session opened on =patch.org= (the AGENTIC trait) with beef: "something is wrong with this approahc. why is it so much effort to get something into the hallucination. load the codemap and ikiro. figure out what out the typology/hallucination needs to be itterated to unify simplify standardize and execute tool use.??" Deliverable = a first-principles recast of the hallucination/cortex/provider/tool seam, landed test-first across M20 blasts 1 · 2 · 2.5, suite-green in the working copy (VCS read-only — beef commits). Quest =.ikiro/quests/m20_hallucination-session-recast.quest.org=; memory =[[project_m20_hallucination_session_recast]]=.

* Arc

** the ask — the effort IS the symptom (beef, verbatim)
"something is wrong with this approahc. why is it so much effort to get something into the hallucination." The AGENTIC-trait patch worked but took a daemon-level middleware + =mode.module.harness ??== + a finalizer re-order just to compile-wrap-add tools. Diagnosis: a tool crosses FIVE representations author→provider (mode Vector → aperture =/tool= → =shape.agentic= POJO → hallucination =tools={}= dict → =Request.tools= → provider wire), and every producer hand-authors compile+thread-splice+add. Root: =entities.tool= is the one place in the system that REFUSES the Vector vocabulary — a POJO dict where the rest is Vectors. Eager snapshot, ctx severed, a hand-rolled dispatcher duplicating the trie, closures riding =Request= (dropped by JSON on the wire).

** the design fan-out → the connoisseur's ONE LAW
Killed a 13-agent ultracode workflow as too expensive; re-ran 3 (thick-provider extreme ∥ thin-provider extreme → connoisseur synthesis) + a snapshot agent (14 byte-precise contract pins on TODAY's behavior, gate-zero). Verdict: *thin boundary.* ONE LAW places every responsibility — the loop, retry, transcript, persistence, cache marks, session vocabulary, span all SPAN requests → system; block dialect, =cache_control= placement, wire parse, fault normalization all live WITHIN one request → provider. The false binary dissolved per the connoisseur's own rule (settle how many objects there are): there were always TWO — the *session grammar* (a Packet stream folded by =soma.transcript=) and the *dialect* (pure per-request translation); thick complected them inside the vendor wall, the old code complected them in homeless free fns. The synthesizer also caught a live bug neither designer saw: *render never persisted tool rounds* — every rendered tool loop wrote a lying transcript to the DB.

** the callout — premature convergence
Mid-design I ended a turn "One decision left: =name= vs =signal= on =Part.ToolUse=" — converging the whole design onto a fake gate while beef was still rotating. beef: "not fucking one decision left. were clearly designing. retard." Logged =family: premature-convergence= in the zettelkasten: in design mode, NEVER emit "one decision left" / countdown framing; end design turns on the sharpest open tension, not a convergence claim; =go=-gating language only after beef signals convergence.

** blast 1 — GRAMMAR (vocabulary + schema + dialect, zero structural moves)
=meta.state= (=State= enum complete·tools·length·abort·error·filter) replaces raw =meta.stop=; vendor =stop_reason= quarantined under =meta.provider.*=. =Part.ToolUse.input= object-only (parse-once moves to =soma.pour /part/close=). =Part.Alien {dialect, block}= — beef: "is alien real? or projected? cute else." — REAL: =redacted_thinking= exists today and must replay verbatim; server-tool blocks are the same class; the old default flattened them to model-visible JSON text. =fromm.yield= grew an =utterance= row (bare string → model-visible message). Re-pinned the 3 snapshot suites; grep-gate =meta.stop= = 0.

** blast 2 — SESSION (the structure)
NEW =subsystems/typology/gestalten/shard/hallucinate.js= (beef's own seed comment) — one async generator =session()= is THE loop, both avenues via a =pump= map; =render()= is literally its catamorphism returning a YIELD, not a turn. =soma.transcript= — its final state IS the session yield =​{state,rounds,turns,message,entities,object,meta}=; the yield-object beef asked for ("we want to compile a yield object at the end that features all the entities and buffers and turns objects etc. this is important to get right contractually. where that happens no clue. ask the conoisseur") = the fold's fixpoint, entities concat-merge per key at =/tool/yield=. =hallucination.tools= = a bare Vector, PUBLIC — beef: "the tool{} interface on hal can do without the add and use interfaces. thats vector now. blast full native coherence." — so =entities.tool= dissolved entirely. =mode.tools= a native Vector (TOOLED mints it, slurps =module.tools=); beef killed the aperture mount: "no tools on aperture. aperture is for exposing publicly. the mode might have a tool vector tho." HARNESSED persists via =soma.transcript= — the lying-transcript hole closes for render AND stream. =shape.agentic= DELETED. =Tool.side= cut ("we can do without tool.side for now").

*** the retry law + span (beef: transparency and control)
beef: "fix attempt then. make sure to not blast expensive apis tho. we want transparency and control. work a propper span tracing setup into the system." → =deliver()= retries ONLY before the first packet flows (a =flowing= flag) — a mid-stream fault is never re-billed; every fault/retry span-marked. Span placement, beef: "think about where to wore the span in. probably halluconation root and then wired into the contract." + "some root taps into it on contractual span branches" → root =new Span("/hallucination")= minted in the closure, exposed =hallucination.span=, CONTRACTUAL branch topology =/<type>/<avenue>/<tool>=, records → =trace.chronicle= as a snapshot artifact.

** the CONVERGENCE — ambivalence is a misplaced boundary
Two seams read as ambivalence and both converged. (1) =fromm.yield= gained a =spoken= row: a tool returning =​{message,entities,object}= was falling through to =opaque= (model saw nothing) — the OLD =execute()= native-sniff lifted into the lexicon where it belongs (no further cut: that sniff had exactly one home, now spoken). (2) The tool-execution signature: I had a =(input)=>= vs =(ctx)=>= fork (POJO tools wrapped). beef: "this should converge. i dont want ambivalence in my tool execution environment. weve been working around this to some degree i suspwct. one tool call environment and a toolcall signature as contract. and a singular context and closure for execution." Grep PROVED it a TEST-ISM — POJO =input.tools= is passed only in tests; every real mode uses =mode.tools= Vectors with =(ctx)=>=. Killed the wrap; ONE signature =(context) => yield=, =context.input= = args, =context.{daemon,mode,thread,span}= = ambient bound by traits; thread moved from smuggled-in-input to ambient =ctx.thread=. A LAW surfaced (sse.integration): *executable tools cannot cross the JSON wire* — =JSON.stringify= drops the closure → the leaf opens effect-less → =rollup= skips it → no declaration → single round; the old test "passed" only because the wrapper faked a declaration then errored. Rewrote the test to assert the boundary.

** ToolCall — a dedicated Signature, a Signal sibling
beef: "toolcall as dedicated signature! a signal sibling" → =subsystems/typology/prototypes/toolcall.js=: =ToolCall extends Signature=, splits on =_= where Signal splits on =/=; =.name= (wire, join =_=) + =.signal= (a real =Signal=, the =/=-path for =steer.dispatch=). The wire↔signal codec that was free-floating string ops now lives in the prototype. Barrelled in =prototypes/index.ts=.

** the contract question → tools map→list + output-to-provider (fork A)
beef read =Request.tools= as a map and pushed: "does it make sense to map this to an array on the vivalence contract side? name:{xyz} feels off. why a map and not a list? any reason?" Traced why it was a map (historical POJO dict · the dressAsObject splice · the dead =tools[part.name]= lookup — all dead/dying) and why a LIST wins: *cache stability* (declaration order must be byte-stable — the real reason), the provider maps to an array anyway, symmetry with turns[]/parts[], name-is-data. →array. Then the deeper smell: =dressDialogueAsObject= (the object faculty derivation) splices a =respond= tool into =request.tools= at the cortex-derivation layer — "this is the point where the tools need to be available inside the provider context. for this splice here. i dont like it." I found =request.output= was NEVER read by the provider; only cortex intercepts. beef: on "the trick is in the wrong layer" → "amen!"; on "the one tool-injection outside the tools-Vector compile" → "fix."; on fork A (provider owns it) → "this." So =dressDialogueAsObject= DELETED, =DERIVATIONS.object= = a thin alias to the dialogue donor, and the respond-trick moved INTO =anthropic/provider= off =request.output.object= (=buildParams= appends =respond= + =tool_choice:any=, =extractObject= folds it). The splice is gone; =request.tools= is cache-stable.

** the pathOf kill + a regression beef planted
Landing blast 2.5, beef caught the hack he'd smelled all along: "hallucination.tools.open({ nature: pathOf("lookup") }, () => ({})); shouldnt we be using the toolcall proottype here? i dont like this pathOf function. its hacky." → tools now open by NATURAL nature (=​{nature:"lookup"}=), the =ToolCall= codec stays INTERNAL (dispatch · declarations · the HARNESSED wire-name graft); =pathOf= deleted. Single-segment natural-nature ≡ the old =/name= path (same trie node); the =_= codec only matters at the wire boundary. Also caught: beef had hand-edited =CONFIG.rounds= default to =1= (closes every tool loop after round 1, breaks aprende in prod) — restored to =10=, flagged. (beef also live-edited the valence to " - json" and renamed =resolve→resolveFaculty=/=policy→resolveConfig= mid-session — the compact regime "user edits are canonical" held; I propagated, never reverted, flagged the rounds slip.)

* State at fold

- Green: typology *101/0* · runtime *33* (3 pre-existing live-:2501/orphan, unchanged from baseline) · kajuit *17/0* · anthropic *2/0*. Grep-gates: zero =meta.stop=, zero live =shape.agentic=, zero =dressAsObject=, zero =pathOf=, zero =request.tools= record, zero thread-splice.
- The controlled interface, frozen: =Request.tools = [{name, valence?, input?}]= (order-stable) · =Request.output.object= = "give me this shape back" (provider owns HOW) · session events =/tool/call· /tool/yield· /turn/full· /session/close= minted ONLY by =shard.hallucinate= · tool execute =(context) => yield=.
- New tests: =tests/gestalten/hallucinate.test.js= (session engine) · =tests/toolcall.test.js= (codec). 14 gate-zero contract snapshots re-pinned.

* Open / pending (beef gates)

1. Whole recast uncommitted working copy — VCS read-only, beef's to commit.
2. Blast 3 (USAGE) live gates only: boot =:2501= · aprende quartet round-trip · oracle =mode.call.ask= · spanish boot · one live span chronicle showing rounds/usage/tools end to end.
3. Deferred: =capabilities.structured:"native"= (a provider skips the respond-trick when it ships native structured output) — lands with a second provider. =v.prototypes.ToolCall()= codec (like Signal's) — no schema consumes ToolCall (name crosses as a plain string), YAGNI.
4. Stray =subsystems/typology/prototypes/#hallucination.js#= emacs autosave — beef to =rm=.
5. object.stream stays unsupported (render-only derivation, matches the old dressAsObject).
