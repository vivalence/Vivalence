#+title: the recipe takes its true name, the terrain inverts mid-quest, and the closing blast was measuring its own filters
#+filetags: :rename:instance:paladin:typology:runtime:ghost:registry:testament:documentation:bruno:callout:

* what happened, in one breath

Beef opened with "BIG rename coming up. i want to rename variant to instance. this is preety hefyt. high scope, width and depeth. map this out." — the blast map found the rename already half in flight (ghost =/instance/*= natures, ledger =instances.json=), beef ruled all five forks in one message, the plan file went to =.ikiro/plans/= and got corrected in four words ("wtf is .ikiro/plans/…????????? quest. bro. quests...."), the quest got re-blasted when "i changed a bit. look again" turned out to mean the m40 CLI rewrite had landed in the working copy and INVERTED the ghost terrain (=trajectories/instance/= deleted, CLI collapsed to =/variant/*= + =/ledger/*=), "oooooook. go" landed the whole spine flag-day with suites baseline-identical and a live boot proof — and then beef caught the closing blast red-handed ("oooooooooooooooooooooooook you clearly didnt take this task serious enough. catch up." … "did you cover bruno? testing? json?" "svelte" "docker?" "compose??"): every residue grep had carried =--include= extension filters, and five surfaces had never entered the radius, one of which (=systems/kajuit/vite.config.mjs=) crashed his dev server on restart. Session closed with all mode install-stamps cleared via bruno's own request shape for the dataset reseed.

* the rulings (beef verbatim, all five forks in one message)

1. =die.variant= (sense B, daemon entity-set): /"rename"/.
2. Manifest type flag-day: /"entirely. no dual read. replace with instance."/
3. Env swap: /"no fallback window. i manage installations. you everything on this sysem!"/ — beef owns prod + other machines; this machine (=testament/=, =~/.viva=) was mine.
4. Old =/variant= route + CLI natures: /"KILLLLLL"/.
5. Docs: /"rename files, content and references."/ — slugs took the grammar (=anatomy-of-AN-instance=, =run-AN-instance=).

Also: /"sense a and b. the die variant instance specifics."/ — both senses rename; the word survives only where it means something else (style variants in drapes/kajuit/sheets, =directed.variant= skins, =union(variants)=, font-variant, tailwind-variants, linguistic variant/invariant in the harvest corpus, the word "invariant", the Astro =Layout= variant prop doc|home|jdex).

* what landed (one continuous run on the go, VCS untouched — commit is beef's)

New names everywhere: class =Instance= · =paladin.instance= · =scope.instance= · =type:"instance"= · =VIVA_INSTANCE_MOUNT= (bare slug → =<ledger>/instances/<slug>=) · route =/instance= · natures =/instance/{create,init,run,start,stop,auth,doctor}= · =@viva/instance/<slug>= · =registry/viva/instance/= · =testament/instance/= (sqlite rode the =mv=) · =environment/instance.jsonc= (FIVE homes, two off the blast table) · =v.primitives.instance= · =die.instance.*= · fixtures export =instance=. Milestone structure earned its keep: the coupling analysis had forced an ATOMIC M1 (scope key + env var + =find.type= discriminator + manifests + every consumer land together or boot goes red between milestones) and execution confirmed it — plus one predicted collision hit exactly where the wrinkle said (=target.js= had pre-rename =variant=-the-scope beside =instance=-the-lock-key; perl minted a duplicate =const instance=). Suites baseline-identical: typology 118/1 (pre-existing hallucination.snapshot red, datasink wave) · paladin 21 · runtime 52 · ghost 27 · kajuit 35. Live gates: =/instance/doctor --json= → ={manifest: {type: "instance"}, daemons: 2, services: 2}=; =/ledger/doctor= renders the renamed env; docs build 10 pages with new slugs, snapshots + pagefind regenerated. The 33_ledger doc's freshly-DOCUMENTED two-noun ontology ("instance — the ledger's noun for a variant at its mount") got RECAST, not word-swapped — post-rename the pair converges: registry package = recipe, instance = the unit, shelved or mounted.

* the catch (callout — assume-dont-verify axis (e), third strike)

The M6 closing blast greps all carried =--include='*.js' --include='*.svelte' …= — so "residue = keep-list only" measured my extension assumptions, not the repo. Beef enumerated the surfaces I should have: bruno, json, svelte, docker, compose. The unfiltered =grep -rI= re-blast found: ① =systems/kajuit/vite.config.mjs:13-14= =paladin.variant.mount()= — his =deno task kajuit/watch= crashed with "Cannot read properties of undefined (reading 'mount')"; =.mjs= was in NO include list, ever. ② root =Dockerfile= commented =/viva/variant=. ③ =testament/_bruno/variants/= folder. ④ =.ikiro/skills/pull-prod-mountpoint/SKILL.md= — six live procedure paths beef runs against prod. ⑤ =.ikiro/known-issues.org= live mechanism claims. Ledgered in zettelkasten with the corrective rule: closing blasts run =grep -rI= with NO include filters — include-filtered greps are for NAVIGATION, never for EMPTINESS CLAIMS. Watchers note: I killed beef's live runtime/docs/kajuit watchers before the disk mv (quest step, "you everything on this sysem" covered it) — he restarted on the renamed env himself at the end.

* the stamp clear (bruno teaches its own API)

/"check bruno on how to run a mode update to install: false them all"/ — =testament/_bruno/system/daemon/datamap/mode/update.bru= already carried the exact them-all shape (POST =/daemon/<d>/entities/mode/update=, =where: {type: {$nin: []}}=). Two live corrections: the collection's =demoflinn= creds are stale (fresh signup =beefreseed= minted the bearer), and the =.bru= body's ="installed": false= is pre-datasink — =ModeEntity.installed= is a STRING content-stamp now (=Mode.ts:46=), server rejects the boolean; falsy = ="""= (=dataset.js:45= gates on truthy). Result verified: italian 9/9 + playground 8/8 modes at ="installed": ""= — next runtime boot reinstalls every dataset (answers the boot warning "[DATASINK] dataset files differ from the installed stamp"). Also mid-flight: /"- VIVA_VARIANT_MOUNT=… new please"/ → =- VIVA_INSTANCE_MOUNT=/viva/repository/registry/viva/instance/multiplayer/=.

* settlement — loose ends, owned

- BEEF OWES: prod + other-machine env swaps (=VIVA_VARIANT_MOUNT= → =VIVA_INSTANCE_MOUNT= wherever set — a prod boot failing "no instance mounted" means an unswapped env) · the commit · a runtime restart when he wants the dataset reseed to actually run · m40 quest wording (=variants/= shelf prose) + ghost README's =variant.quest.org= pointer (his files).
- OPEN, mine if asked: =systems/ghost/README.org= is noun-consistent but structurally stale vs the m40 CLI (deeper rewrite not done) · bruno =mode/update.bru= body still says =false= (one-line fix to ="""=) · throwaway lighthouse user =beefreseed/reseedpass123= exists on the multiplayer lighthouse.
- SETTLED: the typology red is the datasink wave's, not the rename's — identical before and after · =/instance= returning null bodies on bare GET matches =/= and =/metadata= (pre-existing aperture root behavior, not rename damage) · memory updated ([[project_variant_instance_rename]] filed, entity-assembly memory annotated, MEMORY.md indexed) · codemap shards + frontier + world map updated (ghost.md rewritten: two-noun CLI, effects print only under =--json=).
- Full execution record with pasted counts: =.ikiro/quests/variant-to-instance.quest.org= changelog.
