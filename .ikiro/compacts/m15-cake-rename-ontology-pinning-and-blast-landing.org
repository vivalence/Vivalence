#+TITLE: M15 — cake rename: ontology pinned over five exchanges, blast landed same session

Session opened cold on the milestone. beef: /"Okay. We have another
milestone to work on, namely the cake renaming. I want you to find every
occurrence in the system and the code where we name something cake, where
we name something module, where we name something mode, and where we name
something kernel."/

* the inventory — four words, four overlapping concepts

Full-repo sweep (bak/ excluded, corpus vocabulary excluded — =cake= as a
Portuguese lesson word is data, not naming). Result: =cake= 183 lines /
~45 live files; =mode= three distinct things (prototype, DB entity, mask
field); =kernel= two unrelated meanings (daemon core mode-set vs entity
namespace =entities/kernel/=); =module= three uses (mask specifier field,
manifest.type in tests, prose). The knot, one pipeline:
=mask.module= (address) → =vip.accio= → cake (loaded) → =new Mode(cake)=
(live) → =daemon.kernel= (subset). Quest
=m15_cake_rename.quest.org= already existed, name unpinned.

* pinning the ontology — five exchanges, two principles

beef's opening dictation (voice): /"the cake term we drop completely in
the [daemon], we move everything that's [mode] into kernel. We still have
[daemon].modes.type.slug, but it's computed from the array of kernel
modes. … what we called module, I would rename to mode. And then the thing
that we named cake … I would name that module. Or another way to
semantically lay this out would be to name the live instance the mode, and
the reference the module. I'm not sure, but that's the general idea. Load
up"/

Fork A (reference=mode, loaded=module) vs fork B (reference=module,
instance=mode). Argued B: =read.viva= literally does =await import(path)= —
the cake IS a JS module namespace, so the old "conflicts with ES modules"
objection (M11 fork 6) dissolves; and A would rename service reference
fields (=datamap.module=) after one target type — the fork-7 mistake again.
beef: /"I also lean b."/

Then the array-name question. beef: /"do we have polymorphism on the
module term … should we call the kernel in [daemon] actually kernel inside
of the variant … Or should we maybe already declare those as modes?"/
First answer split it — module polymorphism OK (residence), but
kernel(declaration)/modes(live) for the array. beef caught the cheat:
/"I feel like you just declared two principles … the principle is
inconsistent … there, you argue for one term, one thing. Not a fan."/
Corrected to =modes:= both residences, kernel dead.

beef then added the mask layer: /"data map, what we reference here in the
object that has a module key and maybe some variables, some statics
attached, that is a, like, a mode instance reference. … I think we call
that declaration a mask inside of the modes array. … what is now the mode
string … that would be inside of a module key. So that's the general
structure of the mask right there."/ Grammar already latent on disk:
=populate.js= =accio(mask.module)= verbatim; hallucinator tests literally
say =mask: {module, secrets}=; =variant.js= wraps declarations in
=mask("daemon")(hydrate(...))=. beef: /"Okay. I like this distinction. It
works."/

Final iteration — kernel reactivated on a SECOND principle. beef: /"When
we define a data map mode or a hallucinator mode. We use a semantic, not a
lexical name for the property. But if we would use modes in the [daemon]
mask, that would be a lexical name. So I think we should call it in the
[daemon] and in the variant and in the mask … the kernel. The … object
lookup thingy that we have on daemon dot modes dot type dot slug, I think
should stay."/

Settled vocabulary (the two principles do not overlap: 1. residence
disambiguates, one noun per concept — module ref↔loaded, mask
declaration↔instance; 2. mask keys are SEMANTIC roles, lookup surfaces are
LEXICAL types):

| mask   | declaration — {module, statics…}; string = shorthand        |
| module | loadable/loaded unit (was cake — DEAD)                      |
| mode   | live citizen                                                |
| kernel | constitutive array — semantic mask key, variant + daemon    |
| modes  | =daemon.modes.<type>.<slug>= — derived lexical index        |
| kinds  | type→prototype registry (was =daemon/kernel.js= =modes=)    |

* the quest worked to ready

beef: /"go on teh quest. no changes yet"/ — quest filled per method: blast
table (~48 files), structural diff hunks grounded against live reads,
mandatory testing assessment + green ladder, deferred, six forks.
Grounding finds beyond the ontology pass: =daemon.js= held a THIRD kernel
(={orm, em}= machinery bag, zero consumers); =die.kernel.corpus/ontology=
filters never read; =not.cake= zero callers; =die.variant.kernel= zero
consumers; =paladin.bake.process= in =client.viva.js= references a member
paladin doesn't have (dead call); the live index
=good.modes[type][slug]= already existed — ontology lands on existing
structure.

* the blast — beef's GO, full bracket in one session

beef: /"run the runtime and client for yourself now. then run the blast
testing. if that checks out, run the change. run test and blast again.
then clean up trailing tasks like verifying registry and snapshot
integrity."/

- *baseline live*: runtime :2501 ALIVE + kajuit :1794, drove Aprende via
  chrome — board rendered real data (324/223/30 memory bars, streak 4,
  577 scored).
- *② bookends*: typology 104 · paladin 11 (env-file) · runtime 26 — GREEN.
  ghost + kajuit suites bitrotted pre-existing (per-file classification:
  4+8 DEADIMPORT, 3 ghost FAILs incl. =wafer.test= already failing on
  stale counts — none cake-related).
- *③ landing*: ~50 files. Typology (Cake folds into Mode; =ModuleSpec= →
  =Mask= schematic; Domain contract =modes:= → =kinds:=; =not.cake=
  deleted) · paladin (cake→module everywhere incl. pensieve error string)
  · runtime (=kernel.js= → =kinds.js=; ={orm,em}= bag deleted;
  =die.domain= replaces the subset; =variant.kinds=; =resolution.domain=;
  =mode.module.*= across traits/apertures/lifecycle; bench rebuilt on one
  kernel array) · ghost (clone/mod/wafer.test/localhost.viva) · five
  declaration files merged =modes:[]= into =kernel:[]= (two registry
  variants, ghost localhost, testament education + playground) ·
  education domain =modes/= dir → =kinds/=.
- *beef's concurrent edits absorbed mid-flight*: =chaosmonkey.js= appeared
  as its own daemon (merged its array too), playground lost the
  chaosmonkey mode line, =registry/development= → =registry/playground=
  (ledger registry.json consistent with all three locations).
- *④ bookends*: identical to baseline everywhere — 104/11/26 green,
  same pre-existing fails, zero regressions. aperture snapshot green
  in-suite (declaration merge preserved =[...kernel, ...modes]= order).
- *⑤*: zero live =cake= refs (only =prototypes/wafer.js= commented backup
  block, untouched by the backup-during-migration rule); zero
  =register.modes=/=variant.modes=/=mask.modes=; all surviving =.modes= =
  the lexical index by design.
- *trailing*: registry.json intact; post-change stack live — 3 daemons
  (chaosmonkey rail new), brazilian 16 modes, Aprende renders;
  =Migration20260703082445= was chaosmonkey's FIRST (brazilian schema
  untouched, last applied migration predates the session).

Fork resolutions: 1 =resolution.domain= ✓ · 2 ={orm,em}= deleted ✓ ·
3 =kinds/= dir ✓ · 4 snapshot fixture renamed ✓ · 5 =findModules= ✓ ·
6 minimal — =client.viva.js= local renamed, dead =paladin.bake.process=
call left in place (beef's call still open on deleting the call site).

* Resumption

- *Aprende board zeros post-restart* — the one open item. Data PROVEN
  intact (sqlite: 577 Memory rows all on user 019d23f1…, 2223 Literals;
  no runtime errors logged; lighthouse Identity table empty = JWT-stateless,
  normal). Suspect stale client session/auth after runtime restart, not
  the rename — unproven; next step was reading the =/assistant/wakeup/board=
  network response in chrome when beef is back. beef: /"ill check when i
  am back."/
- Deferred (quest): full-mask entries in =kernel:[]= (={module, statics}=
  threading + generalizing accioMany's hallucinator-specific
  ={service, mask}= wrapper) · =entities/kernel/= namespace squatter ·
  =ModeEntity=/table ="Mode"= vs =Mode= prototype · ghost/kajuit test
  bitrot (12 dead-import files, 3 broken ghost trajectories tests —
  predates M15).
- Fork 6 residue: delete the dead =paladin.bake.process= call site or keep.
