#+title: adding spanish exposes every fresh literal unsymbolized — the entity-variant merge collapsed base and domain into one descriptor per type, dropping the base subscriber that derives symbol and ontology — the fix is a per-facet fold: schemas override, subscribers accumulate
#+filetags: :runtime:mikroorm:entities:subscriber:population:regression:connoisseur:

A load-loss bug surfaced by a new corpus. Spanish literals installed with =symbol='{}'= and =ontology=''= while Brazilian looked fine. The hunt walked past the obvious suspect (the subscriber) to the real one (the daemon's entity-variant assembly), then a from-scratch redesign replaced the smell that caused it. Pins [[project_entity_variant_assembly]]; the callout is [[feedback_no_hotfix_architecture]] family =hotfix-cascade=.

* the symptom

beef, opening:
#+begin_quote
i recently added spanish.
this exposed an issue with the data loading.
...
you see how the sontology and symbol prop is empty on spanish literals.
that means that something in the literal entity is not computing the ontology.
#+end_quote

He suspected the =LiteralSubscriber.symbol()/ontology()= (derive nested =symbol= from linked symbol slugs; =ontology= = the one =TOPOGRAPHICAL= symbol). And:
#+begin_quote
read the jj history of that file.
what changed when in the last 3 months.
#+end_quote

* the exoneration sweep

Evidence, live from the Spanish db: junctions were CORRECT (=bueno= linked to =word=(TOPOGRAPHICAL) + attributes, all 1699 literals had junctions), =rank= WAS derived, yet 0/1699 had a populated =symbol=. Brazilian: 2184/2223 populated. Same subscriber code, opposite result → data/install-state, not the entity file.

The file beef pasted was the =subsystems/typology= *base* literal (its subscriber DOES derive symbol/ontology). The *active* entity is =registry/education/domain/entities/kernel/Literal.ts= — its =LiteralSubscriber= derives =rank= only. The base logic was never running. Then his sharpest steer:
#+begin_quote
the weird thing is that this USED to work!
something changed. i dont understand what
#+end_quote
and, forcing the discipline:
#+begin_quote
did we change anythign about libsql service or either of the literals or the installtion or default loading or whatever that could have brought this change?
#+end_quote

Ran the whole suspect list. libsql (=variant.map(v=>v.subscriber)=) — unchanged. base subscriber — repository-only edits. domain subscriber — rank-only throughout. =dataset.js= install — rename + cosmetic =cake→module=; junctions prove linking works. The ONE change: =systems/runtime/daemon/lifecycle/population.js= entity assembly, =5afb143b= — =[...array]= → =Object.values({keyed})=. Keying by =type= made =Object.values= keep ONE descriptor per =literal= key; =...die.domain.entities= last → the domain (rank-only) subscriber won, the base (symbol/ontology) subscriber was shadowed out and never registered. Brazilian's rows were written before the collapse (stale, never recomputed); Spanish was the first fresh install after it. beef confirmed with his own instrumentation: =typology.beforeCreate= never fired, =domain.beforeCreate= fired thousands of times.

* know vs think

I asserted a MikroORM collision as fact. beef:
#+begin_quote
do you think that or do you know that?
lets setup testers. write both version next to each other commented. then toggle one version and try n run it. peppered with console logs
#+end_quote

A probe (the real =@vivalence/typology/scenarios= provider contract) settled three unknowns empirically — none were guesses after this:
1. the base subscriber (=getSubscribedEntities → base LiteralEntity=) FIRES on the domain SUBCLASS instance — MikroORM matches by inheritance. So a union of subscribers actually works.
2. two CONCRETE schemas sharing a class name (=LiteralEntity=) → =Duplicate entity names are not allowed=.
3. an =abstract:true= base + a concrete child that =extends= it → coexist, no collision. (=schema.meta.abstract= reads correctly pre-init.)

That last one was beef's hunch:
#+begin_quote
and why dont we remove the duplicate name? the entity is marked abstract. abstract no name?! or maybe abstract in teh name??!!
#+end_quote
— right instinct: the clash is the CLASS NAME, and abstract is exempt because it's the parent, not a duplicate.

* the retard — and the redesign

I proposed successive patches (union subscribers → subscriber-only descriptors → a harvest step) built ON the existing =concrete()= + collapse. beef cut through it:
#+begin_quote
i hate this concrete function! its utterly retarded. lets get rid of it!
what would entity handling look like from scratch
#+end_quote

Logged =zettelkasten ## Callouts= (=hotfix-cascade=): once I found the collapse bug I treated the surrounding assembly as fixed ground and layered epicycles instead of questioning the abstraction. Corrective rule: **when a fix needs a SECOND workaround to prop up the first, STOP — name the design flaw before proposing the third.** The flaw named: an entity descriptor bundles facets of DIFFERENT multiplicity — =schema/entity/repository= OVERRIDE, =subscribers= ACCUMULATE — and =Object.values(keyed)= applied override to all of them.

Then the connoisseur pass:
#+begin_quote
let the conoisseur have a pass. whats the right way to solve this typoe of problem? propose me a full diff
#+end_quote

The right shape: a fold over the tier stack with per-facet merge, and =seal= (abstract→table) DERIVED from =schema.meta.abstract= — never the hand-listed =buffer:/literal:/symbol: concrete(...)=. The =concrete()= smell was a fourth facet (table-identity) stapled into the same collapse. Design forks beef decided along the way: literal+symbol are always-on (=for now we want to always have literal and sybols=); domain-less daemons are real and wanted (so =seal= stays — the abstract bases still need tables); concrete-fallbacks-in-typology considered and deferred (=lets go programmatic for now just to be quick=).

* landed

=population.js core()= now: =collate([sets.daemon, sets.kernel, sets.userspace, die.domain.entities])= folds into per-type slots (schema/entity/repository override, subscribers into a =Set=), =.map(seal)= concretizes any still-abstract base, then =die.variant.subscribers = [...new Set(flatMap)]= and =die.variant.entities= (subscribers stripped). =libsql.viva.js= =provider(datamap, variant, subscribers)= takes the accumulated subscribers as a 3rd arg (falls back to =variant.map= for back-compat) — the seam twitch vectors slot into later.

Verified in the RUNNING daemon (fresh reinstall), not just the harness — beef:
#+begin_quote
look at this. we got symbols and ontology??!!
#+end_quote
=bueno.adjective.masculine.singular= → =symbol: { proficiency:{cefr:'a1'}, word:{ lemma:'bueno', part-of-speech:'adjective', gender:'masculine', number:'singular' } }=, =ontology: 'word'=. Both =typology.beforeUpdate= and =domain.beforeUpdate= fire per literal; daemon =Status:ALIVE=.

Clean pass (=go. clean=): probe deleted, the =integration.js= prune debug =({ literal: undefined })= removed, all four instrumentation =console.log= lines stripped from both subscribers.

* scribe trail

- memory: [[project_entity_variant_assembly]] pinned (per-facet merge law + the =5afb143b= regression + the MikroORM inheritance/collision facts), MEMORY.md indexed.
- callout: =hotfix-cascade= appended (append-only), corrective rule executed this session (I stopped patching and redesigned on request — the rule is retro, the redesign is the proof).
- no quest tracked this; no world/ shard asserted the collapsed assembly, so nothing re-stamped.
