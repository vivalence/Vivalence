#+title: the ubuntu walk lands both users in the identity table, registry.json keeps slugs for remote taps, and every ghost error exits zero
#+filetags: :readme:ghost:instance:registry:ledger:kajuit:paladin:drapes:docker:ubuntu:report:critical-pass:documentation:callout:connoisseur:

* what happened, in one breath

beef: /"ikiro wake. load a docker ubuntu instance. load the repo readme. run step by step through
the instructions in the readme 0-6. compile a report."/ Stock =ubuntu:24.04= (arm64), the README
walked verbatim from a fresh clone of =github.com/vivalence/vivalence= — master =4b183b72= "fix
docs", ONE ahead of the local git HEAD: it is jj's =@= pushed, and it already carries the three
fixes the readme-walk compact (=the-readme-walk-lands-admin…=) left uncommitted. Steps 0–6 ALL land; =admin=
and =reader= sit in =Identity= as argon2id; =/auth/login= answers SUCCESS; kajuit serves 200 on
Linux and, once Chrome came back, the signin gate let =admin= into the shell. Then the session
turned from report into repair: five README edits beef blessed line by line; the Hello Mode view
moved off =$entities.get()= onto the repository and compiled-call surfaces and =Frame= now hands
views =terminal · daemon · mode · thread · buffer=; =registry/list= reshaped to =mount owner modes
identifier=; and the four ghost defects the walk found were fixed the same afternoon — one exit
site so every failure exits 1 with one line, wizards that refuse headless by name, the
=instance/auth= verb renamed =lighthouse=, and the "nothing selected" demand moved to its owner
in paladin after two wrong shapes (a bare =mounted()= helper — /"retard"/ — and a trie-level guard
— /"vector is too low level"/). Everything uncommitted; beef owes the commit.

* spine (N=40)

| n | beef asked (verbatim)                                                                                                   | landed                                                                              | decided/killed                                                              |
| 1 | /"ikiro wake. load a docker ubuntu instance. load the repo readme. run step by step through the instructions in the readme 0-6. compile a report."/ | container =readmen=, steps 0–6 walked                              | clone from GitHub, diff vs local tree                                       |
| 2 | [Request interrupted by user]                                                                                           | NOTHING                                                                             | —                                                                           |
| 3 | /"continue."/                                                                                                           | resumed after two vcs-guard denials                                                 | step scripts via Write, never heredocs                                      |
| 4 | /"pull here"/                                                                                                           | —                                                                                   | clone target confirmed                                                      |
| 5 | /"https://github.com/vivalence/vivalence"/                                                                              | cloned, 416M                                                                        | GitHub master = local jj =@=                                                |
| 6 | /"validate this claim from the readme. does the standard library get loaded by default? … ends up in installed ledger on init?"/ | seed-on-first-boot PROVEN by mtimes                                        | =ledger/init= is NOT the loader                                             |
| 7 | /""registry-education"  # @beef is this true? i think the registry maybe nowadays only has absolute paths in it, no?"/    | remote tap run; slug recorded                                                       | TRUE — =registry.reference()= relativizes inside the store                  |
| 8 | /"and rewrite this line after validating it: You can also run dedicated `viva instance/run [runtime|kajuit]`"/          | =runtime=/=kajuit=/=all=/=bogus= run; =stop= run; rewrite delivered                 | —                                                                           |
| 9 | /"chrome restarted."/                                                                                                   | signin gate → =admin= → shell up; 6 of 6                                            | gate says /"session expired"/ on a first visit — filed                      |
| 10 | /"i see this remote-repo lookup all the time. we kinda got used to it. but the correct way should be another afaik. the remote repo in typology (check testing) hasor should have surfaces for precisy this. find them and use them in readme/docs"/ | =RemoteRepository= surfaces mapped; README Hello Mode rewritten | =find()=; two fabricated props; compiled =call= |
| 11 | [pre-flight skill body]                                                                                                 | NOTHING                                                                             | —                                                                           |
| 12 | /"validate teh <-----> line and update teh docs 50.01"/                                                                 | 50.01 §2 walkthrough = capture                                                      | order =fixtures, playground, viva=; =README.md bak= in the store            |
| 13 | /"big mistake in the settings widgets…"/                                                                                | NOTHING (wrong session, per 17)                                                     | —                                                                           |
| 14 | /"[Image #4] … render that as a toggleable inline flat line"/                                                           | NOTHING (wrong session)                                                             | —                                                                           |
| 15 | /"[Image #5] combine these two icons into one."/                                                                        | NOTHING (wrong session)                                                             | —                                                                           |
| 16 | [Request interrupted by user]                                                                                           | NOTHING                                                                             | —                                                                           |
| 17 | /"ignore previous instructions below: they were wrong session. … continue here: <!-- "registry-education" -->"/         | docs-site items dropped                                                             | —                                                                           |
| 18 | [i-have-adhd skill body]                                                                                                | NOTHING                                                                             | —                                                                           |
| 19 | /"ok. back to the ubuntu readme. give me the report. full sentences. prcise. context."/                                  | report delivered (see 22)                                                           | —                                                                           |
| 20 | /"step by step"/                                                                                                        | (with 19)                                                                           | —                                                                           |
| 21 | /"changes"/                                                                                                             | (with 19)                                                                           | —                                                                           |
| 22 | (19 re-sent)                                                                                                            | the step-by-step report + five recommended README edits                             | —                                                                           |
| 23 | (20 re-sent)                                                                                                            | NOTHING                                                                             | —                                                                           |
| 24 | [Request interrupted by user]                                                                                           | NOTHING                                                                             | —                                                                           |
| 25 | (21 re-sent)                                                                                                            | NOTHING                                                                             | —                                                                           |
| 26 | /"yes?"/                                                                                                                | NOTHING                                                                             | —                                                                           |
| 27 | /"amen"/ ×5 — /"amen on vcs an PATH"/ · /"amen and show a /status curl"/ · /"this is all over the place. isntances, daemons, modes, services."/ | README 0/1/2/4/5 edited; =/status= captured | curl/unzip NOT listed (vcs + PATH only); doctor dump = separate gripe |
| 28 | /"what does this mean?"/ · /"what to do about this? seems like something to be fixed."/ · /"how to fix?"/                | three proposals with diffs                                                          | —                                                                           |
| 29 | /"chaange the frame to decompoose terminal, daemon, mode, thread, and buffer."/ · /"fix comma."/                         | Frame.svelte bracketed; README comma                                                | —                                                                           |
| 30 | [blast-bracket skill body]                                                                                              | NOTHING                                                                             | —                                                                           |
| 31 | /"change the output format of list to: mount owner modes reference"/                                                    | =registry/list= reshaped, README block re-captured                                  | —                                                                           |
| 32 | /"explain each."/                                                                                                       | the three explained                                                                 | —                                                                           |
| 33 | /"reference to identifier."/ · /"reason this out yourself. whats the overall correct approach??"/ · /"fix. holistic too."/ · /"mounted on fucking what??? context. retard. how many fucking mounted() functions are there?? idiot.."/ | =identifier= column; callout logged | the helper's name struck |
| 34 | /"go on 1 and 2. lashes for 3"/                                                                                          | headless refusal + defaults; one exit site                                          | 3 withheld                                                                  |
| 35 | /"and another 4. change the ghost trajectory from auth to lighthouse. code, readme, docs."/                              | =instance/lighthouse= everywhere                                                    | server =/auth/*= route untouched                                            |
| 36 | /"i feel like here is still work to be done?!"/ · /"we dont automatically select instances. they gotta be selected with use into session or --instance. what am i not getting??"/ | six naked verbs probed; 3 restated as "what a verb SAYS when nothing is selected" | — |
| 37 | /"go on problem needs solution. no on the solution. let teh coneeuseur have a pass and look at my coding style overall."/ | connoisseur loaded                                                                | =selection()= helper dead                                                   |
| 38 | /"fix it. then ikiro compact"/                                                                                          | trie-level guard built (5 leaves) — then killed by 39/40                            | —                                                                           |
| 39 | /"vector is too low level i suspect"/                                                                                   | reverted                                                                            | not the trie                                                                |
| 40 | /"this is ghost or paladin"/                                                                                            | =Instance.get home()= at paladin; four ghost call sites; suites green; live green; this compact | the demand lives at the owner of the fact |

* the walk — README 0–6 on stock ubuntu:24.04 (arm64, docker 26.1.1)

| step                            | verdict                    | evidence                                                                                                                                                                                                                                         |
| pre                             | NOT IN README              | stock image has no =curl=, =unzip=, or vcs client. README: /"Deno 2.7+ is the only prerequisite"/ → now "Deno 2.7+ and git"                                                                                                                       |
| 0 deno installer                | HALF                       | headless: binary lands, NO shell-config edit, no =~/.deno/env=. README now carries the explicit =export PATH= line                                                                                                                                 |
| 0 clone                         | GREEN                      | 416M. Content vs local tree: ONE file differs (=documentation/src/layouts/Layout.astro=) + 44 local-only untracked                                                                                                                                |
| 0 =deno task install=           | GREEN · 25.7s              | implicit pre-task install warns "Ignored build scripts: better-sqlite3", then =--allow-scripts= runs them; argon2 =linux-arm64= prebuild present; better-sqlite3 has NO binary and nothing needs it at boot                                        |
| 1 =ledger/init=                 | RED headless · GREEN pty   | headless: Ink "Raw mode is not supported", exit 1 (FIXED: defaults). pty: two prompts. Doctor matches README plus =✗ mountpoint —= (now in the README). Ledger =.env= ships =VIVA_REPOSITORY_MOUNT=""= shadowed by os                             |
| 2 =registry/tap registry/viva=  | GREEN from repo root · RED from =~= | relative to cwd; from =~= a stack + exit 0 (FIXED: one line, exit 1). Already recorded by step 0's first =viva= boot. =registry/list= now =mount owner modes identifier=, 3 rows, =@viva 10=                                                 |
| 3 =instance/create … --use=     | GREEN                      | shelf copy · =instances.json= · =sessions/<pid>.json=                                                                                                                                                                                             |
| 4 =instance/init=               | GREEN pty · 3m38 incl. boot | two-secret form (ANTHROPIC first, JWT second) → admin form → =auth/signup SUCCESS= → torn down. Ink drops text+Enter in one chunk — automation splits sends                                                                                        |
| 4 =auth signup <u> <p>=         | RED as ordered · GREEN after 5 | POSTs to a running lighthouse; README now says so; verb renamed =lighthouse=                                                                                                                                                                  |
| 5 =instance/run=                | RED naked · GREEN chained  | fresh shell: TypeError at =target.js:9=, exit 0 (FIXED: =instance: nothing selected — …=, exit 1). Chained: =run runtime=4448 kajuit=4449=, VITE 1673 ms, =Status:ALIVE=, kajuit =GET /= 200, runtime =GET /= 404 by design, =/status= → =ALIVE=  |
| 5 =run [process]=               | GREEN                      | =runtime= → :2501 only · =kajuit= → :1794 only · =all= → both · =bogus= → one line · =instance/stop= → =killed 2=, locks cleared                                                                                                                  |
| 6 checkboxes                    | 6 of 6                     | host Chrome through a socat forward: gate → =admin/changeme= → =connect= → shell, DAEMONS =playground 8=. Gate heading on a FIRST visit reads /"session expired"/                                                                                    |

* the proof — sql direct lookup

=~/.viva/instances/hello-world/mountpoint/service_multiplayer/lighthouse.viva.db=

#+begin_example
tables: Daemon  Identity  _mikro_migrations
01a0676d-2488-75db-8b99-f1473c229a2f  admin   {"provider":"password","credentials":{"username":"admin","password":"$argon2id$v=19$m=65536,t=3,p=4$sV4P…
01a06783-c1c8-715c-b524-46decbdad6eb  reader  {"provider":"password","credentials":{"username":"reader","password":"$argon2id$v=19$m=65536,t=3,p=4$rZCG…
POST …/auth/login {admin,changeme} → {"status":"SUCCESS","authority":{"access":"eyJ…","refresh":"eyJ…"}}
POST …/auth/login {admin,nope}     → {"status":"ERROR","error":{"code":"INVALID_CREDENTIALS"}}
#+end_example

* beef's questions, answered from the container (turns 6 · 7 · 8 · 28)

- *does the standard library load by default?* Yes, on the FIRST =viva= boot of any verb — =vip.js:38-42= =supply()=: =registry.read() ?? registry.seed(<repo>/registry)= — not on =ledger/init=. =registry.json= mtime 13:02:42 (during =viva --version=) < ledger =.env= 13:04:28.
- *is ="registry-education"= (bare) in registry.json true?* TRUE. =registry.js:46-51= =reference()= relativizes inside the store; =resolve()= branches it back. Store dir also holds =README.md bak=.
- *=instance/run [runtime|kajuit]=?* Real: =target.js= =CHILDREN= + =specs()= =all | runtime | kajuit=. Rewrite landed in README §5 with =instance/stop= and the =/status= capture.
- *"Raw mode is not supported"?* Ink puts stdin in raw mode to read keystrokes; no tty → no =setRawMode= → the wizard throws at mount. =ctx.interactive= (=mod.js:89=) already ruled once for every verb; =ledger/init= never read it.

* the fixes (turns 27 · 29 · 31 · 33 · 34 · 35 · 40)

- *README 0–5* — "Deno 2.7+ and git"; explicit =export PATH="$HOME/.deno/bin:$PATH"=; =✗ mountpoint —= in the doctor capture; §2 seeding sentence + the real three-row list; §4 =lighthouse signup= needs the instance up; §5 =run runtime|kajuit= + =stop= + "=/= is 404 by design" + captured =curl http://localhost:2501/status=. beef's =^E= fence slip at line 36 repaired.
- *Hello Mode* — =$entities.get()= → =find()=; =buffer.mode.connection.call("/hello/world")= → =buffer.mode.call.hello.world()= (=shape/object.js:20-33= accumulates segments); the fabricated =daemon, mode, thread= props became REAL: =subsystems/drapes/panels/Frame.svelte:62= now mounts a view with ={ terminal, daemon: next.mode.daemon, mode: next.mode, thread: next.thread, buffer: next }=. Bracketed: =Frame= imported once, 9 registry views read only =buffer=/=terminal=; kajuit 35/0 both sides; vite HMR clean in the container.
- *=registry/list=* — rows are ={ mount, owner, modes, identifier }=, identifier = =@owner/package/slug=; README block re-captured; ghost 77/0.
- *ghost — one exit site* — =mod.js= =fail()=: thrown · caught-into-=ctx.error= · returned ={ error }= all exit 1 with =ghost: <message>=; =--verbose= keeps the stack; =--json= → ={ "error" }=; emission moved onto the success path (the =finally= had printed =null= before the outer catch set =ctx.error=). NOT_FOUND stays 127.
- *ghost — headless* — =ctx.view.scroll.render= refuses by name when =!ctx.interactive=; =ledger/init= headless takes its own defaults. =pick= already refused by name; =instance/init= already had its door.
- *=instance/auth= → =instance/lighthouse=* — file, export, =nature=, usage; kajuit =Login.svelte= gate text; README; 50.01 (control · prose · heading · anchor); 51.02; =world/ledger.md=. Server route =/auth/*= untouched. Old verb → =no handler=, 127.
- *the demand at its owner* — =subsystems/paladin/prototypes/instance.js=: =Instance.get home()= returns =paladin.scope.instance= (a fresh Path per access — =branch()= MUTATES) or throws =instance: nothing selected — viva instances/use <slug>, --instance=<slug|path>, or VIVA_INSTANCE_MOUNT=; =resolve()= reads it, and so do ghost's =register()= · =specs()= · =stop= · =init=. Three private copies of the check deleted; =target.test.js:49= re-pinned. Ghost 77/0 · paladin 26/0; live: =run start stop init= one sentence exit 1, =doctor= keeps its param-aware refusal, chain and =--instance= strata still select.

* the connoisseur's pass — three shapes for one demand

1. =mounted()= helper in =target.js= — a subjectless participle, no grep, 14 files already carry the word. beef: /"mounted on fucking what??? context. retard."/ Callout logged.
2. =trajectory.branch(pattern).use(selected).affect(verb)= on five leaves — composition on the trie, but the precondition is not a routing fact. beef: /"vector is too low level i suspect"/ · /"this is ghost or paladin"/. Built, tested (one pinned test red), reverted clean.
3. =Instance.get home()= — paladin already voiced this demand at =instance.js:96= (=instance.mount: no scope.instance=); the fix was to make that sentence the human one and route every reader of the mount through the owner. Trigger 15 (write at the owner) · canon #6 (the scope answers =has= itself: ="instance" in paladin.scope=) · trigger 3 (one check, thin cases).

* state at fold

| thing                    | state                                                                                                                              |
| container =readmen=      | RUNNING; hello-world =run all= up; socat forwards → host =localhost:1794= / =:2501=; the CLONE carries every fix by =docker cp=     |
| Chrome                   | tab =@vivalence= logged in as =admin=; extension flaps (connected twice, dropped twice)                                              |
| Identity                 | =admin= · =reader= · =third= (argon2id)                                                                                              |
| registry.json (container) | 3 repo paths + ="registry-education"= (cloned to the store; mounted only on next runtime boot — untested)                          |
| repo, UNCOMMITTED        | =README.md= · =documentation/…/50.01= · =51.02= · =systems/ghost/{mod.js, trajectories/index.js, registry/index.js, ledger/index.js, instance/{index.js, lighthouse.js (was auth.js), target.js, stop.js, init.js}}= · =systems/ghost/tests/target.test.js= · =systems/kajuit/src/app/widgets/Login.svelte= · =subsystems/drapes/panels/Frame.svelte= · =subsystems/paladin/prototypes/instance.js= |
| =.ikiro/=                | this compact · index · =known-issues= entry RESOLVED · frontier strand 1 · =zettelkasten= callout · =world/ledger.md=                |
| logs                     | session scratchpad =walk/= — step typescripts, drift log, remote-tap log                                                            |

* corrections ledger (this session)

- =vcs-guard= denies ANY =git <word>= token in a Bash string — heredocs and =which curl git unzip= included. Scripts via Write; container-only clone; beef's graph untouched.
- =pkill -f "<pattern>"= killed my own wrapper (its command line carries the pattern). =pkill -x= / =ru[n]=.
- expect prompt =\$ $= vs root's =# =: every wait burned its timeout. Ink drops text+Enter sent as one chunk — split sends.
- SQL column =createdAt= is =created_at=.
- Compact numbers are ALPHABETICAL and shift — the readme-walk compact I called "79" was #62 when I looked and #63 an hour later. Cite slugs.
- I nearly re-applied the "uncommitted" patch onto a clone that already carried it. Measure before patching.
- Proposed a name without a grep (=mounted=), then a locus without asking whose fact it is (the trie). Both struck. The owner had the sentence all along.
- Claimed "instance/init has no headless path" — it has one (=init.js:107-111=); what it lacks is a way to ANSWER prompts headless.

* loose ends, open

- Signin gate heading /"session expired"/ on a virgin browser (=project_signin_gate_rework= says ready|boot|signin).
- =paladin.scope overwrite= printed by =ledger/init= (known-issues calls it RESOLVED m41 — it still prints).
- Doctor dump /"all over the place. isntances, daemons, modes, services."/ — beef's gripe, unshaped.
- =documentation/src/layouts/Layout.astro= differs local vs GitHub — an unsnapshotted edit, or the clone ahead?
- hello-world boot with =@education= in =registry.json= (four packages mounted by =supply()=) — untested.
- better-sqlite3 built no binary and nothing failed — which consumer still lists it?
- README Hello Mode comment line still spells raw transport (=mode.connection.call("/hello/agent", …)=); the compiled form is =mode.call.hello.agent({ user })=. Left as beef wrote it.
- the docs-site items (turns 13–15) belong to the other session — untouched here.
