#+TITLE: the provider Request contract lands — tools + output leave the config grab-bag for a named Request both the hallucination and its providers speak, config now IS that shape, and the Anthropic provider gets its first test
#+TOPIC: A "close my @beef notes" pass on the riddler tripped into the provider-contract recast. It began small — the last open riddler note (=emitter:23=, a random status-banded literal =sample()=) landed — then =hallucination.js:44= (*"does it maybe make sense to recast the input interface aka the contract with the providers?"*) opened the real work. I first YAGNI'd beef OUT of the redesign (=retard=, =status-quo-defense=). Corrected: a faculty's =(turns, config)= with tools smuggled into a =config= grab-bag became a named =Request = { turns, tools?, settings?, output?:{ object } }= — bilateral, tools/output first-class, =input= deleted (Vector-descriptor vocabulary with no referent on a composed request). The shape came out of a naming rotation (=choice= dropped, =params→settings=, =output.object= = the structured target, =tool_choice= is dressAsObject's private business). beef's last cut was the holistic one: *"config is part of the contract. we dont have to shuffle shit around if we set it up right holistically"* — so =config= now IS the contract shape and the compile just spreads it, no re-wrap. Landed across 3 holy files + the provider + callers + ~6 tests; docs + codemap synced; the whole suite validated (typology 1116 · runtime 43 · providers 34); the previously-untested Anthropic provider got a thorough offline test (30 steps) behind a new pure =buildParams= seam.

* Where this sits
Continuing the riddler @beef sweep from [[riddler-flattens-to-the-turn-standard-object-faculty-fills-output-and-the-system-hoist-lands-in-the-hallucination-per-connoisseur]] + [[typology-grows-a-v-slug-scalar-builder-the-object-faculty-fills-its-output-and-the-system-hoist-lands-at-its-owning-boundary]]. Those closed the flatten + the object-fill; this one closes the contract question they left as =@beef:44=.

* the last riddler note — sample() lands
beef: *"cool. sample. replace graduated with unknown. go."* =emitter:23= wanted a random sample of literals within a set of memory statuses. No repo method did =RANDOM()= + =memory.status $in= (=due/novel/byStrength/byLastSignal= checked). Added =LiteralRepository.sample(where, {status, limit, blacklist, populate})= = =find(merge({memories:{status:{$in:status}}}, where), {orderBy:{[raw("random()")]:"asc"}, limit})= (=Literal.ts=; =raw= from =@mikro-orm/core=, established by the =bak.methods= QueryBuilder use). Emitter's level feed → =sample({ontology:"word"}, {status:["LEARNING","KNOWN","UNKNOWN"], ...})= — a real level band, not =feed='s due-then-novel. beef renamed my =statuses=→=status= + =/riddle/fromSymbol=→=fromSymbols=. Live-only caveat: =raw("random()")= resolving to =ORDER BY random()= in libsql is DB-verifiable only on the run. Remaining riddler notes after this: =hallucination:5= (logging/telemetry, pre-existing) — =hallucination:44= became the whole contract work below, now RESOLVED.

* the retard — status-quo-defense
On =:44= I wrote a long "verdict: don't recast now, YAGNI" — argued =turns= was fine (true) and =config= "leaky but harmless." beef: *"and i might want to see the providers rewritten too... i told you retard! contract. contract both sides. ...???!!! why are the tools inside config?!"* Two failures in one: (1) I met a "does it make sense to redesign X?" lean with a minimal-delta dismissal — talked him OUT of a redesign he wanted; (2) I stared at =config: { ...config, ...tool.compile() }= — a request INPUT (tools) crammed beside a loop-budget (rounds) — and called it "harmless" instead of reading the grab-bag as the tell of a MISSING contract. And "contract both sides" means bilateral; I only analysed what the provider READS. Logged =family: status-quo-defense= (proposed). Rule: when beef points at an interface and asks whether to recast the contract, ENGAGE the redesign; a contract is the one shape BOTH producer and consumer speak; a grab-bag options object = a missing contract, split it, never excuse it.

* the contract, rotated into shape
A naming rotation converged the shape (each beef quote a turn):
- =turns is right.= — a uniform =turns= array (system as a role, hoisted) is the provider-agnostic lingua franca; DON'T split into ={system, messages}= (that bakes Anthropic's shape into the contract — the same mis-file the earlier hoist corrected).
- *dressAsObject stays a cortex PROXY* — beef: *"3. dress as object proxies the object render faculty when none is provided and dialogue is."* I'd wrongly proposed dissolving it into the hallucination; it's the fallback that lets =findOne({type:"object"})= answer uniformly (native object faculty wins, dialogue dressed otherwise). It stays; it just reads =request.output.object= now.
- =not params, settings. what is choice?= — =choice= was my abbreviation of =tool_choice=; its only user is dressAsObject forcing =respond=, so it's NOT a contract member — it lives in =settings= (a model knob), set INTERNALLY by the proxy. And =input= is DEAD: beef: *"we dont pass inputs, we compose a hallucination… schema.input only exists on emitter, apertures, tools and other vectors"* — Vector-descriptor vocabulary, no referent on a composed request → deleted.
- =output:{object:schema}= — beef: *"output is part of the contract with the providers… output is where we place additional instructions about the output within their semantic name as keys, and objects is always a schema. settings is about configuration of model behavior."* So =output= is the return-shaping namespace keyed by kind (=object= → schema; speech/verbatim later), NOT a top-level =object= member. Four orthogonal members, none modality-specific.

* landing the Request contract (holy, blast radius)
beef: *"perfect. spec it. check the blast radius."* then, on my spec's compile shuffle: *"we have changed the contract but not config?! makes no sense. config is part of the contract. we dont have to shuffle shit around if we set it up right holistically."* The holistic fix = =config= holds the contract shape natively (=config.output= IS ={object}=), so the compile spreads it with no re-wrap; the ONE bare→={object}= translation is the named facade =hallucination.output.object(X)=.
#+begin_src
subsystems/typology/
  schematics/primitives/hallucination.js   + Settings · Output · Request
  prototypes/hallucination.js              CONFIG {rounds,tune,settings,output} (input GONE, output IS {object});
                                           compile spreads (no re-wrap); render/stream(faculty, request, rounds, tools);
                                           facade hallucination.output.object() (schema.input/output dropped)
  prototypes/cortex.js                     dressAsObject(request) → request.output.object, forces via settings.tool_choice, v.fill
registry/viva/hallucinator/anthropic/provider/index.js   render/stream(request) via buildParams
systems/runtime/daemon/traits/harnessed.js  + riddler emitter/aperture   .output.object(schema) facade
tests ×6                                     mocks destructure ({turns, tools, output, settings})
#+end_src
=rounds= (loop budget) + =tune= (resolved to the faculty at findOne) stay hallucination-INTERNAL, never on the wire. Blast-radius MISS caught mid-run: =cortex.integration.test.js='s =captureSonnet= wrapped the faculty as =(turns, config)= — not in my grep; fixed to =(request)=.

* docs · codemap · testing validated · gap 1 closed
beef: *"update docs adn codemap"* → =docs/…/47.02_hallucination-triad.org= (tangled: prose contract + mock + object test + two "why" bullets; parked =config.schema→output= intent marked LANDED; .js kept in sync) + codemap =typology.md= (Hallucination) + =typology/schematics.md= (primitives) now carry =Request=.
beef: *"validate all testing is running and representative"* → typology 104 files/1116 steps · runtime LLM-path 6/43 · all 0 failed; representative (mocks assert =request.tools=/=settings.tool_choice=/=output.object=, not vacuous). Surfaced a real GAP: the Anthropic provider had NO test (only speech providers did).
beef: *"close gap 1 thoroughly"* → extracted a pure =buildParams(model, request, stream)= into =translate.js= (DRYs the render/stream param duplication + makes the request→params logic testable), then =anthropic/tests/provider.test.js= (30 steps): every =translate= fn (hoist, multi-system, tool_use/result, image/thinking, all stream events) + =buildParams= every branch (defaults · maxTokens · stream · thinking gate ×3 · tools · tool_choice) + faculty shapes offline. Anthropic went from the only untested hallucinator to the most covered.

* Loose ends / frontier (beef-decided, survive)
- LIVE (beef drives): the real provider run — emitter/reply/grade AND now the contract end-to-end against a live key.
- =hallucination:5= — logging/telemetry infra, pre-existing @beef note, untouched.
- =/assistant/resolve= dropped (client dev button 404s), =emitter:26='s successor now landed as sample; async-eval trigger; =v.rel= id+slug diff; full client transcript binding — all still open.
- speech/verbatim (=elevenlabs/deepgram=) stay out of the Request contract — not turns-based (textChunks/audioSource); a =settings= unification is a separate move.
- beef trimmed =hint= out of the emitter's seeded =buffer.data= (reply.system still carries it) and simplified =literals=.
