#+title: the docs survey wakes on a stale map, beef walks the README on his own machine, and the signing secret learns to mint itself
#+filetags: :docs:readme:documentation:ghost:paladin:instance:init:env:astro:home:m46:quest:map:landing:

* what happened, in one breath

beef: /"ikiro wake docs"/ (session named "docs survey"). Wake read the kernel, frontier, the docs memory,
compacts 84 and 87 and m46, built the site (10 routes, green), diffed the README's three hello-world fences
against =commons/instances/hello-world/= (byte-identical), and found the map still naming a =docs/= dir that
no longer exists — corrected, with a measured note on the stale =@education= known-issue. Then beef pasted his
own README walk on macOS: singular =instance/use= and =instance/remove= answered with nothing, the env wizard
asked the optional Anthropic key before the required JWT, a short JWT booted and signed up before the doctor
called it INVALID, and the doctor's env table at 66 columns was six-character mush. His one design ask —
/"generate a missing default value functionally … lazy after. if simple, go directly"/ — was simple (typology
stores =.default(fn)= raw, two ghost readers) and LANDED: =envfile.fallback=, =init.js= prefill, hello-world's
=SECRET_VIVA_JWT= mints 32 base64 chars at scaffold; pinned in =ledger.test.js=, walked on a scratch ledger
(=env: present=, =problems —=), README fences regenerated, 50.01 §3/§4 recaptured from his run. Mid-turn he
rewrote the home totems (six, =AI-Harnessed= first, =Modal — modes over plugins=, =Functional Architecture=)
and asked the real core line count: 17.8k with ghost, corrected to *15,200* — /"ghost not considered core"/.
Zero VCS; everything uncommitted.

* spine (N=7 · this session)

| n | beef asked                                                                                                                                                                                                                                                                | landed                                                                                                                                                                                                                                                                                                                                                                                                                       | decided · killed                                                                                                                                             |
|---+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+--------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | /"ikiro wake docs"/ (+ =/rename docs survey=)                                                                                                                                                                                                                             | kernel · frontier · quest index · docs memory · compacts 84 + 87 · m46 intent/defects read; =astro build= 10 routes 1.79 s; README fences == disk ×3; content tree measured (8 live pages, only =home.example.js= captures); =world/map.md:24= =docs/= → =documentation/=; =known-issues= hello-world =@education= entry: measured note (zero refs, key =.optional()=); wake report with a dead/stale table + five gated proposals | proposals a–e (50.01 prereqs, README:336 =@viva=, 32.01/51.02 on the dead standalone, 41.01:69, =## Instance= stub) WAIT; emacs hazard on 50.01 flagged |
| 2 | /"this is me running through the docs. should i do another run for you to get special info?"/ + the macOS transcript                                                                                                                                                      | five items measured against code: singular =instance/use= (not a C-bag row) · =instance/remove= · wizard order = =check.js:123= alphabetical · F5 live (=Init.jsx:68= commits without re-check) · doctor 6-char cells; sessions 3 → 1 after delete; wizard-order sort sketched                                                                                                                                                 | one more run asked only for what I cannot reach: =tput cols= · private-window gate at =:1794= · =ps= after ^C                                             |
| 3 | /"it should be easy for us to generate a missing default value functionally, no? ie its a string default or a function default and function gets executed if nothing is provided. lazy after. if simple, go directly. otherwise brief and proposal diff. work my example into ikiro and where it fits the docs."/ | SIMPLE → went directly: =envfile.fallback= + =scaffold= · =init.js:36= prefill · hello-world JWT =.default(() => btoa(…24 random bytes…))= · =ledger.test.js= minted-once + stable (15/15) · scratch walk =create → use → init= headless =env: present=, doctor =problems —= · README fences regenerated + §4 sentence + line 190 clause · 50.01 §3/§4 CAPTURED blocks (his run + scratch) · m46 third walk section, L25–L29, changelog, =* release= line · known-issues macOS subsection · frontier · memory ×2 | full ghost suite 80/1 — =strata.wet= "two shells" RED with and without the inherited pid: fake pids, =ledger/doctor= reaps them; not this diff's           |
| 4 | MID-TURN /"add anohter one of these at first position: … rewrite: AI-Harnessed - as a first principle. # first slot · rewrite to Modal - modes over plugins. # second slot · add another one: functional architecture - core is ~26k lines. ??? whats the actual number? work those into the docs on home."/ | =home.mdx= totems → six; counts measured (typology 10,640 · paladin 2,109 · runtime 2,451 · ghost 2,614 · sheets 1,129 · dapper 2,340 · drapes 3,236 · kajuit/src 13,004); pillar wrote =~18k= (core + ghost); build green, totems rendered                                                                                                                                                                                | 26k ≈ core + the three UI libs; README trajectory's runtime ~4k noted stale, untouched                                                                      |
| 5 | /"ghost not considered core. only typology · paladin · runtime"/                                                                                                                                                                                                          | pillar → =~15k= (15,200); m46 changelog + docs memory count law corrected; rebuilt                                                                                                                                                                                                                                                                                                                                             | CORE = typology + paladin + runtime, nothing else                                                                                                            |
| 6 | =/compact= (local)                                                                                                                                                                                                                                                        | —                                                                                                                                                                                                                                                                                                                                                                                                                            | NOTHING                                                                                                                                                      |
| 7 | /"ikiro compact"/                                                                                                                                                                                                                                                         | this file; =world/ledger.md= + ghost shard stamped with the mint law; index regenerated; quest report                                                                                                                                                                                                                                                                                                                         | —                                                                                                                                                            |

Balance: rows 1–3 carry the work and most citations below; rows 4–5 one section; 6–7 the fold. No last-third weighting.

* the arc

** the wake finds the map lying about a directory (row 1)

=world/map.md:24= still said the human-facing docs live in =docs/= (org-mode, "8 files, dirs 42–45 empty"). The
dir is gone; =documentation/= is the site — Astro 7 on Deno, =deno run -A npm:astro build= ≈ 2 s, 10 routes,
=docs.vivalence.org= 200. Rewritten to the measured shape (live pages · =bak/content-scaffold/= 33 · =.mdx.bak= hidden).
Same pass: the README's =Hello, Mode!= / =Hello, Instance!= fences were diffed against disk — three IDENTICAL
(30 · 25 · 85 lines) — so the "recapture when beef settles the shape" owed by compact 85 is settled: the shape
is =mode.viva.js= + =App.svelte= + =instance.viva.js=. Dead surfaces measured, not touched: 32.01 + 51.02 built on
=@commons/instance/standalone= (does not exist; their =<Example/Trace/Source>= panels have no =*.example.js= and no
snapshot — build stays green because capture cannot fail) · 50.01:22 "Deno 2.7+ is the only prerequisite" ·
README:336 =@viva= · README:90 =## Instance= two comment stubs · =tmp.md= at root is 0 bytes (compact 32's "13.01 cut
sits in tmp.md" is FALSE now; 13.01 carries a 63-line uncommitted diff of beef's).

** beef walks it himself (row 2)

His transcript is the third walk in =m46-linux-readmen.org= (verbatim, L25–L29). What it measured:

- =viva instance/use hello= → =no handler= twice — the verb is PLURAL =instances/use=. C1 lists seven ways to select;
  none is "the singular fails silently".
- =viva instance/remove= → =no handler= twice — =delete=.
- the wizard's first field was =SECRET_VIVA_ANTHROPIC_API_KEY= (optional), =SECRET_VIVA_JWT= (required) second:
  =subsystems/paladin/belt/check.js:123= =rows.sort((a, b) => a.key.localeCompare(b.key) …)=. Declaration order dies
  at the doctor; =init.js owed()= inherits. Sketch (gated): required first, then =Object.keys(environment.properties)= order.
- he typed a short JWT; the wizard committed, booted, =auth/signup SUCCESS=, =runtime pid 90730 · alive=; =instance/doctor=
  afterwards: =SECRET_VIVA_JWT INVALID must not have fewer than 24 characters=. =Init.jsx:68 commit(values)= writes
  and moves to =boot= with no re-check; =init.js:24= keeps INVALID rows for the NEXT init only.
- the doctor at ~66 columns: =key= cells =PUBLIC= / =SECRET= / =VIVA_C=; =runtime= row prints the object's keys
  (=slug statics datamap=); =vars= shows raw =${…}= next to =env='s resolved values. L15's face, narrower.
- =instance/delete= asked =rm -rf …, 3 sessions?= — and =~/.viva/sessions/= holds 1 file after.

Answer to /"another run?"/: no for the five — reproducible from a scratch ledger. Yes for three things only he can
give: =tput cols=, a PRIVATE window's first visit at =:1794= (the "session expired" gate carried across three
compacts), and =ps= after ^C on the ready frame.

** function defaults, landed on "go directly" (row 3)

Mechanism, measured before writing: =.default(val)= is stored RAW by typology (=schematics/v.js:20=
=enhance(derive(target, { default: val }))=); =v.environment()= unwraps each property to a plain schema, so a function
there is a real thunk, never the enhance setter leak; env schemas are read at exactly two ghost sites — =envfile.scaffold=
(first init authors =.env=) and =init.js owed()= (wizard prefill); =check.js= converts before it checks and NEVER
Defaults. Typology untouched, so no HOLY gate; beef's "if simple, go directly" held.

#+BEGIN_EXAMPLE
systems/ghost/belt/envfile.js
+export const fallback = (held) => (typeof held?.default === "function" ? held.default() : (held?.default ?? ""));
-      lines.push(`${key}="${held.default ?? ""}"`);
+      lines.push(`${key}="${fallback(held)}"`);

systems/ghost/trajectories/instance/init.js:36
-          : (schema[row.key]?.default ?? ""),
+          : envfile.fallback(schema[row.key]),

commons/instances/hello-world/instance.viva.js:83
-  SECRET_VIVA_JWT: v.string({ minLength: 24 }).desc("Lighthouse signing secret. Generate with: openssl rand -base64 24").group("keys"),
+  SECRET_VIVA_JWT: v.string({ minLength: 24 }).desc("Lighthouse signing secret. Minted at first init; rotate with: openssl rand -base64 24").default(() => btoa(String.fromCharCode(...crypto.getRandomValues(new Uint8Array(24))))).group("keys"),
#+END_EXAMPLE

"lazy after" = the thunk fires once, at scaffold; the file holds the value; every later read is the file. Test first
(=ledger.test.js=: a =SECRET_VIVA_PROBE_TOKEN= with a =minted-<uuid>= thunk — written at first init, absent from =fill=,
unchanged after the second init), then the three edits, then green 15/15. Scratch-ledger walk, bypassing the =viva= shim
(=VIVA_LEDGER_MOUNT= · =VIVA_REGISTRY_MOUNT= · =VIVA_PROCESS_ID= exported, =deno run --config <repo>/deno.jsonc -A
<repo>/systems/ghost/mod.js=): =ledger/init= → =instance/create @commons/instance/hello-world= → =instances/use hello-world=
(=stratum session=) → =instance/init= headless =env present · next viva instance/init <username> <password>= → =.env=
holds a 32-char JWT → =instance/doctor= =problems —=. README fences regenerated from disk (three IDENTICAL again),
README §4 "defaults and minted secrets are written, you're prompted only for what can't be deduced", line 190
"A default may be a function…". 50.01 §3 gained his =instance/create= capture (source/slug/from/target/env) and the
bare-slug sentence; §4 gained =instances/use= (scratch, =stratum session=), the ready frame (his), the =.env= (scratch,
JWT masked to four chars), doctor =problems —=, and the mint sentence. Build green.

Full ghost suite: 80 passed / 1 failed — =strata.wet.test.js= /"two shells select two instances in parallel"/. Re-run with
=env -u VIVA_PROCESS_ID= (beef's zshrc exports one): still red. The test fakes pids 61001/61002, then runs the doctor, and
=ledger/doctor= reaps sessions whose pid is not alive — the =ledger-doctor-reaps-a-sibling-shell-mid-boot= fork in
known-issues. My diff touches nothing on that path.

** the home totems and the number (rows 4, 5)

Mid-turn: six totems in his order — =AI-Harnessed · as a first principle= first, =Modal · modes over plugins= second
(was =Modal Systems · everything is a mode=), the three existing, =Functional Architecture= last. =Home.astro:16-22=
renders totems as inline spans, no grid — six flow. The number: =wc -l= over js/jsx/svelte/mjs minus tests/bak/node_modules:

#+BEGIN_EXAMPLE
typology  10,640    paladin  2,109    runtime  2,451    →  core 15,200
ghost      2,614    sheets   1,129    dapper   2,340    drapes 3,236    kajuit/src 13,004
#+END_EXAMPLE

I shipped =~18k= (core + ghost, matching the README's trajectory figure). beef: /"ghost not considered core. only
typology · paladin · runtime"/ → =~15k=, rebuilt, count law in the docs memory. His =~26k= guess ≈ core + sheets + dapper
+ drapes (24,519). README's trajectory paragraph still says runtime ~4k (pre-m49) — his, untouched.

* state at fold

| thing              | state                                                                                                                                                                                                                                                                                       |
|--------------------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| code (uncommitted) | =systems/ghost/belt/envfile.js= (=fallback= + scaffold) · =systems/ghost/trajectories/instance/init.js:36= · =systems/ghost/tests/ledger.test.js= · =commons/instances/hello-world/instance.viva.js:83= (JWT mints) — ghost ledger suite 15/15; full ghost 80/1 (pre-existing red above) |
| docs (uncommitted) | =README.md= (fences regenerated ×3, §4 sentence, line 190 clause) · =documentation/content/50-59_practice/50.01_slowstart.mdx= §3/§4 captures · =documentation/content/home.mdx= six totems, =~15k= — =astro build= 10 pages green                                                          |
| =.ikiro/=          | this compact · =world/map.md:24= · =world/frontier.md= m46 line (7,172 chars) · =world/ledger.md:28= mint law · =world/codemap/ghost.md= init sentence · =known-issues.org= (=@education= note; m46 macOS subsection) · =quests/m46-linux-readmen.org= (third walk, L25–L29, changelog, =* release= line) |
| memory             | =project_environment_schematics= (function defaults) · =project_documentation_site= (home totems + count law)                                                                                                                                                                              |
| beef's machine     | shelf =~/.viva/instances/hello-world/= recreated in his walk from the NEW recipe, but BEFORE the mint landed — his =.env= still holds the short JWT; =viva instance/init= there now prefills the INVALID secret row with a minted value (=owed()= → =envfile.fallback=) — commit, done      |
| scratch            | =scratchpad/walk/= ledger + =spine.py= — ephemeral                                                                                                                                                                                                                                          |

* corrections ledger (this window)

- =~18k= with ghost counted as core → beef: ghost is not core → 15,200. The README's own paragraph had set the composition; I should have used it and named the cut.
- my scratch walk's first pass passed every multi-word verb as ONE argv (zsh does not word-split =$c=; =${=c}= does) → three false =no handler= lines; re-run before reading anything into them.
- the =.env= masking =sed= used =#= as delimiter with =#= inside the replacement → "bad flag" — =|=.
- the wake report called 50.01 an emacs hazard; measured next turn: the autosave is a day OLDER than the file — stale, safe. Measure mtimes before flagging.
- the first =cat= of the docs memory overflowed the Bash tool (32 KB) — the Read tool for memory bodies.
- =cd documentation && astro build= left the Bash cwd there; the next relative =git diff --stat README.md= failed as "unknown revision". Absolute paths after any =cd=.

* loose ends

SETTLED this window (measured):
- README hello-world fences vs disk — IDENTICAL ×3 before and after the recipe edit; the "recapture when beef settles the shape" is closed.
- =tmp.md= "holds the 13.01 cut" — FALSE, 0 bytes; 13.01 carries beef's own uncommitted diff.
- 50.01 emacs autosave — stale (older than the file), safe.
- known-issues hello-world =@education= — zero refs; Anthropic key =.optional()=; measured note added.
- =world/map.md= =docs/= line — replaced.
- beef's shelf hello-world "OLD shape" (compact 32) — he deleted + recreated it in his walk; new shape.
- F5 for hello-world's JWT — MOOT on a fresh create (mints); the wizard-commit-without-recheck remains for any other typed value.

CARRIED (no clear-cut rule resolves them):
- wizard order =check.js:123= — sort sketched (required first, declaration order), gated.
- =strata.wet= "two shells" RED — pre-existing; the doctor reaper fork is beef's.
- README:336 =@viva= → =@commons= · README:90 =## Instance= stub · README trajectory runtime ~4k · README:196 Slowstart root link.
- 32.01 + 51.02 on the dead standalone with dangling panels — fork: rewrite on hello-world with a real example, or =.mdx.bak= now (rec).
- 41.01:69 registry tree pre-m47.
- doctor dump (L15/L29, =runtime= keys, raw =vars=) — own quest.
- beef's one run: =tput cols= · private-window gate at =:1794= · =ps= after ^C.
- singular =instance/use= / =instance/remove= reach — C-bag has no row; whether ghost should say "did you mean" is a design call.

Next: beef's =viva instance/init= on his shelf (the minted prefill fixes his INVALID JWT) · his one run · commit.

* quest report (derived at fold — `methods/quest-report.py`)

| quest                        | progress  | status  | next    | sessions   |
|------------------------------+-----------+---------+---------+------------|
| m25-user-and-thread-handling | —         | open    | survey  | 0          |
| m27-memory-and-notes         | —         | open    | survey  | 0          |
| m42-history-purge            |   0% 0/4  | blocked | fork    | 2 (1 live) |
| m44-authoritative-slugs      |  41% 7/17 | landed  | control | 7          |
| m46-linux-readmen            |  17% 3/18 | wip     | drain   | 2 (2 live) |
| m50-the-three-way-weave      |  60% 3/5  | wip     | blast   | 0          |
| m51-standalone               |   0% 0/18 | design  | go      | 1 (1 live) |
