#+TITLE: M11 goes from designed to LANDED in one arc — ② baseline captured live, the owner-key rename dies before birth (fork 7), supply migrates to the system ledger with a registry.json record (forks 8/9), the bracket blasts green through five mid-flight corrections (forks 10/11 among them), circuitry is eradicated as a word, and the testament resettles twice before the dust holds

#+TOPIC: Direct continuation of beef-edits-the-quest-directly-forks-4-6-and-two-tree-corrections.org (same quest arc, new session). Where that compact ended — quest balanced, UNBLASTED, awaiting the blast go — this arc runs the ENTIRE remaining lifecycle: baseline, five more forks, the blast itself, and post-blast operational cleanup. M11 is DONE.

* Trigger 1 — ② baseline (beef: "load up m11 packages and run our first blast testing. run the runtime, client, check the testament / variant, check chrome, run all tests. re-cast specific snapshots of entities that we will change - ie around paladin and from runtime perspective.")
BASELINE = 38 pensieve leaves, pasted into the count-test tangle. All =bak/= dirs excluded (the canonical =--watch= tasks never exit; paladin's task also lacks =--no-check= unlike every sibling). Recorded reds: paladin 2 (System.lock =locks()= + System.log stub), runtime 1 (integration expected =game/{exhibit,pick,match}= which beef had commented out of the declaration), ghost wholly red (shell→ghost WIP), typology+kajuit green. Fossil tests mapped: 4 ghost + 8 kajuit files importing deleted modules. Snapshots re-cast both perspectives. CONFESSION logged: killed beef's healthy runtime watcher on a false-404 diagnosis — bare curl probes 404 on wrong route shapes regardless of health; the real health answer was a 401 on an entities route (auth-gated = alive).

* Trigger 2 — "you run the runtime! what is this issue … i think that sounds inside radius?! tell me."
The paladin reds, grounded: =System.log= was a stub (=console.log("log(type, slug) LEGACY")=, the Log class commented out, log.js never existed anywhere) and =locks()= returned =slug= where the test expected =.process= — the tests are the OLDER spec; the later System rewrite went process-first. Verdict delivered: inside the bracket's GATE-radius (④ demands =paladin/test= green), outside its CHANGE-radius — beef right operationally. Repair folded in as ③ step 8 / rows ⑱⑲⑳.

* Trigger 3 — the eight-question turn (beef, culminating in fork 7)
1. =hydrate= = recursive thunk-caller in variant.js (lazy env/secret refs → values at resolve time). 2. One unreconciled =@beef= inline: resolveSource → paladin (typology rejected STRUCTURALLY: =index.client.js= ships to browsers, =Deno.env=/=Deno.cwd= have no client-safe form) → =paladin.source= via =belt/source.js= installer. 3. Span fit CONFIRMED — ghost =mod.js:21='s own breadcrumb (=pipe.tap(span=> … paladin.system.log())=) anticipated it; =Log.append(span)= writes =span.json= to =spans.jsonl=, both methods thin over the existing =paladin.state= belt. 4. system.test.js IS the ledger-directory coverage. 5. =prototypes/system/= exists (directory, not file). 6. A lock = a spawned CHILD PROCESS of an instance — key =(instance, process)=; instance-first won (matches beef's =<variant>/<type>/<slug>= instinct; =locks(instance)= is the question doctor actually asks). 7. "go on the tangle. tangle the blast". 8. FORK 7 (beef verbatim): *"i dont like the package name here: package: pkg. maybe this is one of those cases where we not name the think after its mode/module type. maybe owner was the better name. module type is package. but the key on manifest i think makes sense as 'owner'."* — the owner→package key rename REVERTED before ever landing. Pure shrink: blast rows ③④⑤⑥ died (manifest.js/cast/is/schematics untouched), pensieve collapsed to default→throw, every =pkg= binding + the reserved-word convention died unborn. Also this turn (beef): *"notify me of any weird one hoc or adjustments youre making if shit dont work out as planned"* → standing rule, memorized (=feedback_notify_adhoc_adjustments=). Also found + healed: two changelog entries orphaned at EOF past the tangle. "end the blast" closed the turn.

* Trigger 4 — fork 8: "I think we've made the design boo boo. I think the source of packages should actually be the system ledger. I think variant should not declare its packages. The … Pensive should rather throw an error if a package is loaded that isn't in the registry. But the population of the registry should happen elsewhere. It's similar to how there is multiple instances of Python packages … per system. … Let the connoisseur have a pass against the architecture and tell me what he thinks and plan the blast of this."
Connoisseur pass CONCURRED: the design's own locked pair (system = supply, variant = demand) was being violated by =variant.packages= — demand authoring supply. The flake.nix analogy retired for pip/site-packages/cellar. Net DELETION: ⑨b/⑨c died; runtime/ghost/bench converged on ONE =vip.supply()= (ghost's chicken-and-egg dissolved BY CONSTRUCTION — supply needs only pre-variant state; beef's earlier "same!" came true one round late). =vip.accio= gained the two-throw totality: "package not supplied" ≠ "Module 404" (policy in Vip; =pensieve.revelio= stays a pure descent, canon #5). Then two rapid refinements: *"i have a variant path at the root of the system and a ledger path … the ledger path is from now on the directory that contains the lock files and the instances.JSON. It now further contains a registry.json containing a record of package locations."* + correction *"a log directory and a lock directory"* → =registry.json= lands NOW (array of ABSOLUTE locations — per-machine pins; a name-keyed record was rejected as a second naming authority fighting derive/lock), =Registry= class (read/write/seed) beside =Instances=, =paladin.source= UNPARKED after exactly one turn parked (its real consumer arrived). FORK 9 in the same breath: *"lets rename the system directory to ledger. including the blast radius of env vars"* → =testament/ledger=, =VIVA_LEDGER_MOUNT=, scope key ="ledger"=; the ACTOR keeps its name (=System= prototype, =VIVA_SYSTEM_MODE/ROLE=, =viva system= command) — "the system keeps its ledger". Ghost's own comment had already called it "the system ledger" — the name was emergent. beef: "have we resolved my design bubu?" → point-for-point yes, on paper.

* Trigger 5 — "go" — the blast itself (③④⑤ in one run)
Awk-tangled 15 files from the quest, applied ~20 diffs by hand, ran the mv/inline/sweep scripts. ALL SUITES GREEN at ④: paladin 11 · runtime 26 (172 steps) · typology 104 · kajuit 8 · ghost keeps its pre-existing WIP reds (no NEW failure mode — gate held). Deviations, each caught live and logged in the quest changelog: (1) tangle carried =CHANGE START/END= markers into live files — stripped; (2) =belt.source(this)= missing from the Paladin CONSTRUCTOR (installers run there, not via the barrel); (3) bare =mv registry/playground registry/development= would have NESTED (tangle had pre-created the dest) — content-move; (4) secrets.jsonc folded into =.env= via a deno one-liner BEFORE deletion (values never through assistant output; .env had zero SECRET lines — cold delete = silently dead hallucinators; rotation still owed, keys leaked in VCS history); (5) circuitry-inline produced DUPLICATE =export const services= (pre-inline they merged in variant.js) — merged by hand; (6) dissolution left TWO variant cakes under one mount (=variant.mount= throws on ≠1); (7) REAL BUG proven live: =Signature.branch(x)= links the child into the parent's fin chain — =root.branch()= GROWS =root.absolute= (slurp semantics; old code never saw it because =paladin.scope.*= resolves a FRESH Path per Proxy access) — vip.mount switched to string composition, typology untouched, memorized (=project_signature_branch_mutates=); (8) the sweep's lookup pass ran before its path pass and half-rewrote deep-import PATHS (=registry/kernels/@education/…=) — second perl pass repaired.

* Trigger 6 — two mid-blast corrections became forks 10/11
FORK 11 (beef): *"we dont hardcode package.viva.js !!!! what?!?! we do a recursive find of viva files to populate the registry. the packages have a manifest.type property that identifies a pacakge. wrong approach!!!"* → =vip.mount= reads NO magic filename: the walk itself finds the package cake (=manifest.type === "package"=) among the cakes; its =manifest.owner=, when authored, LOCKS the mount. The four =package.viva.js= files keep their names by CONVENTION only. FORK 10 (beef): *"the variant file that we want to have running … inside of our testament folder next to the dot env file … basically the old variant back with the daemons and services decomposed into two … imported files. Circuitry, ignore that term. one playground with all of the playground stuff we have been building, one education with all of the language learning stuff … that's it for now."* → =test.viva.js= importing =education.js= (slug =brazilian=, FULL set: 13 games + clinic + five-fold-session + aprende + dewey + dataspace) + =playground.js= (all six modes); spanish daemon NOT carried (VCS history has it); count test → BASELINE+6. Bit by M11's own lesson en route: =tactic/survival/= DIR holds slug =five-fold-session= — dirs cosmetic, manifests rule; both tactic manifests lacked =version= (never live before). The integration describes for exhibit/pick/match/cloze — commented out at ④-adjust when undeclared — were UNcommented and went green against the live runtime. Also (beef): *"There is no circuitry's export. Doesn't exist. Nowhere."* → term eradicated repo-wide: schematics =primitives/circuitry.js= merged into =primitives/variant.js=, barrel line dropped, =v.primitives.circuitry.*= → =v.primitives.variant.*=, runtime =/metadata/circuitry= → =/metadata/variant=, dead comments deleted. Grep-zero.

* Trigger 7 — post-blast operations: login, CORS, the clean testament
Chrome check of daemons: login gate is beef's (passwords/tokens = hard-prohibited even supplied — beef asked explicitly about devtools token injection, declined, gave a self-serve console one-liner grounded in =stores/lighthouse.js='s =lighthouse:<url>= localStorage shape). Daemons proven via the auth-free =/metadata/daemons= aperture IN chrome: brazilian 23 routes + playground 6. beef's login failed in chrome — his hypotheses (user not installed / lighthouse mountpoints mishandled) both cleared by live API login SUCCESS; real cause proven by replaying the preflight with both origins: =shard/cors.js= allowlists =localhost= but NOT =127.0.0.1= — the tab I'd opened. beef doubted ("i doubt thats the issue. i can login via firefox") — the error text itself named the 127 origin; curl table settled it. *"ah. ok legalize it."* → ="127.0.0.1"= added to the origins array (the pattern builder appends the port group itself), hot-reload verified. Then beef pasted the full testament tree: *"i think you worked unclean in the testament. remove the old variant surgically … they even have their own mountpoint. not wanted."* → cut =variant/bak=, =daemon_spanish=, =daemon_typing-practice= mountpoints, stale =*_temp.lock= files, stale instances entry (unrecoverable DBs tarred to scratchpad first as insurance). *"stop the runtime process"* — beef took the ports back; both watchers killed. Final resettle: *"Move all of these … into this directory!"* → everything variant-ish nested into =testament/variant/= (=.env= included); 8 deno task strings + =.env= values + =.env.example= updated; paladin green against the new paths. Standing testament shape: =testament/{ledger/{locks,logs,instances.json,registry.json}, variant/{.env, test.viva.js, education.js, playground.js, environment/, mountpoint/}, _bruno/}=.

* Resumption
M11 is LANDED — ①②③④⑤ complete, all gate suites green, quest stamped, memory updated (=project_m11_package_ontology= reflects BLASTED + forks 7-11). Open ends, all beef's:
- fork 4 (lock-demo residence) + fork 6 ("cake" naming, leaning =crate=) — parked by design;
- the 4 leaked API keys still want rotation (in VCS history regardless of the secrets.jsonc deletion);
- =middleware.js next() called multiple times= fires in the transport chain's hard-failure path — masks the real error with a second one, wants its own small bracket;
- ghost suite wholly red (shell→ghost WIP, pre-existing) — beef's active construction site;
- the quest's tangle blocks for vip.js/test.viva.js/scenario-table drifted from the landed reality during forks 10/11 (changelog records everything; a tangle-resync pass is cheap if the quest should stay regenerable);
- 11.4 (Dockerfile variant mount + docs sweep) never ran — the only unblasted milestone remnant.
