#+title: the stripwire page keeps its clean demo and regains a feature gallery, and doc examples get pinned to public combinators not internal forms
#+filetags: :documentation:stripwire:public-api:doc-doctrine:

* the tail

Closing fold after the two big folds of this session ([[the-documentation-site-is-born-astro-7-runs-on-deno-past-the-vite-plugin-virtual-module-wall-a-hand-built-great-docs-theme-lands-and-a-build-capture-inline-snapshot-regime-renders-tangle-1-to-1-examples]] · [[the-deno-astro-playbook-every-wall-and-its-fix-the-hand-built-great-docs-theme-internals-and-the-build-capture-snapshot-mechanics-distilled-to-reference]]). Two corrections landed.

* the coherent rewrite went too far — regain the gallery

When I rewrote =47.03_stripwire.mdx= into the clean, one-example, tangle-1:1 demonstration, I stripped the feature showcase with it. beef:
#+begin_quote
you mangeld this page a big. re-add the previous version below. highlighting more of the features!
#+end_quote

Resolution — keep BOTH, layered: the clean =<Source>= + =<Example>= (stdout, 1:1) demo stays at top; a =## the docs toolkit, on this page= gallery re-added BELOW it, showing off the machinery the rewrite dropped — **tangle** (fence → =content/docs/.tangled/= + badge) · **admonition** (=:::note=) · **filename bar + line-highlight** (=```lang title="" {3}=) · **Tabs** (local/remote) · **span channel** (=<Trace>= over a new =47.03-trace.example.js=: =Span.open/note/close= → depth-nested tree) · **CM6+vim editor** (display-only). Both capture channels now demoed on one page: stdout (=<Example>=) and span (=<Trace>=). **Doctrine: a coherence pass must not silently drop demonstrated capability** — layer (clean demo + gallery), don't replace.

* LAW — doc examples use PUBLIC combinators, never internal forms

beef caught the example building a Vector with the INTERNAL form:
#+begin_quote
vector.branch("/echo").effect = (context) => context -- ?? we never have this syntax. use vector.open() ?!
#+end_quote

He's right. =prototypes/vector.js=: =open(signature, effect) { this.branch(signature).effect = effect }= — =open(sig, fn)= is the PUBLIC monadic combinator; =branch(sig).effect === the private implementation it wraps. Docs teach the public surface. Fixed all four sites (the connection example, the trace example, the titled code block, the editor seed): =vector.branch("/echo").effect = (c)=>c= → =vector.open("/echo", (c)=>c)=. Re-captured — =open= yields the **byte-identical** wire (=3 assertions passed=, =error: null=), proving the two are equivalent and the tangle-1:1 guarantee holds with idiomatic API. (Root effect stays =new Vector().affect(fn)= — =affect= is the public root-setter, =branch("/")≡affect=.)

**Doctrine (pinned): example code in docs must use the authored public API idiom, not the internal mechanism it compiles to.** Reach for the combinator (=open=, =affect=, =use=, =slurp=), never the underlying field assignment — a doc that teaches internal syntax teaches the wrong thing, even when it runs identically.

* state at close

=47.03= = clean 1:1 demo + full feature gallery, all on idiomatic =open()=. Build green (9 pages), capture via =specimen.snapshot= (2 channels), =47.03= = 200. Nothing committed (VCS read-only). Session closes with the documentation site BORN and self-demonstrating. See memory [[project_documentation_site]].
