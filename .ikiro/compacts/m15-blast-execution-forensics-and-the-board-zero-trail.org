#+TITLE: M15 blast — execution mechanics and the board-zero forensic trail

Companion to =m15-cake-rename-ontology-pinning-and-blast-landing= — that
one carries the ontology arc; this one preserves the execution detail and
the open investigation's evidence chain.

beef's go, verbatim: /"run the runtime and client for yourself now. then
run the blast testing. if that checks out, run the change. run test and
blast again. then clean up trailing tasks like verifying registry and
snapshot integrity."/

* run surface mechanics (rediscovered, worth keeping)

- Boot: =deno task runtime/run= (root) → =systems/runtime= cwd task with
  =--env-file=../../testament/variant/.env=; kajuit: =deno task
  kajuit/watch= (vite dev, VIVA_SYSTEM_ROLE=CLIENT). Ports from
  =testament/variant/environment/variant.jsonc=: runtime :2501, kajuit
  :1794.
- ALL deno.jsonc test tasks carry =--watch= → never exit. Bookends must be
  direct: =deno test -A --no-check --ignore='**/bak/**' <dir>= — paladin
  AND runtime need =--env-file= (paladin =variant.snapshot.test= throws
  "variant.mount: no scope.variant" without it; env-only failure, not
  code).
- The mounted variant is =testament/variant/test.viva.js= (slug
  test-runtime), NOT the registry wafers — daemon declarations live in
  =testament/variant/{education,playground,chaosmonkey}.js=. Any future
  declaration-shape change must touch testament too; the quest blast
  table had missed it until baseline run surfaced the manifest slug.

* baseline test classification (the honest ④ comparator)

typology 104 · paladin(env) 11 · runtime(env) 26 — GREEN. Bitrot,
pre-existing, identical before/after:

| ghost DEADIMPORT  | parse, resolve, shellsignal, processes (shell-migration debris: =lifecycle/=, =lib/= gone) |
| ghost FAIL        | start, wafer, init.gather — init trajectory throws before assertions ("Uncaught error") |
| kajuit PASS       | dock-turns, terminal.stall, pincer/dock.geometry, pincer/markdown, belt/narrow |
| kajuit DEADIMPORT | lifecycle-vector, scenario×3, daemon×2, typology×2 (imports of =src/daemon/*=, =entities/mode.js= — moved dirs) |

=ghost/wafer.test= expectations were stale even pre-M15 (modes 15 vs 18
declared); updated to merged shape (kernel 20, modes assertion deleted)
but the test still fails upstream on the broken init.

* landing mechanics

- Structural via Edit (files read same session, quest hunks); mechanical
  via =perl -pi 's/\bcakes\b/modules/g; s/\bcake\b/module/g'= per file
  list, verified by rg sweep after each batch — one missed pocket
  (population.js modes() APPLICATION/FRAUGHT hunks) caught by the sweep,
  not by memory.
- =prototypes/wafer.js= commented cake lines LEFT — backup-during-migration
  rule; the ⑤ gate reads "zero live hits excluding wafer.js backup
  comments".
- bench.js rebuilt on ONE kernel array (spec.modes gone) — smoke.test
  callers merged. bench's die literal mirrors the new lifecycle shape:
  ={domain, variant: {kinds, traits, entities}, register: {kernel}}=.
- beef edited concurrently DURING the landing: =chaosmonkey.js= new daemon
  appeared (playground lost its chaosmonkey mode line),
  =registry/development= → =registry/playground=. Absorbed without
  conflict — the sweep-then-verify loop caught the new =modes:[= via a
  final rg over declarations, not via the plan.

* the board-zero trail (OPEN — resume here)

Symptom: post-change relaunch, Aprende homepage renders but board zeros
(baseline same page showed 324/223/30 bars, streak 4, 577 scored). Hard
reload didn't change it. Chrome extension timed out on
network/console reads (SSE holds the page busy); beef interrupted the
retry: /"ill check when i am back. short fast answer: besides the startup
uncertainty, anything unforseen?"/ (answer: no — rest clean).

Evidence chain, all verified server-side:
- =daemon_brazilian/test-language.viva.db=: Memory 577 rows, Literal
  2223, ALL memories on user =019d23f1-98a1-70e9-bea0-f9adaecd2a69= —
  data intact, single-user, no split.
- =Migration20260703082445= (boot log) belongs to =daemon_chaosmonkey='s
  FIRST boot; brazilian's =_mikro_migrations= latest predates the session
  → schema untouched by the rename.
- brazilian =User= table EMPTY and lighthouse =Identity= table EMPTY →
  sessions are JWT-stateless (SECRET_VIVA_JWT), user id embedded in
  token; emptiness is normal, not the cause.
- runtime log: zero errors during page loads; "pruned mode: chaosmonkey"
  = integration.prune clearing the stale Mode row after beef moved
  chaosmonkey out of playground (healthy).
- user-filter wiring unchanged by rename: =resolution.domain= still binds
  =datamap.shard.bind("user", ctx.user.id)= — body identical, only fn
  name changed.

Ruled out: data loss, schema migration, rename-broken domain slurp (26
runtime tests incl. composition + SSE green). NOT ruled out: stale client
token/session vs a real read-path regression in
=/assistant/wakeup/board=. Next step: read the board request/response in
chrome (network read) or mint a token and curl it — needed the browser
free, beef checking on return.

* fork-6 residue

=systems/kajuit/client.viva.js:29= — local renamed (=const module =
paladin.bake.process(...)=) but the CALL IS DEAD: paladin has no =bake=
member anywhere (only a residue comment =prototypes/mask.js:20= "bake
provider"). Delete-or-keep is beef's call; =bake= as cake-culture
vocabulary is orphaned either way.
