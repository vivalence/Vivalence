#+TITLE: The connection layer was rebuilt on =multiplex= (one ws PER DAEMON, =/multiplex= always scoped inside =/daemon/:slug=), retiring the whole =tunnel= saga and my =retry.socket= fix with it. Reading up on it surfaced the real lesson when I proposed an ugly diff: socket MOUNT and request SIGNAL are SEPARATE axes — don't derive a per-socket constant by slicing a per-request path.
#+TOPIC: connection handling = =Connection= (url + terminal transport + koa =carry[]=; =child= inherits parent =dispatch= as its terminal = the transport-dual cata) · TRANSPORT rebuilt: =retry(multiplex({authority}))= — =tunnel= DEAD (zero refs), =multiplex= = ONE =Socket= per daemon, every request rides a FRAME (token in the ws-URL query), carry is span-instrumented (=shard.track.span → ctx.telemetry= + the Socket self-spans) so spans-for-logging is BUILT IN at this layer · CONTRACT (beef): =/multiplex= is ALWAYS scoped inside =/daemon/:slug= (server mounts it on the daemon-local aperture via =daemon/die.js= resolve → =aperture/multiplex.js= → =die.good.aperture.open("/multiplex", serve.multiplex(die.good.aperture))=; =expose()= slurps the daemon aperture into =/daemon/:slug=) · THE LESSON: **socket-mount ⊥ request-signal** — mount = WHERE the one ws lives (=/daemon/:slug/multiplex=, a per-daemon CONSTANT = the =mount= param) ; signal = the frame's routing path (=request.url.pathname=, per-request, UNTOUCHED). I convoluted them by =pathname.slice(prefix.length)= to reverse-engineer the mount from the request — "reaching into the per-request path to derive a per-socket constant. Wrong axis." · my whole =retry.socket=/=tunnel= work + the prior compact are SUPERSEDED; everything ABOVE the transport (harness dialogue, Oracle Stages 3/4, oracle-dialogue/oracle-conversation tests, telemetry fix) is transport-blind and survives · client scenarios ROTTED (=Module not found src/entities/mode.js= — moved to =src/typology/entities/=)

* Arc

** read-up → the layer moved under me
beef: =read up on our connection handling=. Reading =prototypes/connection.js= + =gestalten/shard/{transmitter,connection,track}.js= + =entities/daemon/dossier.js= revealed the transport was REBUILT since the conversation saga: =daemon.connection = new Connection(url, shard.transmitter.retry(shard.transmitter.multiplex({authority: ctx.lighthouse.$authority}), {maxRetries:2})).use(shard.track.span(pathname, ctx.telemetry)).use(shard.track.request()).use(shard.track.fault()).use(shard.connection.timeout(8000))=. =tunnel= grep = zero refs. =Connection.socket()= is now direct raw ws (token in query). The =Socket= prototype was also rebuilt into a FRAME-multiplexer (=frames= Map, =#onmessage= routes by =packet.frame=, verbs =open/flow/feed/shut=, self-spans via =trace.open=/=span.note=).

beef: =where does this overlap with your work? this is just implementation layer, right?= — YES. Only file-overlap = my =retry.socket= line in =transmitter.js= (forwarded =transport.socket= for the dead tunnel channel) → now vestigial; beef already removed it, =retry= is clean again. Everything else binds STABLE surfaces (=mode.harness.dialogue.stream=, =Connection.socket=, the =Conversation= object, the thread's reactive stores) and is transport-blind.

** reconcile → the per-daemon correction
beef: =reconcile. one coherent system!= Reconciled the prior compact with a SUPERSEDED coda. Then two corrections:
- =note that multiplex is per daemon not per runtime!= (I'd read =daemon/die.js= =Die extends Wafer= as the runtime and mis-stated a root mount.)
- =meaning /multiplex is always scoped inside /daemon/:slug=.
So the live console (=multiplex closed http://localhost:2501=, all daemons =unavailable=) is the client dialing origin-root =ws://localhost:2501/multiplex= (its =establish(request.url.origin)= keys per-ORIGIN, header comment still says "One WebSocket per origin") while the server serves =/daemon/:slug/multiplex=. Plus the running daemon was stale (no =--watch=).

** the ugly diff → the lesson
Asked for the fix, beef said =no change yet. just diff=. I produced a diff that (a) keyed/dialed multiplex off a passed =scope=, AND (b) did =request.url.pathname.slice(prefix.length)= to strip =/daemon/:slug= off the frame signal. beef: =noooo. what?!?! thats incredibly ugly and technically wrong! your convoluting two things here that are separate. request signal vs socket mount.=

The correction — TWO axes, never conflated:
- *socket mount* = the one ws's endpoint = =/daemon/:slug/multiplex=. A per-daemon CONSTANT. It IS the existing =mount= param (=establish= already dials =new Url(origin).branch(mount)=). Set once.
- *request signal* = =request.url.pathname=, per-request, LEFT ALONE.
My sin: reverse-engineering a per-socket constant by slicing a per-request path — the wrong axis, and a false-binary I should have split at design time (two objects, not one derivation). The clean lever: hand the multiplex its scoped =mount=; touch nothing on the signal.

** testing directive (standing)
beef: =ensure proper testing on both sides! client side especially needs an update to the scenarios. maintain and extend those continuously in the background as we work.= Server multiplex is well-covered + green (=subsystems/typology/tests/multiplex.test.js=, =systems/runtime/tests/multiplex.{integration,filter}.test.js= — integration 5/5). CLIENT scenarios ROTTED: =systems/kajuit/tests/scenario/{buffer,daemon,daemon.wafer}.test.js= import dead paths (=../../src/entities/mode.js=, =../../src/daemon/*= → moved to =src/typology/=). Spawned a background agent to fix+extend them, then beef =no change yet= → stopped it before it wrote anything.

* The landing (carry forward)
- *the connection stack* (current): =Connection= = url + terminal transport + =carry[]= (koa =middleware.compose(carry)(ctx, transport)=); =child(seg)= = =new Connection(url.branch(seg), ctx => this.dispatch(ctx))= (parent's full dispatch = child's terminal → transport-dual cata); surface =request/fetch/call/aim/stream/observe/subscribe/publish/socket=. Terminal transports (=transmitter.js=): =fetcher= (direct HTTP), =inline= (test), =multiplex= (per-daemon frame ws), =retry= (combinator). Carry middleware: =shard.connection.{authorize,timeout,batch,track}= + =shard.track.{span,request,subject,fault}= (the Span trace spine, so the connoisseur's "spans in the transport" prescription already landed HERE).
- *multiplex wire* (=transmitter.js=): =establish(scope)= = one keeper per scope, =new Socket(ws → scope.branch(mount).with({token}), vector)=; =transport(ctx)= = =socket.open(signal,{input,verb,token,stream})= → =socket.flow(frame)=, streaming → =socket.feed=, SSE → =follow=. Errors =multiplex refused/closed=.
- *server serve* (=serve.js= =multiplex(vector)=): =websocket((ws,ctx) => new Socket(ws, vector, {bearer: ctx.request.url.searchParams.get("token")}))=; dispatches each frame's signal through =vector= = the daemon-local aperture (so signals are daemon-local =/metadata/manifest=, NOT =/daemon/:slug/…=).
- *the fix, cleanly* (NOT applied — beef driving): hand the client multiplex its per-daemon =mount= (=/daemon/:slug/multiplex=); leave =request.url.pathname= alone. Open question left with beef: does =mount= come from the daemon's own =mount= at dossier time or is it read off the connection base — and is the signal side already right on his end.

* Lessons
- *A per-socket constant is not a per-request value.* When a "fix" reaches into request data to compute a connection-level property, stop — that's two objects on different axes. Name/config the constant (the =mount=), leave the per-request path untouched. (The false-binary → split at design time.)
- *Read the layer before binding to it.* Half a session of tunnel-channel debugging was made moot by a transport rebuild I hadn't re-read. Re-read the transport before proposing transport diffs.
- *The transport already spans.* =shard.track.span → ctx.telemetry= + Socket self-spans — the connection layer logs via the Span spine by construction; don't bolt on console.

* Open / Pending
- *client multiplex mount fix* — not applied; the clean lever is the =mount= param (scoped =/daemon/:slug/multiplex=), signal untouched. beef's call on where =mount= is sourced + whether the signal side needs work. Live daemons stay inert until it lands (+ a non-stale daemon).
- *client scenarios ROTTED + un-fixed* — =scenario/{buffer,daemon,daemon.wafer}.test.js= repoint =src/entities/*=→=src/typology/entities/*=, =src/daemon/*=→current; then a client multiplex contract test (dials =/daemon/:slug/multiplex=). Standing directive: maintain+extend continuously in the background.
- *prior compact + memories* reconciled to multiplex (tunnel dead, per-daemon, scoped); =retry.socket= line already removed by beef.
