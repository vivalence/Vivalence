#+title: the transcript teaches the ladder, the domain keeps its slug, and the reviewer is told what it already reviewed
#+filetags: :francesca:harness:dojo:tools:armed:agentic:domain:generate:anchors:symbols:lookup:lemma:nlp:appraise:review:dedupe:decomposition:show-dont-tell:m38:
#+spine: 344 beef turns in the session (raw 515, 171 queue duplicates collapsed) — this fold walks rows 326–344; rows 1–325 live in compacts 45–47, 50 and 51.

* the walk (rows — every section cites these)
| n | beef (verbatim at gates) | landed | decided / killed |
|---|---|---|---|
| 326–327 | the wake order arrives MID-TURN during compact 51 + =/compact= | queued | NOTHING (re-fired below) |
| 328 | /ikiro wake. i will provide you with a transcript of my agent interaction and want you to upgrade the harness./ — four points: /1: encode the breaking down of learnables … like my trouble with these collapsed interjections or what they are (dalla, degli etc) … first i want to get the infinitives right. then some subset of conjugations. work that to full conjugation paradigms. then to setences … have the harness DEMONSTRATE via many turn example interactions. not explanation. show dont tell. !! show dont tell!/ · /2: make hte harness independent of user and more specific in terms of subject matter/ · /3: improve francesca's harness overall/ · /4: investigate how i wanted the tool calling done. find gaps with the current dojo tools. improve the buffer generation tool capability/ + =turns.json= path | the whole upgrade — see the arc | the order itself = the go for the files it names |
| 329–331 | the same wake text re-queued ×2 + the continuation summary | — | NOTHING (duplicates across the compaction seam) |
| 332 | /remove the turn trace when done/ (MID-TURN) | =turns.json= (786 KB, 330 turns) deleted after the audit | — |
| 333 | i-have-adhd skill re-invoke | behavioral mode re-armed | — |
| 334–335 | my gated list pasted back + /what this mean/ (MID-TURN) | plain-language explanation of the four findings | quoted turn's payload is its tail |
| 336 | /3. Deploy + italian reseed (yours) - mine/ (MID-TURN) | — | deploy + reseed confirmed beef's |
| 337 | /1 go but i'd like the tools to still carry the mode slug. 2 go 3 fix polish. 4 whats the solution here?/ | 1 = dedupe with the prefix KEPT (arc) · 2 = =dojo_symbols= armed · 3 = unknown-tool error names the armory · 4 = design answer only | my strip-the-prefix polarity KILLED before build |
| 338 | /do a and b. b silent fail tho/ | lookup family walk + lemma retry, silent (arc) | the m29 conjugation-blindness tail CLOSED |
| 339 | /since we have francescas review capabilities HOOKED in i think we should make it clear that she should never use this tool unless instructed. or is there a logic issue on my side?/ (MID-TURN) | no logic issue his side — the review TOOL's own valence was the contradiction; rider-exception added, doctrine hardened | /an exchange you do not review never happened/ demoted to the no-rider case |
| 340 | /also the hook sometimes reviews the same literal multiple times which MUST be prohibited/ (MID-TURN) | mechanical dedupe in =appraise()= | model discipline alone rejected |
| 341 | row 339's text re-queued | — | NOTHING (duplicate) |
| 342 | my guard line pasted back + /maek sure to limit this to just a few turns. we WILL want the same literals reviewed within the same conversation/ | window 1 → 2 assistant turns | unbounded suppression forbidden |
| 343 | /yeah thats too narrow. bumb that to last 4 turns. but make sure the model knows what was already reviewed./ (MID-TURN) | window → 4 + the prior set injected into the reviewer prompt | filtering without telling the model = wasted review slots |
| 344 | /ikiro compact/ | this file | — |

Coverage: 326–344 = 19 rows, 5 NOTHING-class (326–327, 329–331, 341), none skipped. Balance: the arc's heaviest beats cite 328/332 (the audit + doctrine) and 337/338 (the armory round) as much as the tail rows 339–343.

* arc
** the transcript audit (row 328 → the doctrine)
330 turns of beef drilling with francesca, read whole (flattened to 72 KB, tool i\/o truncated). What it proved: the decomposition method came from BEEF, not her — /break it onto fucking smaller groups retard. start woth two and expand/ and /lets just do the roots. isolation/ are the learner doing the teacher's job. The worst leak was structural, not stylistic: her imitation =appraise {}= calls errored (/unknown tool: appraise/, 15+ times) and the RECOVERY turn printed the pending answer as bare text — prompt, error, answer, item dead. Plus: prompt disambiguators dropped after the first pass (/i told you i meed (context)!/, /cintext deipped again/), answer-in-the-annotation (/just one word retard. dont leak the fucking answer omg/), 30 contractions re-authored as =knowables= when every one existed as a corpus literal (reviews orphaned: /a — not in the corpus/), =dojo_generate= pooling from the FEED instead of the learner's vocabulary (/you build sentences. not pull. fuck/ · /neither are in my known set. idiot./), and 8 tool rounds flailing at "what are the pronouns and determiners" because nothing answered category questions.

** the harness upgrade (rows 328, 332)
=francesca.md= rewritten (17 KB): the ladder formalized — /split → ground → contrast → extend/ (gated on two clean spaced hits BOTH directions) /→ integrate → vary/ — and DEMONSTRATED, per the show-dont-tell order, as two many-turn example dialogues that ARE the doctrine: /dalla/ decomposed to the five roots two-at-a-time, di\/da contrasted where the glosses collide, the di family pinned on screen by slug, back to /dalla/, sentences anchored on it; and /leggere/ from floor-check → infinitives → the io slot across three verbs (one variable moves, the others are grounded) → the conjugations table → passato prossimo → sentences. New laws carry contrastive examples: the prompt disambiguator names the USE and never contains an answer (=✗ of \/ from (possession: la macchina di Marco)= vs =✓ of \/ from (possession \/ origin)=); tool error mid-drill → re-ask, NEVER print the pending answer; stacked answers graded in prompt order; no offer-endings anywhere; pin-over-author. User-independence: interference and tone generalized — read the learner's other languages from their errors, never from assumption; level from progress, never interrogation. Italian subject matter kept and deepened. =dojo_generate= rebuilt: pool = the learner's TOUCHED vocabulary (=byStrength= half, =sample= fill, =feed= only when history is thin) + =anchors= (slugs forced into every sentence via a dedicated system section) — the integrate rung, on screen. Then the trace deleted on order (row 332).

** the armory round (rows 334–338)
Four findings explained (/what this mean/), then the go bundle. (1) The double catalog root-caused: =domain.viva.js= declares TOOLED, so AGENTIC harvested the domain as a flatmode under its slug while harnessed layer ④ armed the same tools bare — she called BOTH names every opener. My fix stripped the prefix; beef flipped the polarity — /i'd like the tools to still carry the mode slug/ — so layer ④ now BRANCHES under =daemon.domain.manifest.slug= (canonical names = =language-learning_progress= for every harnessed mode), =agentic.js= filters =manifest?.type !== "domain"=, and the appraise hook's retro-fitted round is =language-learning_review= — catalog-valid, so imitation lands on a real tool and =appraised()= guards the double-write. (2) =dojo_symbols= armed — the =/symbols= fold extracted as =catalog(daemon, input)=, shared by aperture and tool; the 8-round category flail becomes one call. (3) The m29 backlog polish: =shard/hallucinate.js= dispatch appends the armed name list to every unknown-tool error. (4) Asked as design, answered as design; then /do a and b. b silent fail tho/: =LiteralRepository.family(rows)= walks matched word forms → paradigms via =uses= → =trait.CONJUGATED.infinitive= (receipt =↳ form in <paradigm> · infinitive: <line>=; paradigms already among the direct matches filtered), and lookup retries by LEMMA through =ctx.daemon.services?.nlp= when nothing matches — try\/catch to =[]=, no service and a throwing service both land on the plain miss line, pinned by a throwing-stub test. En route, a pre-existing runtime red fixed: =stripwire.contract.test.js= DECLARABLE never learned m37's =feeds= (=strip.js:8= plucks it; francesca's converse route snapshots =feeds: "object"=).

** the reviewer is told, then filtered (rows 339–343)
Beef's suspicion (/or is there a logic issue on my side?/) was right in the other direction: the doctrine already forbade inline review, but the review TOOL's own valence — /an exchange you do not review never happened/ — steered straight against it. The valence gained the rider exception (if review rounds appear in the history without you calling, they are wired in — never call unless explicitly instructed) and the doctrine hardened to match. The duplicate reviews got a MECHANICAL guard, sized by beef across three tellings: 1 previous turn (mine) → /just a few turns/ → 2 → /bumb that to last 4 turns/ — reviews whose literal appears in the last FOUR assistant turns' rounds are dropped, in-batch doubles collapse, same-conversation re-reviews flow from the fifth exchange on. And the half I had not offered: /make sure the model knows what was already reviewed/ — the prior set is injected into the reviewer prompt (/Already reviewed in the recent exchanges, and dropped if repeated: …/) so the 10 review slots go to fresh items and the filter stays a backstop.

* mechanisms (carry forward)
- the decomposition ladder: split → ground (2–3 items, tight rotation) → contrast (minimal pairs where glosses collide) → extend by ONE, gated on two clean spaced hits both directions → integrate (the composite, pinned on screen) → vary (anchored sentences). Doctrine = demonstrations, not rules.
- domain tools are slug-namespaced for EVERY harnessed mode: =armed.branch("/" + daemon.domain.manifest.slug)= at layer ④; AGENTIC skips domain-type flatmodes; francesca's wired round = =language-learning_review=.
- =dojo_generate= pool: =byStrength= (weakest, half) + =sample= (touched fill) + =feed= fallback; =anchors= resolve via =findByIdentifiers= and ride a MUST-contain system section.
- lookup lineage: =family()= = uses-junction walk (word → paradigm → =CONJUGATED.infinitive=); lemma leg = =daemon.services?.nlp= classify, silent on any failure.
- appraise dedupe: prior = review-round literals of the last 4 assistant turns (window ≤ the hook's last-10 read), told to the model in the prompt AND filtered mechanically; in-batch doubles collapse.
- unknown-tool errors name the armory: =armory(tools)= = =steer.trie.rollup(tools, () => null)= mapped through =nameOf=.
- the comment-litter gate blocks agent edits to EXISTING stale comments (=harnessed.js:38= still says four layers) — surface to beef, never fight the hook.

* state at fold
- ALL UNDEPLOYED, riding the m38 bag. Owed beef: deploy · italian reseed · =harnessed.js:38= stale layer comment (gate blocks me) · live verify of the new doctrine + appraise discipline (prompt injection, 4-turn window) on a real session.
- suites at fold: domain 15\/119 · dojo 12\/64 · runtime 52\/325 · typology hallucinate 4\/58 — all green; =harness.js= parse-checked.
- m29 tails: conjugation blindness CLOSED (row 338); unknown-tool-names-catalog CLOSED (row 337); symbol discovery CLOSED (=dojo_symbols=). Still open from m29: valence language law · types.js twins · progress-as-only-board.
- accepted costs, flagged: a card recurring within 4 exchanges has its re-rep review swallowed (aligns with the schema's withhold-mid-rotation law) · the dojo =conjugations= tool error says /via lookup/ generically rather than the slug-prefixed name (a domain slug hardcoded in dojo would be the wrong coupling).
- no callouts this fold — the codeword appears only inside the audited transcript's own rows (francesca's session data, compact 51 logged that era's two).
