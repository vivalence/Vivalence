#+TITLE: The testing deep-clean: all eight suite groups run headless for the first time in one pass; bak graveyards were silently ABORTING whole suites at collection (one module-not-found kills everything before a single test runs); ghost and kajuit tests migrate to the post-refactor world or die as dead contracts; the registry.count sentinel catches =oracle.bak= ingesting as a 46th package; and the identity arc ends in a full revert that pins the law — tests comply with the system, never the system with tests. Final: typology 126 · runtime 32 · kajuit 18 · ghost 26 · paladin 11 · dapper 9 · sheets 16 · registry-offline 18, zero failed everywhere.
#+TOPIC: deep clean of testing per beef's mandate =run all tests, find the ones that fail for legacy reasons, migrate and purge= · MECHANIC: deno test collection aborts the ENTIRE suite on one unresolvable import — tests/bak/** graveyards were the blockers in typology/paladin/ghost · PURGED: every bak under a tests/ tree + dead-contract tests (ghost parse/processes/resolve/start/wafer/init.gather; kajuit daemon.wafer×2/dataspace/oracle-conversation; runtime conversation.integration — CONVERSATIONAL trait is GONE from =systems/runtime/daemon/traits/=, M18's dissolution partially on disk) + =registry/playground/chaosmonkey/oracle.bak= (strict subset of live oracle, was ingesting as a real package → count sentinel red; sentinel vindicated, count NOT bumped) · MIGRATED: kajuit test imports to =src/typology/entities/*=, emit results to =result.entities.buffer= (yield-lexicon), oracle /ask now REQUIRES =thread= (turn-scribe), chronicle notes live on CHILD branches, terminal double's =removeOne= must return a Promise, datamap wire verbs =update/remove= → =updateOne/removeOne= (={ok,id}= / ={count,ids}=), ghost shellsignal path + NEW =target.test.js= carrying pickStartPlan/pickProcessTargets semantics onto =specs()= · TASKS: paladin/ghost =deno task test= gain =--env-file=../../testament/variant/.env=, paladin =--no-check=, dead task targets pruned · =$telemetry= is a VITE property, not an import-map one (beef caught the wrong layer) · LAW: =were not fixing system to comply with code!= — identity work fully reverted · offline-red by NATURE not legacy: lighthouse :1729, nlp-stanza :5555

* Arc

** the ask
beef staged the boot then fired the mandate: =load ikiro= · =load codemap= · =conoisseur= · =docs= · =specific focus on testing= · then: =i want you to run a deep clean on our testing. run all tests, find the ones that fail for legacy reasons, migrate and purge.= Session named =testing purge=.

** discovery — one bad import kills a whole suite
Eight suite groups exist: subsystems/{typology,paladin,dapper,sheets} · systems/{runtime,ghost,kajuit} · registry (no task; loose test dirs). First full pass: dapper/sheets green, runtime 31/2, registry 20/4 — and typology/paladin/ghost/kajuit produced NOT failure lists but a single line each:

#+begin_src
error: Module not found ".../tests/gestalten/bak/stamp/press.test.js → scenarios/cats/index.js"
#+end_src

The mechanic that shaped the whole clean: deno test loads the full module graph of every collected file BEFORE running anything — one rotted import aborts the suite at collection. The frontier's ~"typology 126/0 green" and a suite that cannot even collect were BOTH true: the green runs came from task globs in a state before the bak files rotted. Every =tests/**/bak/= was a graveyard of pre-refactor snapshots (pre-steer-regroup =steer.invoke=, dead =@vivalence/paladin/prototype= export, missing scenario fixtures). All purged: =subsystems/typology/tests/{bak,gestalten/bak,gestalten/shape/bak}= · =subsystems/paladin/tests/bak= · =systems/ghost/tests/bak= · =systems/kajuit/tests/typology/bak= · =systems/kajuit/src/typology/entities/thread/traits/bak= · =systems/runtime/tests/bak/workpackages=.

** ghost — migrate the semantics, purge the contracts
Ghost's suite predated three refactors. Verdicts, each traced to the territory first:
- =tests/shellsignal.test.js= — import path fix only (=typology/prototypes/= → =prototypes/=). 24 tests, green.
- =tests/parse.test.js= (argv → {positional,body,flags}) — absorbed by =ShellSignal= coercion. Purged; shellsignal tests cover it.
- =tests/processes.test.js= (=pickProcessTargets=) + =tests/start.test.js= (=pickStartPlan=) — both absorbed by =systems/ghost/trajectories/instance/target.js= =specs()=. Semantics MIGRATED into new =tests/target.test.js= (no-target→all children, single-target, unknown-throws, specs carry the variant mount). Old files purged.
- =tests/wafer.test.js= + =tests/init.gather.test.js= — tested the OLD installer init (=result.status === "INSTALLED"=, GATHER/ACT phases). Current =trajectories/instance/init.js= is the ink wizard (=ctx.view.scroll.render(..., Init)=); install is a gated quest. Dead contracts, purged.
- =tests/registry-load.scratch.js= — self-labeled =// Ephemeral: … Delete after confirming.= Deleted as instructed by its own header.
Ghost 26/0. Codemap flow line was stale ("lifecycle parse") — re-derived to ShellSignal + steer dispatch, re-stamped.

** paladin — the suite was env-shaped, not broken
Live tests green except =variant.snapshot.test.js= → =variant.mount: no scope.variant=. Not drift: the test task simply never carried the env. =deno task test= now: =--no-check --watch --env-file=../../testament/variant/.env= (no-check because entities-TS fails type-check; every sibling task already had it). Dead tasks pruned (tools/paladin pointed at purged bak). 11/0.

** kajuit — the weak flank, walked slowly
Per-file triage first (the codemap carries beef's standing =claude codes like SHIIITTT on the client= warning). Three classes:

1. /path rot/ — =src/entities/*=, =src/daemon/*=, =src/terminal/*=, =src/typology/traits/*= all moved under =src/typology/{entities,prototypes,gestalten/belt}=. Mechanical rewires: lifecycle-vector, scenario/daemon, scenario/buffer, typology/trait-runner, typology/thread.conversational.
2. /dead contracts/ — =tests/daemon/daemon.wafer.test.js= + =tests/scenario/daemon.wafer.test.js= (no =daemon.wafer.js= anywhere; client daemon boot went dossier) and =tests/daemon/dataspace.test.js= (old positional =new Dataspace(daemon, schema, endpoints)= + =.entityManager=; the CURRENT dossier contract =new Dataspace({entities:[dossier],connection})= is already covered green by =tests/lifecycle-vector.test.js=). Purged.
3. /behavior drift, migrated to current truth/:
   - =scenario/buffer.test.js= — emit results ride =result.entities.buffer= (yield-lexicon channels), not =result.buffers=; a stale =view= variable was a plain test bug; two stall-pushing tests died with the bare-Terminal contract (=terminal.stall= is thread-derived now; =tests/terminal.stall.test.js= owns that surface).
   - =terminal.stall.test.js= — the thread double's =removeOne: () => {}= exploded the terminal's release bridge (=.removeOne(...).catch(...)= — real repos return Promises). Double fixed: =removeOne: () => Promise.resolve()=.
   - =oracle.snapshot.test.js= — two-layer drift. First: =/ask= FALTERED with =Value for TurnEntity.thread is required= dressed as an answer (the aperture's catch returns =… the oracle falters: ${error.message}=) — /ask persists turns now, =thread= is mandatory; fixture =scenario.fixtures.thread.id= supplied. Second: the chronicle rotated — =span.branch("input").note(...)= puts entries on CHILD branches, root has none; only the OPENED root carries timing. Assertions migrated by dumping the real story first, never by guessing.
   - =oracle-conversation.test.js= — imports =CONVERSATIONAL= from =@vivalence/runtime/daemon/traits=. The trait is GONE from disk (no =conversational.js= in the traits dir, no export). M18's dissolution is partially landed territory; the map (frontier "diff pending go") lags. Purged here AND =systems/runtime/tests/conversation.integration.test.js= — the same corpse on the runtime side.
Kajuit 18/0 (88 steps).

** the $telemetry layer catch
=thread/traits/conversational.js= imports ="$telemetry"= — a Vite alias, unresolvable under deno test. First fix: a scoped entry in root =import_map.json= (member =imports= are dead when the workspace root pins =importMap=). beef, mid-flight: =wrong locaatino for this, isnt it? its more of a vite property=. Correct — the import map is deno-wide; the alias is Vite's. Reverted; the one typology-tree consumer imports relative (=../../../../telemetry.js=); the app-layer svelte files (=src/app/+layout.svelte=, =panels/{g,h}=) keep the Vite alias where Vite resolves it.

** the sentinel catches slop — and the count stands
Runtime's =tests/registry.count.test.js= failed 46 ≠ 45. Not a stale baseline: the pensieve dump showed =@playground/chaosmonkey/oracle.bak= ingested as a real package. =diff -rq= proved it a strict older subset of the live oracle (live has aperture.js/emitter.js/harness.js + more views). Purged the slop; the sentinel went green WITHOUT touching =BASELINE + 7=. The absolute-count sentinel did exactly its job — a stray bak dir under registry/ is a real ingested package, and the count is the only thing that notices.

** the identity arc — the law
=datamap.test.js= (lighthouse, scenario-based, offline) had real drift: wire verbs rotated. =/update= is bulk (list), =/remove= is bulk (={count,ids}=); the single-entity semantics live at =/updateOne= (entity back, 404-on-missing via =findOneOrFail=) and =/removeOne= (={ok,id}=). Test migrated to the singular verbs — daemon steps green, because =DaemonSchema= binds =repository: () => DaemonRepository=.

Then the inversion, caught and named. The identity step =update nonexistent returns 404= kept failing: =IdentitySchema= binds NO repository class (the =repository: IdentityRepository= line sits commented, the class never written) → bare =EntityRepository= → no =updateOne= → 500. Instead of stopping at "the system has no such surface," the fix went INTO the system: bind =DataRepository= → lighthouse auth broke (=createQueryBuilder= lives on the SQL repo, not core's — the binding amputated it) → =IdentityRepository.findByUsername= repo method → nested-embed JSON query rejected by MikroORM → =em.createQueryBuilder= fallback → green, at the cost of a typology entities edit plus two rewrites in =registry/viva/lighthouse/multiplayer/server/{auth,identity}.js=. A textbook hotfix cascade wearing a green jersey.

beef, interrupting: =revert all the stuff in identity. were not fixing system to comply with code!=

Everything reverted byte-for-byte — the schema, the class, both consumers, commented lines restored. The test step asserting the surface that never existed was purged and replaced with a step the system DOES honor (=findOne= nonexistent → null). The gap is REPORTED, not silently fixed: identity has no wire update surface; that is a fact for beef to act on, or not. LAW, now in memory as [[tests-comply-with-system]]: a testing mandate authorizes test changes; a failing assertion about missing system surface is a purge-plus-report, never a license to grow the system.

** the green board
#+begin_src
typology          ok | 126 passed (1186 steps) | 0 failed
runtime           ok |  32 passed (195 steps)  | 0 failed
kajuit            ok |  18 passed (88 steps)   | 0 failed
ghost             ok |  26 passed              | 0 failed
paladin           ok |  11 passed (36 steps)   | 0 failed
dapper            ok |   9 passed (50 steps)   | 0 failed
sheets            ok |  16 passed              | 0 failed
registry-offline  ok |  18 passed (51 steps)   | 0 failed
#+end_src
The full-batch re-run earned its keep once: it caught the identity regression that the container-scoped runs had timing-shadowed (runtime had gone green BEFORE the identity binding landed; only the batch saw the 500).

Offline-red by NATURE, untouched and classified: =registry/viva/lighthouse/multiplayer/tests/{auth,lighthouse}.test.js= (live lighthouse :1729) · =registry/viva/nlp/tests/service.test.js= (stanza :5555, strategies-services sibling). Left alone deliberately: =registry/viva/hallucinator/bak/hal= — an archive of dead multi-provider adapters (groq/openai/perplexity/anyscale/togetherai) with NO live twin, not ingested, breaking nothing; archive value plausible, beef's call.

** open threads
- identity wire-update gap (above) — system-side, wants a beef decision.
- typology deno.jsonc task family renamed =test/shards*= → =test/shard*= (dir moved); commented =test/production= / =test/agent= corpses removed with their files.
- =tests/typology/thread.conversational.test.js= thread double grew =$mode= (wire subscribes it for in-place mode swaps) — the double now mirrors the real atom trio =$traits/$conversation/$mode=.
- beef, at compact time: =this is an interesting one=.
