#+TITLE: one bracket per exchange, and the review round becomes honest
#+filetags: :dock:kajuit:francesca:appraise:review:intelligent:rounds:buffer:harnessed:m29:

Window: beef turns 211–223 of N=223 (compact 48 sealed through 210). Short prod-drive fold: four defects beef hit live after the appraise arc, each traced and landed same-session.

* Arc

** the bracket complaint → dock merges rounds (212/214)
beef: /"something weird started happening where each tool call gets its own bracket in the conversation. this is a bit annoying. identify why. and adjust the design a bit. make it tighter."/

Cause: the tool loop (=shard/hallucinate.js respond()=) persists EACH model round as its own assistant turn (=turns = [...turns, turn, answered]= → the harness persist middleware seals each separately), and the dock rendered one bracket per turn. Not new machinery — armed tools made multi-round exchanges common, so the seam finally showed.

Fix: =Dock.svelte enrichedTurns= — consecutive assistant turns MERGE into one item (tools concatenated in call order, text/thinking joined =\n\n=, failures summed, census recomputed). Pure-result turns already skipped. One exchange = one bracket. Day-divider only breaks a run across midnight — accepted.

** the tool-call limit (212/214, second half)
beef: /"make tool call limit tracker variable PER conversation turn. i think right now they are per conversation. add the tool call limit a part of the intelligence widget per thread"/

Finding first: the limit was ALREADY per conversation turn — =respond()='s =rounds= counter is local to each =/dialogue/stream= invocation, default 10 (=prototypes/hallucination.js POLICY=). What was missing was the per-thread knob. Landed: =schematics/entities/thread.js= =INTELLIGENT.rounds v.integer({min 1, max 50}).optional()= · runtime =harnessed.js= projects =iq.rounds= into policy (between iq.tune and invocation tune) · =Intelligent.svelte= chips =1 · 2 · 5 · 10 · 25=, click-again clears = mode decides, 1 = "answers directly, no tool loop".

beef later extended the trait himself: =thinking: v.boolean()= (shown/hidden chips in the widget, =thinkMode= read in the dock) — a DISPLAY toggle, distinct from =effort: "none"=. His edits, canonical.

** buffer dead until F5 → subscription + tap (216 → 217 → 218)
beef (216, mid-turn): buffer generated by dojo_provision "is not runnable. also its not showing up in F panel. i need to refresh the app F5" — and he named the levers himself: /"1: the buffer subscription per thread seems to be failing. 2: the kajuit side middleware that processes these requests on connection level to register entities seems to be either lost to time or not working - we recently changed the yield return schema"/.

Both levers real:
- Lever 1 CONFIRMED: =kajuit daemon/dossier.js= subscribed intent + thread + turn — buffer NEVER. Server side fine (=aperture/userspace.js= exposes =/userspace/entities/buffer= reactive). One line on beef's "go": =daemon.entities.buffer.subscribe()=.
- Lever 2 real but different: the merge middleware in =mode/traits/harnessed.js= is alive and yield-lexicon-correct, but only sees UNARY response bodies — the dialogue stream's =/tool/yield= packets bypassed it entirely, so m29 armed tools creating buffers mid-stream harvested nothing client-side. (The old copy at =mode/mode.js:55= is commented out — "lost to time" was half right.)

The subscription is the durability source; beef then asked the sharp question (217): /"we stil might need the tap for the Buffer Run button to work, no? can we still tap in there? what would we yield?"/ — yes: the chip renders off the stream packet before the subscribe SSE lands; click in the gap = silent no-op. Tap landed on "go on 3": merge loop hoisted, second branch wraps =rqx.response.body= when it's an asyncIterator, =/tool/yield= packets get =result.output.<entity>= arrays merged in place (managed instances), =Array.isArray= guard so paging keys (=next=) pass untouched. Packet shape unchanged — =Connection.stream()= iterates =response.body= after fetch, so a middleware body-swap is exactly the seam.

** the model imitates a tool it doesn't have → forge the REAL round (216 → 217 → 218)
Screenshot: francesca narrating /"I called appraise four times and it isn't a tool — those grades never landed. The verb is review"/. beef: /"why does the tool think it has a tool where it dont?"/

Diagnosis: catalog and history disagreed. The sync-shaped retro-fit put =appraise= tool_use parts into history; the catalog has no =appraise=; imitation beats catalog on the fast tier — she copies the visible pattern, dispatch answers unknown-tool, grades die. The accepted-risk line in compact 48 ("she may imitate the seen round and call uncataloged appraise → one error round") underpriced it: four calls, one sitting.

beef's ruling (218): /"ahhhh ok. whats the actual tools name? review? use that. go."/ — make history honest: the retro-fit forges the round the model could have made itself. =francesca/harness.js= appraise now EXECUTES first (per-review =/review/literal=, receipt lines + retentions exactly as =domain/tools/review.js= folds them), then wires:
  =tool_use  {name: review-family, input: {reviews}}=
  =tool_result {output: {message: lines, entities: {retention}}}=
Guard = ROUNDS name list (=review= / =language-learning_review= / legacy =appraise=) — a model-called sync review round makes the hook stand down for that exchange, single-writer preserved from either side. Prompt + descs reworded appraise-call → review-call.

beef then evolved it (his edits, canonical): forged name → =language-learning_review= (matches the slug-branched domain catalog — see cross-session note below) · a =prior= Set of literals from the last 4 assistant turns' review rounds, injected into the prompt ("Already reviewed in the recent exchanges, and dropped if repeated: …") AND filtered out of the model's output alongside within-batch dedupe. The duplication war of compact 48, now enforced structurally instead of steered.

** buffer chips by uuid → mode slug + launch semantics (219 → 221)
beef: /"i dont like that the buffers are presented bu ID. mode name at least!"/ — cause: wire pojo's =mode= is the FK id string and =bufferLabel='s =typeof mode === "string"= branch trusted it as a slug. =launchLabel= in the dock: managed row from =$buffersStore= preferred → =data.label/title= → mode id resolved against =daemon.entities.mode.$entities= → slug → =bufferLabel= last resort.

Then (221, mid-turn): /"if fullscreen - clicking the buffer should close chat and load the buffer into terminal … or if the dock is not full screen then just load the buffer - no closing dock in that case. so set fullscreen off and ensure buffer in terminal!"/ — =launch()= sets =terminal.buffer= UNCONDITIONALLY (the old toggle-off on same-id died; "maybe another buffer is loaded already i want to make sure") and =setDockFull($dock, false)= only when full.

* Cross-session context absorbed (not this window's work, touches it)
Another session (m38-wake) slug-branched domain tools — =armed.branch("/" + daemon.domain.manifest.slug)= + =agentic.js= filters =type !== "domain"= — killing the bare-vs-prefixed mixed catalog; that is WHY the forged round's name is =language-learning_review=. Same wave: =hallucinate.js= unknown-tool errors now list the armory · =shape/cortex.js strip= exposes provider/config (the widget's origin line) · =stripwire.contract.test.js= adjusted (the stale-snapshot =feeds= red I'd flagged as pre-existing) · =francesca.md= heavily rewritten · =dojo/tools/symbols.js= new. None of it reverted; all folded around.

* Failures / traps
- The "accepted risk" of an uncatalogued name in history was a mispriced design debt, not a risk — the fast model imitates history RELIABLY. Law: every tool name appearing in history must be callable.
- =bufferLabel='s string-mode branch assumed slug; the wire has always carried the FK id. Display code trusting a foreign key's TYPE to imply its MEANING.
- Comment-litter gate blocks any Edit whose old_string CONTAINS pre-existing =//= lines — split edits so new_string carries none.
- Runtime suite red during bracket work was the stale local stripwire snapshot (pre-m37 =feeds=) — flagged as environmental, and indeed beef's wave adjusted the test.

* State at fold
- Dock: one bracket per exchange · launchLabel mode-slug · launch ensure + fullscreen-off · tests 17/39 green.
- INTELLIGENT: ={tune, effort, rounds, thinking}= — rounds projected server-side, thinking client-display only.
- Client harvest: buffer subscription + stream tap both live; unary lane unchanged.
- francesca appraise: forged =language-learning_review= round, execute-then-wire, prior-set dedupe. LIVE-UNVERIFIED as a whole post-forge — next drill is the test.
- Open, unchanged: kajuit drain() error-close swallow · output.schema/output.object contract decision · duplicate-era retention rows inflated.
