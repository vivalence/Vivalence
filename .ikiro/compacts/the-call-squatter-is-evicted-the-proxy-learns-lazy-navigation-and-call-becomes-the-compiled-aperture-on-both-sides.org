#+TITLE: the .call squatter is evicted, the proxy learns lazy navigation, and .call becomes the compiled aperture on both sides
#+AUTHOR: ikiro

The aperture-calling quest, opened as an ideation ("rename .call or evict it") and closed as a full stripwire: =.call= is now the compiled aperture on BOTH sides, same spelling, different backing. Durable state: =subsystems/typology/gestalten/shape/object.js= (proxy rebuilt), =gestalten/shape/strip.js= (plucks =yields=+=methods=), =gestalten/shape/connection.js= (wire: lazy =:param= hop, =yields=→stream, method fidelity, ambiguity throw), =prototypes/aperture.js= (=methods()= wildcard-ask), =gestalten/steer/strategy.js= (local ask =method: "*"=), =gestalten/is/prototypes.js= (=is.buffers=), =systems/runtime/daemon/traits/index.js= (EXPOSED → =shape.proxy=) + =daemon/traits/emitter.js= (output-channel fold), =systems/kajuit/src/typology/entities/mode/traits/exposed.js= (new) + =daemon/dossier.js= (=daemon.call= wire), 33 view sites in 16 files → =entity.connection.call=, demo =registry/playground/chaosmonkey/oracle/buffer/Oracle.svelte= (=mode.call.ask= beside =mode.emit.vision=), gate =subsystems/typology/tests/gestalten/shape/stripwire.test.js=, quest =quests/aperture-calling.quest.org=, memories [[project_aperture_calling]] + [[project_yield_lexicon]] (output≠object). NOTE: the wire SHAPE claims here (={leaves,branches}=) were subsequently rotated node-centric (={effect?,branches}=) by the later arc — [[no-path-is-a-path-the-root-effect-becomes-addressable-the-own-scaffold-is-ripped-and-shape-strip-rotates-node-centric-to-the-cata-of-the-vector]] — which preserved the params/methods/yields semantics landed here.

* arc
Beef opened with the observation that the EMITTER grammar (=mode.emit = compile.object(emitter)=) never reached the aperture itself, and that =[entity].call = connection.call.bind(...)= felt wrong. Ideation pinned the double move: evict the squatter (all string-path calling explicit through =entity.connection.call= — dot-provenance, no aliases) AND rebuild =shape.proxy=, which re-implemented Pattern internals inside a shape compiler ("weird leakage"). Stripwire symmetry demanded, streaming NOT deferred — architect so the right solution falls out. Phases 1+2 ran paranoid: 4 binds died, 33 sites migrated (multiline =.call(= sites caught by a second grep pass), the proxy became a 20-line lazy Signal accumulator over =scope/feed/invoke= with the spec suite green UNMODIFIED. A retard callout landed mid-report (bare "Dashboard.svelte" — container-rooted paths, always). Beef pinned the three forks (1c transparent+throw with his long-term tiering leaning documented verbatim in the quest; =yields= slot "sounds harmonic"; break=cancel default) and dropped the boundary law: dot-nav is the MODE surface, kajuit compiles it but never consumes it. Phase 3 build: the symmetry test earned its keep IMMEDIATELY — local =side.ticks({})= returned Promise<iterator> where remote returned the iterator; the house idiom (=conversational.js= await-then-iterate) was already the answer. Then =methods()= sole-fallback broke the HTTP 405 specs — resolved by naming the missing channel: local strategies ask with =method: "*"= (the wildcard IS "no method chosen"), sole handler serves a wildcard ask, ambiguous wildcard THROWS, HTTP stays strict. Mid-build the runtime EMITTER family went red; a full-revert baseline PROVED it wasn't Phase 3 — beef confirmed his in-flight edit ("my wiring is shit tho. check them"): the envelope fold added to the pool AFTER draining it, and bare-buffer returns fell out of the classic path. Rewired: pool before drain, =is.yieldish= OR =is.buffers= → pool, other non-nill returns → =result.output= — discriminator honest because =app.buffer()= creates =BufferEntity= via =em.create=. Beef corrected a second premature claim ("test-runtime is runtime level, says nohting about daemon!" — the earlier "no daemon mount" verdict came from a truncated curl): live =:2501= had daemon =playground= all along, and the real validation ran — the NEW wire over real HTTP+auth, =daemon.metadata.manifest()= round-trip, oracle =ask= callable, then in-browser: kajuit boot probe healthy with the aperture fetch + =daemon.call= wire in the dossier, and clicking =aperture · render= in the live oracle buffer fired the migrated =buffer.mode.call.ask({prompt})= through the whole stack (SERVER TRACE =/ask → /ask/render=, real hallucination). Phase 4 slimmed on beef's word: squatter sweep clean, oracle demos both compiled grammars side by side, education views stay =connection.call=.

* the direction (beef verbatim, in order)
- the opening: /"we have the pattenr on daemon and client that we take a vector and compile it to an object. like emitter = Vector; mode.emit = compile.object(emitter). i like that. but were not yet using that on the aperture as a whole. we also have the pattern of assigning [entity].call as connection.call proxy. i am less sure about that."/
- the proxy verdict: /"its shit tho. must be fixed. there is weird leakage of entities into that shape and entites have NOTHING to do iwht each other."/
- the eviction: /"all calling of (\"/path/to\") should be through connection. evict the call squatter. to make room for object compiler. and yes! absolutely stripwire symmetry!! thats why we do what we do with care :)"/
- streaming: /"yes. not defer. fix. architect corerctly such that the right soluteion falls out naturally."/
- the mandate: /"### task: plan the blast chagne!"/ → /"go"/ → /"start with proxy rebuild, and squatter eviction!"/ + mid-turn /"be thorough"/ /"be paranoid"/ /"be complete"/
- the retard callout: /"dashboard svelte is fucking where??!! told you retard, give me files, give me path!! registry?system??"/
- fork 1 pin: /"i lean c because it means we dont have to solve this now."/ + the long-term leaning: /"my long term leaning would be the the compiler handles this gracefully by allowing daemon.call.entities.literal if thats done via open() or has the default value or some heuristic (elegant??!!). adn force call.entities.literal.get/delte etc if its set. maybe. this actually produces a weird shape in practice. hmm. unsure."/ → /"later. just document it well!"/
- fork 2 pin: /"yields slot sounds harmonic."/
- THE BOUNDARY LAW: /"nowhere in kajuit do we use this dot notation pattern! we only use connection directly. the dot notation pattern is purely for the modes! we guarantee it at the buffer boundry!!! except of course testing!! apropos! dont forget testing!!"/
- phase 4 goal: /"ah ok. yes excellent. thats the goal."/
- the readability slap: /"fuck dashboard. where are we? your textblock garbage is unreadable."/
- the emitter confession: /"i made changes to emitter in the meantime. those might be wrong. check them. its about yieldish .output to allow the emitter to ALSO return plain objects. my wiring is shit tho."/
- THE ONTOLOGY: /"it should be output. output and object are different things. output is vector effect output. aka return value. the object is a hallucination!"/ + /"move buffers to typology/is"/
- the correction: /"test-runtime is runtime level, says nohting about daemon!"/
- phase 4 slim: /"just make sure there are no more .call() squatters and in playground some of the modes should use the dot proxy for demo purposes. slim tho."/
- the finish: /"still TBD? Do then."/ → /"are we done??"/

* the laws that landed
- *=.call= IS THE COMPILED APERTURE, BOTH SIDES.* Daemon: =mode.call = shape.proxy(mode.aperture)= (EXPOSED trait). Client: =mode.call = shape.connection.wire(mode.connection, strip)= off =/metadata/aperture= (new kajuit EXPOSED trait, emitter-exemplar two-liner; =daemon.call= wired in the dossier boot =Promise.all=). Same spelling, different backing — the stripwire law extended from traits to the aperture itself. Raw transport is EXPLICIT and only ever =entity.connection.call(path, body)=; nothing may alias =connection.call= onto an entity again.
- *THE PROXY IS A LAZY SIGNAL ACCUMULATOR.* =shape.proxy= = =chart(root, execute, position, segments)=: property get advances =position= via =steer.match.scope= + =feed= (existence check only), apply joins segments and delegates to =steer.dispatch.invoke=. Navigation uses the same feed choice traverse makes — navigation and execution CANNOT disagree. Zero Pattern internals in the shape; the 60-line =matchChild/wrap/proxyRemainder= leakage dissolved.
- *THE BUFFER-BOUNDARY LAW.* Dot-nav =.call= is purely the MODE surface, guaranteed at the buffer boundary: kajuit COMPILES =daemon.call=/=mode.call= but kajuit internals (dossiers, decks, panels, stores) always speak =connection= directly. Consumers of dot-nav = registry buffer views + tests. Sibling of the buffer-bundle law: the boundary provisions, the shell abstains.
- *THE WILDCARD IS "NO METHOD CHOSEN".* Local strategies build ctx with =method: "*"= — Request defaults POST for transport, but local dispatch is NOT transport, and the house grammar already had the word. =methods()=: real method → strict (405 on miss, HTTP semantics intact); wildcard ask → sole handler if unambiguous, THROW if ambiguous (fork 1c — loud, symmetric with the wire compile-throw on multi-method leaves; zero multi-method leaves exist, the first one forces the tiering conversation beef deferred).
- *STREAMING SPELLING: AWAIT THEN ITERATE.* =for await (const packet of await side.ticks({}))= — local returns Promise<iterator> (async strategy), remote returns the iterator (=connection.stream= generator), =await= is identity on the remote. The =yields= edge slot IS the streaming fact (=output= = one value, =yields= = one packet); wire compiles =yields=-leaves to a stream adapter with =options.signal=; =break= from the consumer cancels both sides.
- *=output= ≠ =object=.* =output= = vector effect output, the plain return value; =object= = a hallucination product. The EMITTER envelope fold: pool BEFORE drain; =is.yieldish= OR =is.buffers= → pool; other non-nill returns ride =result.output=. Discriminator positive, no POJO sniffing: =is.buffers= (typology, constructor-name check, client-safe) because =app.buffer()= creates =BufferEntity= instances. Oracle emitter is the exemplar: =ctx.pool.add(buffer); return { trace, object }=.
- *ATTRIBUTION BY REVERT-BASELINE.* When the EMITTER family went red mid-blast, the claim "not mine" was EARNED: every Phase-3 edit copied to scratch, reverted, failure reproduced identically, forward state restored. The twin failure — "no daemon mount" claimed from a truncated curl — got the beef correction it deserved. Both directions of the same discipline: no attribution without a baseline, no absence-claim from a cut-off read.

* what landed (files)
- Phase 1: binds deleted in kajuit =mode.js=/=daemon/dossier.js=, runtime =lifecycle/population.js=, =tests/scenarios/bench.js=; 33 string-call sites in 16 files (12 game views, dashboard ×4, oracle, aprende) → =entity.connection.call=.
- Phase 2: =gestalten/shape/object.js= proxy rebuilt (spec describes green unmodified).
- Phase 3: =strip.js= pluck grows =(signature, effect)= → =yields= + =methods=; =aperture.js= =methods()= wildcard-ask + =sole=→=wildcard= guard; =strategy.js= request+guarded ask ="*"=; =connection.js= wire (eager literals, lazy =:param= Proxy hop for leaf AND branch with the ="then"= guard, =yields=→=connection.stream=, single method explicit, multi-method compile throw); runtime EXPOSED → =shape.proxy= (+ both =exposed.todo.js= deleted); kajuit =traits/exposed.js= + barrel + dossier aperture fetch + =daemon.call=; =tests/gestalten/shape/stripwire.test.js= (one vector, identical assertion block local ⟷ remote over =shard.transmitter.inline=).
- Interlude: =daemon/traits/emitter.js= output-channel fold; =is/prototypes.js= =is.buffers=.
- Phase 4 slim: =Oracle.svelte:120= → =buffer.mode.call.ask({prompt})= beside =:140= =mode.emit.vision= — both compiled grammars in one view.
- Green at close: typology 126/1192 · runtime mode 11/62 · runtime+daemon in-process 5/24 · live wire probe + in-browser oracle round-trip with a real answer ("A stripwire is a tripwire stripped of patience…" — the oracle defined the law itself).

* loose ends (settlement pass)
- *riddler todo UNLOCKED*: =registry/education/game/riddler/aperture.js:35= can now be =ctx.mode.call.assistant.evaluate({ buffer })= — daemon-side proxy exists; beef's call when.
- *dashboard dead subscriptions*: =registry/education/dashboard/dataspace/Dashboard.svelte= =daemon.subscribe?.()= ×2 are silent no-ops (daemon entity never had =.subscribe=); making them live = behavior change; flagged, beef's call. ("fuck dashboard" — parked.)
- *fork-1 tiering* dormant by design: the compile/wildcard throws are the placeholder that forces the conversation when the first genuinely multi-method leaf appears; beef's graceful-tiering leaning documented in the quest.
- *wildcard/remainder branches on the wire*: warn-and-skip, zero consumers.
- *education views → dot-nav*: future blast; needs a settled "compiled surface not ready" boot pattern (=daemon.call= exists only post-metadata-fetch).
- *runtime :2501*: started for validation, background task stopped after — down at session close; =cd systems/runtime && deno task run=.
- *shape superseded*: the ={leaves,branches}= wire contract this arc shipped was rotated node-centric by [[no-path-is-a-path-the-root-effect-becomes-addressable-the-own-scaffold-is-ripped-and-shape-strip-rotates-node-centric-to-the-cata-of-the-vector]]; params/methods/yields/stream semantics survive the rotation. Compacts immutable — this one records the arc as it ran.
