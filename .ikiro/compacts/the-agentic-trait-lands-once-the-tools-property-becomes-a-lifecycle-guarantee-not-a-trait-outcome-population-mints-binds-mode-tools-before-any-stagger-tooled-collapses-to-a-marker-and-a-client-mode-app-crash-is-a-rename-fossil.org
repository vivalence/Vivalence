#+TITLE: the AGENTIC trait lands once =tools= becomes a LIFECYCLE GUARANTEE, not a trait outcome — population mints + binds =mode.tools= (daemon/mode ambient) and slurps =mode.module.tools= BEFORE any trait staggers, so AGENTIC harvests every TOOLED source order-free (=slurp= snapshots =carry=, so the binds must already exist); =TOOLED= collapses to a pure marker, =AGENTIC= = a map-filter namespacing each source under its slug; and a client =mode.app is undefined= boot-crash turns out a RENAME FOSSIL (=mode.buffer= → =mode.app.view= without the =mode.app= init), not the tools work.
#+TOPIC: the closing move of the M20 hallucination recast — the original =patch.org= AGENTIC trait (a mode's agent wields every TOOLED mode's tools) finally lands. Companion to =[[the-hallucination-tool-seam-recasts-around-one-law-...]]= (the seam) and =[[every-ambivalence-is-a-misplaced-boundary-...]]= (the lens). Memory =[[project_m20_hallucination_session_recast]]=; trait grammar =[[project_trait_wraps_module_vector]]=; ownership =[[project_hallucination_harness_cortex_ontology]]=.

* Arc

** the reopen — beef moves AGENTIC off the middleware, onto =mode.tools=
The prior session closed AGENTIC as a per-request middleware injecting into =ctx.hallucination.tools= ("map filter. go"). beef then hand-edited the trait to a commented stub operating on =mode.tools= directly: =mode.tools.branch(mode.mount aka mode.type/slug).slurp(source.tools)=. The "aka" is a note-to-self, not a spec — a boundary still being placed. Left the body commented; said "fix".

** the ordering hazard I surfaced
=slurp= (=vector.js=) SHARES live node references for new patterns but SNAPSHOTS =carry= (the middleware list) at slurp time, and only captures patterns present AT slurp time. So a mode-level static compose (=mode.tools.branch(source.slug).slurp(source.tools)=) is correct ONLY if every source mode is staggered BEFORE the agentic mode — else =source.tools= is unbuilt/unbound and the source is silently dropped, or its =carry= (the daemon/mode binds) is copied empty. =resolution.js= staggers modes sequentially in =flatmodes()= order; nothing guarantees the agentic mode is last. The eager =mode.tools= approach reintroduces exactly the boot-order fragility the middleware dodged by being lazy.

** beef's law — the property is a LIFECYCLE guarantee (verbatim)
"actually, we want to have the tools property guarantueed by the time the traits start populating them with content. we want the lifecycle to create it and slurp up any mode.module.tools *!!!" — this DISSOLVES the hazard rather than patching it. =mode.tools= stops being a trait OUTCOME and becomes a lifecycle-guaranteed PROPERTY: the traits merely POPULATE a Vector the lifecycle has already minted, bound, and seeded. The connoisseur move again — name the ONE owner (the lifecycle), and the ordering question stops existing.

** the placement — population pass, before any stagger
=daemon/lifecycle/population.js= =modes()= (the CONSTRUCT/POPULATE pass, which runs for ALL modes before =resolution.js='s stagger pass touches ANY) now mints per mode:
#+begin_src js
mode.tools = new Vector();
mode.tools.use(shard.context.bind("daemon", daemonDie.good));
mode.tools.use(shard.context.bind("mode", mode));
if (mode.module.tools) mode.tools.slurp(mode.module.tools);
#+end_src
The BINDS move here too, not just the mint — this is the load-bearing subtlety. Because =slurp= snapshots =carry=, if TOOLED still bound daemon/mode during STAGGER, AGENTIC harvesting a source before that source's TOOLED ran would copy an UNBOUND carry → the harvested tool executes with no =ctx.daemon=/=ctx.mode=. Population runs for every mode before any stagger, so the binds are guaranteed present when AGENTIC harvests. Order-immune by construction.

** =TOOLED= collapses to a marker; =AGENTIC= is the map-filter
=daemon/traits/tooled.js= → =export const TOOLED = () => {};= — a pure declarative marker now (the Vector, binds, and =module.tools= slurp are all lifecycle-owned). =daemon/traits/agentic.js=:
#+begin_src js
export const AGENTIC = (mode, daemon) => {
  daemon
    .flatmodes()
    .filter((source) => source !== mode && source.implements("TOOLED"))
    .forEach((source) => mode.tools.branch(source.slug).slurp(source.tools));
};
#+end_src
=source !== mode= keeps a TOOLED+AGENTIC mode from harvesting itself. =branch(source.slug)= namespaces each source: aprende's =flashcard= leaf lands at =/aprende/flashcard=, which =ToolCall= joins to the wire name =aprende_flashcard=. Dispatch runs through the SOURCE's ctx (=ctx.mode === aprende=, =ctx.daemon === daemon=) because =slurp= copied the source's =carry= (its daemon/mode binds) into the branch, and the branch's later binds override the agentic root's.

** tests — TOOLED test retargeted, AGENTIC test new
The old =tooled.test.js= asserted "TOOLED mints tools"; ownership moved, so it's retitled "tool provisioning + TOOLED marker" — a =provision()= helper mirrors the lifecycle 3-liner (scenarios build modes by hand, bypassing =population=), the tests verify a provisioned =mode.tools= executes with =ctx.daemon=/=ctx.mode= and that =TOOLED= is an inert marker. New =tests/mode/agentic.test.js= (4): harvests every TOOLED source under its slug · skips non-TOOLED modes · does not harvest itself · dispatches a harvested tool through the source mode's ctx (=steer.dispatch.invoke(hub.tools, new ToolCall("aprende_flashcard").signal, steer.strategy.guarded)=). Both green; full runtime *29 passed*, the *3* reds unchanged baseline (=integration=/=stripwire.contract=/=aperture.snapshot=, all =404 @ /daemon/brazilian/…= — live-:2501 orphans, not the tools work). NOTE (settled): =population.modes()='s REAL minting is exercised only by the live boot — scenarios use the =provision= mirror; the true lifecycle path is a blast-3 live gate.

** the client crash — a rename fossil, not the tools work
A client boot pasted =TypeError: can't access property "view", mode.app is undefined= at =systems/kajuit/src/typology/entities/mode/traits/application.js:15= for playground/spanish. NOT AGENTIC (server tools). beef: "i changed something recently... might be that." The diff (=jj diff --from de1337f1=) showed his recent =snap= (=okrtqrsp=) renamed =mode.buffer = (desc) => ({...})= → =mode.app.view = (desc) => ({...})= (the =//@beef move to mode.app= intent) WITHOUT the =mode.app= init — line 9's =// mode.app = {...,view}= is the fossil of it. Server =mode.app= exists because the module EXPORTS =app= (=Mode='s =Object.assign(this, module)= copies it); the client mode is HYDRATED from the entity, no module → no =mode.app=. =mode.buffer == worked (top-level assign); =mode.app.view == crashed (nested under undefined). Fix on go: =mode.app = {};= before line 15. brazilian/chaosmonkey =404= are separate — those daemons aren't running (booted inert, expected).

* State at fold

- Green: runtime *29 passed* (tooled + agentic among them); *3* live-:2501 baseline reds unchanged. Grep-gate: zero authored comments in the edited trait/test files.
- The AGENTIC contract, frozen: =mode.tools= is a LIFECYCLE-guaranteed Vector (minted + daemon/mode-bound + =module.tools=-slurped in the population pass) · =TOOLED= = inert marker · =AGENTIC= harvests every OTHER TOOLED mode's =tools= under =branch(source.slug)=, dispatch running through the source's own ctx.
- Client fix landed: =mode.app = {};= restores playground/spanish boot.

* Open / pending (beef gates)

1. Whole recast + AGENTIC uncommitted — VCS READ-ONLY, beef's to commit. This session the hook HARDENED: git AND jj forbidden in ANY form (even =log=/=diff=) — I propose the exact command, beef runs it via =!=. (Stricter than the standing "jj log/diff allowed" note; the gate wins.)
2. Adoption — which mode's manifest gets ="AGENTIC"= (the aprende hub is the likely candidate) is beef's call; nothing wears the trait yet.
3. Blast 3 (USAGE) live gates still open: boot =:2501= · aprende quartet · oracle =mode.call.ask= · spanish boot · one live span chronicle — AND now the real =population= tool-minting path end to end.
4. The =mode.app= shape is intentionally minimal (=​{}= then =.view=) — the commented =// mode.app = {...,view}= spread intent stays beef's to decide.
