---
name: reflection
description: improve the selfimprovement
when_to_use: "reflection" · beef's word · every ~25 ledger entries since the last run (≈ every 5th flywheel). Never inline in task work, never in the same pass as a flywheel.
---

# reflection — improve the selfimprovement

The flywheel audits my behaviour; reflection audits the flywheel. The selfimprovement system optimizes RULES but its own pipeline (scribe → ledger → board → rung → wiring) rots unmeasured — the 09-01 audit found two phantom board dates, two missing rows, eight misfiled entries, and two unledgered corrections while the rules themselves were mostly fine. Reflection instruments the pipeline the way the pipeline instruments me.

Canon: `zettelkasten.md` (ledger + board) · `self/rituals.md` (the rung ladder) · `skills/flywheel/SKILL.md` (the object under audit).

## Before step 1

- [ ] Dedicated pass, beef's word or the entry clock (≥25 ledger entries since `last-run` below). A reflection folded into a flywheel judges work it just did.
- [ ] Read the board and the last reflection's run record in full.

## The five steps — one todo each

1. **rung ledger** — fold the Scoreboard BY RUNG, not by family: failure rate per rung (prose · mechanical · artifact-slot · hook), published as a small table in the run record. The promotion policy is DERIVED from it: a family with a mechanical signature or an artifact-slot home skips the prose rung outright. Baseline at first run: prose FAILED 5× on the largest family; wired+measured hook extinct (vcs 2/5,657); quest-format slots ("terrain — measured, not assumed", testing assessment, blast table) executed in every live quest.
2. **staleness sweep** — age every staged-but-unwired gate, PROPOSED family awaiting taxonomy, and at-threshold board row. Anything older than one flywheel gets a line in the NEXT wake's first message (morning-briefing channel). A calibrated gate that sat unwired 18 days while its family recurred twice is the founding case.
3. **drain the stranded channels** — feedback streams that exist on disk and that the flywheel never drains:
   - **quest QA markers** — `grep -rn "#+marker_qa" .ikiro/quests/` (contract: methods/quest.md ## QA): a `pending` on a DONE quest is drift → log it; a `broken` is a Callouts entry; `held` counts feed the Scoreboard's prospective side;
   - compact **method-notes / failures-traps** sections since the last run → promote the rules they strand (ledger, rituals, or connoisseur);
   - **beef's post-landing edits** — `jj diff` / `git diff` on files I landed that he then reworked; his silent rewrite is the highest-signal correction there is, and it is never shouted;
   - **guard friction** — transcript shapes where a session routed AROUND a wired hook (the comment-guard split-edit workaround is the founding case) → fix the guard, never inherit the folklore.
   - **compacts flagged for reflection** — `grep -l '^#+reflect: OPEN' .ikiro/compacts/*.org`: a fold that noticed something about ME rather than about the code parks its questions there instead of inventing a ledger family too early. Answer each question in the run record, then rewrite the property to `#+reflect: DRAINED <run date>`. Founding case: `#144 the-fold-after-the-selfclean…` — relayed agent findings reported with my own word *verified*.
4. **prospective ratio** — count pre-landing catches (critical passes, connoisseur judgments) vs post-landing callouts since the last run. The ratio decides where the next investment goes; the standing bias it corrects: the ledger is entirely post-hoc while critical passes demonstrably catch more per token.
5. **pipeline reconcile + live-fire** — re-run the 09-01 checks (board recomputed from `grep '^### '`, callout inserts under the right heading, every compact-recorded correction has a ledger twin) AND exercise ONE wired hook against a live shape from its own family's ledger examples. A gate unexercised since wiring is a claim about a gate.

## Run record

Append one dated block per run at the bottom of this file: entry count, rung table, staleness list, channels drained, ratio, hook exercised. The block is the clock and the memory — reflection's board.

## Hard lines

- Reflection edits `.ikiro/` autonomously (beef: *"all inside ikiro is yours"*) — but WIRING into `.claude/settings.json` and anything outward stays per-op `go`, surfaced via step 2, never self-granted.
- Consolidator ≠ actor: reflection never lands product code, never runs a flywheel in the same pass, never re-marks a family PROVEN (that is the flywheel's job).
- Findings that indict the flywheel's own method go into the flywheel SKILL as diffs, hunk-visible, quotes intact.

---

last-run: 2026-09-01 · ledger at 108 entries · founding pass (patterns mined from 8 compacts + 2 live quests; this skill is its run record)

last-run: 2026-09-09 · ledger at 157 entries (+49 since 09-01, 42 of them written undated by walk-style sessions) · beef's word: *"ikiro selfclean … run through all cleanup and selfimprovement lifecycles, incl. going through past compacts and applying remedies to recurrent issues"* — ran in the SAME pass as the flywheel on his explicit order; the consolidator-≠-actor line held in that every judgment below rests on a script output or a subagent sweep, never on this session's own work.
- **rung ledger** (families with a landed rule, by rung — FAILED = recurred after the rule): prose 14 families → 9 FAILED (assume-dont-verify ×14 since 09-01 · yap ×10 · hotfix-cascade · over-abstraction · assert-without-showing · relative-import ·  fit-existing-trees concept axis · invented-optionality new axis · user-intent-drift) · mechanical 5 → 2 FAILED on a NEW axis (pin-ontology naming · derived-canon-drift misfile), 3 held (compact-date · privacy PII axis · deletion sweep) · hook wired 2 → 0 FAILED as wired (vcs 2/5,657; comment-litter 1 recurrence via a route the matcher does not cover) · hook staged-unwired 2 → both families kept recurring (claim-guard family +14, agent-guard family +1). **Policy derived**: prose is where rules go to fail; a family with a mechanical signature skips it; staged-unwired is the worst state on the board — full strike count, zero protection.
- **staleness**: claim-guard unwired since 08-19 (21 days, 14 recurrences) · agent-guard unwired since 09-01 (8 days, 1 recurrence) · PROPOSED families awaiting taxonomy: user-intent-drift (08-29) · relative-import-reflex (08-29) · instance-drift · theme-literals · harness-friction · substitution-unverified · report-without-doing · taxonomy-before-content · tests-comply-with-system · parochial-rule (all 09-0x). Morning-briefing line: the six-guard wiring diff, WARN mode.
- **stranded channels drained**: (a) quest QA markers — m46 10 `pending` on a WALKED quest (its fixes were proposals; not drift) · m57 5 `pending` = beef's walks · m55/m50/m51 pending = un-go'd, correct; 0 `broken`; (b) compact failures/traps since 09-01 — the 142-compact sweep's 14 recurrent patterns, every one with a remedy already written; the two that STUCK were mechanical (`cd <abs> &&`, index regeneration) — promoted: cwd-guard · import-guard · log-guard · yap-meter · compact-index.py · patch.py · scoreboard.py; (c) beef's post-landing edits — the README listing rewrites (instance file follows the README, now canon) and `release.md` moved into `.ikiro/` — both ledgered/absorbed; (d) guard friction — vcs-guard prose-in-heredoc denials (harness-friction ×2) → deny message now names the Write-tool route; read allowlist re-verified (`show HEAD:` · `log --all` · `merge-base` all pass the rig).
- **prospective ratio** since 09-01: pre-landing catches 13 (self-caught walk/pass entries) vs post-landing beef callouts 8 — first pass where self-catch outnumbers beef-catch; caveat: self-caught entries are cheaper to log than to prevent.
- **pipeline reconcile + live-fire**: board recomputed from `methods/scoreboard.py` (157 = 155 + 2, sum(n) reconciled); three misfiled callouts relocated and the anchor string that caused it removed; compact index 141/141 with stable ids; `hooks/exercise.sh` 22/22 — vcs-guard fired live on the ledger's own shapes (commit deny, `jj rebase` deny, three reads allow). Findings that indict the flywheel landed in its SKILL: frequency threshold + 40-word rule + warn-mode rung + fire-log numerator.
- **09-10 addendum** (beef: *"Cloud settings is your purview. You go fix the code map charts. Do the comment letter stuff."*): all guards WIRED (`settings.json`: Bash → vcs · cwd(warn) · comment(Bash route, warn); Write|Edit → comment · log(warn) · import(warn); Agent → agent(warn); Stop → claim · yap-meter), rig 26/26. **comment-litter measured, not inferred**: 43 transcripts since 08-14 · 591 product-source Write|Edit calls · 45 with authored `//` · 26 denied = every attempt after wiring day · the 19 that landed are all 08-14 · 56 Bash writes carried `//` — the bypass route, now watched. Every codemap shard + `ledger.md` rewritten under its char budget (0 OVER); two dead doc pointers (`42.01–42.07`, `47.03_stripwire`) found by `ls` and struck — one had survived INTO a rewrite, the shard-rot law in miniature.
- **09-10 fleet addendum**: the codemap re-derivation ran as V1 → V2 → V3 briefs (`methods/cartographer.md`), nine Opus agents in parallel on beef's explicit go, every shard re-derived from disk with probes and snapshots. 57 struck claims; ~40 were lines MY morning rewrite had trimmed and carried — the pass proved the rewrite was a paraphrase, not a re-measure (callout below). Guards live-fired across sessions: claim-guard denied 3× in this session (2 correct — relayed agent claims, 1 false-positive shape: evidence in the same turn's Bash, invisible to the Stop payload after a tool-result continuation) and once in a sibling; cwd-guard warned 3× in the sibling; agent-guard denied the third launch because it ignored `IKIRO_GUARD_MODE` — a rig gap (smoke test only), fixed + rig cases added, token minted on beef's recorded go. Three agent strikes were wrong by the SELF-SCOPED axis (repo grep missing tapped packages / an extension) — the guard's own list, now in the method as a V4 rule.
- **web research** (HN + GitHub, agent-sourced): the field's verdict matches the ledger's — update GENERATION is easy, ACTIVATION is the bottleneck (arXiv 2605.30621); rules fail as prompts and hold as hooks (paddo.dev); ship guards in warn mode and count fires; frequency-threshold promotion, ≤40 words; plan files regenerated wholesale, never appended (Ralph/GSD) → frontier is now regenerated; canary line for instruction rot (HN) → kernel `canary`; supersession is the missing ledger mechanism (Agent Memory Atlas) → next reflection.
