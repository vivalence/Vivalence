# loop backlog — the /loop work-queue

<!-- writer: agent · the durable driver for beef's /loop runs · CUT 09-09 selfclean: closed runs and their narration are records of built things (git holds them: `git show f30b6aa2b:.ikiro/loop-backlog.md`); only OPEN items stay · limit: 6000 chars -->

## PRIME DIRECTIVE

An open-ended duration order (*"work through the night"*) is satisfied by **ELAPSED TIME, not a task-list**: night ≈ 8h → 480/20 ≈ 24 iterations. When the list empties, pull from § Backstop or spawn ONE finder to refill. Stop only at the horizon or beef's word. beef: *"i told you to work through the night retard. night is how many hours? whats /20 mins of that/?? thats the loop count"*

A loop report is the DIFF block + a ≤3-line verdict + a pointer here; the prose lives in the backlog entry, never twice.

## STAGED — awaits beef's word

- [ ] **guards from warn → deny** — after ~10 sessions read `~/.claude/projects/…/hooks.log` per guard (rows with session `exercise` are the rig's): a real fire rate with no false positive flips that guard's `IKIRO_GUARD_MODE`; a guard that never fires stays warn. beef ruled 09-10: `.claude/settings.json` is mine (*"Cloud settings is your purview"*) — wired 09-10, `bash .ikiro/hooks/exercise.sh` 26/26.

## OPEN — mine, next loop

- [ ] `[SAFE]` **MEMORY.md prune, second pass** — 264 files / 19 KB index is the largest always-on channel; next: fold sibling `feedback_*` files that state one rule twice.
- [ ] `[SAFE]` **comment-litter Bash route → deny** — the 09-10 sweep: 26/26 Write|Edit attempts denied since wiring, 56 Bash heredoc/python writes carried `//` (the bypass). comment-guard's Bash branch is warn + log; count its fires, then deny.
- [ ] `[SAFE]` **cartographer V4** — fold `methods/cartographer.md ## fleet feedback → V4` into the protocol: budget in BYTES · `// snapshot:` fence tag · shares as targets below 6 000 · territory rows from `ls` · scratch-ledger doctor · **grep `~/.viva/registry` + every extension before any "dead / zero consumers" claim** (three fleet strikes were wrong by that axis) · elisions must leave a block readable as code. Then a shard is re-derived whenever its territory moves, never hand-edited (ledger: the 09-10 callout).
- [ ] `[VERIFY]` **claim-guard calibration** — one false-positive shape day one: a text continuation whose evidence sat in the same turn's Bash (the Stop payload after a tool-result continuation did not carry the call). Read `hooks.log` claim-guard rows with their transcripts before touching the threshold.
- [ ] `[VERIFY]` **drain `#+reflect: OPEN` compacts** — `#144 the-fold-after-the-selfclean…` parks three: does relayed evidence need its own vocabulary in a report · is claim-guard's true-positive rate holding (4 fires, 3 correct) · did "a shard is re-derived, never hand-edited" take. Channel is wired into `skills/reflection` step 3; answer in the run record, then flip the property to `DRAINED`.
- [ ] `[SAFE]` **world test-map** — every test file → covered source → `world/codemap/tests.md` + gaps.
- [ ] `[VERIFY]` **stale-test investigation** — pensieve.test lookup→revelio, paladin.test hal257: dead/live verdict; fixes propose-only.
- [ ] `[SAFE]` **zettelkasten ## Open triage** — buckets that grew (testing · br-pt · tatoeba) → quest spins or deletion; the `[loop directive]` line stays (beef's).

## Backstop — inexhaustible

- [ ] `[SAFE]` **selfmod deep-dive (repeatable)** — beef's standing order: *"research into the current meta and status quo of skills and agent architecture and adjust yourself"*. One meta topic per pass → one concrete self/methods/skills adjustment, source-grounded. Last run 09-09 (HN + GitHub; remedies landed: fire logging · warn-mode rung · frequency threshold · stable compact ids · regenerated frontier · canary).

## notes

- compact ids are now `#+index:` properties (stable); cite `#<id> <slug-prefix…>`.
- Bash cwd PERSISTS across calls — `cwd-guard.sh` denies a bare relative `cd`; use absolute paths or a subshell.
- 13/14 background agents die at exactly 600 s of stream silence — keep agent tasks under that or make them print.
