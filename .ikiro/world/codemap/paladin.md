---
paths: ["subsystems/paladin/**"]
---
<!-- writer: agent · derived-from: subsystems/paladin + corpus read · verified: session c795a2f7 · limit: 30 lines -->
# codemap: paladin — composition compiler (singleton, never runs anything)

- **boot** (`mod.js`, grounded 13 lines): `new Paladin()` → `populate.env` (VIVA_*/PUBLIC_* → `paladin.env`, SECRET_VIVA_* → `paladin.secret`; optional `VIVA_ENV_FILE` dotenv) → `populate.scopes` (conditional-Proxy resolvers: ledger/repository/registry/variant/environment/mountpoint) → citizen: `integrate.statements`. `variant.mount()` is LAZY (`fn.once`): environment jsonc-fold → resolve (find.type variant marker, hydrate, Mask daemons/services) → validate (v.primitives.variant casts) → `publish()` (PUBLIC_* → Deno.env).
- **prototypes**: Paladin (env/secret/variant/system) · Vip (`supply` = ledger registry.json ?? seed-by-discovery → `mount` per location; `mount` walk→stamp-owner→register — owner DERIVED from mount dirname, a walked package cake (`manifest.type === "package"`) with authored `manifest.owner` LOCKS it, NO magic filename; `accio` two-throw: "not supplied" ≠ "Module 404") · Pensieve (nested Map owner→type→slug→version; `revelio` pure descent, `latest` semver fold; register THROWS on missing owner) · System (spawn; ledger = `testament/ledger/{locks/,logs/,instances.json,registry.json}`; `lock(instance, process)` → `<instance>_<process>.lock`, `alive()` via SIGCONT; `Log.append(span)` → `logs/<instance>/spans.jsonl`; `Registry` read/write/seed — ABSOLUTE locations, self-reseeds when deleted).
- **PRIMITIVES — never hand-roll**: `paladin.find.viva(dir)` · `paladin.read.viva(path)` · `paladin.source(location)` (4 styles: absolute / ./cwd / {file,source} / bare-repo-segment) · `paladin.vip.accio(query)` · `paladin.vip.accioMap(obj)` · `cast.lookup("@owner/type/slug@ver")`.
- **M11 LANDED** ([[m11_packages.quest]]): three packages `registry/{development,education,viva}`; supply is SYSTEM-level (`vip.supply()`), variant declares NOTHING about packages; never `.branch()` a reused Path — [[project_signature_branch_mutates]].
- deploy: local = testament mounts (`testament/variant/.env`) · docker = `VIVA_*_MOUNT` envs, no testament in image — Dockerfile still expects the dead `registry/simulation/variant` bake, fresh decision owed.
