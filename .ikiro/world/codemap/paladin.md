---
paths: ["subsystems/paladin/**"]
---
<!-- writer: agent · derived-from: subsystems/paladin + registry read, claims re-verified against disk · verified: 20%-cut loop, re-stamp pass · limit: 30 lines -->
# codemap: paladin — composition compiler (singleton, never runs anything)

- **boot** (`mod.js`, grounded 13 lines): `new Paladin()` → `populate.env` (VIVA_*/PUBLIC_* → `paladin.env`, SECRET_VIVA_* → `paladin.secret`; optional `VIVA_ENV_FILE` dotenv) → `populate.scopes` (conditional-Proxy resolvers: ledger/repository/registry/variant/environment/mountpoint) → citizen: `integrate.statements`. `variant.mount()` is LAZY (`fn.once`): environment jsonc-fold → resolve (find.type variant marker, hydrate, Mask daemons/services) → validate (v.primitives.variant casts) → `publish()` (PUBLIC_* → Deno.env).
- **prototypes/** (`paladin.js` · `vip.js` · `pensieve.js` · `variant.js` · `manifest.js` · `ledger/`): Paladin (env/secret/variant/system) · Vip (`supply` = ledger registry.json ?? seed-by-discovery → `mount` per location; `mount` walk→stamp-owner→register; NO dirname derivation — a mount with modules but no package-owner THROWS `declares no owner`, same class as a missing slug; a module's own `manifest.owner` overrides; `accio` two-throw: "not supplied" ≠ "Module 404") · Pensieve (nested Map owner→type→slug→version; `revelio → own(module)` per-daemon MINT — clones mutable descriptors, SHARES immutable leaves, `WeakMap` cycle-guard relinks `Vector.ancestor`; `latest` semver fold; register THROWS on missing owner) · ledger/ = the System surface (`instances.js` `lock.js` `log.js` `process.js` `registry.js`; ledger at `testament/ledger/{locks/,logs/,instances.json,registry.json}`, `lock(instance, process)` → `<instance>_<process>.lock`, `alive()` via SIGCONT, `Log.append(span)` → `logs/<instance>/spans.jsonl`, Registry self-reseeds when deleted).
- **skills/** (m29 armory, attached `paladin.skills = skills` in `mod.js`): `fs.js` — `fs_tree` (depth≤5, 200-entry cap, skip-list bak/archive/slp/node_modules/.git) · `fs_find` (name regex) · `fs_read` (16 kB cap + range steering) · `fs_write` · `shell.js` — `shell_run` (zsh -c, timeout≤120 s, 8 kB tail, exit code = information not failure). Every path resolves under `ctx.root` (bound by harnessed to `mode.module.mount.dirname`); escapes throw naming the root; absolute inputs fold under it. Armed for ANY harnessed mode with a module mount — beef: "no new trait". `process_*` deliberately not built. Escape guard + behaviors pinned by `tests/skills.test.js` (8 steps: `../` + absolute-path folding, bak skip, range slice, "(empty)", nonzero-stays-NOMINAL).
- **PRIMITIVES — never hand-roll**: `paladin.find.viva(dir)` · `paladin.find.walk(pattern)(dir)` · `paladin.find.data(dir)` (m22: the corpus walk — `index.js` SKIPPED so aggregators never double-count, codec dispatched per extension because Deno refuses a `.json` specifier without `with {type:"json"}`) · `paladin.read.data(path)` (`.default ?? module`) · `paladin.state.scribe(path, text)` (m22: atomic tmp+rename, write-if-changed, returns whether it wrote — the fixpoint primitive) · `paladin.read.viva(path)` · `paladin.source(location)` (4 styles: absolute / ./cwd / {file,source} / bare-repo-segment) · `paladin.vip.accio(query)` · `paladin.vip.accioMap(obj)` · `cast.lookup("@owner/type/slug@ver")`.
- **M11 packages — FIVE, and the marker is the manifest, NOT the filename**: `education`(@education) · `fixtures`(@fixtures) · `playground`(@playground) · `viva`(@viva) · `young-ladys-primer`(@young-ladys-primer). A package root is any root `*.viva.js` with `manifest.type === "package"` — only 2 of the 5 are literally named `package.viva.js`; the other 3 are `<slug>.viva.js` (`education.viva.js`, `young-ladys-primer.viva.js`). Grepping for the filename finds three-fifths of them. **On disk ≠ mounted: FOUR ingest** — the live pensieve is `@playground @education @fixtures @viva`; `@young-ladys-primer` is absent entirely (deliberate, [[project_young_ladys_primer]]). Any assertion naming the mounted set is pinning a literal that mounting it would break. `registry/fixtures` is a REAL package (`package.viva.js` + `data/` + `language-learning/corpus.viva.js` + `snapshots/`), not the empty non-package dir this shard used to claim.
- supply is SYSTEM-level (`vip.supply()`); variant declares NOTHING about packages; never `.branch()` a reused Path — [[project_signature_branch_mutates]].
- deploy: local = testament mounts (`testament/variant/.env`) · docker = `VIVA_*_MOUNT` envs, no testament in image. The old `registry/simulation/variant` bake is GONE from every Dockerfile (grep: zero hits across `Dockerfile` + `documentation/Dockerfile`) — that "fresh decision owed" note is retired.
