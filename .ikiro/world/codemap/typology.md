---
paths: ["subsystems/typology/**"]
---
<!-- writer: agent · derived-from: subsystems/typology + corpus read · verified: session c795a2f7 · limit: 40 lines -->
# codemap: typology — the library (HOLY — ask before touching core types)

Composable building blocks; power emergent from composition. Read greedily first — typology IS the vocabulary. See [[connoisseur]] for the exemplar canon living here.

- **prototypes** (~40): Signature (root; coerce: Pattern splits `/`, Path normalizes FS, Url origin) · Vector (`use` mw, `branch`, `open(sig,fn)`, `slurp` share / `swallow` own) · Aperture (Vector + method-keyed leaf fold = the HTTP server) · Connection (transport dual; `branch/aim/stream`) · Wafer (empty lifecycle verbs) · **Stall REBUILT** (thread-owned `$phase` inert/manual/continuous/escort; `engage` gate; `settle` cursor authority; targets call `buffer.release()`) · Pipe/Queue/Broadcaster/Pool (channel family, shared `waiter` atom) · Env (transparent Proxy).
- **gestalten**: `is · cast · not · fromm · belt · shard · steer · shape`. steer = `fold`/`descend` (tree.js) + walkers (walk.js) + invoke/rollup/survey/traverse; shape = Vector compilers (http, object, proxy, strip, messenger, agentic, mcp).
- **schematics**: `v` = typebox@1.3 wrapper. `cast` = Default+Convert (request INPUT only) · `fill` = Default-only (entities/buffers/output — Convert mauls MikroORM Collections). enhance derives, never spreads. NO passthrough/strict/transform/refine/partial/nullable.
- entities flow: Mode → Intent → Thread → Buffer → Turn. Channel grammar: `/{handshake|dialogue|speech|verbatim}/{open|packet|close|abort|error}`.
- buffer-view bundles import ONLY `@vivalence/typology` (kajuit barrel drags vite aliases + worklets). `atom.chain` lives here.
- tests flat under `tests/`; in-flight under `tests/workpackage/`. Belt majority untested (known gap).
