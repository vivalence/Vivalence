---
paths: ["testament/**"]
---
<!-- writer: agent · derived-from: testament + corpus read · verified: session c795a2f7 · limit: 15 lines -->
# codemap: testament — the machine run surface (gitignored, excluded from workspace)

`instance/test.viva.js` (type:instance manifest + runtime/clients/services; imports the two decomposed daemon files) · `instance/education.js` (daemon brazilian: kernel + FULL mode set) · `instance/development.js` (daemon devyground: all six modes) · `instance/.env` (VIVA_LEDGER_MOUNT / VIVA_INSTANCE_MOUNT / VIVA_MOUNTPOINT_MOUNT + SECRET_VIVA_* keys — deno tasks point here) · `instance/environment/*.jsonc` · `instance/mountpoint/daemon_*/` (sqlite + migrations — **never delete DBs or migrations**) · `ledger/` (locks/ + logs/ + instances.json + registry.json — package-location record, self-reseeds when deleted) · `_bruno/` (API testbench; axios buffers SSE).
