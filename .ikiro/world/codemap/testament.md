---
paths: ["testament/**"]
---
<!-- writer: agent · derived-from: testament + corpus read · verified: session c795a2f7 · limit: 15 lines -->
# codemap: testament — the live dev variant (excluded from workspace)

`variant/test.viva.js` (type:variant + runtime/clients/services) · `variant/circuitry.js` (daemons brazilian + playground: kernel/modes/hallucinators/consume) · `variant/environment/*.jsonc` (env + secrets) · `variant/mountpoint/daemon_*/` (sqlite + migrations — **never delete DBs or migrations**) · `system/` (instances.json ledger + locks) · `_bruno/` (API testbench; axios buffers SSE) · `temp.js` ([[totems]] koans) · `.env`.
