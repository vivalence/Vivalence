---
paths: ["testament/**"]
---
<!-- writer: agent · derived-from: testament + corpus read · verified: session c795a2f7 · limit: 15 lines -->
# codemap: testament — the machine run surface (gitignored, excluded from workspace)

`variant/test.viva.js` (type:variant manifest + runtime/clients/services; imports the two decomposed daemon files) · `variant/education.js` (daemon brazilian: kernel + FULL mode set) · `variant/playground.js` (daemon playground: all six modes) · `variant/.env` (VIVA_LEDGER_MOUNT / VIVA_VARIANT_MOUNT / VIVA_MOUNTPOINT_MOUNT + SECRET_VIVA_* keys — deno tasks point here) · `variant/environment/*.jsonc` · `variant/mountpoint/daemon_*/` (sqlite + migrations — **never delete DBs or migrations**) · `ledger/` (locks/ + logs/ + instances.json + registry.json — package-location record, self-reseeds when deleted) · `_bruno/` (API testbench; axios buffers SSE).
