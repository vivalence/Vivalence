---
paths: ["systems/ghost/**"]
---
<!-- writer: agent · derived-from: systems/ghost, trajectory groups re-verified against disk · verified: 20%-cut loop, re-stamp pass · limit: 20 lines -->
# codemap: ghost — operator (the `viva` CLI)

**Ledger spine (verbs table · frame law · ~/.viva anatomy) is OWNED at `world/ledger.md`** — read it before touching ledger/instance/registry trajectories; restamp it on any landing here.

- **flow**: `ghost.sh` (`deno run --config <root>`) → `ShellSignal` (argv → signature; `prototypes/shellsignal.js`) → steer dispatch over the trajectory Vector (`mod.js`) → run. `trajectories/` is canonical (`slp/` was slop, deleted); target selection = `trajectories/instance/target.js specs()`. tests/ green post deep-clean (shellsignal + target; old parse/processes/resolve/start/wafer/init.gather tests purged — dead contracts).
- **commands** — post-m40 the CLI collapsed to TWO nouns, then the variant→instance rename settled the spelling; trajectory groups on disk are `instance` · `ledger` · `sheets`: `viva instance {create,init,run,start,stop,auth,doctor} · viva instances {list,use} (PLURAL = the set)` (`instances/use <ref>` (PLURAL — use selects one out of a SET) = per-shell selection via VIVA_PROCESS_ID + `<ledger>/sessions/<pid>.json`, `--ledger` = machine default, trailing params chain) (`create <@owner/instance/slug|path> [target]` — no target shelves under `<ledger>/instances/<slug>`; `auth {signup,login}` reads `instance.lighthouse.statics.remote` → `new Connection(remote).call(...)`; `init` seeds `.env` then wizard-boots — its signup FALLS THROUGH to `/auth/login` on `USERNAME_EXISTS` (init ensures the admin, never duplicates; guard reads `error.context.response.body.error.code`; stamped: verified live — re-init same creds returned the same identity id); `init.js owed()` counts ANY unset schema key incl. unread `documented` rows, so schema-only keys like the ORIGIN pair gate headless init until set (beef's call whether intended)) · `viva ledger {init,doctor}` + `viva registry {tap,untap,bootstrap}` (branches `/ledger` and `/registry` slurp their `index.js`; tap/untap EMIGRATED from ledger, no aliases. `bootstrap [destination] [source?]` = author + tap; a cloned package's declaration is REPLACED with the destination's own owner/slug, never patched — the pensieve keys on owner/type/slug). Old `system`/`variant` groups + `/variant/*` natures are DEAD — killed, no aliases.
- **the picker stack**: `belt/lens.js` = LENSES (one per dataset → `{label,rows,keys,facets,columns,reference,index?}`; `modes({type})` folds the pensieve, `instances()` folds `ledger.instances.list()`), `belt/pick.js` = 0/1/N arbitration (one match answers headlessly through the SAME `sheets/state/search.js` fold the picker runs; a picker in a pipe is a hang, so non-interactive ambiguity THROWS naming candidates), `belt/clone.js` = `tree()` (distinct from `paladin.clone`, the git/jj checkout). `create`/`use`/`bootstrap` all take a slug and resolve it.
- **path law, whole**: `belt/path.js` — `pin()` (operator cwd frame) · `source()` (a remote survives verbatim; `paladin.clone.remote` is the ONE remote test, next to the fetcher) · `instance()` (bare slug → `ledger.instances.shelf`, separator → `pin`). **MOUNT MEANS PATH** — every writer resolves before storing; `populate.js` THROWS on a slug in `VIVA_INSTANCE_MOUNT`.
- **`ghost.sh` owns the session id**: `VIVA_PROCESS_ID="${VIVA_PROCESS_ID:-$PPID}"`. `ledger/init` must NEVER write `$$` into `~/.config/viva/env` — ghost.sh SOURCES that file, so `$$` is its own pid, fresh every invocation, and `instance/use` could never be read back.
- **process spec keys are `{instance, process}`** — not `type`/`slug`. `ledger/process.js` builds its lock from them.
- **known-issues** ([[known-issues]]): detached spawn dies on parent exit (use `instance/run` or `init`) · `deno task` cwd-rewrite (INIT_CWD workaround) · scope-overwrite warning RESOLVED by m41 (cwd stratum).
- registry access rides `vip.supply()` everywhere — the old "mounts registry FLAT via vip.mount(scope.registry)" claim was stale; no raw mount call sites remain outside comments. Effects ALWAYS print: the view middleware default-renders any unrendered non-null effect through `subsystems/sheets/components/Effect.jsx` (generic non-interactive ink tree; m41 late rulings — "silent by design" DEAD, JSON only under `--json`); JSX views (ledger's `Doctor.jsx`) mark `ctx.rendered` and suppress the default; `ctx.call` inner contexts are pre-marked so chains print once.
