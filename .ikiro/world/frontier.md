# frontier — the live edge
<!-- writer: agent · derived — REGENERATED whole at every disintegrate, never appended · sources: quests/index.md (status + next) · known-issues.org · compacts (owed lines) · limit: 5000 chars -->

**Live quests live in `quests/index.md` (13 live) and the quest report (`methods/quest-report.py`) — status · next · sessions per row, derived from disk.** This file no longer restates them: two sessions writing one status line breached the budget four folds running, and the quest already holds the truth.

## the gates (what stops the arc)

1. **beef's click-walk** — kajuit live walks are the debt, not design: m57 T-revise · T-label · T-figure · T-heard · T-thin; m56 panel C at both aspects; m55 M2 `go`; drapes editor 8 click-throughs; m53 v4 restart.
2. **anthropic SSE bug** — blocks toolcalling scale, cross-mode emit, audio verify.
3. **the three-quest seam** — m54 buffer FILE · m53 T1.1 · m53 v3-vdex all wait on go for the same seam; RECONCILE before any lands.
4. **m59 landed, ghost carries m57's mid-flight rework** — m59 is applied to the live trees and green (paladin · typology · runtime · hello-world, end-to-end boot verified). Ghost is RED on the live tree — `paladin.mounts` undefined — from the SIBLING m57 session's unfinished ghost rework (`config.mounts` → `paladin.mounts`), NOT m59; m59's own ghost hunks ran 89 green in the pre-drift sandbox. m57 must finish or back out its ghost `mounts` refactor before ghost is green again.

## owed to beef (the standing list — compacts POINT here, never restate)

- **rotate the Anthropic key** — the snapshot is NOW redacted at the source (m59 landed, `paladin-instance.snapshot.json` carries `***`, grep `sk-ant` = 0), but a cleartext copy still sits in the uncommitted `.ikiro/zettelkasten.md` edit, and the live key is unrotated. Rotate + scrub the zettel copy. Owed since ten folds.
- **commit** — everything since `f30b6aa2b wip` is uncommitted: m47 commons · m52 pinhole · m53 v4 · m55 M1 · m57 · m59 · drapes editor · lighthouse hoist · instance rename · the selfclean.
- **m59** — LANDED (both patches applied, eight db renames done, snapshot regenerated, boot verified). Left for beef: **commit** (above) · the **education push** (its own repo — the three `registry/education/instances/*` recipes were patched in the ledger) · **start his `runtime/watch`** (my verification boot was stopped; nothing is on 2501).
- **education push** + **prod M5** (m47).
- **MARKERS cortex-snapshot entry** — OPEN, his ruling.
- **six playground modes** still carry `v.buffer({ data: {} })` — he said hello-world only.

## simmering strands (beef's logs — recurs, unshipped)

1. **docs/README** — #1 recurring: clean-box walk · doctor dump · `## Instance` stub → [[project_documentation_site]]
2. **launch WBS** — install → contributions → fundraising; text + games, no audio.
3. **kajuit architecture unease** — *"still .... ish. bit convoluted"* → m56.
4. **lighthouse security** — `identity/find` leaks argon2 hashes to any bearer · `GET /multiplex` 500 (known-issues).
5. **entities-TS 72 errors** — blocks npm/jsr publish.

## design held only here (no quest file — restate before building)

closed-class · exhibit-absorbs-shadow · herald · decorum M2-M5 (+dapper-second-theme) · siphon · ghost-autocompleting · identity-collapse · m5 widget-glyphs · m9 playground-layout · m10 monkey · m13 module/mode/kernel alignment · m14 testing fixtures · twitch-vector (`fanout`)
