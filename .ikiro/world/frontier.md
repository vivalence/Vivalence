# frontier — the live edge
<!-- writer: agent · derived-from: quests/ + compacts/ + private logs · verified: session 8327f8a6 · limit: 90 lines -->

Derived: re-derivable from quests + compacts + logs. Update at every session disintegrate. State verdict from the full corpus read: **architecture coherent and contradiction-free; the debt is live-validation, not design.**

## the four gates (nearly everything open funnels through these)

1. **M4.0 baseline** — beef's manual click-walk of the playground rig. Gates the whole M4→M5→M9→M10 stall/widget/monkey chain. beef-driven.
2. **one live daemon run** — PARTIALLY CLEARED: playground got a real live boot this session via chaosmonkey/oracle (harness.object.render proven against the real anthropic API, both aperture-mediated and client-strip-wired paths) — surfaced a real browser 6-per-host SSE connection-budget bug (fixed: standalone chaosmonkey daemon retired, modes ride playground kernel). G2/G3/G4 proper, nyan playroom, spanish boot, longdistance audio still *shipped-architecture-never-run-live*.
3. **beef go / fork-pick** — M11 packages (DESIGN+QA'd) · closed-class (spec locked) · identity-collapse · aprende↔nyan refill (3 forks).
4. **M3 anthropic SSE stream bug** — blocks toolcalling scale (cache_control), cross-mode emit, audio verify.

## quests by status (post corpus-read; stale statuses corrected)

**WIP/ACTIVE**: m4 phase-playground (G1 ✓ + Phase widget ✓ shipped — quest text lags; G2/G4 engage-refresh next, M4.4 capstone) · pincer 18+ · c-panel-rebuild (metadata cutover tail) · terminal-first-client P1 · trait-reactivity (fork A/B/C open) · m7 snapshot (3 gaps) · tatoeba (first 500) · language-learning-modes tier 2 · wafer-lifecycle P5 (runtime) · variant M3+ · toolcalling M3-blocked · longdistance audio (enum ✓, variant wire + live boot next) · elegance-debts (#6 DaemonDie left) · **hallucination/harness/cortex** LANDED (uncommitted) — the triad BUILT test-first on beef's skeleton: Cortex=Map repository (register/find/findOne share one cast+checked `where()` io; findOne owns derive via `dressAsObject` grammar-adapter, backward `executeTools` dep dead); `cortex.hallucination(config)`=the spawn seam → Hallucination(cortex, configuration)=closure factory (`configuration` applied as eager `configure()`, one validated path), Vector of /type/verb routes → `shape.object(vector, steer.echo)`, tool loop at the leaves (once), render/stream/execute lifted file-level (belt candidates soma.render/stream/execute); harness spawns via `daemon.cortex.hallucination({...config,tune,output})` + forwards via return (5-line terminals); canonical doc `docs/40-49_repository/47_integration/47.02_hallucination-triad.org` + tangle-runnable `systems/runtime/tests/hallucination-triad.doc.test.js`; light v-schematics guard cortex+config (schematics/primitives/hallucination barrel); **tune axes pinned** intelligence·reasoning·speed·thrift (each [0,1], capability trades economy; `axes` canonical in the schematic, `.desc()` on Tune/Tier, tiers annotated). Suites green: typology 105 · runtime 26 · providers 2. Revert-or-rebuild fork CLOSED (rebuilt; staged `jj restore` dead). Open (non-blocking): promote soma.render/stream/execute into belt proper; 4th-axis half-set (providers ship 3, cortex pads thrift 0.5) documented not resolved. See compact [[the-cortex-hallucination-triad-built-test-first-on-beefs-skeleton]] · riddler turn-free (buffer.data.history IS the memory; /riddle/fromSymbol emitter; Riddler.svelte gutted) · mode.buffer→mode.app.buffer LANDED. **CORTEX STRIPWIRE LANDED** (uncommitted): cortex now callable client↔server via `shape.cortex.strip` ⟷ `shape.cortex.wire` (shape generalized to DISPATCH-STRUCTURE compilers: Vector=path-trie, Cortex=nearest-tune); provider `Request` = the wire contract; client runs the REAL Hallucination over stub faculties, only `via.render(request)` proxies to daemon `/cortex/{render,stream}`; `connection.stream({method,body})` + `inline` SSE passthrough; `nearest`→`belt/recipe.js`. Green: typology 1119 · cortex.stripwire 10 · cortex.integration 23. Docs stale-pending (42.01/47.03/codemap). See compact [[the-cortex-stripwire-lands-as-shape-cortex-strip-wire]] + memory [[project_shape_cortex_stripwire]]. · **VECTOR SINGULAR-EFFECT ROTATION LANDED** (uncommitted, quest vector-affect): the Vector's `effects:Map<Pattern,Effect>` collapsed to ONE `effect` per node (a leaf IS a trajectory node with `.effect`; `open`=`branch(sig).effect=fn`, direct-overwrite; the effects/trajectories duality + `feed` heir-pick + `object` reunification all dissolved; `walk.js:37` both-node descent activated from dead). Steer regrouped → 4 namespaces `steer.{match,trie,dispatch,strategy}` = 4 files (tree→trie), arity-fire atom `dispatch(effect,ctx)`→`fire(e,c)`; boundary audit = nothing→belt (generics already in belt.middleware+fromm). **LAW: metadata lives on the EDGE (the `Map<pattern,vector>` key/pattern), NOT the vector** — beef killed `vector.pattern` ("the vector doesnt know how its arrived at, it might be arrived at in different ways"); a `stamp` blacklist + `Pattern.enrich` also rejected; the lost case (deep-branch-bare→shallow-open-schema) never occurs → YAGNI, shipped clean. **LAW: aggregation is an EFFECT concern** — shotgun's dup-path fan was a Map-key-by-object artifact the singular effect kills; multi-handler = the ONE effect is a `fanout` multiplexer; a dedicated Twitch Vector w/ fanout DEFERRED (fixed datamap.scoped with two twitches). Gotcha (not the rotation): EMITTER trait is two-phase — test scenarios `tests/scenarios/{mode,daemon}.js` discarded its commit closure → empty mode.emit/404; fixed. Green: typology 118/0 · runtime 29/2 (2 non-Vector: empty-mode edge + registry manifest path). Downstream ghost/kajuit blocked at load by pre-existing missing modules (mask/module/mode refactor). See compact [[the-vector-effects-map-collapses-to-one-singular-effect-per-node-steer-regroups-into-four-families-and-metadata-stays-on-the-edge]] + memory [[project_vector_singular_effect]]. · **NODE-CENTRIC WIRE + "NO PATH IS A PATH" LANDED** (uncommitted): the singular-effect rotation's tail-consequences closed. **LAW: no path is a path** — the root/empty/`/` signal (null-nature) addresses a node's OWN effect; a DUAL: `Vector.branch("/") ≡ affect()` (construction) + `traverse` root-arrival (dispatch); `scope`/`greedy` stay pure edge-matchers (root-arrival is the WALKER's job). Fixes `Aperture.get("/")` (was 404) + point-vector `new Vector().use().affect()` dispatch. **LAW: shape.strip is the CATA of the Vector** — wire contract rotated `{leaves,branches}`→`{effect?,branches}` (mirrors `{effect,trajectories}`; `leaves` DISSOLVED as a dead-effects-Map vestige); `connection.wire`+`messenger` became the transport-dual of `object` (callable-AND-namespace both sides); root strips AND wires symmetric (`side()` local≡remote). A `.own` boolean flag was the SMELL beef's nose caught (asymmetric — only messenger read it; speculative — zero consumers); ripped, node-centric chosen over the root-as-leaf shortcut (breaks `side()` ergonomics). Also LANDED: **`stagger`** (`daemon/traits/index.js` staggered two-phase trait applicator — prod+scenarios+test route through it, kills the forget-phase-2/EMITTER-commit-discard bug category); **`Signature.tilde→root`** (trie-family accessor unified). Blast: strip/connection/messenger core + tests + 26 committed runtime snapshots (deterministic leaves→node-centric transform, no daemon) + 4 kajuit consumers (`emitter.leaves`→`emitter.branches[nature].effect`) + docs 47.03/42.01 + world-file codemap + live quests. Green: typology 126/0 · runtime 30/3 (3 pre-existing non-mine: 2 env :2501 404s + 1 sibling registry). Completeness-claim disproved once (grep filtered `leaves:`, kajuit suite unrun) → all current-facing refs migrated. See compact [[no-path-is-a-path-the-root-effect-becomes-addressable-the-own-scaffold-is-ripped-and-shape-strip-rotates-node-centric-to-the-cata-of-the-vector]] + memory [[project_vector_node_centric_wire]]. · **APERTURE .CALL STRIPWIRE LANDED + LIVE-VALIDATED** (quest aperture-calling, COMPLETE): `.call` = the compiled aperture BOTH sides — daemon `mode.call = shape.proxy(mode.aperture)` (EXPOSED) ⟷ client `shape.connection.wire` off `/metadata/aperture` (new kajuit EXPOSED trait + dossier `daemon.call`); squatter EVICTED (4 binds dead, 33 sites → `entity.connection.call`); `shape.proxy` rebuilt = lazy Signal accumulator over scope/feed/invoke (Pattern leakage dissolved). LAWS: buffer-boundary (dot-nav = MODE surface only; kajuit compiles, never consumes; consumers = registry views + tests) · local ask `method:"*"` (sole-on-wildcard, throw-on-ambiguous, HTTP strict 405) · streaming = `yields` slot + await-then-iterate + break=cancel · `output`≠`object` (effect return vs hallucination; EMITTER fold pools `is.yieldish`/`is.buffers`, rest rides `result.output`). Gate `tests/gestalten/shape/stripwire.test.js` (identical assertions local⟷remote). Live: kajuit boot healthy + in-browser oracle `mode.call.ask` round-trip (real answer); Oracle.svelte demos `mode.call.ask` beside `mode.emit.vision`. Dormant: riddler `ctx.mode.call.assistant.evaluate` todo unlocked · dashboard dead subscribes (beef's call) · fork-1 multi-method tiering wakes on first throw · education views → dot-nav future blast. See compact [[the-call-squatter-is-evicted-the-proxy-learns-lazy-navigation-and-call-becomes-the-compiled-aperture-on-both-sides]] + memory [[project_aperture_calling]]. · **MULTIPLEX LANDED + LIVE on :2501** (uncommitted): the browser 6-per-host cap solved by putting SIGNALS on ONE ws. `tunnel`→`Trunk`(class)→**`multiplex`** = a class-free base transport combinator in `shard.transmitter` (keeper = closed-over `pools`, one `Socket` per ORIGIN, `retry(multiplex({authority}))`); `Socket` is the ONE dispatcher both sides — PACKET=wire-json, FRAME=correlated exchange (`frames` Map), verbs `open/flow/feed/shut/call/push`, `#answer` consumes iterable outputs INSIDE the carry (ALS/`/turn/close` fix structural). Naming (beef every term): circuit→FRAME · `Queue.buffer→backlog` · `pump→feed` · `Trunk/Plex` class DELETED. **THE CODEC MOVE**: every `F extends Signature` (Signal/Path/Url/Action/Pattern; Span excluded) is a `Type.Codec(String).Decode(new F).Encode(serialize)` (ana/cata) in `schematics/prototypes/signatures.js`, via `v.prototypes.Signal()`; `v` gained `decode/encode`; the packet IS a schema `v.primitives.connection.Packet` checked at ingress (ENVELOPE only, cargo `v.unknown()`, `token` OPAQUE `v.string()` — typing it `JWTToken` was the silent-hang bug). `new Signal(...)` GONE — the codec's Decode owns it; multiplex passes `request.url` (a `Url`) straight in ("request is our prototype"); `Url` drops query→routing rides `Signal`+separate `packet.query`. Failed-connection `console.warn` at the 4 `Socket` drop sites (beef: ignore spans, just log). MOUNT at RUNTIME ROOT (`integrate.js`, per-origin) — CONTRADICTS the prior compact's per-daemon; **reconcile #1**. Live: `ws://:2501/multiplex` `Open`→`/daemon/playground/entities/mode/find`→`spawner` ✓. Green: typology socket 4·multiplex 23·schematics 11·conversation · runtime multiplex 10/10. See compact [[the-connoisseur-dissolves-the-trunk-class-socket-grows-frames-multiplex-is-a-class-free-shard-combinator-and-the-signature-family-becomes-a-codec]] + memory [[project_multiplex_transport]]. · **M18 — CONVERSATION PROTOTYPE DISSOLVES** (DESIGN, quest `m18_conversation_dissolves`): oracle "awaiting handshake" debugged to CLIENT-locus (server+authorize+token proven good via raw-WS; the dead 2nd WebSocket carried token as query, authorize only reads header). Rejected nest→session-primitive→socket-recursion (all beef-nixed), then reframe — "what do we need conversation for anymore on the client??" — **nothing**: a dialogue turn = a streaming request over the multiplex connection (`connection.observe("/conversation/dialogue",{method:POST,body})`→`soma.pour`), history=`turn.subscribe`, render=harness→hallucination. DELETE `Conversation` + the dedicated WS + `Connection.socket()` branch + the `{session}` socket-recursion stab (REVERT). Server mount → `async function*` streaming route. harness/hallucination/soma/entities untouched. Diff pending go. Forks: speech-out channel demux, continuous-mic duplex frame. Memory [[project_conversation_handshake_client_locus]]. · **HARNESS DIALOGUE STREAMS + PERSISTS OVER THE WIRE + OWNER ROUTING LANDED** (uncommitted): the M18 realization BUILT — `mode.harness.dialogue.stream()` streams client-side (yields-marked leaves + `/metadata/harness` strips mounted) and persists turns via `datamap.inject` + `shard.carry` (a lazy response body re-enters the ORM `RequestContext` per pull; `// @beef hacky deep wire`). The live bug: assistant turn STREAMED then VANISHED — the user-scoped broadcast read its owner from a SEPARATE un-carried ambient ALS (`ambient.current()`) while the user already rode the ORM context `carry` re-enters. A sprawling intrinsic-owner detour (denormalize `user` onto Turn/Buffer + stamp every write site + migrate) REVERTED under "no userless threads!" + "too sprawlling", collapsing to **3 lines**: `reactive` reads `repo.getEntityManager().getFilterParams("user")?.user` (the SAME param `bind` writes to scope find; carried for free), null→THROW (silent-drop was the retard — loud tripwire now). `shard.ambient.store` DELETED from resolution.js (dormant shard stays). Also fixed as fallout: stripwire aperture-name collision (`entity-*` find-vantage snapshots ≠ mode wire apertures), M18 orphan test deleted, 3 stale M11 oracle imports. Green: typology 127/0 · kajuit 17/0 · runtime 34/0. Live: `DAEMON BUFFER SUBSCRIPTION op:create` reaching client. Open: dialogue-turn broadcast in-app look; aprende→riddler `emit.riddle.cast` mismatch (own quest). See compact [[the-broadcast-owner-was-read-from-an-uncarried-ambient-while-the-user-already-rode-the-orm-context-carry-re-enters-so-the-intrinsic-column-sprawl-reverts-to-a-three-line-getfilterparams-read]] + memory [[project_reactive_owner_orm_context]].

**DESIGN gated on go**: **generative-views** (=quests/m19_generative_views.quest.org=, supersedes patch.org — View absorbs App+Bundle [bundle.js dies], buffer.view = db POINTER {kind,hash,mount,bundles[]} product of mode.gen.view, bundle sources `(name)=>{text,type,integrity}|null` fold at shard.serve.bundles per mode, GENERATIVE trait [ex-PROCEDURAL] + HARNESSED /tool injector hoist [aprende copy deletes], per-daemon store under mask mount, reader exemplar; codemod app.buffer→app.view ~35 + new App→new View ~30; tangle + surgical edits + rm app/bundle.js all beef-per-op; see compact [[generative-views-the-view-absorbs-app-and-bundle-the-row-carries-a-pointer-bundle-sources-fold-at-one-route-and-coined-terminology-dies-for-technical-names]] + memory [[project_generative_views_design]]) · m13 module/mode/kernel semantic alignment (seed — note: the module/kernel rename itself already landed per M15, this is the remaining semantic-alignment work) · m14 testing fixtures (@fixtures package + lock-demo, seed) · m5 widget-glyphs · m9 playground-layout · m10 monkey (still reserved, distinct from chaosmonkey) · twitch-vector (seed — the vector-affect backhand: a dedicated Twitch Vector like Aperture with `fanout` aggregation built in, so multi-subscriber-per-path is first-class without the container holding lists; build when a real N-handler-per-path consumer lands) · closed-class · exhibit-absorbs-shadow · herald · decorum M2-M5 (+ dapper-second-theme behind it) · siphon · viva-init/install · ghost-autocompleting · identity-collapse (proposal) · documentation M2-M6.

**DONE recent**: **DAEMON ISOLATION LANDS + APRENDE 3 LAUNCHERS** (uncommitted) — the two-standalone-daemon FK (prior compact's unresolved spiral) SOLVED by a per-daemon MINT at the registry boundary: `subsystems/paladin/prototypes/pensieve.js` `revelio → own(module)` — clone MUTABLE descriptors / SHARE immutable leaves (functions, classes, typebox `~kind` Proxy schemas), `WeakMap` cycle-guard relinks `Vector.ancestor`; **supersedes the repoint-LAW** (mode.module is per-daemon now, zero runtime edits). Spanish daemon BOOTS (also fixed stale `ontology→topology`/`corpus→topography` variant kernel). beef "i hate a. but ok. build it into the registry." Aprende hub = THREE symbol-steered launchers **Deck/Nyan/Riddle** in `Actions.svelte` over `LiteralRepository.constrain` (slug-array→`$and` junction, AND-per-symbol, verified 792/765/0; **ontology COLUMN empty → ontology IS a symbol** word/sentence/conjugation); nyan RENAMED off `/activation` + mode(GAMEPLAYS)/layout props (beef "too simple. doesnt match semantics. naming bad. thin on propr"); riddler pool fixed slug-based (`{symbols:{slug:{$in}}}`). Callout logged: **diagnostic-theater**. OPEN: hardcoded language-label across game views ([[project_language_label_hardcoded]], un-go'd) · riddler `emitter.js:108` slug-write watch · dynamic symbol chips. See compact [[daemon-isolation-lands-as-a-registry-mint-own-at-pensieve-revelio-then-the-aprende-hub-grows-three-symbol-steered-launchers-deck-nyan-riddle-over-a-repo-owned-and-junction-filter]] + memory [[project_aprende_launcher_symbol_steering]] + [[project_daemon_standalone_isolation]] + [[project_language_label_hardcoded]]. · **@young-ladys-primer BORN** (uncommitted, thin slice) — 2nd domain beside @education: the Diamond-Age Primer over Marble's `os-taxonomy` children's-curriculum DAG. **LAW: re-points the education kernel, no new machinery** — literal=concept · symbol=curriculum axis (subject·strand·age) · concept `trait.REQUIRES.concepts`=the prereq DAG (mirrors ANNOTATED/CONJUGATED→`uses`) · Memory=mastery · harness=storyteller. Slice = First words→reading (15 concepts). `topologies/curriculum/`(16 symbols) + `topographies/first-words-reading/`(15 concepts) + `modes/home/{atlas,primer}`. **atlas = dataspace re-pointed** — drapes `Canvas`+`stage.graph` (ECharts force-graph, `echarts@6` already vendored; drapes missing ~nothing), strand-coloured, tap→ancestry "N prerequisites traced to start". **primer = frontier fold + one `/assistant/turn` cortex scene loop + storybook buffer** (riddler turn-free `buffer.data.history` + aprende `harness.object.render`). Proven headless: DAG acyclic · frontier fold (8 layers to master 15) · atlas `buildOption` · 6/6 files PASS adversarial verify (78 API calls). NOT runnable — needs mount in a HARNESSED daemon + a primer domain (own Literal/Memory or reuse education's) + LLM key; `testament/variant` registration = out of the package scope (beef's). Scale = copy-paste to full os-taxonomy 1590. Types `topology`/`topography` per the landed rename ([[project_module_type_taxonomy]]). See compact [[the-young-ladys-illustrated-primer-becomes-a-new-domain-the-education-kernel-re-points-at-a-childrens-curriculum-dag-an-atlas-graph-re-skins-dataspace-and-a-frontier-driven-narrative-tutor]] + memory [[project_young_ladys_primer]]. · **documentation docker image** (uncommitted) — the docs site ships as `vivalence/documentation:alpine` (serves `:2020`), a deno-only 3-stage build, built + smoke-verified green end-to-end (root/subpage/pagefind 200, 404→404). **LAW: Astro's Rolldown can't load under deno on alpine** — Astro 7 → its own vite8 → Rolldown (Rust-only, NO JS fallback); deno won't link the `linux-arm64-musl` binding and the glibc one dies on `libc.so: cannot open shared object file`; `gcompat` breaks deno itself (`__res_init` reloc); a `package.json` (node/npm) is the antipattern beef nixed (re-declares every client dep `import_map` carries). So the bundle MUST run on glibc. Stages: *capture* `FROM vivalence/viva:alpine` (real deno workspace, ZERO config — `@vivalence/typology` resolves only via the workspace, `import_map` has no `@vivalence/*`) → snapshots · *build* `FROM denoland/deno:debian` (glibc; one-line `{nodeModulesDir,importMap}` deno.jsonc — LOAD-BEARING, the `--import-map` FLAG doesn't reach astro's client-dep resolution so codemirror 404s) → `astro build`+`pagefind` · *serve* `nginx:alpine` `:2020`. kajuit dodges the whole wall only by pinning `npm:vite@6`→Rollup 4 (musl-OK under deno); vite7+/rolldown would hit the same wall. Wired: root `deno.jsonc` `documentation/{bundle,build,tag,push}`+aggregates · multiplayer compose `documentation` service `:2020` · CI build/tag/push lines. Process scar — FIVE hacks (NAPI env · debian-split · bake-docs-into-base · gcompat · node/package.json) reached beef unverified before a seconds-fast repro harness measured the cause on rung two (*"why is this required?!"* → *"why this hack?"* → *"WHYY??? ugly"*) → [[feedback_reproduce_before_proposing_infra_fixes]]. See compact [[the-documentation-docker-image-lands-as-a-three-stage-deno-build-because-astro-rolldown-cant-load-its-musl-binding-under-deno-on-alpine-so-capture-runs-on-viva-alpine-and-the-bundle-on-debian-glibc]] + memory [[project_documentation_site]]. · **module-type rename** (uncommitted) — kernel `type` `corpus`→`topography` (terrain, ex-corpus) · `ontology`→`topology` (structure, ex-ontology) · `domain` unchanged; the ENTITY-layer `ontology` (`literal.ontology` field, `ONTOLOGY[]` maps, `ONTOLOGICAL`/`TOPOGRAPHICAL` Symbol traits) UNTOUCHED per beef's "the trait for topological data remains ontology". Purely nominal — `Manifest.type` is `v.string()` (no enum), no runtime branches on the string (only `type==="domain"`); `identifier=owner/type/slug` so every DERIVED specifier tracked (`@education/ontology|corpus/*`→`topology|topography` in variant kernel lists, else `accio` 404). 8 manifests — grep missed the gitignored FRAUGHT `english-to-brazilian:vocalized` (`rg` respects .gitignore, `populate.js` walks disk → sweep `rg -uu`/pensieve dump) — + `tests/corpus/`→`tests/topography/` + codemap. Verified: 5 new identifiers accio-resolve, old 404, pensieve TOTAL 44 (leaf-invariant; the `registry.count` 45 is a PRE-EXISTING stale BASELINE from uncommitted playground churn, NOT this rename — flagged for re-baseline). ghost `@vivalence/ontology|corpus/*` left dead. See compact [[corpus-becomes-topography-and-ontology-becomes-topology-as-module-types-terrain-versus-structure-while-the-entity-ontology-layer-stays-put]] + memory [[project_module_type_taxonomy]]. · **documentation site BORN** (uncommitted) — `documentation/` = a static Astro **7.0.9** docs site running on **Deno** (G1: the full native stack — vite8/Rolldown/Sätteri-Rust/lightningcss/sharp — boots under `deno run npm:astro`). **LAW: never `@deno/vite-plugin` with Astro** (its deno loader rejects Astro's `virtual:` modules; SvelteKit dodges, Astro doesn't) → the only reason to bundle typology client-side is gone → **typology never bundles into the browser**; Solid dropped for the same subpath-resolution class ("not structural"). **LAW: npm deps live ONLY in root `import_map.json`** (member `deno.jsonc "imports"` IGNORED when root sets `importMap`; the RETARD was authoring a `package.json` in a Deno repo — logged `family: npm-reflex-in-deno-repo`). Structure: standard Astro `src/` + top-level `content/docs/*.mdx` (a content collection; suites INLINE at `content/docs/suites/`); glob-loader `generateId` override preserves the jdex dot (`42.01` not `4201`, else 404). **Keystone — build-capture-inline snapshot regime**: `capture.js` runs each `*.example.js` at build, auto-detects channel by `run()` return (stdout→`<Example>` / typology `Span.records`→`<Trace>` depth-tree), writes via **`specimen.snapshot`** (the corpus tool; mkdir base first, `dry`=preview). **Tangle-1:1**: `<Source>` shows the exact file via `?raw`, `<Example>` shows the capture of running it — one source of truth, `47.03_stripwire.mdx` the exemplar (Vector→`shape.strip`→wire JSON→3 assertions, green build IS the passing test). Hand-built great-docs theme (NO Starlight): sidebar/Pagefind-search-on-deno/TOC/breadcrumb+title/prev-next/mobile/copy/filename-bar/line-highlight/wikilinks+backlinks/dark-light-toggle/anchors/last-updated/admonitions/tabs/CM6+vim-editor(display-only). 6 docs imported org→mdx (3 parallel agents; MDX-safety: backtick raw `<`/`{`). Inline-test reach: typology FULL (86 exports) + runtime barrel (5); scenarios DON'T import (registry paths). Run-inline (socket-to-repo VM · pre-bundle worker) both SHELVED — static build-capture shipped. Green: build 9 pages · capture via specimen.snapshot · `deno task test` · Pagefind indexes. See compact [[the-documentation-site-is-born-astro-7-runs-on-deno-past-the-vite-plugin-virtual-module-wall-a-hand-built-great-docs-theme-lands-and-a-build-capture-inline-snapshot-regime-renders-tangle-1-to-1-examples]] + memory [[project_documentation_site]]. · **docs content migrated + suites inline** (uncommitted) — the flat demo REPLACED by the real `docs/` JDex tree NESTED into `content/docs/40-49_repository/…` + `50-59_practice/…` (11 docs org→mdx via 4 parallel agents on ONE shared faithful contract; 4 new: 46.12/13/14, 52.02; QA = line-count sanity + leaked-entity scan). **LAW: nested-on-disk, FLAT basename-slug routes** (`pattern:"**/*.mdx"` + `generateId` strips the dir, keeps the dot) so wikilinks + jdex-frontmatter breadcrumbs/sidebar stay untouched by nesting; `Layout` mtime → `entry.filePath`. **Suites + snapshots INLINE beside the doc they prove** (`47_integration/47.03-connection.example.js` + `47.03-connection.snapshot.json` + `47.03-stripwire.{suite,doc.test}.js`) — central `suites/` dissolved; `content/docs/capture.js` WALKS `**/*.example.js` writing each snapshot beside its example; components glob the tree + key by BASENAME (location-independent, placement is free). **Gitignore via naming**: snapshots renamed `*.snapshot.json` ride the EXISTING root `*.snapshot.json` rule (the specimen doctrine, any depth); scoped `documentation/.gitignore` adds `dist`/`.astro`/`content/.tangled`. `47.03` rebuilt = real stripwire prose + live `<Source>/<Example>/<Trace>` + tangle. Green: build 13 pages · capture 2 channels · `deno task test` 2/2 · wikilinks resolve. OPEN: milestone doc CONTENT (README/M2-M6) still unwritten; trimmed `47.03` admonition + line-highlight demos (beef's call to relayer). See compact [[the-flat-demo-is-replaced-by-the-real-docs-migrated-into-the-nested-jdex-tree-and-suites-and-snapshots-inline-beside-the-doc-they-prove]] + memory [[project_documentation_site]]. · **dock per-terminal** LANDED (uncommitted) — the kajuit "start chatting"/"hide chat" toggle (`panels/f/f.svelte`) flipped a flag the dock PANE (`panels/a/a.svelte`) ignored: the button gated on HARNESSED, the pane on CONVERSATIONAL-engagement (a `thread.$traits` marker only dead `.bak` auto-engage widgets set) — two authorities for one toggle. **LAW: one authority = HARNESSED** — F/A/shoulder/panel-B all gate on `mode.implements("HARNESSED")`; CONVERSATIONAL-gates-dock dead (fits M18 conversation-dissolves). Geometry `{side,share,collapsed}` relocated singleton `bridge.$dock` → per-terminal `terminal.$dock` (beside `$buffer`), so N terminals carry N docks; PLAIN DATA not an entity ref → rides `terminal.toJSON().dock`, hydrates direct, stays OUT of the settle tri-state, dies with the terminal (orphan-GC category impossible; a `bridge.$docks` map-by-id rejected). Mutators = free fns over the atom in `stores/bridge/dock.js` (`state is data`). Green: dock.geometry 12 + terminal.stall 3; `deno task bundle` EXIT 0 ✓1748. See compact [[the-start-chatting-button-flipped-a-flag-the-dock-pane-ignored-the-button-gated-on-harnessed-the-pane-on-conversational-and-the-cure-is-one-authority-plus-a-per-terminal-dock]] + memory [[project_dock_per_terminal]] + quest [[conversation-dock-recast]]. · **corpus snapshot regime** LANDED (uncommitted) — the dataspace-dashboard entity-viewer intent's stage 2: `systems/runtime/tests/corpus/` snapshots the learning corpus (10 literals·8 symbols across ontology) from THREE byte-identical vantages (mode `daemon.entities.<x>.find.toJSON` ≡ aperture `/entities/<x>/find` wire ≡ client `RemoteRepository.find` cast) → ONE canonical `entity-{literal,symbol}.snapshot.json` cross-asserted (equality = daemon↔wire↔client round-trip proof). Harness opens the REAL brazilian DB READ-ONLY (no refresh/flush, WAL-safe beside live `:2501`); lean at the QUERY (`options.fields` drops redundant `symbols[]`). `deno task {runtime,typology}/test/snapshots` (root delegates; `SNAPSHOT_HOT=1`, default dry; gitignored). **Snapshot suite REINED 22263→2773 lines** (measured, not the assumed 3000): killed `oracle-ask` 13843→38 (dropped recursive entry `data`), aperture trees → `routes()` path-summary (homepage-aprende 1729→92, teacher-dewey 1328→59), vantage triplication, dead `*-brazilian` cruft (die.js generator commented). Residual = legit references; the hard-1500 last-mile trades diagnostic value (beef's call). Dashboard stages 1 (interfaces: generic `Dataspace` reader, server-`where`-only search) + 3 (Airtable dataviewer over the JSONs) still open. See compact [[the-dataspace-dashboard-gains-an-entity-viewer-intent…]] + memory [[project_corpus_snapshot_regime]]. · **viva-init getting-started** LANDED (uncommitted) — README gains a runnable create-a-mode hallucination demo + a deno-install-down getting-started, made REAL by four ghost trajectories: `/ledger/install [path]` (scaffold + `registry.seed`), `/variant/clone <@id|path> [target]` (accio|paladin.source → cloneDir), `/variant/lighthouse/auth/{signup,login}` (POST the variant's lighthouse). `localhost` registry variant PROMOTED from the working `testament/variant` (education+playground, self-contained `.env.example`, `secrets.jsonc` killed = fork A). **LAW: package-owner is AUTHORED, never derived** — `vip.mount` dropped the `@${dirname}` fallback, throws `declares no owner` (same class as missing slug); `@viva`/`@education`/`@playground` authored on each `package.viva.js`; module-owner still overrides. **LAW: auth is a variant-scoped lighthouse CONSUMER** — the variant declares `export const lighthouse = {statics:{remote}}`, `paladin/variant.js` parses it to `variant.lighthouse`, the ghost trajectory reads `variant.lighthouse.statics.remote` → `new Connection(remote).call("/auth/signup")` (beef: "paladin variant parses the lighthouse consumer. the ghost trajectory reads from that and uses its connection. done"). SIGNUP + LOGIN **VERIFIED LIVE** (SUCCESS + JWTs against the running lighthouse). `kajuit/run` task added. Green: paladin vip+pensieve 2/9; runtime package-mount ok (3 orthogonal EMITTER/reactive failures unrelated to owner). OPEN: docker fresh-env run BLOCKED (daemon down) — the untested fresh-clone env-ordering (`VIVA_LEDGER_MOUNT` unset at `mod.js` `ledger.mount()`); space-form `viva variant lighthouse auth signup` needs ShellSignal multi-segment. See compact [[the-getting-started-becomes-real…]] + memory [[project_viva_init_getting_started]] + quest [[viva-init-install.quest]]. · **testing deep-clean** (ALL EIGHT suite groups GREEN: typology 126/1186 · runtime 32/195 · kajuit 18/88 · ghost 26 · paladin 11 · dapper 9 · sheets 16 · registry-offline 18; bak graveyards purged repo-wide incl. `registry/…/oracle.bak` which the registry.count sentinel caught as a 46th ingested package; dead-contract tests purged both sides of the dead CONVERSATIONAL runtime trait; kajuit/ghost suites migrated to current paths+contracts; paladin/ghost test tasks gained `--env-file`; **LAW pinned: tests comply with system — never grow system surface to green a test** (identity updateOne arc, fully reverted); live-service tests stay offline-red by nature: lighthouse :1729, nlp :5555) · aprende harness tool quartet (query/activation/drill/riddle; query = entity-query abstraction seed; bare-{results} cortex return-split rule; quality pass on query/drill OUTSTANDING — review agents stopped mid-flight; live round-trip unverified) · m11 packages LANDED (11.4 docker/docs remainder wants re-plan — fork 10 killed its written plan) · m15 cake→module rename LANDED · m12 MILESTONE 12.0 mechanism BUILT+LIVE-VERIFIED (chaosmonkey/oracle: harness=fixed lexicon not semantic, /ask=EXPOSED aperture, cortex.js .object interface, playground STANDALONE gate bugfix) · cortex · nyan engine+emitters · aprende board · riddler · flatten-corpora · survival-conjugation+paradigm-cells · spanish M1 · voice impl (verify pending) · redact S6 · sheets lib.

## simmering strands (from beef's logs — recurs, unshipped; expect these to surface)

1. **docs/README** — HIS #1 recurring item (*"i need a readme"*, *"maaybe a video?! readme.mp4"*); jdex tree re-pasted 3×, never materialized. docs/ has 8 files. Highest-leverage unblock.
2. **launch WBS** — install base → contributions → fundraising; A1-A7/C1-C6/R1-R10; *"Text + games only — no audio yet."*
3. **ad-hoc svelte buffer renderer** — LLM emits Svelte → rendered buffer (*"would be INSANE"*); needs context population + tools to read existing modes.
4. **kajuit architecture unease** — *"still .... ish. bit convoluted"* — typology-rotation + client-layout quests carry it.
5. **span logging / piping** — REBUILT+LANDED: the `log` connoisseur redesign happened wholesale — Span=cursor (root `journal[]`+`channel`), flat `record {span,trace,path,verb,at,data?}`, `trace.chronicle` fold (+`dictate` mirror, `live`, `hold`/`decant` policies), `tracks.js` DEAD; consumers migrated (shard.track, shape.http server-timing, ghost, oracle triple with `/ask trace: records`, kajuit `$telemetry`+`$span`, g/h = one debugger: h tree drills, g details). Suites green: typology 717 steps · runtime tracked+stripwire. Open tail (beef-deferred): paladin scribe sinks · relay/streamed server logs · runtime daemon root-span wiring (`scribe.root` hypothesis) · Svelte panels not live-verified. See compact [[the-span-interface-dies-and-the-record-chronicle-algebra-lands-across-four-waves]] + memory [[project_span_reactive_spine]].
6. **docker/prod verify** — recurring unchecked `a.2 docker`; shadow-emit suspicion (*"faulty circuitry"*).
7. **lighthouse security flag** — *"at some point we need to close the lighthouse. soon."*
8. **detached spawn** — known-issue #1; blocks `viva instance start`.
9. **entities-TS 72 errors** — blocks npm/jsr publish of typology.
10. **rename musings** — LANDED: module types `corpus`→`topography`, `ontology`→`topology` (semantic pairing pinned; DONE-recent + [[project_module_type_taxonomy]]); the entity-layer `ontology` deliberately kept.

## standing beef-orders in force

percentage fader sticky · code-heavy answers permanent · caveman active · VCS read-only forever · manifest immutable · quests for plans · no unsolicited expansion.
