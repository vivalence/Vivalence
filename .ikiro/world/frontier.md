# frontier — the live edge
<!-- writer: agent · derived-from: quests/ + code + private logs · eviction ledger: known-issues.org "frontier evictions" (whole items, loss-free) · limit: 8000 chars -->

Derived — re-derivable from quests + code + logs. Update at every disintegrate. Corpus verdict: **architecture coherent; the debt is live-validation, not design.**

## the four gates

1. **M4.0 baseline** — beef's click-walk of the playground rig; gates M4→M5→M9→M10.
2. **one live daemon run** — MOSTLY CLEARED (italian end-to-end, prod boots 3). m26 partial: francesca turn with thread-pinned tune owed. IPv6 `[::1]:2501`; 401 = mounted. Never-run-live: G2/G3/G4 · nyan playroom · longdistance audio.
3. **beef go / fork-pick** — closed-class · aprende↔nyan refill · m11 11.4 re-plan.
4. **M3 anthropic SSE bug** — blocks toolcalling scale, cross-mode emit, audio verify.

## live quests

**HOT** — **m43 stucatch** (PIPELINE LIVE — @stucatch package/domain/mode mounted as its own instance; tower ruled literals-only (stage·sensor·signal·fix·track, NO symbols), domain owns entities+fold+twitch, mode BOOTED ingests padded stairs capture, APPLICATION map has playback+photo backdrop; ontology per compact 72 SUPERSEDES the quest's symbol tower; open: placement write-back, reactive literal subscription, hub STILL unflashed across two captures, Egor's tilt gate eats horizontal 0.2–0.66g) → [[m43-stucatch]] · **m29 armory** (gate 2 live; m38-wake round UNDEPLOYED, live-verify owed) → [[m29-agent-tools]] + [[project_armed_tool_stack]] · **m27 mind** (4 forks) → [[m27-memory-and-notes]] · **m28 primed** (file in `discarded/`) → [[m28-primed]] · **m25 user-and-thread** (beef)
**APPLIED, I.5 live-verify owed** — **m37 converse** (suites green; mode-voice fork open, rec B; file in `discarded/`) → [[m37-converse]] · **m33 dictation** → [[m33-dictation]]
**LANDED, sunset, uncommitted** — **m52 schematic pinhole** (M1·M2·M3 + 7 recipes + README; settle = dormant → cast → faults → decode → inherit; ONE gate `check.instance().throw()` at Ledger.boot + runtime + kajuit; ghost `instance/run` mounts before boot (walk); suites at baseline; OWED beef: commit · education push · live restart) → [[m52-schematic-pinhole]]
**LANDED, questless** — **lighthouse hoist** (OWED beef: commit; body EVICTED whole → known-issues "frontier evictions") → [[project_lighthouse_hoist]]
**T0 + T1 + s1/s2 LANDED · T1.1 + V3 DESIGNED** — **m53 vcompany** (vdex LIVE on his JDex, restarted — screens App bundled; T1.1 = the vdex harness + tooling pass, 12 rows on his 7 comments (slug verbs ONCE in the aperture, tools their application, `open` an EMITTER, per-path `sync`, prune inline — the domain's `nativeDelete` prune was silent to the twitch); V3 pass = 11 files compile-green in sibling file `m53-vcompany.v3-vdex.org`, LATER list marked, waits on go + T1.1 p1; `* T1.#2` his call; body EVICTED whole → known-issues "frontier evictions") → [[m53-vcompany]] · compacts `the-office-comes-before-the-executive…` … `the-status-speaks-in-quest-ids…`
**DESIGNED + TIED UP, nothing landed** — **m54 buffer FILE** (RULED: FILE = the system asset trait on literal AND buffer · LABELED in M1; QUESTIONED: the verbs' home — `/buffer/file/*` aperture + `daemon.call` is inside out, reopen mikro-first as `BufferRepository` verbs → contract → shard → `RemoteRepository` → kajuit; `v.asset` pinned; plurals = relations + `fs_*`/`entity_find`; `mode.reinstall` RETRACTED → twitch `/after/freight/receive`; M1 stands without the verbs; RECONCILE with m53 `* T1.1` + `m53-vcompany.v3-vdex` first — siblings put the vdex verbs in the aperture (`open` = EMITTER, file watch + per-path sync) and split the App into shell + 7 parts; three quests on one seam, all waiting on go, beef arbitrates) → [[m54-buffer-file]] · compact `the-save-answers-the-wrong-shape…`
**M1 LANDED, uncommitted · M2 gated** — **m55 recursive terminals** (`Thread.parent`/`children` in the Turn shape + `Buffer.thread` cascade + typology relations, suites unchanged, migration mints at next boot; LAW: thread is daemon, terminal is client, terminal = f(thread) — NO terminal entity on the runtime; M2 = kajuit terminal `up · down · status · stop · run · watch` + tree + kajuit-local schematic + `$view` on the terminal; struck: `buffer.parent` · shard `verbs` · `TerminalEntity`; forks his: point vs flat props · schematic home · `mode.js:55` auto-merge; OWED beef: `go m2` · commit · restart) → [[m55-recursive-terminals]] · compact `the-terminal-is-a-function-of-the-thread…`
**DESIGNED, gated · DEEP PASS held** — **m50 the three-way weave** (`Wafer` GONE, `die` kept, dossiers; two schematics; signal table + SIGKILL standard; absorbs m49 M2/M3; pass: `detached`+piped MEASURED, header `3/5` = report mis-parse (reword gated), 4 sketch hunks gated with M1 → compact `the-deep-pass-on-m50…`) → [[m50-the-three-way-weave]]

**DESIGNED, prerequisite CLEARED, every op beef's** — **m42 history-purge** (filter-repo 5 dead paths; owed before the scratch clone: commit the dirty tree · push · zip · filter-repo install; FORKS 6–9 beef's — three remotes, stale push-* refs, the sibling working-copy snapshot; body EVICTED whole → known-issues "frontier evictions") → [[m42-history-purge]] · compact 62
**WALKED ×5, proposals gated** — **m46 linux-readmen** (ubuntu + alpine + docker-live + tty + m52 walks: README 0–6 holds, 24 defects · 17 proposals, L1 L2 L4 APPLIED, TASKBAGS B1–B13 · C1–C12, connoisseur drain gated; `readmen-m52` container LEFT RUNNING 127.0.0.1:2501/1794; body EVICTED whole → known-issues "frontier evictions") → [[m46-linux-readmen]] · compacts 98 · 102

**ACTIVE** — wafer-lifecycle P5 · pincer 18+ · stage-canvas-devtools · language-learning-modes tier 2 · variant M3+ · kajuit_client-layout · terminal-first-client P1 · **kajuit-boot-display** (LIVE, 2 bugs) → [[project_kajuit_boot_display]]
**LANDED, LIVE-DRIVEN** — **dojo** m34 · m35 · **m38 bag** UNDEPLOYED · italian reseed owed → [[project_dojo_mode]] · compacts 45–47, 50–51

## design held only here (no quest file — restate before building)

closed-class · exhibit-absorbs-shadow · herald · decorum M2-M5 (+dapper-second-theme) · siphon · viva-init/install · ghost-autocompleting · identity-collapse · documentation M2-M6 · m5 widget-glyphs · m9 playground-layout · m10 monkey · m13 module/mode/kernel alignment · m14 testing fixtures · m17 client-ctx grammar · twitch-vector (`fanout`)

## simmering strands (beef's logs — recurs, unshipped)

1. **docs/README** — #1 recurring; the whole strand (clean-box walk · ubuntu walk · trajectory+slowstart · docs-read+hello-world registry-first) EVICTED whole to `known-issues.org` "frontier evictions". OPEN heads only: gate "session expired" · doctor dump · `paladin.scope overwrite` print · 50.01:22 old prereq claim · `## Instance` stub → [[project_documentation_site]]
2. **launch WBS** — install → contributions → fundraising; text+games, no audio.
3. **ad-hoc svelte buffer renderer** — LLM emits Svelte → rendered buffer.
4. **kajuit architecture unease** — *"still .... ish. bit convoluted"*.
5. **docker/prod verify** — shadow-emit suspicion.
6. **lighthouse security flag** — close soon · MARKED (known-issues): `identity/find` leaks argon2 hashes to any bearer · `GET /multiplex` 500.
7. **detached spawn** — blocks `viva instance start`.
8. **entities-TS 72 errors** — blocks npm/jsr publish.

## standing beef-orders

All in the kernel (`.ikiro/ikiro.md`) — fader sticky · code-heavy · caveman · VCS read-only · manifest immutable · quests for plans · no unsolicited expansion · `.ikiro/` autonomous.
