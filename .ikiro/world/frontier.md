# frontier — the live edge
<!-- writer: agent · derived-from: quests/ + code + private logs · eviction ledger: known-issues.org "frontier evictions" (whole items, loss-free) · limit: 8000 chars -->

Derived — re-derivable from quests + code + logs. Update at every disintegrate. Corpus verdict: **architecture coherent; the debt is live-validation, not design.**

## the four gates

1. **M4.0 baseline** — beef's click-walk of the playground rig; gates M4→M5→M9→M10.
2. **one live daemon run** — MOSTLY CLEARED (italian end-to-end, prod boots 3). m26 partial: francesca turn with thread-pinned tune owed. IPv6 `[::1]:2501`; 401 = mounted. Never-run-live: G2/G3/G4 · nyan playroom · longdistance audio.
3. **beef go / fork-pick** — closed-class · aprende↔nyan refill · m11 11.4 re-plan.
4. **M3 anthropic SSE bug** — blocks toolcalling scale, cross-mode emit, audio verify.

## live quests

**HOT** — **m43 stucatch** (PIPELINE LIVE — @stucatch package/domain/mode mounted as its own instance; tower ruled literals-only (stage·sensor·signal·fix·track, NO symbols), domain owns entities+fold+twitch, mode BOOTED ingests padded stairs capture, APPLICATION map has playback+photo backdrop; ontology per compact 72 SUPERSEDES the quest's symbol tower; open: placement write-back, reactive literal subscription, hub STILL unflashed across two captures, Egor's tilt gate eats horizontal 0.2–0.66g) → [[m43-stucatch]] · **m29 armory** (gate 2 live; m38-wake round UNDEPLOYED, live-verify owed) → [[m29-agent-tools]] + [[project_armed_tool_stack]] · **m27 mind** (4 forks) → [[m27-memory-and-notes]] · **m28 primed** (file in `discarded/`) → [[m28-primed]] · **m25 user-and-thread** (beef)
**APPLIED, I.5 live-verify owed** — **m37 converse** (suites green; mode-voice fork open, rec B; file in `discarded/`) → [[m37-converse]] · **m33 dictation** → [[m33-dictation]]
**LANDED, questless** — **lighthouse hoist** (ONE `export const lighthouse {module, statics.remote}`, daemon inherits at the pinhole, `clients.*.statics.lighthouse` DELETED — never read; repo · shelf · `@education` recipes; paladin 37/150; ghost `localhost.viva.js` → `systems/ghost/bak/`; release lines seeded; OWED beef: commit · education push · M5 prod · m51:261-292 old shape) → [[project_lighthouse_hoist]] · **pinhole secrets · user rebind · render fault · `output.object`** (`hydrate` walks fired values, DEFERRED gone; `bind("user")` behind `authorize()`; OWED beef: restart, commit 8) → [[project_hydrate_pinhole_secrets]] · [[project_user_bind_behind_authorize]] · **create --init + locate** (`instance/create --init` pins the new mount at `flag` and chains init, independent of `--use`; `instance/target.js locate()` = ONE lens fold for `use · delete · doctor <t> <f>` — `delete hello` resolves; `doctor <one>` stays exact-or-filter, lens keys match mounts) → compact `the-create-verb-learns-init…`
**DESIGNED, gated** — **m50 the three-way weave** (what a vector instantiates · the control vector · the process contract; the wafer verdict found in beef's logs — `Wafer` GONE, `die` kept, dossier pattern; 14-noun settlement, `Signature`/`Signal` collisions named; two typology schematics sketched; signal table + SIGKILL standard; absorbs m49 M2/M3) → [[m50-the-three-way-weave]]

**DESIGNED, prerequisite CLEARED, every op beef's** — **m42 history-purge** (filter-repo 5 dead paths; re-measured: 269.8 MB / 8615 mp3 still in history, HEAD purge-paths 0, 410 `refs/jj/keep` pin 706 hidden snapshots; education + m39/m40/m41 + variant→instance already on `origin/master`; owed before the scratch clone: commit the dirty tree (16 paths at survey, sibling-widened by commons + m45–m49 since) · push · zip · filter-repo install; FORKS 6–9 beef's: THREE remotes (origin gitea · github · presentation disjoint) → publish per remote · 40 stale `push-*` on origin, prune before or carry · `github/master` = sibling working-copy snapshot origin never got · M3 carry-over list measured) → [[m42-history-purge]] · compact 62
**WALKED ×4, proposals gated** — **m46 linux-readmen** (ubuntu + alpine walked README 0–6 on `b27e0e62e`, both reach hello-world UP; 24 defects · 17 proposals, L1 L2 L4 APPLIED; TASKBAGS B1–B13 · C1–C12; CONNOISSEUR PASS six-step drain, gated; `instances.resolve` LANDED uncommitted. **beef's macOS walk**: singular `instance/use`/`remove` reach, wizard asks optional first (`check.js:123`, gated), F5 live. **LANDED: function defaults** (`envfile.fallback`; JWT mints itself; `strata.wet` "two shells" RED pre-existing). **docker-live walk**: stock ubuntu:24.04 + the LOCAL tree (GitHub `b27e0e6` has NO `commons/` — README ahead of the push) → hello-world LIVE on host `127.0.0.1:2501/1794`, `bot`/`agent` clicked in Chrome; the docker recipe = 4 `.env` lines (SERVE origins on `0.0.0.0`, PUBLIC remotes on localhost); **L30 first-attach race LANDED** (go): `secure.authenticate` (identity, root) / `authorize` (user) split, `/userspace/handshake` = the ONE enrolling upsert, kajuit handshakes before its burst; `enroll.test.js` 40 parallel → 1 row, `beta`'s first visit healthy; containers all KILLED on beef's order; **unset-is-commented LANDED** (`paladin.state.line`, scaffold writes `# KEY=""`, wizard fills in place — the ledger's Anthropic key no longer shadowed by an instance blank; beef: "commented always. no copy default from global .env")) → [[m46-linux-readmen]] · earlier: `instance/delete` LANDED (B-bag), `Instances.resolve` (C1/C2); beef's `~/.zshrc` exports `VIVA_PROCESS_ID=$$` + repo-root `.env`; C6 open · **walk containers are a TRAP** — a `readmen*` on `0.0.0.0` shadows the local instance for Firefox (`[::1]` vs v4) → known-issues `readmen-container-shadows-the-local-instance`; `run.js` decodes `128+n` into `signal`; hello-world `App.svelte` light drapes; language-learning dev gate SKETCHED, no go

**ACTIVE** — wafer-lifecycle P5 · pincer 18+ · stage-canvas-devtools · language-learning-modes tier 2 · variant M3+ · kajuit_client-layout · terminal-first-client P1 · **kajuit-boot-display** (LIVE, 2 bugs) → [[project_kajuit_boot_display]]
**LANDED, LIVE-DRIVEN** — **dojo** m34 · m35 · **m38 bag** UNDEPLOYED · italian reseed owed → [[project_dojo_mode]] · compacts 45–47, 50–51

## design held only here (no quest file — restate before building)

closed-class · exhibit-absorbs-shadow · herald · decorum M2-M5 (+dapper-second-theme) · siphon · viva-init/install · ghost-autocompleting · identity-collapse · documentation M2-M6 · m5 widget-glyphs · m9 playground-layout · m10 monkey · m13 module/mode/kernel alignment · m14 testing fixtures · m17 client-ctx grammar · twitch-vector (`fanout`)

## simmering strands (beef's logs — recurs, unshipped)

1. **docs/README** — #1 recurring; the whole strand (clean-box walk · ubuntu walk · trajectory+slowstart · docs-read+hello-world registry-first) EVICTED whole to `known-issues.org` "frontier evictions". OPEN heads only: gate "session expired" · doctor dump · `paladin.scope overwrite` print · 50.01:22 old prereq claim · `## Instance` stub → [[project_documentation_site]]
2. **launch WBS** — install → contributions → fundraising; text+games, no audio.
3. **ad-hoc svelte buffer renderer** — LLM emits Svelte → rendered buffer.
4. **kajuit architecture unease** — *"still .... ish. bit convoluted"*.
5. **docker/prod verify** — shadow-emit suspicion.
6. **lighthouse security flag** — close soon.
7. **detached spawn** — blocks `viva instance start`.
8. **entities-TS 72 errors** — blocks npm/jsr publish.

## standing beef-orders

All in the kernel (`.ikiro/ikiro.md`) — fader sticky · code-heavy · caveman · VCS read-only · manifest immutable · quests for plans · no unsolicited expansion · `.ikiro/` autonomous.
