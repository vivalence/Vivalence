# frontier — the live edge
<!-- writer: agent · derived-from: quests/ + code + private logs · eviction ledger: known-issues.org "frontier evictions" (whole items, loss-free) · limit: 8000 chars -->

Derived — re-derivable from quests + code + logs. Update at every disintegrate. Last full corpus read: **architecture coherent and contradiction-free; the debt is live-validation, not design.**

## the four gates (nearly everything open funnels through these)

1. **M4.0 baseline** — beef's manual click-walk of the playground rig. Gates the M4→M5→M9→M10 stall/widget/monkey chain. beef-driven.
2. **one live daemon run** — MOSTLY CLEARED. **italian** has a full end-to-end live run; prod now boots 3 daemons (brazilian · spanish · italian, seen in the m30 walk dump). **m26 PARTIALLY validated live** — watcher booted post-emigration; a full francesca turn with a thread-pinned tune still owed. **IPv6 only** — `http://[::1]:2501`; `/daemon/italian/metadata/modes` → 401 means mounted+routing. Never-run-live: G2/G3/G4, nyan playroom, longdistance audio.
3. **beef go / fork-pick** — closed-class (spec locked) · aprende↔nyan refill (3 forks) · m11 remainder 11.4 (docker/docs; plan killed by fork 10, wants a re-plan).
4. **M3 anthropic SSE stream bug** — blocks toolcalling scale (cache_control), cross-mode emit, audio verify.

## live quests

**HOT** — **m29 armory FIRST PASS LANDED** → [[m29-agent-tools]] (entity trio + card 5.9× · paladin fs/shell hands · domain progress/lookup/queue/review resurrect a dead-since-birth layer, review through `daemon.call` (the compiled domain aperture) · rep-o-gram preset/conjugations/generate · TOOLED services transport tools `/service/<slug>/<tool>`, nlp_classify · armed binds daemon/mode/user/thread ITSELF; **gate 2 VALIDATED LIVE** — dock streams, francesca drills, detached `appraise` hook wires reviews INTO the sealed turn as a sync-shaped tool round (translate splits both providers; judgment rules = output-schema `.desc()`s: thinking-first · withhold · composite fault-attribution · negative honesty, LIVE-UNVERIFIED; compact 48); backlog per-item) · **m27 mind** (note entity DESIGNED, 4 forks open; recall LATER) → [[m27-memory-and-notes]] · **m28 primed** (REFRAMED under m27 as seeder+claim; beef rules) → [[m28-primed]] · **m25 user-and-thread** (PRELIMINARY — beef picks up)
**APPLIED — stage I live-verify owed (I.5)** — **m37 converse** (voice in · voice out · two-sided; capability stages, not rollout; m33 patch superseded by =m37-converse/voice-in.patch= — 44 files after passes III+IV (IV: 3 failure-path fixes — process-killing unhandled rejection in the deepgram pump, silent deepgram fault → now a 500 on ONE stream, stale-session mic clobber → generation token), APPLIED 44/44 file-by-file + re-verified in place — typology 117 · runtime 52 · kajuit 34 · deepgram 2 suites, 0 failed, vite build ✓; converse frame = `feeds`+`yields` on the pattern, input beside the stream, mirror compiles `feeds`⇒`connection.converse`; Verbatim/Audio are `event:"/…"` packets; assembly TOTAL; polish concurrent + segment-addressed at utterance grain; next = I.5 live checklist on francesca (beef); OPEN FORK, word owed: mode voice capability — Frame today hands modes exactly {buffer, terminal}, no mic; A raw box prop · B recorder capability + Dictaphone/ghost graduate to drapes · C chrome-only — rec B) → [[m37-converse]] · **m33 dictation** = stage I body (patch + passes I+II) → [[m33-dictation]]
**LANDED, uncommitted** — **m39 kernel references** (a daemon `kernel[]` entry stops being only a bare specifier: absolute path · relative-to-the-variant-file · inline module object; plus `svelte\`\`` → `{source}` so a mode's app needs no file — rides `bundler.bundle({kind:"svelte", source})`. Deep pass vs moved worktree, then landed via plain `patch -p1` (non-VCS, same bytes). Post-land: typology 110 · paladin 18+1 pre-existing env-fail · runtime 51 `--no-check` (TS pre-exists in interval `Literal.ts`) · probe green on worktree code · isolated boot :2599 ALIVE, italian+playground 401=mounted. Tests graduated: `paladin/tests/kernel.test.js` + App-source case in `typology/tests/view.test.js`. beef: commit + prod redeploy; fork A resolver-family deferred) → [[m39-kernel-references]]
**ACTIVE** — wafer-lifecycle P5 · pincer 18+ · stage-canvas-devtools · language-learning-modes tier 2 · variant M3+ · kajuit_client-layout · terminal-first-client P1 · **kajuit-boot-display** (LANDED+VERIFIED LIVE; 2 bugs open → [[project_kajuit_boot_display]])
**LANDED, LIVE-DRIVEN** — **dojo** m34 drawer · m35 paradigm · phone lists · dress-on-the-entry · MOBILE REGRESSION FIXED (*"feels good again"*) · **m38 bag**: quiet keyboard incl. `writingsuggestions` · hints recast (CONJUGATE facets+infinitive on hint; answer-side example else mask) · rail = beef's LITERAL `completed/set size/rep/set count` = `2/5/3/1`, legend on hover (took four tellings — callout 08-20b) · table cell nav `streak.focus` · whole-table review = BINOMIAL `{enum, ratio}` (the driver always spoke ratio, zero callers; enum = trace label) · `dojo_provision` takes set/pins/full axes + `/set` emitter route · francesca screen-exercise root cause = doctrine→unmounted impara, dojo fallback landed · effort `none` = no thinking (beef ruling, runtime law amended) — ALL UNDEPLOYED · italian reseed owed (annotations + contraction/preposition symbols) → [[project_dojo_mode]] · compacts 45–47, 50–51

## design held only here (no quest file — restate before building)

closed-class · exhibit-absorbs-shadow · herald · decorum M2-M5 (+dapper-second-theme behind it) · siphon · viva-init/install · ghost-autocompleting · identity-collapse (proposal) · documentation M2-M6 · m5 widget-glyphs · m9 playground-layout · m10 monkey (distinct from chaosmonkey) · m13 module/mode/kernel semantic alignment · m14 testing fixtures · m17 client-ctx grammar · twitch-vector (`fanout`; build on first N-handler consumer). · **entity-provision-as-trait + TRANSPORTED** (beef: "rn only domains can provide entities … make that a trait, available to any mode. degrade the domain; upgrade the traits" — collate's domain tier becomes a kernel sweep over trait-carrying modules (manifest check, pre-Mode); domain degrades to traits/kinds/aperture hooks. Sibling: TRANSPORTED marker on the entity descriptor mounts `shard.datamap.repository`+`reactive` generically, deletes the two hardcoded route lists (`aperture/datamap.js`, education `resolve()` hook), and is the lighthouse-closure lever — default off = entities stop leaking. m39 demos V–VI hold the walked context.)

## simmering strands (from beef's logs — recurs, unshipped)

1. **docs/README** — #1 recurring (*"i need a readme"*, *"maaybe a video?! readme.mp4"*). Docs site BORN (Astro 7 on Deno), CONTENT unwritten.
2. **launch WBS** — install base → contributions → fundraising. *"Text + games only — no audio yet."*
3. **ad-hoc svelte buffer renderer** — LLM emits Svelte → rendered buffer (*"would be INSANE"*).
4. **kajuit architecture unease** — *"still .... ish. bit convoluted"*.
5. **docker/prod verify** — recurring unchecked `a.2 docker`; shadow-emit suspicion (*"faulty circuitry"*).
6. **lighthouse security flag** — *"at some point we need to close the lighthouse. soon."*
7. **detached spawn** — known-issue #1; blocks `viva instance start`.
8. **entities-TS 72 errors** — blocks npm/jsr publish of typology.

## standing beef-orders in force

percentage fader sticky · code-heavy answers permanent · caveman active · VCS read-only forever · manifest immutable · quests for plans · no unsolicited expansion · **`.ikiro/` autonomous — no gate** (beef: *"all inside ikiro is yours"*; self-mod transparent + git-reversible).
