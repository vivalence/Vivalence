#+TITLE: ikiro/redact — corpus consolidation
#+METHOD: ikiro/redact
#+SELF-CLEANING: t
#+DATE: 2026-04-25
#+STATUS: STAGE-6-DONE (#4 already-fixed-by-Finn; #5 #6 #8 executed; #7 declined). Awaiting jj commit.

* Intent
Redact the .ikiro corpus. Old root claude.md (91KB sprawling system docs) → root IKIRO is *me* (self / traits / methods / active). Subsystem ikiros stay container-centric. Non-active workpackages → .ikiro/bak/. Self-cleaning per D14 — this file carries the record of THIS run only.

* Stages
1. setup [DONE]
2. checkpoint 1 → 4 IKIRO_DRAFT.md (root + typology + html + runtime) [DONE]
3. scout → IKIRO_DRAFT.md at paladin + registry tree [DONE]
4. collect → finalize drafts + bak manifest [DONE]
5. checkpoint 2 → full diff for sign-off [DONE — Finn approved #5 only]
6. apply → atomic rename / move / mkdir bak/ [IN PROGRESS — #5 renames executing; #4 #6 #7 #8 still pending]

* Directives — pending this run
- D7 workpackage disposal = bulk-move to .ikiro/bak/ (used at stage 6 #6)
- D14 workpackage is self-cleaning (acts on this file)

Crystallized to root IKIRO: D1, D2, D4, D6, D8–D11, D13.
Architectural pending crystallization: D3 wakeup as `/cmd` slash command; D5 orb mount-bearing trait emitting to /private/logs; D12 ikiro folders only at established subsystem level.

* Open Questions (Finn decisions)
- root filename: claude.md (Claude Code auto-load) vs IKIRO.md + symlink — *implied claude.md by approving #5 atomic renames as IKIRO_DRAFT.md → CLAUDE.md*
- bak/ flat vs grouped (bak/done, bak/superseded) — pending (gating #6)
- caveman skill install — user action only (`/plugin marketplace add JuliusBrussee/caveman`)

* Bak Manifest (gating #6)
8 root workpackages move to .ikiro/bak/ at stage 6 (D7 — bulk):
- client-typology (Phases 1-4 DONE; Phase 5 superseded by terminal-first-client)
- conjugation-ontology (DONE 2026-03-30)
- dialogue-verbatim-rename (explicit "DONE — retained for historical context")
- effect-saturation (DONE 2026-04-22; regression untriaged — see Delta #4)
- intent-as-template (DONE 2026-04-11)
- paladin-isolation (file Apr 6, no recent activity)
- pool-prototype (DONE 2026-04-02)
- react-bundler (no STATUS, design only, oldest content 2026-03-11)

12 keep active in root .ikiro/: redact, longdistance + .port.plan, cortex, pincer, decorum, language-learning-modes, terminal-first-client, wafer-lifecycle, stage-canvas-devtools, very-important-packagemanager, exhibit-absorbs-shadow, herald.

Subsystem .ikiro/ workpackages (6) — bulk decisions deferred to stage 6.

* Delta to DONE

** decisions Finn must make
1. root filename — `claude.md` (default, implied by #5 approval) vs `IKIRO.md` + symlink. ✓ resolved by #5.
2. `bak/` structure — flat vs grouped. Default-to-flat. ⏳ pending.
3. caveman skill install — user action only. ⏳ deferred.

** writes
4. **regression fix** at `subsystems/typology/tests/repository.persist.test.js:348` — `JSON.parse(storedrepo.encode([mode]))` is concat-typo. Recommended: replace with `JSON.parse(repo.encode([mode]))`. ⏳ pending Finn approval.
5. **9 atomic renames** — IKIRO_DRAFT.md → claude.md at root + 8 subsystems. ✓ DONE 2026-04-25. Old content recoverable from jj history. Compression by design — sections like Vocabulary/Conventions/Routines absorbed into root IKIRO `## traits`/`## methods`/`## active`; subsystem Where-Used/Work-Packages/Testing-Gaps trees folded into `## architecture`/`## context` split.
6. **mkdir .ikiro/bak/** + 8 mv operations. ⏳ pending Finn approval.
7. **promote 2 typology tests** — `tests/workpackage/{session, voice.session}.test.js` → flat structure. ⏳ pending Finn approval.
8. **subsystem workpackages** — bulk decisions deferred per D7. ⏳ pending Finn approval.

** stage 6 path
filename decided (claude.md via #5) → spot-check → execute 9 renames → [pause for #4 #6 #7 #8 approvals] → run typology / runtime / html / paladin tests (5 paladin pre-existing failures expected; repository.persist.test.js newly green after #4) → workpackage moves to bak/ (next redact picks up fresh copy per D14) → Finn commits via jj.

** risks
- subsystem CLAUDE.md may have orphan content not in IKIRO_DRAFT.md → spot-check before each rename
- regression fix may surface deeper round-trip issue → run full repository.persist suite when #4 lands
- cascading test failures from typology renames → search hardcoded paths before commit

** estimated
~20 atomic moves/renames (9 done at #5, 8 mv at #6, 2 promotions at #7) + ~5 min test runs + one Finn jj commit.

* Journey

- Stage 2 v1–v4: 4 drafts redrafted. Final reframe (v4) — root IKIRO = assistant *self*, not system docs. D1 promoted from D15.
- Workpackage refactor passes: surface trim, then directives split into procedural / architectural-pending / crystallized.
- ikiro/compact addendum (Finn verbatim): "ah. one important addendum imperative to the ikiro/compact method: ALLWAYS copy my text messages verbatim. include context but you can trim me quoting code for compression." Then correction (Finn verbatim): "Context may be paraphrased MUST. my messages MUST be contextualized." → claude.md ikiro/compact carries two MUSTs (verbatim user voice AND mandatory context).
- Stage 3 (Finn verbatim: "ok. exectute the workpackage. methodological analysis. step by step. populate drafts."): 5 IKIRO_DRAFT.md authored at paladin + registry tree per C4 × totem × divio (D11), each opening with central-pattern aphorism (paladin "circuitry → variant"; kernels "engine + symbols + instances"; services "manifest declares; provider creates"; modes "traits are the wiring contract"; registry "every entry exports manifest").
- Stage 4 sweep (Finn stacked verbatim: "do step 4." / "do another full sweep of the workpackages and the root ikiro / claude.md files" / "collect further" / "more" / "do another anlysis of the history of the repo. go through jj history (read only) and the workpackages" / "correlate them" / "track how changes echoed through the repo" / "hybothesise how workpackages might have been improved" / "exclude all the stuff you already processed" / "look for regressinos" / "go" / "2 hours"): ikiro/redact authored in claude.md `## Routines`; jj history correlated (last 2-week commit zlzwxtqu = 9-workpackage bundle); echo cascade documented; concrete regression found at `repository.persist.test.js:348`; 6 in-flight uncommitted changes catalogued; bak manifest of 8.
- Testing audit (Finn verbatim: "do another pass on testing. from the ground up. look through any of our workpackages that touched testing. whats there to be learned. is there feature overhang?"): 91 test files mapped, **inverted pyramid** (54 typology > 12 runtime > 10 html > 5 paladin = 5 services > 4 dapper > 1 kernel). dapper subsystem (4 tests, theming) discovered absent from IKIRO system map.
- Stage 5 prep (Finn verbatim: "5. from modes through domains to daemon to client. the story of traits! featuring: honorable mentions of alternative trait-like systmes ({input/output/nature/tree} signatures)" / "do." / "go." / "full sweep" / "then update the workpackage with your assumption about the delta of is to done."): 5 adjustments applied — dapper to root IKIRO; trait arc subsection (5 layers + 6 honorable mentions); tests/workpackage/ to root methods; workpackages master index rebuilt; typology IKIRO regression flag + tests/workpackage/ note; kernels IKIRO "biggest test blind spot" callout.
- ikiro/compact (Finn verbatim: "ikiro compact"): zettelkasten compressed.
- Self-clean (Finn verbatim: "continue self clean"): activity audit + hypotheses migrated to zettelkasten "## Method" + "## Testing" sections; "Adjustments needed for stage 5" removed (applied); Journey condensed while preserving Finn's verbatim text.
- Stage 6 #5 (Finn verbatim: "apply number 5 i guess"): root filename decided by implication (claude.md, lowercase per existing System Map convention). Spot-check surfaced expected compression (570→200 root, 418→77 typology, 420→97 html, etc.) — sections dropped are by-design (system docs → identity reframe). 9 atomic renames executed. All 9 claude.md files now carry IKIRO_DRAFT content; old content recoverable via jj.
- Stage 6 #4 #6 #7 #8 (Finn verbatim: "no. rest go", in response to my "which 2 tests?" clarification on #7): #7 declined. #4 already fixed by Finn's uncommitted edit at `repository.persist.test.js:348` — line reads `JSON.parse(repo.encode([mode]))`, no `stored` remnants. #6 mkdir `.ikiro/bak/` + 8 root mv executed (client-typology, conjugation-ontology, dialogue-verbatim-rename, effect-saturation, intent-as-template, paladin-isolation, pool-prototype, react-bundler). #8 subsystem disposal: session-first-routing (STATUS=COMPLETE) → systems/html/.ikiro/bak/; survival-tactic (superseded by tactic-analysis-routine) → registry/modes/@vivalence/.ikiro/bak/. client-layout kept active (STATUS=ACTIVE marker). 12 active workpackages remain in root .ikiro/.
