#+title: italian curriculum — the rank permutation is the pedagogy
#+filetags: :curriculum:topography:english-to-italian:sentences:words:ranked:survival:tatoeba:harvest:
#+status: PATCHED, NOT LANDED — ships as =italian-curriculum.patch= (verified =git apply --check= clean against master 3d140e4). Owed: the phrasebook audio run (M4, blocked in-session on network allowlist), one live rep through survival to feel the new 1–160.

* intent

beef, verbatim: /"right now the ordering is whack. rework the ranking. propose a more useful ordering. still keep it to be generally such that its highlighting grammear. also do a pass on vicabulary and align with that. ie vicab and sentences ranking is coherent. the prio is: a mix of simple dialogue of common phrases first. shot like my name is, where are you from etc. just the most common phrases. then its about sentences used in food, ordering, payment, etc. if a sentence diesnt have freight attached its on the chopping block and can be chnaged. i feel like ive done a hundred sentences and cant say shit."/

And the bar, verbatim: /"by sentence fifty i want to know shit. by 150 i want to be able to get through every day short interactions. rber were doing vicab and verbs on the side"/

One idea unifies the whole quest: *the rank permutation IS the pedagogy*. Sentence order, word order, and audio coverage are three views of one curriculum; a fact about any of them (rank, VOCALIZED, proficiency.survival) lives in the entity, never in file position — which is what lets the datasink quest canonicalize files without touching the curriculum.

* terrain (verified this session)

- =dataset/literals/sentences.js= — 1088 entities, =RANKED.rank= a dense 1..1088 permutation, sentence-side rank-only (no zipf). Array order is append order (rank 533 first) — meaningless.
- The OLD rank order was thematically blocked (1–26 greetings · 27–43 shopping · 44–64 restaurant · 65–88 transport · 89–100 hotel · 101–110 emergency) then ~1000 grammar drills theme-mixed. The whack: =ordinare=/=prenotare= conjugation drills ranked 1061–1087, ~1000 after the phrasebook forms using them at 46–51; payment split from food; old ranks 100–200 = pharmacy, lost passport, =Il tavolo è verde=.
- Word ranks (=RANKED.{rank,zipf,fpm}=) are corpus-frequency; rank numbers are shared per surface form across POS files (=lo.determiner= = =lo.pronoun= = 33) — 163 such dupes. zipf is the corpus truth; rank is the mutable curriculum axis, same split as sentences.
- Freight rule: VOCALIZED = frozen learning-side text (506 sentences, audio-first from the nm1 Tatoeba pool — content was CHOSEN FROM available audio, which is why Tom and =È carne di scimmia= exist and stay). English gloss is NOT freight — editable even on vocalized entities.
- =proficiency.survival= symbol: 113 entities pre-quest — the existing annotation for exactly the curated band this quest builds.

* the decision trail (locked)

1. *Three-tier rank scheme.* T0 social core → T1 food/ordering/payment → T1b everyday transactional (shopping/transport/directions/hotel/health) → T2 = everything else, sorted by =(-anchorZipf, anchorLemma, tokenCount)= where anchor = the rarest content word looked up in the word datasets (articles/preps/names free). T2 gives both wants for free: sentences unlock in vocab order (a sentence never outranks its own vocabulary — the /"vicab and sentences ranking is coherent"/ requirement) AND same-lemma drills cluster (the grammar highlighting). Tier membership = explicit slug lists in =rerank.js=; taste edits happen there.
2. *Additions with elegant redundancy* (45 new, no freight). Three structural patterns, not padding: adjacency pairs (=Grazie mille= → =Prego=/=Di niente=; =Buon appetito= → =Grazie, altrettanto=), minimal pairs on a frame (=Non capisco=/=Ho capito=/=Non ho capito=; =Scusi=/=Scusa=; =Sono di=/=Abito a=; =per due=→=per uno=), register ladder (=Certo che sì → Sì, volentieri → Può darsi → Purtroppo no → Un attimo=). The single highest-yield add: the =Per me, X= ordering frame.
3. *No single-word sentences.* beef: /"I don't like single word sentences. That's kind of not the idea."/ Every bare interjection recast as a natural multiword utterance doing the same job (=Buonasera.= → =Buonasera, come va?= · =Scusa!= → =Scusa il ritardo.= · =Permesso.= → =Posso passare?= · =Forse.= → =Può darsi.=); =Prego.= dropped (=Di niente.= holds the slot). Net upgrade: =Scusa il ritardo= and =Può darsi= are daily phrases the bare words never taught.
4. *Chopping block outcome:* almost nothing choppable — 31/32 Tom sentences are vocalized freight; sole free kill =tom-mi-ha-dato-una-penna=. Freighted junk sinks to wherever its vocab puts it instead.
5. *"After you" ×2* (beef: /"i have 'afyer you' twice and its confusing"/): =dopo-di-te= / =dopo-di-voi=, both vocalized → Italian frozen, glosses disambiguated to =(informal)= / =(plural)=. Gloss edits on freighted entities are lawful.
6. *Words: ingredients before contractions.* beef: /"before I touch a single of the concatenations, I have all of the ingredients from which the concatenations are constructed."/ The whole set = =contraction.js= (30, the full {di,a,da,in,su} × {il,lo,la,i,gli,le} grid — no con/per forms exist, no =l'= entry, so 11 ingredients cover all 30). Worst violation found: =del= rank 12 vs ingredient =su= rank 40. Fix = minimal permutation over the 41 occupied rank slots: 11 ingredients first (relative order kept), 30 contractions after (relative order kept) → ingredients max 23 < contractions min 27. zipf/fpm untouched (corpus facts). Homograph clitics (=lo.pronoun= et al.) deliberately NOT moved — different lexemes, not ingredients; the same-form-same-rank invariant is knowingly broken, rank is curriculum now.
7. *Audio: band the sources.* beef: /"i need to hear the sentences"/ + /"band the search beyond this singular source"/. Sentences resolve against ALL Tatoeba speakers (api_v0 exact match =query==text=, not the nm1 pool) with a license gate — CC0/CC BY only, NC and unlicensed rejected — because off-nm1 audio has mixed licenses; attribution ledger mandatory per Tatoeba audio terms. 143 targets = the unvocalized in ranks 1–160.
8. *The milestones landed as a generator, not an edit.* =rerank.js= (deno, pure) is the source of truth for the permutation; the dataset diff is its output. Reruns are free when tier lists or vocab change.

* the shape — what the patch carries

- every existing entity: =RANKED.rank= rewritten to the new permutation (1..1133 dense, asserted)
- 45 new sentence entities, full schema (=slug · traits [TRANSLATED,RANKED] · trait · symbols [sentence, proficiency.survival]=)
- 9 pulled-forward drills gain =proficiency.survival= (they now sit in the curated band); post-patch 160/168 survival entities in ranks 1–160, the 8 strays are old mis-tags (see deferred)
- =dopo-di-te= / =dopo-di-voi= gloss fix
- words: 13 rank changes across =adposition.js= / =determiner.js= / =contraction.js=
- =.harvest/harvest-targets.json= (143) + =.harvest/harvest-phrasebook.js= (resumable: state file, license gate, loudnorm =I=-18:TP=-2:LRA=11= 44.1k/128k, attribution ledger, injects VOCALIZED only for landed mp3s)

Milestone → landmark ranks after patch: 1–57 social loop closed (greet any hour · self-intro · survive not understanding · exit) · 58–105 the full restaurant arc incl. =Per me, X= and payment · 106–160 shopping/transport/directions/hotel/health. Grammar starts at 161 with vocab-aligned ordering.

* tangle

The dataset files are generator OUTPUT — never tangled (26k lines; a partial reproduction silently drops rows). Truth for the landed state = =italian-curriculum.patch=; truth for the NEXT rerank = the generator below. Tier lists T0/T1/T1b are the editable surface — taste edits happen there, then rerun from =.harvest/=:
=deno run --allow-read --allow-write rerank.js= → =sentences.reranked.js= + =rank-report.txt= (inspect 1–160 before adopting).

#+BEGIN_SRC js :tangle ../../registry/education/topographies/english-to-italian/.harvest/rerank.js :mkdirp yes
import base from "../dataset/literals/sentences.js"
const sentences = base
import verb from "../dataset/literals/words/verb.js"
import noun from "../dataset/literals/words/noun.js"
import adjective from "../dataset/literals/words/adjective.js"
import adverb from "../dataset/literals/words/adverb.js"
import pronoun from "../dataset/literals/words/pronoun.js"

const words = [verb, noun, adjective, adverb, pronoun].flat()

const lex = words.reduce((m, w) => {
  const form = w.trait.TRANSLATED.learning.toLowerCase()
  const zipf = w.trait.RANKED?.zipf ?? 0
  const lemma = w.slug.split(".")[0]
  const prev = m.get(form)
  return (!prev || zipf > prev.zipf) ? m.set(form, { zipf, lemma }) : m
}, new Map())

const FREE = new Set(`il lo la i gli le l un uno una di a da in con su per tra fra
al allo alla ai agli alle del dello della dei degli delle dal dallo dalla nel nello nella nei nelle
sul sullo sulla e o ma che se perche perché come dov com c ce po piu più
grazie prego ciao arrivederci si sì no signore signora
uno due tre quattro cinque sei sette otto nove dieci venti trenta cento mille
tom marco roma italia berlino euro wifi taxi hotel bar menu`.split(/\s+/))

const toks = (t) =>
  t.toLowerCase().replace(/[''’]/g, " ").match(/[a-zàèéìíòóùú]+/g) ?? []

const anchor = (s) =>
  toks(s.trait.TRANSLATED.learning)
    .filter((t) => !FREE.has(t))
    .map((t) => lex.get(t) ?? { zipf: 2.5, lemma: t })
    .reduce((worst, w) => (w.zipf < worst.zipf ? w : worst), { zipf: 99, lemma: "_" })

const T0 = `buongiorno-come-sta
ciao-come-stai
buonasera-come-va
buonanotte-a-domani
piacere-di-conoscerla
mi-chiamo-marco
come-si-chiama
di-dov-e
sono-di-berlino
abito-a-berlino
sono-qui-in-vacanza
sono-qui-per-lavoro
sto-bene-grazie
tutto-bene-e-tu
non-c-e-male
e-tu
e-lei
per-favore
grazie-mille
di-niente
no-grazie
si-per-favore
mi-scusi
scusa-il-ritardo
posso-passare
mi-dispiace
va-bene
sono-d-accordo
certo-che-si
si-volentieri
puo-darsi
purtroppo-no
un-attimo-per-favore
non-lo-so
non-capisco
ho-capito
non-ho-capito
non-parlo-italiano
parlo-solo-un-po-di-italiano
sto-imparando-l-italiano
parla-inglese
parli-inglese
puo-ripetere-per-favore
puo-parlare-piu-lentamente
piu-lentamente-per-favore
puo-scriverlo
come-si-dice-questo
che-cosa-significa
cosa-significa-questa-parola
puo-aiutarmi
puo-aiutarmi-per-favore
arrivederci-a-presto
ci-vediamo
a-domani-2
a-dopo
buona-giornata`.split(/\s+/)

const T1 = `avete-un-tavolo-libero
un-tavolo-per-due-per-favore
un-tavolo-per-uno
e-libero-questo-posto
ho-una-prenotazione
senta-scusi
il-menu-per-favore
posso-avere-il-menu
cosa-consiglia
acqua-naturale-o-frizzante
vorrei-ordinare
ordiniamo
per-me-un-caffe
per-me-la-pasta
anche-per-me
posso-avere-un-caffe
posso-avere-dell-acqua
vorrei-un-caffe-caldo
un-altro-caffe-per-favore
vorrei-un-panino-con-formaggio
vorrei-del-vino-rosso
buon-appetito
grazie-altrettanto
ho-fame
ho-sete
sono-allergico-al-latte
da-portare-via-per-favore
basta-cosi-grazie
il-conto-per-favore
il-conto-e-di-venti-euro
quanto-costa
quanto-viene
posso-pagare-con-la-carta
posso-pagare-in-contanti
va-bene-cosi
dov-e-il-bancomat
vorrei-prenotare-un-tavolo-per-due
prenoto-un-tavolo-per-stasera
prenotiamo-un-tavolo-al-ristorante-per-stasera
ordino-la-pizza-con-formaggio
ordiniamo-due-caffe
ordiniamo-un-insalata-e-del-pane
preferisco-il-caffe
preferisco-il-vino
preferisci-il-caffe-o-il-vino
e-ora-di-cena
a-che-ora-e-la-colazione
la-colazione-e-inclusa`.split(/\s+/)

const T1b = `vorrei-questo
prendo-questo
lo-prendo
sto-solo-guardando-grazie
quanto-costa-la-camicia
avete-questo-in-un-altra-taglia
nient-altro-grazie
c-e-uno-sconto-oggi
a-che-ora-apre
a-che-ora-chiude
a-che-ora-2
e-aperto-di-domenica
che-ore-sono
dov-e-la-stazione
dov-e-la-biglietteria
vorrei-un-biglietto-per-roma
un-biglietto-di-andata-e-ritorno
dove-posso-comprare-i-biglietti
a-che-ora-parte-il-treno
da-quale-binario-parte
il-treno-e-in-ritardo
la-stazione-e-qui-vicino
dove-va-questo-autobus
dove-devo-scendere
la-prossima-fermata-per-favore
puo-chiamarmi-un-taxi
quanto-tempo-ci-vuole
e-lontano
dov-e-la-porta-di-imbarco
a-destra
a-sinistra
sempre-dritto
giri-a-destra-al-semaforo
dov-e-l-uscita
dov-e-il-supermercato-piu-vicino
vorrei-una-mappa-della-citta
dov-e-il-bagno
una-camera-per-due-notti
la-camera-e-al-terzo-piano
l-albergo-e-in-centro
c-e-il-wifi
posso-avere-un-cuscino
posso-lasciare-qui-il-bagaglio
dov-e-la-chiave
non-funziona
mi-sono-chiuso-fuori
sto-male
mi-serve-un-medico
c-e-una-farmacia-qui-vicino
devo-andare-in-farmacia
mi-sono-perso
ho-perso-il-passaporto
chiami-la-polizia
chiami-un-ambulanza
puo-farci-una-foto`.split(/\s+/)

const tier = (s) => {
  const i0 = T0.indexOf(s.slug)
  if (i0 >= 0) return [0, i0]
  const i1 = T1.indexOf(s.slug)
  if (i1 >= 0) return [1, i1]
  const i1b = T1b.indexOf(s.slug)
  if (i1b >= 0) return [1.5, i1b]
  const a = anchor(s)
  return [2, -a.zipf, a.lemma, toks(s.trait.TRANSLATED.learning).length]
}

const cmp = (a, b) => {
  const [ta, tb] = [tier(a), tier(b)]
  for (let i = 0; i < Math.max(ta.length, tb.length); i++) {
    const [x, y] = [ta[i], tb[i]]
    if (x === y) continue
    if (x === undefined) return -1
    if (y === undefined) return 1
    return x < y ? -1 : 1
  }
  return 0
}

const reranked = sentences.toSorted(cmp).map((s, i) => ({
  ...s,
  trait: { ...s.trait, RANKED: { rank: i + 1 } },
}))

const byOriginal = new Map(sentences.map((s, i) => [s.slug, i]))
const output = sentences.map((s) => {
  const r = reranked.findIndex((x) => x.slug === s.slug) + 1
  return { ...s, trait: { ...s.trait, RANKED: { rank: r } } }
})

const chopCandidates = reranked
  .filter((s) => !s.traits.includes("VOCALIZED"))
  .filter((s) => /\btom\b|scimmia/i.test(s.trait.TRANSLATED.learning))
  .map((s) => s.slug)

await Deno.writeTextFile(
  "sentences.reranked.js",
  "export default " + JSON.stringify(output, null, 2) + "\n",
)
await Deno.writeTextFile(
  "rank-report.txt",
  [
    ...reranked.slice(0, 160).map((s) =>
      `${String(s.trait.RANKED.rank).padStart(4)} ${s.trait.TRANSLATED.learning}  —  ${s.trait.TRANSLATED.known}`
    ),
    "",
    "CHOP CANDIDATES (no freight, junk content):",
    ...chopCandidates,
  ].join("\n"),
)
console.log("done", output.length, "chop:", chopCandidates.length)
#+END_SRC

Note: the 45 additions are already merged into =sentences.js= by the patch, so the generator reads them as base corpus; the session-side =additions.js= builder is retired.

The phrasebook harvester (M4) — resumable, license-gated, attribution-ledgered:

#+BEGIN_SRC js :tangle ../../registry/education/topographies/english-to-italian/.harvest/harvest-phrasebook.js :mkdirp yes
const TOPOGRAPHY = new URL("..", import.meta.url).pathname.replace(/\/$/, "")
const HARVEST = `${TOPOGRAPHY}/.harvest`
const FREIGHT = `${TOPOGRAPHY}/freight/audio/sentences`
const STATE = `${HARVEST}/harvest-phrasebook-state.json`
const OK_LICENSES = /^(CC0|CC BY(?!-NC))/

const sleep = (ms) => new Promise((r) => setTimeout(r, ms))

const readJson = async (path, fallback) =>
  await Deno.readTextFile(path).then(JSON.parse).catch(() => fallback)

const api = async (text) => {
  const url = `https://tatoeba.org/eng/api_v0/search?from=ita&has_audio=yes&query==${encodeURIComponent(text)}`
  const res = await fetch(url, { headers: { "User-Agent": "vivalence-harvester/0.2" } })
  if (!res.ok) throw new Error(`${res.status}`)
  return res.json()
}

const pick = (results, text) =>
  results
    .filter((r) => r.text.trim() === text.trim())
    .flatMap((r) =>
      r.audios
        .filter((a) => !a.license || OK_LICENSES.test(a.license))
        .map((a) => ({ sid: r.id, audio: a.id, author: a.author, license: a.license ?? "Tatoeba terms" }))
    )[0]

const download = async (audioId, dest) => {
  const res = await fetch(`https://tatoeba.org/audio/download/${audioId}`, {
    headers: { "User-Agent": "vivalence-harvester/0.2" },
  })
  if (!res.ok) throw new Error(`${res.status}`)
  const raw = `${dest}.raw`
  await Deno.writeFile(raw, new Uint8Array(await res.arrayBuffer()))
  const norm = await new Deno.Command("ffmpeg", {
    args: ["-y", "-i", raw, "-af", "loudnorm=I=-18:TP=-2:LRA=11", "-ar", "44100", "-b:a", "128k", dest],
  }).output()
  await Deno.remove(raw)
  const size = await Deno.stat(dest).then((s) => s.size).catch(() => 0)
  if (!norm.success || size < 4000) {
    await Deno.remove(dest).catch(() => {})
    return false
  }
  return true
}

const inject = async (state) => {
  const fpath = `${TOPOGRAPHY}/dataset/literals/sentences.js`
  const entities = JSON.parse((await Deno.readTextFile(fpath)).replace("export default ", ""))
  const patched = entities.map((e) => {
    const hit = state.done[e.slug]
    if (!hit || e.traits.includes("VOCALIZED")) return e
    return {
      ...e,
      traits: [...e.traits, "VOCALIZED"],
      trait: { ...e.trait, VOCALIZED: { asset: { path: `sentences/${e.slug}.mp3` } } },
    }
  })
  await Deno.writeTextFile(fpath, "export default " + JSON.stringify(patched, null, 2) + "\n")
}

const targets = await readJson(`${HARVEST}/harvest-targets.json`, [])
const state = await readJson(STATE, { done: {}, misses: [], failed: [] })
await Deno.mkdir(FREIGHT, { recursive: true })

const pending = targets.filter((t) =>
  !state.done[t.slug] && !state.misses.includes(t.slug) && !state.failed.includes(t.slug)
)
console.log(`targets ${targets.length}, pending ${pending.length}`)

for (const [i, t] of pending.entries()) {
  try {
    const found = pick((await api(t.text)).results ?? [], t.text)
    if (!found) {
      state.misses.push(t.slug)
    } else if (await download(found.audio, `${FREIGHT}/${t.slug}.mp3`)) {
      state.done[t.slug] = found
    } else {
      state.failed.push(t.slug)
    }
  } catch {
    state.failed.push(t.slug)
  }
  await Deno.writeTextFile(STATE, JSON.stringify(state, null, 1))
  await sleep(2000)
  if ((i + 1) % 20 === 0) {
    console.log(`[${i + 1}/${pending.length}] hits ${Object.keys(state.done).length} misses ${state.misses.length} failed ${state.failed.length}`)
  }
}

await inject(state)
const attribution = Object.entries(state.done).map(([slug, d]) =>
  `${slug}: tatoeba.org sentence ${d.sid}, audio by ${d.author} (${d.license})`
)
await Deno.writeTextFile(`${HARVEST}/phrasebook-attribution.txt`, attribution.join("\n") + "\n")
console.log(`done: ${Object.keys(state.done).length} hits, ${state.misses.length} no-audio, ${state.failed.length} failed`)
#+END_SRC

Word reorder = 13 rank values; the diff IS the clearest form — see the patch hunks on =words/{adposition,determiner,contraction}.js=. Invariant to re-assert after any future word edit: =max(rank of {di,a,da,in,su,il,lo,la,i,gli,le}) < min(rank over contraction.js)=.

* testing assessment

In place: NOTHING guards the curriculum — no suite reads =RANKED.rank= ordering, no dataset-shape test in the topography. COVERAGE GAP: rank density (1..N, no gaps/dupes), the tier invariants, the words invariant, byte-identical JSON roundtrip of the dataset format.

Changes: none (no existing test touches these files).

Adds (owed — the session verified all four as inline asserts in the generator run, not as suite tests):
- rank density assert (existed in generator: =sorted(ranks)==range(1,1134)=)
- words ingredient invariant (asserted at generation)
- =Datasink.canonical= roundtrip of =sentences.js= once the datasink quest lands (shared gate)
- survival-band check: every =proficiency.survival= entity ranked ≤160 or on the known-strays list

Green ladder: M1–M3 gate = generator asserts pass + =git apply --check= clean (BOTH pasted this session: =ok 1133= · =APPLIES-CLEAN=) · M4 gate = harvest state file shows hits>0 + attribution ledger nonempty + a played rep with audio. M4 gate NOT met — see status.

* blast table

| file | change | milestone | kind |
|------+--------+-----------+------|
| =dataset/literals/sentences.js= | full rank rewrite + 45 adds + 9 symbol adds + 2 glosses | M1–M2 | generated |
| =dataset/literals/words/adposition.js= | 2 rank values | M3 | surgical |
| =dataset/literals/words/determiner.js= | 4 rank values | M3 | surgical |
| =dataset/literals/words/contraction.js= | 7 rank values | M3 | surgical |
| =.harvest/rerank.js= | NEW, the rank generator | M1 | authored |
| =.harvest/harvest-targets.json= | NEW, 143 targets | M4 | generated |
| =.harvest/harvest-phrasebook.js= | NEW, resumable harvester | M4 | authored |

* deferred (+ reopen trigger)

- The 8 survival-tag strays (=Il negozio è aperto la domenica= …) — statement drills mis-tagged by the old curriculum; tags left untouched. Reopens with any survival-symbol audit.
- =È carne di scimmia= and the Tom corpus — vocalized, permanent; reopens only if freight is ever re-recorded.
- Misses from M4 (invented phrasebook lines with no Tatoeba recording, e.g. =Sono qui per lavoro= likely) — the state file's =misses= list is the record-it-yourself queue; reopens when a recording pipeline (own voice or TTS) exists.
- Re-rank cadence: rerun the generator whenever word zipf data or tier lists change; the generator is idempotent.

* forks (call before M4)

1. *Network*: run the harvest inside a session (add =tatoeba.org= to the chat network allowlist — in-session attempt got =x-deny-reason: host_not_allowed=) · or run locally: =deno run --allow-read --allow-write --allow-net --allow-run .harvest/harvest-phrasebook.js=.
2. *M4 execution home*: the standalone script (this quest) · or the harvest mode's =/vocalize= (datasink quest) once that lands — the mode subsumes the script; script stays as the no-daemon path.

* changelog

- session: terrain read (order analysis, freight rule, survival symbol found) → three-tier generator → 46 adds → single-word recast to 45 → After-you glosses → words invariant fix → 143-target harvester authored → split patch cut and apply-checked. Coverage delta: 0 suites → 0 suites (generator-assert-only; suite debt named above).
