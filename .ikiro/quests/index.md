# Quest Index

> Derived — regenerate at every sunset/revival (`skills/quest-lifecycle/`). Root = live only; `done/` = landed + verified; `discarded/` = sunset without landing; `bak/` = beef's recovery surface, untouched — NOT on disk at this regeneration: the seven it listed sit in `discarded/` (marked ⌂), bucket judgment beef's. ⚑ = classification is the 09-01 revival's judgment call (header status and landed reality disagreed, or the lean was mine) — beef curates. 89 quests revived from VCS this pass (last-alive revs b8e1f85e1 · b660921a3 · b6eed2f52 · 41480bd1c · 3247a84f9), all via read-only `git show`.

## live (13)

- `m61-chess-domain.org` — m61 chess — four ontologies, one position corpus, two engines behind a service door (DESIGNED from beef's dossier, nothing landed. The dossier's shape survives whole — `game` · `position` · `opening` · `puzzle`, position as the dedup unit, UCI on the wire, engines outside the domain. SEVEN of its mechanisms did not survive the codebase, each corrected with the file that settles it: the ontologies ship as TOPOLOGIES (symbols only) while the seven databases are TOPOGRAPHIES · `literal.ontology` is DERIVED from the one `TOPOGRAPHICAL` symbol and the subscriber throws on two · the match buffer belongs to the mode's `App`, not the domain · `v.node()` does not exist, so the move tree is PGN text and nothing persists a node · there is no `MULTIPLAYER` trait and the lighthouse cannot relay a move, so remote seats defer behind m60 · `v.literal("user")` names the Literal ENTITY, the const constructor is `v.const` · a service is a module with a `provider` consumed by name, not a trait a domain declares. Plus the one law with teeth: `(slug, mode)` + a nullable owner + sqlite's distinct NULLs make "one position, deduplicated across every game" an OWNERSHIP law — one mount, one `PositionRepository.reach()` writer. `@games` is a new tap; M0–M7 each with a green gate; 12 markers pre-registered; 6 forks owed before M1)
- `readme-can-i.org` — readme the "Can I…" fan — a capability section in the reader's voice, appended after the quickstart (AUTHORED from beef's raw 18-item block, /"create a mini quest"/; nothing on disk. Copy claim-checked against the tree: providers WRONG — `commons/hallucinators/` already holds anthropic · openrouter · deepgram · elevenlabs; the readiness cross-reference WRONG — positional, breaks on reorder; two THIN claims gained the command that proves them (`registry/tap registry-education`, `instance/doctor` → `dormant`). RULED: placed between §6 and the Noticeboard · the planned half loses its ten "Yes."es to one `### …eventually` heading, because you cannot answer yes to what does not exist. Blast = ONE file, two hunks — no suite reads README.md; the unrestricted grep found ONE live prose dependent, `documentation/…/12.01_slowstart.mdx:7`, which survives an append after §6. Planned/shipped is 9/9 and only 2 planned answers open with "Yes." — measured after the claim-guard caught me inventing seven-of-ten. Two forks owed: the planned half's form, whether "use it at work" may say twice what README:9 says once)
- `m60-lighthouse-security.org` — m60 lighthouse security — two principals, one verify path (DESIGNED, tangleable, nothing landed: a system key EXCHANGED at `/auth/system` for a 1h bearer, never carried; signup `open | invite | closed` default invite with system-minted single-use invite JWTs; `/entities/{identity,daemon}` behind `secure.authorize()` + the one new `secure.claim` — identity reads its own row only, daemon writes system-only; `Identity.authentication` hidden, the row drops its tokens (zero readers); `tokens.json` keyed by sha256, 8 sessions per identity; verify pins HS256 + issuer, `jwt.js` a per-service closure; per-username throttle. Runtime scenario rebuilt to host the REAL `compose()` → `gates.test.js` 15 cases + typology `secure.test.js` 3. Two forks owed: the claim verb's name, the signup default)
- `m58-application-noun.org` — m58 the application noun — `app` → `application` (M1) · `TOOLED` → `TOOLING` (M2) (both SCOPED, nothing landed. M1: `Object.assign(this, module)` makes the export name the mode property, so one rename is three faces; blast = 6 seam files · 10 repo kernels + 1 consumer · 5 test files · education 4+14 · vcompany 3+3 (no VCS, capture first) · primer 1 · installed hello-world; 3 decisions owed — route `/metadata/app`, kajuit `$app`, out-of-repo same pass. M2: the trait name is a string `stagger` resolves against the export, and the row's `enumItems`; blast = 6 seam (runtime only) · 3 repo kernels · 3 test files + 8 gitignored snapshots · education 6+1 · vcompany 4 · primer 0 · installed hello-world · codemap 4 + documentation 2; 1 decision owed — the persisted `Mode.traits` row ACCUMULATES, recommend override so the row mirrors the manifest; steps 1–6 per milestone = the blast bracket)
- `m57-hello-world-researches.org` — m57 hello-world researches (**M0-M4 · M6 ITERATE · M8 IMAGES LANDED uncommitted; hello-world 19/63 · reader 7/43 · runtime 54/342 · kajuit 37/133.** GENERATIVE owns `view_render·revise·inspect·list` on `mode.generator.tools` (ex `mode.gen`), HARNESSED arms, `mode.module.generator` STEERS; revise = findOneOrFail·assign·flush in the tool, NO repo verb; harnessed persists a response on a FORKED em, one flush at close (a tool's root flush had carried half a response out; a restart mid-call left a dangling tool_use); kajuit `Buffer.view` is an atom; reader absolutises `img[src]` https-only + `images[]`, FIGURE pattern. Quest = header + code. OWED: restart, revise + figure walks, rip probes, M5 README, P-spoken ruling.)
- `m56-the-pincer-turns.org` — m56 the pincer turns — a bone reads its own rect, and panel C stops being a triptych (DESIGNED from beef's Claude Design project `Pincer responsive redesign directions`, nothing landed; three comps + the sync ledger read, v3 WALKED LIVE at o=90. Four findings reading v3 alone could not give: v3 is ONE LEAF of a fan (three panel-C designs, two pincer interactions); the wireframes RULE terminal an anchor that never folds/docks/closes — beef STRUCK it ("terminal should absolutely dock and close"), v3's symmetry is the law; the comp's palette IS dapper `paper`, 21/21 hexes measured, so the port is a token substitution; and v3's `norm` leaves dead space when every open pane is folded, reachable through `openPane`. M1 = the t-bone: one `axisFor(rect)` rule for shoulder·crown·spine + the drag HUD + two pieces of dead CSS deleted. M2 = the pane engine in a pure `panes.js`. Six rulings, 1 SETTLED — no anchor; five owed)
- `drapes-editor.org` — drapes the file editor — CodeMirror 6 wrapped in drapes and reached as a subpath, replacing the vdex `raw` view's hand-rolled painted-textarea (LANDED WHOLE, GREEN: `editor/{org,index,Editor,languages,theme,vim}` + 2 suites, the bundler subpath line, the vdex swap; `org.js` is a stateful tokenizer with folding since CM6 has no org grammar; the real App 636 kB prod / 5.6 MB dev, under estimate, vim out; LAWS: a drapes subpath entry is always `.js` · an interpolated class attribute masks Svelte's unused-CSS pruner; still live only because his 8 click-through gates are unrun)
- `m54-buffer-file.org` — m54 the desk — a system FILE trait on buffer AND literal: the working copy of one asset that the harness (`system.file` section) and the App (`$trait` deepMap key path) both read and write; save through the literal's FILE face to the freight, hash = the concurrency token; `v.asset` pinned as the ONE primitive under freight · VOCALIZED · DOCUMENT · FILE; plurals = relations + `fs_*`/`entity_find`, no `FILES` trait; `mode.reinstall` RETRACTED → twitch `/after/freight/receive`; kajuit/drapes render a FILE buffer by MIME, domain-blind (DESIGNED + TIED UP from a five-agent sweep; RULED: FILE · LABELED in M1; QUESTIONED: the verbs' home — `/buffer/file/*` aperture + `daemon.call` is inside out, mikro-first `BufferRepository` verbs are the reopen path (M3); M1 stands without it, hunks on go)
- `m55-recursive-terminals.org` — m55 recursive terminals — a buffer renders a buffer: the thread is the daemon's, the terminal is the client's function of it (M1 LANDED uncommitted: `Thread.parent`/`children` in the Turn shape + `Buffer.thread` cascade + typology relations, suites unchanged, migration mints at boot; LAW: thread is daemon, terminal is client, NO terminal entity on the runtime; M2 DESIGNED gated: kajuit terminal `up · down · status · stop · run · watch` + tree + kajuit-local schematic + `$view` on the terminal, `down` never deletes the thread, `up({thread})` adopts; M3 the nested App on m53's Desk; struck: `buffer.parent` · shard `verbs` · `TerminalEntity`; known issue: child terminal dock/conversational)
- `m53-vcompany.org` — m53 vcompany — the office stack, then the chief officers (RECAST to ONE file: the pre-draft, the V3 vdex pass and v4 mail+calendar folded in, originals sunset; LANDED + LIVE, uncommitted: `@vcompany` package + `vivalence` instance, four literal ontologies vdex · message · event · transaction, `office/vdex` on two mounts `vdocs` · `corpo` (eight slug verbs ONCE in the aperture, `open` an EMITTER, per-path sync + `Deno.watchFs`, index as rows, the V3 App shell + 8 parts, CodeMirror raw), `office/email` + `office/calendar` over `dummy/mail` · `dummy/calendar` (the provider seam = ONE static; a dummy owns both directions + a DATASINK; the held send, `/key` never armed), 7 mode rows; single-owner rows `(slug, mode)`, scheme shared / computed owned; `manifest` = the ONE identity record; FRAUGHT vs MOUNTED; repo hunks in paladin · typology · runtime · drapes · kajuit; package 25/113 + 2 stale vdex reds. Officers = skeletons, chat + ledger = slots. `* future directions` = owed now · vdex LATER · messaging · T3 · the officer stage · seams · forks; `* resources` = designs · OpenExecutive · JDex · providers · compacts · memory · commands; `* release` accrued)
- `m51-standalone-slowstart.org` — m51 standalone — one instance, every system: the case study that is also the localhost default
- `m50-the-three-way-weave.org` — m50 the three-way weave — wafer, dossiers, masks, and processes. what a vector instantiates · the control vector · the process contract (DESIGNED; wording settlement + two schematics + dossier recast + signal table; M1 typology gated; absorbs m49 M2/M3; deep pass held — header 3/5 is a report mis-parse, 4 sketch hunks gated)
- `m46-linux-readmen.org` — m46 linux readmen — two distros walk the README 0–6 under witness, logs inlined (WALKED; L1 L2 L4 APPLIED — README §0 + install.sh voice + kajuit one-vite; 17 proposals; taskbags: ghost process management B1–B13 · use/--instance cluster C1–C12; connoisseur pass over both — sketches + drain; `instances.resolve` LANDED; cluster A landed as m49 M1)

## done/ (78)

- `activation-loop.quest.org` — Intent
- `aperture-calling.quest.org` — aperture calling — evict the .call squatter, compile the aperture stripwire
- `c-panel-rebuild.quest.org` — C Panel Rebuild — D/E/F panels, the /metadata self-description surface, and the masked·aimed·queueing trait controls
- `client-typology.quest.org` — Client Typology Reorganization ⚑
- `conjugation-ontology.quest.org` — Conjugation Ontology — Conjugation as Literal
- `contractions.quest.org` — contractions — where the checker's language knowledge lives
- `conversation-dock-recast.quest.org` — Conversation + Dock re-cast (post C-panel rewrite)
- `cortex-faculties-vector.quest.org` — Cortex — table(Map) → faculties(Vector) recast
- `cortex.quest.org` — Cortex — Hallucinator Orchestration & Mode Hallucination Interface
- `dialogue-verbatim-rename.quest.org` — Dialogue / Verbatim — cortex faculty rename pass
- `effect-saturation.quest.org` — Effect Saturation — identity-gated install, single integrate verb
- `english-to-spanish.quest.org` — ikiro/english-to-spanish — Castilian Spanish corpus (mirror of english-to-brazilian)
- `flatten-corpora.quest.org` — ikiro/flatten-corpora — survival/a1/a2 → english-to-brazilian
- `ghost_ghost-client.quest.org` — Ghost — Operator Interface
- `identity-collapse.quest.org` — identity-collapse — rewrite full history to single canonical author `beef <beef@vivalence.org>` ⚑
- `ikiro-modernization.quest.org` — ikiro modernization — the skills/agent-architecture meta vs ikiro
- `intent-as-template.quest.org` — Intent-as-Template Refactor
- `italian/` — dir, 4 files; carries italian-curriculum.quest.org
- `kajuit-build-target.quest.org` — Kajuit Build Target → Runtime
- `kajuit_client-layout.quest.org` — Client Layout — Mobile-First Viewport & Keyboard Management
- `kajuit_datamap-client-migration.quest.org` — Datamap Shard + Client Entity Migration — Work Package
- `language-learning-modes.quest.org` — Modes — Game Modes & Tactics for Language Learning
- `longdistance.port.plan.org` — Longdistance — Port (first integration) implementation plan
- `longdistance.quest.org` — Longdistance — voice call modes + hallucinator TTS/ASR
- `m11_packages.quest.org` — M11 — Packages (the registry dissolves into a package supply) ⚑
- `m12_streaming_harness_toolcalling.quest.org` — M12 — streaming harness, tool-calling, buffer generation
- `m15_cake_rename.quest.org` — M15 — the "cake" rename (global term settlement)
- `m16_client_provisioning.quest.org` — M16 — client provisioning doctrine (structures · effects · surfaces)
- `m17_client_ctx_grammar.quest.org` — M17 — the client ctx grammar (context is the source, props are the fallback)
- `m18_conversation_dissolves.quest.org` — M18 — the Conversation prototype dissolves (a dialogue is a streaming request) ⚑
- `m19_generative_views.quest.org` — m19 generative views — bundle is the primitive. 6 steps, ~30 min total
- `m20_hallucination-session-recast.quest.org` — m20 — hallucination session recast
- `m22-datasink.org` — M22 — datasink: the katabolic counterpart to DATASET (daemon entities drain back into registry modules)
- `m23-debuggable-hallucinations.org` — m23 — debuggable hallucinations
- `m24-rep-o-gram.org` — M24 Rep-O-Gram — one rep machine to rule them all
- `m26-hallucinate-contract.org` — m26 — the hallucinate contract
- `m29-agent-tools.org` — m29 — agent tools: the armory
- `m30-kajuit-performance.org` — m30 — kajuit performance: the client that recomputes the world
- `m31-studies.org` — m31 studies — brew · straight.el · vivalence nature · beef verbatim
- `m31-very-important-package-registry.HISTORY.quest.org` — m31 HISTORY — provenance, recovery verbs, retired + orphaned decisions
- `m31-very-important-package-registry.org` — m31 — very important package registry: the registry becomes user-managed
- `m33-dictation/` — dir, 2 files
- `m33-dictation.org` — m33 — dictation
- `m39-kernel-references/` — dir, 2 files
- `m39-kernel-references.org` — m39 — kernel references: a daemon kernel entry stops being a package name
- `m40-ledger-convention.org` — m40 ledger convention — one home: registry + variants live in the ledger
- `m41-stratified-env.org` — m41 stratified env — who gets to say VIVA_*: one fold, seven voices
- `m43-stucatch.org` — m43 stucatch — the daemon grows ears: mesh lines become literals become a view
- `m44-authoritative-slugs.org` — m44 authoritative slugs — instances.json names the instance, the manifest names the recipe (LANDED green: paladin 36/145 · ghost 80 + the pre-existing `strata.wet` two-shells red; 10 markers settled on a scratch ledger at sunset — 7 held · 3 revised by m49's one-lock shape; release lines lifted into root `release.md`; commit beef's)
- `m45-abandoned-coherence.org` — m55 abandoned coherence (file is m45 on disk) — one file, one pinhole, one authored schema (ALL EIGHT STEPS APPLIED, suites pasted; open + beef's: `Url.absolute` NaN one-liner, deploy migration)
- `m47-commons.org` — m47 commons — the checkout ships ONE package: registry/** → commons/, every lookup @commons/<type>/<slug> (LANDED on go, green both sides, live-verified; release lines lifted into root `release.md`; beef owes commit · education push · M5 prod)
- `m48-environment-schematics.org` — m48 environment schematics — the env schema is a v schema: typed values, optionality by the grammar, one law for keys (LANDED under the blast-bracket, M1·M2·M3 + F5 = INVALID values OWED by init; `v.url()` = RFC 3986 URI-with-authority grammar; repo + instances + tapped `@education` recipes migrated, OPENROUTER/ELEVENLABS/DEEPGRAM optional; 14 markers held; release lines lifted into root `release.md`; beef owes commit ×2 — repo + education checkout)
- `m49-process-ontology-and-control-vector.org` — m49 process ontology + control vector — M1 LANDED + live-proven (Process/Die on Wafer, one token lock, detached supervisor), code UNCOMMITTED (beef commits); sunset on beef's order after the wafer verdict — M2/M3 + the recast live in m50; release lines lifted
- `m52-schematic-pinhole.org` — m52 schematic pinhole — the recipe declares, the schematic settles: typed statics (Url codec over the RFC scalar), faults as data (doctor runs the pipeline, boot edge throws), one hallucinator law (null or blank secret = dormant) (LANDED on `go 52`, sunset same session: M1·M2·M3 + 7 instance/education recipes + README; walk T1–T5 on a scratch instance, ONE amendment = ghost `instance/run` mounts before `boot`; 10 markers held/revised; beef owes commit · education push · live restart)
- `m53-vcompany.v3-vdex.org` — m53 · the V3 vdex App pass — surface + functionality over the V3 design (LANDED in `~/.viva/instances`, suites green: `buffer/Vdex.svelte` + 7 parts + `jdex.js`, block editor · raw · crumbs · status line · zone-4 overlays · `Paper.svelte` pdf viewer · the cargo fix for spaced names; sunset at the m53 recast — its LATER list and laws live in `m53-vcompany.org`)
- `m53.v4-mail+calendar.org` — m53 · v4 mail + calendar — `office/email` + `office/calendar` as APPLICATION modes plus ONE dummy provider mode PER office mode (LANDED WHOLE + GREEN on "fine. apply": `event` = a fourth ontology, the held send + `/key`, `dummy/{mail,calendar}` with DATASET + DATASINK + recording apertures, four landing bugs fixed; six office test files owed; sunset at the m53 recast — carried forward in `m53-vcompany.org`)
- `m59-instance-as-a-function.org` — m59 the instance as a function — one schematic, three stages, defaults declared once (LANDED on beef's /"fix my comments. then implement."/: both patches applied to the live trees — 46 repo files + 21 ledger files — the eight db renames done, the redacted snapshot regenerated. Beef's three comment-rulings folded: F13 lifecycle owns `mount` (prototype is data, `remount` retired) · F14 `v.collect` walks the schematic for `statements` · F15 no `type` on runtime. Root `lighthouse` · `datamap` · `hallucinators` inherit as COPIES whole-slot, ONE schematic with required daemon slots, identity is a manifest everywhere, `mount` → `mountpoint`, `Mask` deleted, pinhole = `paladin.hydrate`. Live GREEN: paladin 41/185 · typology 124/502 · runtime 55/347 · hello-world 19/63 (the two reds pre-existing); ghost red from SIBLING m57 drift, not m59. END-TO-END: the real runtime boots hello-world on `multiplayer.viva.db`, data intact. Booting caught the one miss no suite hit — `multiplayer/server/lib/jwt.js` `service.mount` → `service.mountpoint`. Owed to beef: commit · education push · start his watch)
- `m4_phase-playground.quest.org` — Intent
- `m7-snapshot-testing.quest.org` — M7 — Snapshot Testing
- `multiplex.quest.org` — multiplex — the daemon session: Socket exchanges frames
- `paradigm-cell-completion/` — dir, 3 files
- `paradigm-cell-completion.quest.org` — ikiro/paradigm-cell-completion — restore thirdSingular cells across imperfect/conditional/pres.subj
- `pincer.quest.org` — Client Layout — T-Bone, Viket, and the Panel Engine
- `pool-prototype.quest.org` — Pool — Emitter Composition Primitive
- `provider-request-contract.quest.org` — The provider Request contract — name the shape both the hallucination and its providers speak
- `redact.quest.org` — ikiro/redact — corpus consolidation
- `remote-entity-dataspace.quest.org` — Remote Entity Dataspace — recognize the fold that already exists; stop the slop cosplaying one ⚑
- `riddler.quest.org` — Riddler — the standalone LLM riddle game (emitter + aperture compile their own context)
- `stage-canvas-devtools.quest.org` — Stage — Rendering Primitives & Dashboard Mode
- `survival-conjugation-expansion.quest.org` — ikiro/survival-conjugation-expansion — verb set 14→38, three new tenses for the 14, full nonfinites
- `tatoeba-harvest.quest.org` — ikiro/tatoeba-harvest — BR sentence + audio harvest
- `terminal-first-client.quest.org` — Terminal-First Client
- `topology-to-corpus.quest.org` — ikiro/topology-to-corpus — kernel type rename: topology → corpus, topologies → corpora ⚑
- `typology_v-schema-builder.quest.org` — v — Fluent Schema Builder
- `variant-to-instance.quest.org` — variant → instance — the recipe gets its true name, flag-day, no aliases
- `variant.quest.org` — Variant — Root Manifest for a Vivalence Deployment
- `voice.quest.org` — Voice — single-tier shuo-style implementation
- `wafer-lifecycle.quest.org` — Wafer Lifecycle — Vector-based Process Composition

## benched/ (3) — beef's bench: parked live quests (bucket outside the lifecycle skill's structure; indexed as found)

- `m25-user-and-thread-handling.org` — m25 — user and thread handling
- `m27-memory-and-notes.org` — m27 — mind: memory + note
- `m42-history-purge.org` — m42 history purge — filter-repo the dead registry mass out of git history

## discarded/ (42) — ⌂ = listed under `bak/` at the revival, folded here by disk

- `2026.07.02.org` — (no title)
- `closed-class-completion.quest.org` — ikiro/closed-class-completion — finish structural inventory: contractions, demonstratives, possessives, conjunctions, particles, intensifiers, fillers, shapes, superlatives
- `cortex-hallucination-harness.quest.org` — cortex/hallucination/harness — the request as an event-log fold over one internal vector
- `dapper-second-theme.quest.org` — Dapper Second Theme — warm-red "ember" (sampled from the flap clip)
- `decorum.quest.org` — Decorum — Zone-Based Theming Architecture
- `documentation.org` — (no title)
- `documentation.quest.org` — documentation
- `elegance-debts.quest.org` — Intent ⚑
- `exhibit-absorbs-shadow.quest.org` — Exhibit absorbs Shadow — recite as a style of present
- `ghost-autocompleting-commands-and-service-trajectory.quest.org` — Ghost FEAT autocompleting commands and service trajectory
- `hallucination-faculty-render.quest.org` — Hallucination owns faculty render · harness/emitter as live-vector middleware
- `herald.quest.org` — Herald — unified self-description route family
- `ikiro-modernization.staging.md` — ikiro modernization — STAGING (deep-research deliverable, for tomorrow's attempt)
- `kajuit_typology-rotation.quest.org` — Typology Rotation — kajuit-local restructure ⚑
- `m10_hallucinating_monkey_playground.quest.org` — M10 — Hallucinating Monkey (playground harness testbed)
- `m13_semantic_alignment.quest.org` — M13 — module · mode · kernel semantic alignment
- `m14_testing_fixtures.quest.org` — M14 — testing fixtures (the @fixtures package + lock-demo residence) ⚑
- `m28-primed.org` — m28 — primed: the procedural trait ⌂
- `m32-memoriter/` — dir, 2 files ⌂
- `m32-memoriter-anki-clone-demo.org` — m32 — memoriter: Anki as one out-of-tree vivalence package ⌂
- `m36-supabase.org` — m36 — supabase per die: one cluster, schema-per-die datamap, lighthouse as GoTrue facade, and schema evolution with nowhere to put migration files ⌂
- `m37-converse/` — dir, 1 files ⌂
- `m37-converse.org` — m37 — converse: voice in · voice out · two-sided ⌂
- `m44-record-authoritative-slug.org` — m44 record-authoritative slug — instances.json names the instance, the manifest names the recipe ⚑
- `m53-vcompany.org` — m53 vcompany — the 8285-line WORKING file (T0 · T1 · T1.1 · T1.B · screens · V3 landings, every tangle whole) SUPERSEDED by the recast of the same name at the root on beef's order ("recast this quest … cut down to one file"); kept for the tangles and the decision trail
- `m53-vcompany.pre.org` — m53 vcompany: the office stack, then the executive — beef's draft + the first populate (executive-first mapping of OpenExecutive, his m:n ownership sketch, the pre-T1 task plan); superseded by the working file, then by the recast
- `m5_widget-glyphs.quest.org` — M5 · widget glyphs — a control lever + status reporters in vinca (the glyph UX language)
- `m9-playground-layout.quest.org` — M9 · Playground Layout — design possibilities + the data-UI primitive system
- `modes_tactic-analysis-routine.quest.org` — Tactic emitters — analysis / routine middlewares
- `paladin-isolation.quest.org` — Paladin Isolation — Process Boundary Defense and Client-Side Modes
- `playground-reactivity.quest.org` — Intent ⚑
- `progress.org` — progress feedback — the learner sees their own retention ⌂
- `react-bundler.quest.org` — React-in-Svelte Micro-Frontend Integration
- `siphon-downloader.quest.org` — Siphon — yt-dlp Downloader Daemon
- `survival-into-a1.quest.org` — ikiro/survival-into-a1 — SUPERSEDED by flatten-corpora
- `toolcalling.quest.org` — Toolcalling — TOOLED trait + MCP exposure + standard tool library
- `trait-reactivity.quest.org` — Intent
- `vector-affect.quest.org` — Intent
- `very-important-packagemanager.quest.org` — Very Important Package Manager — Registry as Discovery Scopes
- `viva-init-install.quest.org` — viva-init-install — turnkey dev-boot from a fresh clone ⚑
- `viva-init.quest.org` — Viva Init — sheets wizard library + process telemetry + init trajectory
- `viva-install.quest.org` — Viva Install — product distribution channels
