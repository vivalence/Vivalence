#+TITLE: cortex/hallucination/harness — the request as an event-log fold over one internal vector
#+STATUS: SUPERSEDED — the triad LANDED test-first on beef's repository/aperture skeleton (Cortex=Map
#+STATUS: register/find/findOne+derive; Hallucination(cortex)=closure factory, shape.object surface,
#+STATUS: tool loop at leaves; tune axes pinned intelligence·reasoning·speed·thrift). All suites green.
#+STATUS: See compact [[the-cortex-hallucination-triad-built-test-first-on-beefs-skeleton]]. Below: the
#+STATUS: DEAD five-pass churn (kept as decision trail only).
#+STATUS: (was) REJECTED — beef: "all of this is shit." Five passes preserved as the decision trail;
#+STATUS: tangles are REFERENCE, not intent. Live seed is beef's from-scratch repository-Cortex +
#+STATUS: aperture-Hallucination skeleton (his hand); the strategy/precompile/swallow ornamentation
#+STATUS: below is DEAD. Only survivors: tool-loop-as-steer-strategy (koa double-next ban), the
#+STATUS: parse-out unconsumed-generator streaming seam, turns-as-ARRAY. See compact
#+STATUS: [[the-cortex-hallucination-redesign-churn-rejected-and-what-survived]].
#+SEED: beef — the @beef pass over cortex.js/hallucination.js/harnessed.js/emitter.js, then:
#+SEED: "cortex has a function .hallucination() that spawns a Hallucination() which is a function
#+SEED:  that internally holds a vector and returns a object of primitive hallucination shape with
#+SEED:  faculties and render/stream baked in. [...] when the {render/stream} are called we compile
#+SEED:  a oneOff hallucination monad from the vector." + "Hallucination that looks like Stall()."
#+SEED: + "tools are not in input! they are on the mode. check tooled.js" + "faculties as a set."
#+SEED: + "cortex precompiles the execution vector. hallucination brings its own execution handling."

* Intent — desired end state in plain language

The Hallucination stops being a class with mutable fields and becomes a Stall-shaped
factory closing over ONE internal Vector. Its verbs (=condition/add/arm/configure=) each
record ONE shard on that vector's carry — the verbs are the event log. Every
=render/stream= dispatch is a one-off fold: the carry composes a fresh primitive record
={context, turns, tools, config}=, and the request's OWN execution strategy consumes the
avenue leaf — compile + volley. Same law as Nyan (event-log + project()) and Redux.

The Cortex is a pure registry over a flat =Set= of faculties (resolution is metric —
nearest-in-tune-space — never path-addressed) AND the supply-side compiler: it
PRECOMPILES the execution vector — a =/type/{render,stream}= trie whose leaves ARE the
avenues (tune-resolution + faculty-config marriage folded in), rebuilt lazily after
=extend()=. =cortex.hallucination(descriptor)= spawns the request, handing over the
precompiled trie and NOTHING else — the request never receives the cortex. It SWALLOWS
the trie (owns its copy) and brings its own execution handling as steer strategies.
Supply/demand named: cortex = declaration, hallucination = record + consumer.

The harness stays the tooling on the request: ingress builds =ctx.hallucination= through
the request's own verbs (no normalize step — every verb is total), arms it from
=mode.tools= (the TOOLED trait's live vector — tools NEVER ride the wire input), mounts
delegating faculty terminals via =Hallucination.faculties(daemon.cortex)=, and keeps the
/dialogue turn handling INLINE (no scribe extraction — beef veto).

* The model — pinned laws

- verbs = event log; dispatch = fold. A render never mutates the request; two renders are
  two independent folds. Payloads are captured (flattened/validated) at verb-call time.
- four primitives, one total verb each: =context= (conditioning prose) → =condition()= ·
  =turns= → =add()= (strings THROW: "prose is condition()") · =tools= (a Vector trie) →
  =arm()= · =config= → =configure()= (=tune=, =input= (the object schema), =rounds= live here).
  =tune()/tool()/absorb()= die. =MAX_TOOL_ROUNDS= dies into =config.rounds ?? 10=.
- supply/demand pair: the cortex PRECOMPILES the execution vector (leaves = avenue
  functions =(turns, config) → turn|packets=, tune-resolved per call from =config.tune=,
  married to =faculty.config= aim-style); the hallucination brings its own execution
  handling — =execution.render/.stream=, custom members of the steer strategy family
  (echo/bare/request/guarded): the carry folds the record, the terminal runs the volley.
  The push/pull fork dissolves into this pair.
- =swallow=, not =slurp=: the request OWNS its tree (request-side branch decoration never
  leaks back into the cortex declaration or sibling requests). Effect handlers stay
  shared by ref — the copy is containers only.
- compile() is the cata: context lines lead as system turns; =shape.agentic= compiles the
  tool trie (trie path IS the name; =pattern.input= validates via =steer.guarded=).
- ONE volley loop, in the request's strategies. Derivations are SINGLE-SHOT avenues:
  =DERIVATIONS.object= injects =respond= + =tool_choice:any=, seals an =end_turn= on
  detection, otherwise returns the raw =tool_use= turn for the volley to extend. cortex.js
  imports nothing from execution — the old backward =executeTools= import stays dissolved.
  (Resolves the object-synthesis A/B/C fork.)
- import direction: cortex → hallucination, only as the spawner (=cortex.hallucination()= —
  the Connection-imports-Request direction). The request receives ONLY the compiled
  vector — never the cortex; =Hallucination.terminals= likewise derives from the vector.
- faculty face derived, never stitched: =hallucination.faculties= is built by walking the
  swallowed trie's type branches (=.faculties.object.render()=; =Object.keys= = servable
  types). No hardcoded getters, no lingo copy; =primitives= (a root effect) stays out.
- tools are mode-supply: TOOLED surfaces its live context-bound vector as =mode.tools=;
  the harness ingress arms from it at CALL time (trait-order independent). Wire input is
  ={system, prompt, turns, schema, tune, config}= — no =tools= key.
- faculties = =new Set()=: flat pool, insertion order (nearest tie-break contract holds),
  identity-dedupe (double-extend idempotent). =slug= stays metadata, nothing indexes on it.
- missing faculty at the surface = =NotFound /type/verb= (leaf never compiled — the trie is
  the capability statement). =primitives()= = the same fold with an identity leaf (the
  Stall =toJSON= analog) for inspection/tests.

* Blast — beyond the tangles

- =subsystems/typology/tests/hallucination.test.js= — conditioning block recast:
  string→system test dies (→ =condition()=), =tool/tune/configure= chain → =arm/configure=,
  field pokes (=hallucination.turns/tools/config=) → =primitives()= or behavior; =input()=
  helper reshape (=tune=→=config.tune=, =schema=→=config.input=, tools move); =makeHarness=
  → ingress verbs + =slurp(Hallucination.terminals(cortex.vector))=. Plus an invariant test:
  verb sources unmutated, order preserved (conditioning-order contract).
- =subsystems/typology/tests/cortex.test.js= — green as-is (=stored/resolve/has/nearest=
  surfaces unchanged over the Set).
- =systems/runtime/tests/cortex.integration.test.js= — tool-loop cases stop passing
  =tools= in input; the scenario mode gains a =module.tools= vector (TOOLED path).
- =systems/runtime/tests/mode/tooled.test.js= — add =mode.tools= surface assertion.
- =registry/education/homepage/aprende/harness.js= — =ctx.hallucination.add([persona])= →
  =.condition(persona)=.
- =registry/education/game/riddler/riddler.viva.js= — harness middleware:
  =add(persona(...))= → =condition(persona(...))=.
- =registry/education/game/riddler/emitter.js= — unchanged (sugar keys keep working;
  =schema= maps to =config.input= at ingress).
- =subsystems/typology/gestalten/shard/index.js= — one line:
  =export * as schematics from "./schematics.js";=

* Open / gated

- =Object.freeze(module)= in the Mode ctor (module read-only enforcement, harnessed.js:126
  note) — HOLY mode.js touch, separate go. Paladin-level deep enforcement = later decision.
- staleness: =extend()= invalidates the cortex's compiled vector; requests spawned BEFORE
  a later extend keep their swallowed copy of the old trie. Harness spawns per call: fine.
- avenue-typed effects: the execution vector's leaves are =(turns, config)= functions, NOT
  ctx-handlers — only the request's execution strategies may consume them (an echo-style
  invoke against those leaves would misdispatch). Named + contained; documented at both ends.
- behavior change: object derivation that never responds now THROWS at the rounds ceiling
  (was: silent =undefined=). A turn carrying =respond= + a real tool still drops the real
  call (current behavior, kept).
- naming alternates on the table: =condition=→=prime= · =arm=→=wield= · =volley=→=round=.
- thread?/buffer? ctx binds stay parked for m17 (client ctx grammar).

* Tangles
** subsystems/typology/prototypes/hallucination.js

#+begin_src javascript :tangle ../../subsystems/typology/prototypes/hallucination.js :mkdirp no
     import { is, object, shape, soma, steer, Vector } from "@vivalence/typology";


     export const tiers = {
       frugal: [0.1, 0.3, 0.9, 1.0],
       balanced: [0.4, 0.6, 0.6, 0.6],
       capable: [0.6, 0.8, 0.4, 0.4],
       unleashed: [0.9, 1.0, 0.2, 0.2],
       eager: [0.3, 0.5, 0.5, 0.1],
     };

     export function nearest(faculties, target) {
       if (typeof target === "string") target = tiers[target] ?? [0.5, 0.5, 0.5, 0.5];
       return array.nearest(faculties, target, (faculty) => faculty.tune);
     }

     export function Hallucination(cortex) {

   const hallucination =  {
       configure,  // ctx
       condition,  // context
       add,  // turns
       arm,  // tools
   }


       const leaf = new Vector()
     	  .open("/stream",execution.stream)
     	  .open("/render",execution.render)

         const hal = new Vector();
         for(const faculty of cortex.faculties) {
   	  hal.branch(faculty.type).slurp(leaf);; 
   	  hallucination[faculty.type] = {render/stream}  where render/stream compile and execute hal steer.invoke()
     }

       // const apply = (fold) => {vector.use(async (ctx, next) => (fold(ctx), next())); return hallucination;};

       // vector.use(async (ctx, next) => {ctx.context = []; ctx.turns = []; ctx.tools = new Vector(); ctx.config = {}; await next();});

       // one verb = one recorded shard. Payload captured at call time; empty records nothing.

         // render: (type = "dialogue") => steer.invoke(vector, `/${type}/render`, execution.render)(),
         // stream: (type = "dialogue") => steer.invoke(vector, `/${type}/stream`, execution.stream)(),


       // const compile = (ctx) => {const { tools } = shape.agentic(ctx.tools); const config = { ...ctx.config }; if (Object.keys(tools).length) config.tools = tools; const turns = [...ctx.context.map((text) => ({ role: "system", parts: [{ type: "text", text }] })), ...ctx.turns,]; return { turns, config };};

       // const volley = async (conversation, turn, tools) => {const results = await executeTools(tools, turn.parts); return [...conversation, turn, { role: "user", parts: results }];};

       // hal
       //   .condition(descriptor.context) // if
       //   .add(descriptor.turns) // if
       //   .arm(descriptor.tools) // if
       //   .configure(descriptor.config) // if

   }
   // const execution = {render: (carry, avenue, steps, signal) => async () => {const ctx = { signal, steps }; await carry(ctx, async () => {const { turns, config } = compile(ctx); let conversation = turns; for (let round = 0; round < (config.rounds ?? 10); round++) {const turn = await avenue(conversation, config); if (turn?.meta?.stop !== "tool_use") {ctx.output = turn; return;} conversation = await volley(conversation, turn, config.tools ?? {});} throw new Error(`[hallucination] '${signal.pathname}' volley exceeded ${config.rounds ?? 10} rounds`);}); return ctx.output;}, stream: (carry, avenue, steps, signal) => async () => {const ctx = { signal, steps }; await carry(ctx, async () => {ctx.output = (async function* () {const { turns, config } = compile(ctx); let conversation = turns; for (let round = 0; round < (config.rounds ?? 10); round++) {let turn = null; for await (const packet of await avenue(conversation, config)) {turn = soma.pour(turn, packet); yield packet;} if (turn?.meta?.stop !== "tool_use") return; conversation = await volley(conversation, turn, config.tools ?? {}); yield* soma.drain(conversation.at(-1));} throw new Error(`[hallucination] '${signal.pathname}' volley exceeded ${config.rounds ?? 10} rounds`);})();}); return ctx.output;},};

     async function executeTools(tools, parts) {
       const results = [];
       for (const part of parts) {
         if (part.type !== "tool_use") continue;
         const tool = tools[part.name];
         if (!tool?.execute) {
           results.push({
             type: "tool_result",
             id: part.id,
             output: { error: `unknown tool: ${part.name}`, armed: Object.keys(tools) },
           });
           console.warn("[hallucination] unknown tool called", { name: part.name, armed: Object.keys(tools) });
           continue;
         }
         const input =
           typeof part.input === "string" ? (part.input ? JSON.parse(part.input) : {}) : (part.input ?? {});
         const returned = await tool.execute(input);
         const native =
           is.object(returned) &&
           ("message" in returned || "entities" in returned || "object" in returned);
         const { message, entities, object: data } = native ? returned : { message: returned };
         results.push({ type: "tool_result", id: part.id, output: message, entities, object: data });
       }
       return results;
     }

     // Hallucination.terminals = (avenues) => {const terminals = new Vector(); for (const [pattern, branch] of avenues.trajectories) {const terminal = terminals.branch(pattern.nature); for (const [verb] of branch.effects) terminal.open(verb.nature, async (ctx) => {ctx.output = await ctx.hallucination[verb.nature](pattern.nature);});} return terminals;};
#+end_src

** subsystems/typology/prototypes/cortex.js

#+begin_src javascript :tangle ../../subsystems/typology/prototypes/cortex.js :mkdirp no
  import { array, object, Vector } from "@vivalence/typology";
  import { Hallucination } from "./hallucination.js";

  // synthesized faculties are SINGLE-SHOT avenues — the request's volley owns the rounds,
  // so a derivation never loops or executes tools. object = dialogue forced through a
  // `respond` tool; detecting respond seals an end_turn, anything else returns raw
  // tool_use for the request to volley.
  const DERIVATIONS = {
    object: (cortex, tune) => ({
      type: "object",
      slug: "object",
      channels: { in: ["text"], out: ["object"] },
      via: {
        render: async (turns, config) => {
          const respond = { valence: "Return the final result as structured data.", input: config?.input };
          const render = cortex.via("dialogue", "render", tune);
          const turn = await render(turns, {
            ...config,
            tools: { ...config?.tools, respond },
            tool_choice: { type: "any" },
          });
          const done = turn.parts.find((part) => part.type === "tool_use" && part.name === "respond");
          if (!done) return turn;
          const data =
            typeof done.input === "string" ? (done.input ? JSON.parse(done.input) : {}) : done.input;
          return { role: "assistant", parts: [{ type: "object", data }], meta: { stop: "end_turn" }, object: data };
        },
      },
    }),
  };

  // Pure registry of faculties over a flat Set — resolution is metric (nearest in tune
  // space), never path-addressed. Insertion order carries the nearest tie-break;
  // identity-dedupe makes extend idempotent. The cortex is also the supply-side compiler:
  // it precompiles the execution vector and spawns requests over it.
  export class Cortex {
    faculties = new Set();
    // vector = null;

    extend(supplied) {u
      for (const faculty of supplied) {
        if (faculty.tune.length === 3) faculty.tune = [...faculty.tune, 0.5];
        this.faculties.add(faculty);
      }
      // this.compiled = null;
      return this;
    }

    // spawn a request — handing over the precompiled vector and nothing else: the
    // request never sees the registry.
    hallucination(config) {
      const vector = new Vector();
      for (const type of this.types) {
        const branch = vector.branch(type);
        for (const verb of ["render", "stream"]) {
          if (!this.resolve(type, { via: verb })) continue;
          branch.open(verb, (turns, config) => this.via(type, verb, config?.tune)(turns, config));
        }
      }
      return Hallucination(this.vector, descriptor);
    }
    // // every servable type: stored ∪ derivations, gated by has().
    // get types() {
    //   const stored = [...this.faculties].map((faculty) => faculty.type);
    //   return [...new Set([...stored, ...Object.keys(DERIVATIONS)])].filter((type) => this.has(type));
    // }

    // stored(type) {
    //   return [...this.faculties].filter((faculty) => faculty.type === type);
    // }

    // has(type) {
    //   return this.stored(type).length > 0 || (type in DERIVATIONS && this.derivable(type));
    // }

    // derivable(type) {
    //   return type === "object" ? !!this.resolve("dialogue", { via: "render" }) : false;
    // }

    // resolve(type, { tune, via } = {}) {
    //   const eligible = this.stored(type).filter((faculty) => !via || faculty.via[via]);
    //   const native = nearest(eligible, tune ?? [0.5, 0.5, 0.5, 0.5]);
    //   if (native) return native;

    //   const derive = DERIVATIONS[type];
    //   if (!derive || !this.derivable(type)) return native;
    //   const derived = derive(this, tune);
    //   return !via || derived.via[via] ? derived : undefined;
    // }

    // // aim-style pre-fold: the avenue arrives married to its faculty's own config —
    // // request config wins on collision.
    // via(type, verb, tune) {
    //   const faculty = this.resolve(type, { tune, via: verb });
    //   const avenue = faculty?.via[verb];
    //   if (!avenue || !faculty.config) return avenue;
    //   return (turns, config) => avenue(turns, object.merge(faculty.config, config));
    // }
  }
#+end_src

** systems/runtime/daemon/traits/harnessed.js

#+begin_src javascript :tangle ../../systems/runtime/daemon/traits/harnessed.js :mkdirp no
import { Hallucination, Vector, shape, shard, soma, steer } from "@vivalence/typology";

// tooling ON the request. Ingress spawns ctx.hallucination off the cortex and feeds the
// wire input through the request's own verbs (no normalize step — every verb is total,
// falsy keys no-op), then arms it from mode.tools (the TOOLED trait's live vector —
// tools never ride the wire). Faculty terminals come from Hallucination.terminals: the
// shape of a harness, derived from the precompiled execution trie. /dialogue is the
// turn-bearing branch; its turn handling stays inline here.
export const HARNESSED = (mode, daemon) => {
  if (!daemon.cortex) throw new Error("HARNESSED: daemon has no cortex");

  const harness = new Vector();
  harness.use(shard.context.bind("daemon", daemon));
  harness.use(shard.context.bind("mode", mode));
  // harness.use(shard.context.bind("thread?", thread));   // parked for m17 ctx grammar
  // harness.use(shard.context.bind("buffer?", buffer));

  // ingress sugar: string | { system, prompt, turns, schema, tune, config }
  harness.use(async (ctx, next) => {
    const input = typeof ctx.input === "string" ? { prompt: ctx.input } : (ctx.input ?? {});
    ctx.hallucination = daemon.cortex
      .hallucination()
      .condition(input.system)
      .add(input.turns)
      .add(input.prompt && { role: "user", parts: [{ type: "text", text: input.prompt }] })
      .configure({ tune: input.tune, input: input.schema, ...input.config });
    if (mode.tools) ctx.hallucination.arm(mode.tools);
    await next();
  });

  // faculty terminals — the known shape of a harness, derived from the compiled trie.
  harness.slurp(Hallucination.terminals(daemon.cortex.vector)); // @beef what??

  // DIALOGUE — the turn-bearing branch: persist the rendered output, load + chain history.
  harness
    .branch("/dialogue")
    .use(async (ctx, next) => {
      await next();

      if (ctx.output?.[Symbol.asyncIterator]) {
        const source = ctx.output;
        let turn = null;
        let parent = ctx.turn;
        const created = [];
        ctx.output = (async function* () {
          try {
            for await (const packet of source) {
              turn = soma.pour(turn, packet);
              if (packet.event === "/turn/close") {
                parent = ctx.daemon.entities.turn.create({
                  role: turn.role,
                  parts: turn.parts,
                  meta: turn.meta,
                  parent,
                  thread: ctx.input.thread,
                  mode: ctx.mode.id,
                });
                created.push(parent);
                turn = null;
              }
              yield packet;
            }
            await ctx.daemon.entities.em.flush();
          } catch (error) {
            for (const entity of created) ctx.daemon.entities.em.remove(entity);
            throw error;
          }
        })();
      } else if (ctx.output?.role) {
        await ctx.daemon.entities.turn.chain({
          role: ctx.output.role,
          parts: ctx.output.parts,
          meta: ctx.output.meta,
          parent: ctx.turn,
          thread: ctx.input.thread,
          mode: ctx.mode.id,
        });
      }
    })

    .use(async (ctx, next) => {
      const history = await ctx.daemon.entities.turn.history({ thread: ctx.input.thread });
      ctx.turn = await ctx.daemon.entities.turn.chain({
        role: "user",
        parts: ctx.input.parts,
        parent: history.at(-1) ?? null,
        thread: ctx.input.thread,
        mode: ctx.mode.id,
      });
      ctx.hallucination.add(history, ctx.turn);
      await next();
    });

  // persona / mode dressing — the mode's own harness middleware, added to the base carry
  // so it runs on every faculty. Slurped in; never .use()'d on the read-only module.
  if (mode.module.harness) harness.slurp(mode.module.harness);

  return () => {
    mode.harness = shape.object(harness, steer.echo);
    mode.aperture.branch("/harness").slurp(harness);
    // mode.module is read-only — freeze enforcement gated (see quest Open items).
  };
};
#+end_src

** systems/runtime/daemon/traits/tooled.js

#+begin_src javascript :tangle ../../systems/runtime/daemon/traits/tooled.js :mkdirp no
import { Vector, shard } from "@vivalence/typology";

export const TOOLED = (mode, daemon) => {
  if (!mode.module.tools) return;

  const tools = new Vector();
  tools.use(shard.context.bind("daemon", daemon));
  tools.use(shard.context.bind("mode", mode));
  tools.slurp(mode.module.tools);

  mode.tools = tools; // the live, context-bound vector — the harness arms requests from here
  mode.aperture.branch("/tool").slurp(tools);
};
#+end_src

** systems/runtime/daemon/traits/emitter.js

#+begin_src javascript :tangle ../../systems/runtime/daemon/traits/emitter.js :mkdirp no
import { shape, shard, Blacklist, Pool, Vector } from "@vivalence/typology";

export const EMITTER = async (mode, daemon) => {
  if (!mode.module.emitter) return;

  // trait habit: middleware rides a live Vector we own; the read-only module declaration
  // is slurped in — never .use() directly on mode.module.emitter.
  const emitter = new Vector();

  emitter.use(shard.schematics.input);
  // emitter.use(shard.schematics.output); // when output casting is wanted

  emitter.use(async (ctx, next) => {
    ctx.daemon = daemon;
    ctx.mode = mode;

    if (!ctx.input) console.log("INPUT MISSING?!", { ctx });

    // thread is OPTIONAL — turn-free / standalone modes (riddler) emit without one.
    if (ctx.input.thread) ctx.thread = await daemon.entities.thread.findOne(ctx.input.thread);

    await next();
  });

  emitter.use(async (ctx, next) => {
    ctx.input.blacklist = new Blacklist(ctx.input.blacklist);
    await next();
  });

  emitter.use(async (ctx, next) => {
    ctx.pool = new Pool();

    await next();

    if (ctx.output != null) ctx.pool.add(ctx.output);

    const result = await ctx.pool.drain();

    // buffers bind + index onto the thread only when a thread is present.
    if (ctx.thread && result.condition === "NOMINAL") {
      for (const buffer of result.entities.buffer) {
        buffer.thread = ctx.thread;
        buffer.index = ctx.thread.counter++;
      }
    }

    await daemon.entities.em.flush();
    ctx.output = result;
  });

  emitter.slurp(mode.module.emitter);

  mode.emit = shape.object(emitter);
  mode.aperture.branch("/emit").slurp(emitter);
};
#+end_src

** subsystems/typology/gestalten/shard/schematics.js — NEW

#+begin_src javascript :tangle ../../subsystems/typology/gestalten/shard/schematics.js :mkdirp no
// step-declared schematics as carry: cast the input on the way in / the output on the
// way out, from the pattern {input, output} the effect was opened with.
export const input = async (ctx, next) => {
  for (const step of ctx.steps ?? []) step.input?.cast(ctx.input);
  await next();
};

export const output = async (ctx, next) => {
  await next();
  for (const step of ctx.steps ?? []) step.output?.cast(ctx.output);
};
#+end_src

* Changelog

- (planning) three proposal passes folded to convergence: pass 1 class + four primitives
  as fields; pass 2 (beef respell) Stall-shaped factory + internal vector + one-off fold +
  =cortex.hallucination()= spawn; pass 3 (beef corrections) tools from =mode.tools= not
  wire input · no scribe extraction (/dialogue inline) · =faculties= as =Set= experiment
  (kills the Vector.find / steer.has HOLY touches).
- (planning) pass 4 (beef: "cortex precompiles the execution vector. hallucination brings
  its own execution handling"): cortex gains =get vector= (lazy, extend-invalidated) whose
  =/type/{render,stream}= leaves ARE avenues; Hallucination SWALLOWS it (owns its copy)
  and consumes it through =execution.render/.stream= — custom steer strategies where the
  carry folds the primitive record and the terminal runs compile + volley. Supply/demand
  pair named; render/stream engines leave the request surface and become strategies.
- (planning) pass 5 (beef: "i hate this abstraction" on the faculty getters + "i dont want
  the hallucination to receive the cortex"): the hand-stitched =faculty()= + four getters
  die — =hallucination.faculties= is DERIVED by walking the swallowed trie (one lingo
  source; =primitives= stays out as a root effect). =Hallucination(avenues, descriptor)=
  now receives ONLY the precompiled vector — =cortex.hallucination()= hands over
  =this.vector=, nothing else; the harness-terminal builder renamed
  =Hallucination.terminals(avenues)= and likewise derives from the trie, not the cortex.
