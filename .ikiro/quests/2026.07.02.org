first m11 - registry and development setup into packages. also consider deployment. aka wafers?!

## m12
then m12 the harness tooling streaming up till buffer generation. must break down into steps.

### two cases, the aprende homepage -

1. helpdesk interaction 1.2 helpdesk aperture emits buffers and kajuit/stall renders using shared return type primitive shared between buffer emitters and harness and stall.

2. aprende via conversation aka streamed chat. can also emit buffers via tool calls. same tools.
   tools internally can call the emitters.


